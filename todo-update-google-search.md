# Google Search URL 변경 후속 작업

작성일: 2026-08-14

기준 변경: `f300f26` — 앱·매뉴얼 URL 구조 개편

후속 변경: 6개 앱의 한·영 Tech Notes 14개 추가, 이후 bt-keyboard 상세 노트 6개 추가(§9, §10),
2026-09-13 로마 숫자 변환기 제품·설명서 4개 추가(§15), 2026-09-14 환율 계산기·넘패드 제품 4개 추가(§16),
2026-09-15 단위 변환기 제품 2개 추가(§17),
2026-09-15 신규 앱 4종 출시 반영(신규 URL 0개, §18),
2026-09-16 미배포 정정 3묶음 배포·GSC 조작을 CDP 로 전환(신규 URL 0개, §18.8·§19)

## 1. 결론

이번 변경은 도메인이나 protocol 변경이 아니라 **같은 Search Console 속성 안에서 URL path만 바꾼 것**이다.

- 기존 Search Console 속성 `https://droidactor.github.io/`를 그대로 사용한다.
- 새 속성을 추가하지 않는다.
- **주소 변경(Change of Address) 도구는 사용하지 않는다.** Google은 같은 사이트 안의 path 이동에는
  redirect와 sitemap 갱신을 사용하라고 명시한다.
- Search Console의 **삭제(Removals) 도구로 옛 URL을 지우지 않는다.** 이 도구는 content 이동용이 아니며,
  redirect·canonical 신호의 정상 처리를 방해할 수 있다.
- sitemap 제출 주소는 계속 `https://droidactor.github.io/sitemap.xml`이다.
- 경로 개편으로 생긴 새 canonical URL 30개와 Tech Notes 계열 20개, 합계 50개를 sitemap으로 알린다.
  (Tech Notes 20개 = §2.3 의 14개 + §9 의 2개 + §10 의 4개. sitemap 총 URL 은 여기에 §2.4 의 20개를
  더한 **70개**다. 아래 본문의 44·64·66 은 각각 8-14·8-15 시점의 실측 기록이므로 그대로 둔다.
  **2026-09-10 이후에 나오는 44·66 은 뜻이 다르다** — 44 는 색인 URL ∩ sitemap 70 의 교집합,
  66 은 보고서의 알려진 페이지 합계다(§6 2026-09-10 절 2·3번).
  **2026-09-13·09-14·09-15 세 배포로 §15·§16·§17 의 10개가 더해져 live sitemap 은 80 개다** — 이
  절과 아래 본문에 나오는 70 은 그 이전 시점의 값이므로 그대로 둔다.)
  경로 개편 핵심 URL 10개를 먼저 URL 검사 → 색인 생성 요청하고, Tech Notes 허브 2개는 다음 요청일에
  확인한다. 나머지는 7~14일 뒤에도 미발견·비색인일 때 원인을 확인한 후 선별 요청한다.
- 옛 제품 URL 14개는 최소 1년, 가능하면 계속 유지하되 현재 호환 페이지의 `noindex,follow`는 제거한다.

근거:

- [Google: URL 변경을 수반한 사이트 이동](https://developers.google.com/search/docs/crawling-indexing/site-move-with-url-changes)
- [Google: 주소 변경 도구를 사용하지 않는 경우](https://support.google.com/webmasters/answer/9370220)
- [Google: Redirect와 Google Search](https://developers.google.com/search/docs/crawling-indexing/301-redirects)
- [Google: 삭제 도구를 사용하지 않는 경우](https://support.google.com/webmasters/answer/9689846)

## 2. 변경 범위

### 2.1 이동한 기존 제품 URL — 14개

| 언어 | 이전 URL | 새 canonical URL |
|---|---|---|
| en | `/bt-keyboard/` | `/apps/bt-keyboard/` |
| ko | `/ko/bt-keyboard/` | `/ko/apps/bt-keyboard/` |
| en | `/bt-ppt/` | `/apps/bt-ppt/` |
| ko | `/ko/bt-ppt/` | `/ko/apps/bt-ppt/` |
| en | `/bt-mouse/` | `/apps/bt-mouse/` |
| ko | `/ko/bt-mouse/` | `/ko/apps/bt-mouse/` |
| en | `/wifi-scout/` | `/apps/wifi-scout/` |
| ko | `/ko/wifi-scout/` | `/ko/apps/wifi-scout/` |
| en | `/ssh-scout/` | `/apps/ssh-scout/` |
| ko | `/ko/ssh-scout/` | `/ko/apps/ssh-scout/` |
| en | `/yt-downloader/` | `/apps/yt-downloader/` |
| ko | `/ko/yt-downloader/` | `/ko/apps/yt-downloader/` |
| en | `/lgtv/` | `/apps/lgtv/` |
| ko | `/ko/lgtv/` | `/ko/apps/lgtv/` |

GitHub Pages에서는 server-side `301`을 설정할 수 없으므로 현재 배포된 옛 URL은 다음 신호를 함께 사용한다.

- `meta refresh` 0초 → 대응하는 최종 URL로 직접 이동
- 새 URL을 가리키는 `rel="canonical"`
- sitemap과 내부 링크에서는 옛 URL을 제거

최초 개편 때는 여기에 `noindex,follow`도 함께 걸었으나 **2026-08-14 commit `d6e04f0` 으로 제거했다.**
아래 정리 항목이 그 작업이며, 지금 배포된 신호는 위 세 가지뿐이다.

Google은 0초 `meta refresh`를 permanent redirect 신호로 처리한다. 다만 Google은 같은 사이트 안의 canonical
선택 수단으로 `noindex`를 권장하지 않고 redirect 또는 `rel="canonical"`을 사용하라고 안내한다. 이동 신호가
불필요하게 섞이지 않도록 Search Console 작업 전에 다음 상태로 정리했다.

- [x] 옛 호환 페이지 14개에서 `<meta name="robots" content="noindex,follow">`를 제거한다.
      2026-08-14 commit `d6e04f0` 로 14개 전부 제거·배포했다. README 의 호환 페이지 설명도 함께 고쳤다.
- [x] 0초 `meta refresh`, 새 URL canonical, 새 URL로 가는 1:1 링크는 유지한다.
      제거 전후 모두 14개 전부 `content="0; url=<대응 새 URL>"`로 1:1 정확.
- [x] 변경을 배포한 뒤 옛 URL 14개의 live HTML에 `noindex`가 없고 대상 URL이 정확한지 확인한다.
      2026-08-14 11:46 KST 배포 반영 확인. 14/14 가 `200` + `noindex` 없음 + `meta refresh` 1:1 정확 +
      canonical 이 대응 새 URL. 이제 이동 신호는 `meta refresh` + `rel="canonical"` 둘뿐이다.

Server-side `301`/`308`이 가장 권장되지만, 현재 GitHub Pages 제약에서는 0초 `meta refresh`가 공식적으로
인정되는 대안이다. 향후 server redirect를 설정할 수 있는 hosting으로 옮기면 `301`/`308`로 교체한다.

### 2.2 완전히 새로 추가한 URL — 16개

허브 4개:

- `https://droidactor.github.io/apps/`
- `https://droidactor.github.io/ko/apps/`
- `https://droidactor.github.io/manual/`
- `https://droidactor.github.io/ko/manual/`

매뉴얼 12개:

- `https://droidactor.github.io/manual/bt-keyboard/`
- `https://droidactor.github.io/ko/manual/bt-keyboard/`
- `https://droidactor.github.io/manual/bt-ppt/`
- `https://droidactor.github.io/ko/manual/bt-ppt/`
- `https://droidactor.github.io/manual/bt-mouse/`
- `https://droidactor.github.io/ko/manual/bt-mouse/`
- `https://droidactor.github.io/manual/wifi-scout/`
- `https://droidactor.github.io/ko/manual/wifi-scout/`
- `https://droidactor.github.io/manual/ssh-scout/`
- `https://droidactor.github.io/ko/manual/ssh-scout/`
- `https://droidactor.github.io/manual/lgtv/`
- `https://droidactor.github.io/ko/manual/lgtv/`

`yt-downloader`는 제품 URL만 이동했으며 매뉴얼은 추가하지 않았다.

### 2.3 후속 추가한 Tech Notes URL — 14개

허브 2개:

- `https://droidactor.github.io/tech-notes/`
- `https://droidactor.github.io/ko/tech-notes/`

6개 앱의 한·영 기술 노트 12개:

- `https://droidactor.github.io/tech-notes/bt-keyboard/`
- `https://droidactor.github.io/ko/tech-notes/bt-keyboard/`
- `https://droidactor.github.io/tech-notes/bt-ppt/`
- `https://droidactor.github.io/ko/tech-notes/bt-ppt/`
- `https://droidactor.github.io/tech-notes/bt-mouse/`
- `https://droidactor.github.io/ko/tech-notes/bt-mouse/`
- `https://droidactor.github.io/tech-notes/wifi-scout/`
- `https://droidactor.github.io/ko/tech-notes/wifi-scout/`
- `https://droidactor.github.io/tech-notes/ssh-scout/`
- `https://droidactor.github.io/ko/tech-notes/ssh-scout/`
- `https://droidactor.github.io/tech-notes/lgtv/`
- `https://droidactor.github.io/ko/tech-notes/lgtv/`

`yt-downloader`는 Tech Notes 대상에서도 제외했다. 이 14개는 이전 URL이 없는 신규 문서이므로 redirect나
주소 변경 처리는 하지 않고 sitemap, 내부 링크, self canonical로 발견을 알린다.

### 2.4 변경되지 않은 URL — 20개

- 한·영 홈 2개
- 한·영 블로그 허브 2개
- 한·영 블로그 글 16개

이 URL들은 path가 바뀌지 않았으므로 별도의 이동 처리는 필요 없다. 이번 배포에서 내부 링크가 바뀌었으므로
Google이 다시 crawl하면 갱신된 link graph를 읽는다.

### 2.5 경로 개편과 무관한 미색인 URL — 7개 (2026-08-31 발견, 2026-09-05 요청 착수, 2026-09-10 요청 완료)

§2.4 의 "변경되지 않은 URL" 쪽인데도 색인되지 않은 것들이다. 8-31 이 미색인 42개의 전량 목록을 뽑다가
찾아냈고(§6 2026-08-31 성과 2번), **경로 개편으로 생긴 URL 이 아니라서 §4.3 의 어느 우선순위에도
들어 있지 않았다.** 9-02 가 "요청 계획에 넣을지 여부가 아직 결정된 적이 없다" 고 남긴 뒤
**2026-09-05 에 사용자 결정으로 전부 요청 계획에 편입됐다**(§6 2026-09-05 절 5번).

- [x] `https://droidactor.github.io/ko/` — 국문 홈. 2026-09-05 요청 접수
- [x] `https://droidactor.github.io/blog/` — 영문 블로그 허브. 2026-09-05 요청 접수
- [x] `https://droidactor.github.io/ko/blog/` — 국문 블로그 허브. 2026-09-10 요청 접수
- [x] `https://droidactor.github.io/blog/use-android-phone-as-bluetooth-mouse/` — 2026-09-10 요청 접수
- [x] `https://droidactor.github.io/ko/blog/use-android-phone-as-bluetooth-mouse/` — 2026-09-10 요청 접수
- [x] `https://droidactor.github.io/blog/use-android-phone-as-powerpoint-remote/` — 2026-09-10 요청 접수
- [x] `https://droidactor.github.io/ko/blog/use-android-phone-as-powerpoint-remote/` — 2026-09-10 요청 접수

**2026-09-10 에 남은 5개를 전부 요청해 이 묶음이 닫혔다.** 5/5 가 `색인 생성 요청됨 — URL이 우선순위
크롤링 대기열에 추가되었습니다` 이고 할당량 초과는 0건이다. 요청 전 live 사전 점검도 5/5 통과
(`200` + self canonical + `noindex` 없음 + live `sitemap.xml` 의 `<loc>` 포함).

**요청 시점 단계가 갈렸다 — `/ko/blog/` 만 `크롤링됨 - 현재 색인이 생성되지 않음` 이고 블로그 글 4개는
`Google에는 아직 알려지지 않은 URL입니다`(모든 세부값 `해당사항 없음`) 다.** 8-31 이 이 4개를 미색인
42 목록에 넣은 것과 어긋나지 않는다 — 그 42 는 `sitemap 70 - 색인 28` 의 차집합이라 "색인 아님" 만
뜻하고 사유 단계를 말하지 않았다(§6 2026-08-31 성과 2번).

**`/ko/blog/` 의 세부값이 이 묶음에서 처음 읽혔다 — GSC 가 보고하는 차단 신호는 전부 정상이다.**
최근 크롤링 `2026. 8. 8. 오전 11:46:25`, 크롤링 허용 / 페이지 가져오기 / 색인 생성 허용 = 예 / 성공 /
예, 사용자 선언 표준 URL = self, **Google에서 선택한 표준 URL = `검사된 URL`**(self 일치). **즉 표준
URL 은 이미 인정됐는데 색인만 안 된 상태이고, 그 크롤은 경로 개편 배포(8-14)보다 6일 앞선다** —
8-14 이후 이 URL 이 재크롤된 기록은 없다. **배제되는 것은 여기까지다** — robots·fetch·`noindex`·
canonical 은 정상이지만 본문·링크·신뢰 신호 쪽은 GSC 가 URL 단위로 말하지 않으므로 배제되지 않았다
(§6 2026-08-24 성과 4번이 같은 해석을 추정으로 격리해 뒀다).

두 가지 비대칭이 이 묶음의 성격이었다. **블로그 16개 중 12개는 색인됐는데 `bt-mouse`·`bt-ppt` 두
제품의 글만 한·영 모두 빠졌고**(이쪽은 2026-09-10 에도 그대로다), **영문 홈 `/` 는 색인됐는데 `/ko/` 는
아니었다.** **원인은 확인하지 않았다.**
**후자는 2026-09-10 에 닫혔다 — `/ko/` 가 색인됐다**(§6 2026-09-10 절 6번). 9-05 요청 이후이며 이 절의
`[x]` 는 "요청 접수" 를 뜻하므로, **`/ko/` 를 미색인 후보로 되살리지 않는다.**

### 2.6 이 절 이후에 추가된 URL — 여기가 아니라 §9·§10·§15·§16·§17 에 있다

§2.1~2.5 는 2026-08-14 경로 개편 시점의 범위다. 그 뒤에 늘어난 URL 은 배포 단위로 절을 따로 두었고,
이 절은 중복 목록을 두지 않고 가리키기만 한다. **URL 총수를 셀 때는 반드시 이쪽까지 더한다.**

| 절 | 배포 | URL 수 | 누적 sitemap `<loc>` |
|---|---|---|---|
| §2.1~2.4 | 2026-08-14 경로 개편 | 개편 30 + 미변경 20 + Tech Notes 14 | 64 |
| §9 | 2026-08-14 Google TV HID 조사 글 | 2 | 66 |
| §10 | 2026-08-16 bt-keyboard 상세 노트 | 4 | 70 |
| §15 | 2026-09-13 로마 숫자 변환기 제품·설명서 | 4 | 74 |
| §16 | 2026-09-14 환율 계산기·넘패드 제품 | 4 | 78 |
| §17 | 2026-09-15 단위 변환기 제품 | 2 | **80**(현재 live) |

§2.5 의 7개는 새 URL 이 아니라 §2.4 안에서 미색인이던 것들이므로 이 표에 더하지 않는다.
`currency`·`numpad` 4개는 2026-09-14 배포로 §16 이 됐다.
**§18(2026-09-15 출시 반영)은 새 URL 이 0개이므로 이 표에 행을 더하지 않는다** — sitemap 은 80 그대로다.

## 3. 배포 확인

2026-08-14에 기준 변경 `f300f26`의 다음 live 응답을 확인했다. 아래 `noindex` 제거와 ytdl `lastmod`
정정은 별도 후속 commit으로 배포해야 한다.

- `/apps/` → `200`, self canonical 정상
- `/manual/` → `200`, self canonical 정상
- `/bt-keyboard/` → `200`, canonical `/apps/bt-keyboard/`, `meta refresh` 0초 정상; 현재 `noindex`는 제거 필요
- `/sitemap.xml` → `200`, 50개 canonical URL(Tech Notes 배포 전 live 기준 = 개편 30 + §2.4 미변경 20.
  §1 의 "합계 50개"(개편 30 + Tech Notes 20)와 숫자만 같고 뜻이 다르다)

Search Console 작업 전 아래 항목을 다시 확인한다.

아래는 2026-08-14 11:15~11:40 KST 에 실제 live 응답으로 확인한 결과다.

- [x] GitHub Pages 배포가 기준 변경 `f300f26`을 포함한다.
      사이트 저장소 HEAD `ffb3c23`(6종 앱 한·영 기술 노트 추가), `f300f26`은 그 조상. working tree clean.
- [x] 옛 URL의 `noindex` 제거와 ytdl `lastmod` 정정 후속 commit을 기록한다: **`d6e04f0`**.
      ytdl `lastmod`는 `ffb3c23`에 이미 반영돼 있었고(아래 sitemap 항목), `noindex` 제거가 `d6e04f0`이다.
- [x] GitHub Pages live 배포가 기록한 후속 commit까지 반영한다.
      push 후 약 25초 만에 14개 전부 반영을 확인했다(2026-08-14 11:46 KST).
- [x] 새 URL 또는 이동 후 canonical URL 44개가 모두 `200`이다. — 44/44 `200`.
- [x] 옛 제품 URL 14개가 각각 대응하는 새 URL 하나로 직접 이동하며 `noindex`가 없다. — 14/14 충족.
- [x] 새 페이지에 `noindex`가 없다. — 44개 전부 없음.
- [x] 새 페이지의 canonical과 `hreflang`이 `/apps/`, `/manual/`, `/tech-notes/` 구조를 사용한다.
      44/44 self canonical 일치, 44/44 `hreflang` 2개 이상.
- [x] `robots.txt`가 `sitemap.xml`을 계속 가리킨다. — `Sitemap: https://droidactor.github.io/sitemap.xml`.
- [x] Google 소유권 확인 파일 `googlec8773d84a9d25688.html`을 삭제하지 않았다. — `200`.

## 4. Search Console에서 수행할 작업

### 4.1 속성

- [x] 기존 URL 접두어 속성 `https://droidactor.github.io/`를 연다. — 2026-08-14 수행.
- [x] 소유권이 계속 확인됨인지 본다. — 속성이 정상 열리고 URL 검사·색인 요청이 모두 수락됐다.
- [x] 새 속성을 만들지 않는다. — 만들지 않았다.
- [x] 주소 변경 도구를 실행하지 않는다. — 실행하지 않았다.

### 4.2 Sitemap

**최신 실측은 §4.2.2 의 2026-09-12 삭제·재제출이다.** 사용자 지시로 Search Console sitemap 행을
삭제하고 같은 경로를 재제출했으며, 조회 시점에 API 는 `lastSubmitted` 만 갱신되고 `isPending: true` 가
유지됐다. **다만 상태 판정 자체는 §4.2.1 에서 바뀌지 않았다** — 여전히 미처리이고 원인은 미확인이다.
9-12 관측은 제출 8분 뒤 값이라 판별력이 없다(§4.2.2). **직전 기준선은 §4.2.1 의 2026-09-10 API
실측이다**(`isPending: true` 로 미처리 상태 확인).
이전 기록의 "8-13 읽기 실패 확정", "재시도 없음 확정", "백오프가 원인" 이라는 해석은 철회한다.
당시 화면에서 읽은 날짜·문구는 관찰 기록으로 보존하되, 접근 시도 이력이나 원인으로 확대 해석하지 않는다.

Google은 URL 이동 후 새 canonical URL을 담은 sitemap을 제출하라고 권장하며, 많은 새 URL의 crawl 요청에는
개별 URL 검사보다 sitemap이 적합하다고 안내한다.

- [x] `https://droidactor.github.io/sitemap.xml`의 실제 내용이 64 URL인지 확인한다.
      2026-08-14 실측: `200`, `Content-Type: application/xml`, BOM 없음, well-formed,
      root `urlset` + `xmlns=http://www.sitemaps.org/schemas/sitemap/0.9`, `<loc>` 64개.
      Googlebot UA 로도 `200`.
- [x] sitemap에는 옛 제품 URL 14개가 없고 새 canonical URL만 있는지 확인한다. — 옛 URL 0건.
- [x] 중요한 내용·구조·link가 바뀐 모든 URL의 `lastmod`가 실제 최종 변경일인지 양방향으로 확인한다.
      **실측: 64개 전부 `2026-08-14` 단일 값이다.** 이번 배포로 전역 탐색 링크가 모두 바뀌었으므로
      과다 표기는 아니나, 앞으로 부분 변경 때 같은 방식으로 전량 갱신하면 신호가 무뎌진다.

      **위 `[x]` 는 2026-08-14 시점 기록이다. 그 뒤 8-15·8-16 배포에서 실제로 신호가 무뎌졌고,
      2026-08-17 에 26건을 정정했다(§10.1).** 우려했던 일이 그대로 일어난 것이므로 이 항목은
      "한 번 끝낸 점검" 이 아니라 **배포마다 반복하는 점검**으로 취급한다.

      **그 반복이 2026-08-18 에 또 적중했다(§11.1).** 8-17 의 26건 정정은 `9a397f5` 로 배포됐고,
      8-18 에 `lastmod` 10건과 `dateModified` 20건을 고쳤다. **원인은 두 갈래이고 섞으면 안 된다** —
      `lastmod` 10건과 블로그 `dateModified` 10건은 `9a397f5` 가 새로 만든 어긋남이지만, 나머지
      `dateModified` 10건(매뉴얼 4 + Tech Notes 6)은 `a795df6`·`842eb3d`·`ec0f0cf` 때부터 있던
      어긋남이다. **§10.1 이 sitemap `lastmod` 만 고치고 짝이 되는 페이지 `dateModified` 를 손대지
      않아 남은 것**이고 `9a397f5` 는 그 반쪽을 배포해 드러냈을 뿐이다. 그래서 규칙을 하나 더 둔다 —
      **`lastmod` 를 고칠 때는 같은 URL 의 페이지 `dateModified`·화면 표시 날짜를 짝으로 함께 본다.**
      그 시점의 분포는
      `2026-08-14` 32 + `2026-08-15` 8 + `2026-08-16` 8 + `2026-08-18` 22 = 70 이었다.
      대조는 sitemap `lastmod` ↔ **그 파일의 마지막 "본문 변경" 배포일**로 한다 — 날짜 필드만 고친
      메타데이터 커밋은 콘텐츠 변경이 아니므로 날짜를 앞으로 당기지 않는다(§11.1).

      **세 번째 적중이 2026-08-19 에 나왔다(§12).** 커밋 `a806fbd` 가 홈 2개의 본문을 바꿨는데
      `lastmod` 가 `2026-08-18` 로 남아 있어 `2026-08-19` 로 고쳤다. **이로써 이 점검은 8-17·8-18·
      8-19 세 배포 연속으로 적중했다** — 예외적 사고가 아니라 배포 절차에 빠져 있는 단계라는 뜻이다.
      현재 분포는 `2026-08-14` 32 + `2026-08-15` 8 + `2026-08-16` 8 + `2026-08-18` 20 +
      `2026-08-19` 2 = 70 이다. (분포 수치는 배포마다 낡는다. 절 번호를 박으면 절이 늘 때마다 이
      문장도 고쳐야 하므로 — §6 8-15 절이 세운 것과 같은 이유로 — **최신값은 언제나 §6 의 가장
      마지막 날짜 절과 가장 마지막 배포 절에서 본다.** 앞선 배포 절에 적힌 분포는 그 시점의
      기록이다.)
- [x] `/apps/yt-downloader/`, `/ko/apps/yt-downloader/`를 포함해 이번에 전역 탐색 링크가 바뀐 URL의
      `lastmod`가 실제 변경일 `2026-08-14`인지 확인하고 배포한다. — 두 URL 모두 `2026-08-14`.
- [x] Search Console의 기존 sitemap 행을 **삭제하지 않는다.** 삭제하면 이전 처리 이력만 잃는다.
      삭제하지 않았다.

      **2026-09-12 — 이 항목은 뒤집혔다. 사용자 지시로 행을 삭제하고 재제출했다(§4.2.2).**
      위 `[x]` 와 "삭제하지 않았다" 는 **8-14~9-10 시점 기록**이며 현재 상태가 아니다. 이 항목이
      경고한 비용은 실제로 치러졌다 — **8-13 제출 행과 그 30일치 처리 이력은 GSC 에서 사라졌고,
      그 기준선은 이제 아래 표 계열과 §4.2.1 에만 남는다.**
- [x] 기존 행의 상태·마지막으로 읽은 날짜·발견된 페이지 수를 기록한다.

      **2026-08-14 11:40 KST 기록 — 행이 1개뿐이다.**

      | Sitemap | 유형 | 제출 | 마지막으로 읽은 날짜 | 상태 | 발견된 페이지 |
      |---|---|---|---|---|---|
      | `/sitemap.xml` | 알 수 없음 | 2026-08-13 | (없음) | **가져올 수 없음** | 0 |

      마지막으로 읽은 날짜가 비어 있다 = 성공한 사이트맵 읽기가 보고되지 않았다.
      단 위 실측대로 sitemap·robots·Content-Type·Googlebot 접근은 전부 정상이므로 사이트 쪽
      결함은 확인되지 않으며, 제출 다음날의 미수집 상태로 본다.

      **2026-08-15 재확인 — 목록 행은 그대로이고, 세부 화면에서 위 해석이 정정됐다.**

      | 항목 | 목록 화면 | 세부 화면(`/sitemap.xml` 행 클릭) |
      |---|---|---|
      | 마지막으로 읽은 날짜 | (없음) | **2026-08-13** |
      | 상태 | 가져올 수 없음 | 사이트맵을 읽을 수 없음 |
      | 발견된 페이지 | 0 | 0 |

      당시에는 세부 화면의 날짜를 "8-13 읽기 시도 후 실패" 로 해석했으나, **이 단정은 철회한다**
      (§4.2.1). 화면의 날짜·상태만으로 과거 접근 시도나 재시도 이력을 확정할 수 없다.
      당시 live 실측은 `200` + `application/xml` + well-formed + `<loc>` 66개였다.

      **2026-08-19 정정 — 이 상태를 "개편 배포 전후의 혼란" 으로 설명하던 서술을 지웠다.** 시점이
      맞지 않는다. `f300f26` 은 **2026-08-14 09:21 KST** 로 제출 하루 뒤이고, 8-13 에 공개돼 있던
      파일은 개편 이전의 34 URL sitemap 이다. 그 파일은 `c48305c`(8-08) 이래 `<loc>` 34개로
      구조가 그대로였고, 당일 sitemap 을 손댄 두 커밋(`9fcb906` 08:29, `19a9493` 08:41)이 바꾼 것도
      각각 `lastmod` 두 줄뿐이다. 이 이력으로 개편을 원인으로 볼 근거는 없지만, Google 쪽 일시
      오류나 백오프가 원인이라고 확정할 수도 없다. **원인은 미확인이다**(§4.2.1).

      **2026-08-17 재확인 — 목록 화면 값이 8-15 와 같다.**

      | 항목 | 목록 화면 | 세부 화면 |
      |---|---|---|
      | 제출 | 2026. 8. 13. | (미확인) |
      | 마지막으로 읽은 날짜 | (없음) | (미확인) |
      | 상태 | 가져올 수 없음 | (미확인) |
      | 발견된 페이지 | 0 | (미확인) |

      **세부 화면은 아직 열어 보지 않았다.** 8-15 가 힘들여 정정한 교훈이 바로 "목록의 빈칸을
      단독으로 해석하지 말라" 이므로, 여기서 "마지막 읽기는 8-13 그대로" 라고 단정하지 않는다.
      세부 화면도 확인해 날짜·오류 표시를 기록하기로 했다. **단 세부 날짜만으로 재시도 유무를
      확정할 수는 없다**(§4.2.1). 당시 재제출 판정일 8-20 은 이후 8-24 사용자 결정으로 취소됐다.

      **2026-08-19 live 재진단 — 검사한 응답에서 사이트 쪽 결함은 확인되지 않았다.** 일반 UA·Googlebot UA 둘 다
      `200`, `Content-Type: application/xml`, BOM 없음(`3c 3f 78`), XML well-formed,
      root `urlset` + 정규 xmlns, `<loc>` 70개(중복 0, 전부 https 절대 URL), `http://` 접근은 301
      1회로 https 도달(사슬 1단계), `robots.txt` 는 `200` + `Allow: /` + `Sitemap:` 정확.
      이 응답과 당시 링크·색인 수만으로 크롤 예산이나 백오프를 원인으로 확정할 수 없다.
      Googlebot UA 를 지정한 로컬 요청은 Google 발신 요청과도 다르다. `/tech-notes/` 가 8-14 요청
      3분 뒤 실제로 크롤됐다는 관찰은 보존하되, 수동 요청이 크롤 예산을 우회한다는 설명은 철회한다.

      **2026-08-19 세부 화면 확인 — 8-17 이 미룬 확인을 마쳤고, 표시 값은 그대로다.**

      | 항목 | 목록 화면 | 세부 화면 |
      |---|---|---|
      | 제출 | 2026. 8. 13. | — |
      | 마지막으로 읽은 날짜 | (없음) | **2026-08-13** |
      | 상태 | 가져올 수 없음 | 사이트맵을 읽을 수 없음 |
      | 발견된 페이지 | 0 | 0 |

      **8-15 세부 화면 값과 완전히 같다.** 이것을 "재시도가 없었다" 로 확정하고 재제출이 백오프를
      끊는다고 설명한 당시 판단은 근거가 부족했다. **표시 값이 같다는 관찰만 유효하다**(§4.2.1).

      **재제출 후보는 2026-08-24 사용자 결정으로 취소됐다**(아래 8-20 항목과 §7).
      당시 기록을 재제출 재개의 근거로 인용하지 않는다.

      같은 화면에서 확인한 속성 정보: `robots.txt` **유효**, **크롤링 요청 213개(지난 90일)**,
      계정에 속성 추가됨 **2026-08-06**. 크롤 자체는 정상적으로 일어나고 있다.

      **2026-08-24 재확인 — 목록·세부 화면이 8-15·8-19 와 한 글자도 다르지 않다.**

      | 항목 | 목록 화면 | 세부 화면 |
      |---|---|---|
      | 제출 | 2026. 8. 13. | — |
      | 마지막으로 읽은 날짜 | (없음) | **2026-08-13** |
      | 상태 | 가져올 수 없음 | 사이트맵을 읽을 수 없음 |
      | 발견된 페이지 | 0 | 0 |

      **제출 후 11일째, 성공한 사이트맵 읽기가 보고되지 않았다.** 같은 날 live 실측도 8-19 와 같다 —
      일반 UA·Googlebot UA 둘 다 `200` + `Content-Type: application/xml` + 37,555 bytes, BOM 없음
      (`3c 3f 78`), XML well-formed, root `urlset` + 정규 xmlns, `<loc>` 70개(중복 0, 전부 https 절대
      URL), `robots.txt` `200` + `Allow: /` + `Sitemap:` 정확. **검사한 응답에서 결함은 확인되지 않았다.**
      같은 화면에서 읽은 속성 정보는 `robots.txt` **유효**, **크롤링 요청 302개(지난 90일)**
      (8-19 의 213개에서 늘었다), 계정에 속성 추가됨 **2026-08-06**.

      같은 기간에 Googlebot 이 사이트를 크롤하고 있다(아래 §6 2026-08-17 의 색인 22개).
      당시 검사한 응답에서 robots·접근성 결함은 확인되지 않았다.

      **2026-09-10 재확인 — 목록 화면이 8-14·15·17·19·24 와 또 같다. 제출 후 28일째다.**

      | 항목 | 목록 화면 |
      |---|---|
      | 유형 | 알 수 없음 |
      | 제출 | 2026. 8. 13. |
      | 마지막으로 읽은 날짜 | (없음) |
      | 상태 | **가져올 수 없음** |
      | 발견된 페이지 | 0 |

      **그런데 같은 날 Page indexing 드릴다운은 `/sitemap.xml` 이 `2026-08-31` 에 크롤됐다고 말한다.**
      이 파일이 `크롤링됨 - 현재 색인이 생성되지 않음` 10건 목록에 들어 있고 최종 크롤링 칸이
      `2026-08-31` 이다(§6 2026-09-10 절 4번). **두 화면이 서로 다른 것을 말하는 것이며, 이것을
      "sitemap 이 드디어 수집됐다" 로 읽지 않는다** — 일반 크롤(URL 을 페이지로 가져가는 경로)과
      sitemap 처리 파이프라인은 별개다. **이날 후속 진단에서 세부 화면도 열었고 날짜는 8-13 그대로였다.**
      URL 검사에서는 최근 크롤링 `2026-08-31 13:44:44 KST`, Googlebot 스마트폰, 크롤링 허용 `예`,
      페이지 가져오기 `성공` 을 확인했다. **최근 크롤링 날짜는 최초 접근일이나 사이트맵 처리 시작일을
      뜻하지 않는다.** 이어서 실제 URL 테스트와 API 조회를 수행한 결과는 §4.2.1 에 기록한다.

      **2026-09-12 — 표 계열이 여기서 끊긴다. 아래는 같은 행이 아니라 삭제 후 새로 만든 행이다.**

      | 항목 | 목록 화면 |
      |---|---|
      | 유형 | 알 수 없음 |
      | 제출 | **2026. 9. 12.** |
      | 마지막으로 읽은 날짜 | **(미확인 — 화면에서 읽지 않았다)** |
      | 상태 | **가져올 수 없음** |
      | 발견된 페이지 | 0 |

      **위 값을 8-14~9-10 행과 "변화 없음" 으로 대조하면 안 된다** — 30일 묵은 행과 방금 만든 행의
      비교라 성립하지 않는다. `마지막으로 읽은 날짜` 는 이 세션이 화면에서 확인하지 않았으므로
      **비어 있었다고 추정해 채우지 않는다**(8-17 에 같은 추정으로 한 번 틀렸다). 다음 세션이 이 칸을
      직접 읽어 표를 이어 쓴다. 경위와 API 값은 §4.2.2 에 있다.

- [x] Tech Notes 배포 후 마지막으로 읽은 날짜가 2026-08-16 이후이고 발견된 페이지가 70이면 추가
      제출하지 않는다. — 2026-08-17 현재 **성공한 사이트맵 읽기가 보고되지 않아** 판정 불가. 이월. (세부 화면을 열지
      않았으므로 "마지막 읽기 = 8-13" 으로 단정하지 않는다 — 같은 절이 8-15 에 정정한 해석이다.)
      (기준값이 8-14·64 에서 8-16·70 으로 바뀐 이유는 §10 의 4개가 8-16 에 더 배포됐기 때문이다.)
      **2026-08-24 에 이 항목은 판정 대상이 소멸해 닫혔다.** 아래 항목대로 재제출 자체를 하지 않기로
      했으므로 "추가 제출할지" 를 가릴 조건문이 무의미해졌다. 8-24 실측값은 여전히 마지막 읽기
      2026-08-13 · 발견 0 이므로 조건은 미충족이었다(§6 2026-08-24 1번).
- [x] 7일 뒤에도 개편 이전 sitemap 내용(34 URL)을 기준으로 머물거나 `가져올 수 없음`이면, 기존 행을 삭제하지 않은 채
      같은 `sitemap.xml`을 한 번 제출한다.
      **판정 기준일 = 2026-08-20.** 2026-08-14·15·17 에는 규칙대로 재제출하지 않았다.

      **2026-08-24 — 이 항목은 "재제출하지 않는다" 로 닫혔다. 사용자 결정이다.**
      판정 기준일(8-20)이 나흘 지난 8-24 에도 `가져올 수 없음` 이므로 위 규칙대로라면 1회 재제출할
      차례였다. 그 자리에서 사용자가 **"sitemap 재제출은 하지말것. 해 봐도 늘 동일했고, 몇번째
      반복 중."** 이라고 지시했다. 이 속성에서 재제출은 이미 여러 차례 시도된 조치이고 상태가 한 번도
      바뀌지 않았다는 것이 근거다. **따라서 8-20 판정은 실행되지 않은 채 종결되며, 다음 세션은 이
      항목을 미결로 되살리지 않는다.** 재개하려면 사용자의 새 지시가 있어야 한다(§7 에도 적었다).

      **2026-09-12 — 그 "사용자의 새 지시" 가 나왔고 재제출이 실행됐다.** 다만 이 항목의 문면
      ("기존 행을 삭제하지 않은 채 같은 `sitemap.xml` 을 한 번 제출한다")과도 다르다 — **9-12 는 행을
      삭제한 뒤 제출했다**(§4.2.2). 8-20 판정이 되살아난 것이 아니라 별개 지시로 실행된 것이므로,
      이 항목은 **종결 상태 그대로 둔다.**
- [x] 이후 반복 제출하지 않고 상태 변화를 기다린다. — 재제출 자체를 하지 않으므로 그대로 유효하다.
      sitemap 은 상태 변화를 **관찰만** 하는 대상으로 남는다.

      **2026-09-12 — "재제출 자체를 하지 않으므로" 라는 전제는 더 이상 유효하지 않다**(§4.2.2).
      그러나 **결론은 바뀌지 않는다** — 9-12 제출 이후 이 문서는 다시 관찰만 한다. 반복 제출은
      사용자의 명시 지시가 있을 때만 하며, 그 해제는 9-12 지시에 한한 1회분이었다(§7).

**참조 사이트맵이 보고되지 않았다는 실측(2026-08-15).** 새 URL을 URL 검사하면
`검색 > Sitemaps` 칸이 **`감지된 참조 사이트맵이 없습니다`** 로 나오고, 미발견 URL은 `참조 페이지`도
`감지된 페이지 없음`이다. 내부 링크와 수동 색인 생성 요청으로 크롤링·색인이 진행되는 것은 관찰했으나,
이 두 가지가 유일한 발견 경로이거나 사이트맵 미처리가 미발견 URL 의 원인이라고 확정하지 않는다.

정상 목표:

- 상태: `성공`
- 발견된 페이지: **`70`** (2026-08-16 배포 기준. 8-14 시점 목표였던 64 는 그 뒤 §9 의 2개와 §10 의
  4개가 더해져 갱신됐다.)
- sitemap에 포함된 URL: 새 canonical URL만 표시

근거:

- [Google: Sitemap 작성·제출](https://developers.google.com/search/docs/crawling-indexing/sitemaps/build-sitemap)
- [Google: URL 검사 및 다수 URL 재색인](https://support.google.com/webmasters/answer/9012289)

#### 4.2.1 2026-09-10 — 실제 URL 테스트·API 확인 및 판단 정정

**현재 확인된 상태는 사이트맵 제출 건의 미처리(`isPending: true`)이고, 그 원인은 미확인이다.**
열린 Chrome 의 기존 Search Console 계정과 Google 공식 APIs Explorer 로 조회했다. API 권한은
`webmasters.readonly` 만 선택했다. 아래 응답의 `200` 은 API 조회 성공이며, 사이트맵 처리 성공과 다르다.

`sitemaps.get` 입력: `siteUrl=https://droidactor.github.io/`,
`feedpath=https://droidactor.github.io/sitemap.xml`. 응답 전체:

```json
{
  "path": "https://droidactor.github.io/sitemap.xml",
  "lastSubmitted": "2026-08-12T23:52:31.587Z",
  "isPending": true,
  "isSitemapsIndex": false,
  "warnings": "0",
  "errors": "0"
}
```

- 제출 시각은 **2026-08-13 08:52:31.587 KST** 다. `lastDownloaded`, `type`, `contents` 는 응답에 없다.
- Google 의 정의상 `isPending: true` 는 **아직 처리되지 않음**이다. `errors: "0"` 은 기록된 오류 수이며,
  미처리 상태에서 XML 검증 통과를 뜻하지 않는다. 과거 접근 시도·실패·재시도 유무도 이 응답만으로 알 수 없다.
- **09-10 10:03:02 KST 실제 URL 테스트**: Google 검사 도구 스마트폰, 크롤링 허용 `예`, 페이지 가져오기
  `성공`, 색인 생성 허용 `예`. 직접 조치 보고서는 **감지된 문제 없음**이었다.
- 같은 날 live 파일은 일반 UA·Googlebot UA 모두 `200` + `application/xml`, 리디렉션 0,
  37,555 bytes. XML 파싱 성공, 정규 `urlset` namespace, URL 70개·중복 0, 저장소 파일과 동일했다.
  `robots.txt` 는 `200`, `Allow: /`, 사이트맵 주소 정확. **현재 검사에서 수정할 결함은 발견되지 않았다.**

**이전 판단의 정정:**

- "8-13 에 읽기를 시도해 실패", "날짜가 그대로이므로 재시도 없음", "재시도 백오프가 원인" 이라는
  단정을 철회한다. 과거 화면의 날짜·상태는 사실이지만 그 해석을 뒷받침할 시도 이력은 확보하지 못했다.
- "낮은 크롤링 수요" 는 공식 문서가 열거한 가능성일 뿐, 이 사이트의 원인으로 확인되지 않았다.
  실제 페이지 색인이 진행되는 사실도 사이트맵 미처리의 이유를 설명하지 못한다.
- **8-31 은 최근 일반 크롤링 시각**이다. 제출 후 18일 만의 첫 접근이라고 볼 수 없고, 그날부터
  사이트맵 처리가 시작됐다고 볼 수도 없다. Google 내부 장애·처리 중단·보고 지연 중 무엇인지도 미확인이다.

**논의한 후속 조치와 권한:**

- 읽기 전용 API 조회는 **완료**했다. 더 기다린 뒤 수행할 미결 작업으로 되살리지 않는다.
- **9-17 까지 한 주 관찰**은 제안한 점검 시점이며 Google 공식 처리기한이나 사용자 확정 일정이 아니다.
  자동 점검은 설정하지 않았다. API 조회는 이 대기를 전제로 하지 않는다.
- 이후에도 미처리라면 위 응답·실제 URL 테스트·일반 크롤 기록을 첨부한 Search Central 도움말 커뮤니티
  문의를 후보로 논의했다. **문의는 작성·게시하지 않았고, 게시 승인을 받은 것도 아니다.**
- 사용자는 재제출의 효과를 질문한 뒤 **"문서만 update"** 를 지시했다. **재제출은 실행하지 않았으며,
  §7 의 기존 금지는 유효하다.** 설명 요청을 재제출 승인으로 해석하지 않는다.

재제출은 같은 URL 의 가져오기·처리를 다시 요청하는 것이다. Google 은 가져오기/파싱 실패 후 재시도가
중단된 경우 문제를 고친 뒤 재제출하도록 안내하지만, **현재 `isPending: true` 인 건이 그 경우인지는
확인되지 않았다.** 재제출에 따른 우선순위 상승·즉시 처리·대기 순서 초기화는 보장되거나 공개되어 있지 않다.
향후 사용자가 별도로 재제출을 지시하는 경우에만, 위 응답을 기준으로 다음 차이를 확인한다.

| 재제출 후 관찰할 변화 | 해석 |
|---|---|
| `lastSubmitted` 만 갱신 | 새 제출 기록. 처리 진전은 확인되지 않음 |
| `lastDownloaded` 가 생김 | 사이트맵 다운로드 기록이 생김. 처리 성공은 별도 확인 |
| `isPending: false`, `type`·`contents` 가 나타남 | 처리 결과를 확인할 수 있음. `errors`·보고서 성공 여부·URL 70개까지 대조 |
| 계속 `isPending: true`, 다운로드·형식·내용 필드 없음 | 미처리 상태 지속. 원인이나 대기 순서는 여전히 알 수 없음 |

근거: [Sitemaps 리소스 정의](https://developers.google.com/webmaster-tools/v1/sitemaps),
[읽기 전용 get](https://developers.google.com/webmaster-tools/v1/sitemaps/get),
[제출·재제출 및 처리 일정](https://support.google.com/webmasters/answer/7451001),
[Google 공식 지원 경로](https://developers.google.com/search/help).

#### 4.2.2 2026-09-12 — 행 삭제 후 재제출 실행, 새 기준선

**사용자 지시로 §7 의 8-24 재제출 금지를 해제하고, Search Console 의 sitemap 행을 삭제한 뒤 같은
경로를 재제출했다.** §4.2.1 이 "향후 사용자가 별도로 재제출을 지시하는 경우" 를 위해 남겨둔 비교
기준을 실제로 쓴 첫 사례다. **삭제 여부는 선택지로 제시해 사용자가 골랐고, 삭제를 포함한 경로였다.**

**그때 메인 세션이 사용자에게 설명한 "삭제를 생략하면 `lastSubmitted` 기준선이 보존된다" 는 틀렸다.**
재제출하면 삭제 여부와 무관하게 `lastSubmitted` 는 갱신된다. 삭제하지 않을 때 남는 것은 그 값이 아니라
**행의 누적 처리 이력**(`lastDownloaded`·`type`·`contents` 의 이전 상태 = §4.2 가 "삭제하면 이전 처리
이력만 잃는다" 고 적은 그것)이다. 덧붙여 **"같은 경로 재제출은 덮어쓰기가 된다" 자체가 이 세션이
검증하지 못한 주장이다** — 삭제 경로를 택해 확인 기회가 사라졌고 공식 문서로도 확인하지 않았다.
**다음 세션은 이 두 문장을 근거로 쓰지 않는다.**

**삭제로 실제로 잃은 것:** §4.2 가 8-14·15·17·19·24·9-10 여섯 시점에 걸쳐 기록한 그 행은 GSC 에서
사라졌다. **8-13 제출과 30일치 미처리 이력을 화면에서 다시 확인할 방법은 없으며, 그 기준선은 이제
§4.2 의 표 계열과 §4.2.1 의 API 응답에만 남는다.** 다음 세션이 GSC 에서 8-13 이력을 찾지 못하더라도
"문서가 틀렸다" 나 "GSC 가 리셋됐다" 로 읽지 않는다 — 이 세션이 지운 것이다.

실행 절차(전부 열린 Chrome 의 기존 Search Console 세션, `/uia` lease 경유):
행 클릭 → 세부 화면 → `옵션 더보기...` → `사이트맵 삭제` → 확인 대화상자 `삭제`
(토스트 `사이트맵이 삭제되었습니다`, 목록 `총 0행 중 0~0` 확인) → 입력창에 `sitemap.xml`
→ `제출` (대화상자 `사이트맵이 제출됨`).

**삭제 직후 재제출까지 한 세션에서 끝냈다.** 삭제만 하고 멈추면 속성에 sitemap 이 하나도 없는 상태로
남으므로, 경로를 확정하기 전에는 삭제에 손대지 않았다.

**API 조회 시점은 사용자가 골랐다** — 메인이 "문서 기록만 먼저 하고 며칠 뒤 조회 / 지금 바로 조회 /
기록하지 않고 종료" 를 제시했고 사용자가 즉시 조회를 선택했다. 아래 값이 판별력을 갖지 못하는 것은
그 선택의 결과가 아니라 **제출 직후라는 시점 자체 때문**이다.

`sitemaps.get` 응답 전체(입력은 §4.2.1 과 동일). **제출 `2026-09-12 15:30:07.930 KST` → 조회 응답 확인
`15:38 KST`, 약 8분 뒤다**(캡처 파일 타임스탬프 기준):

```json
{
  "path": "https://droidactor.github.io/sitemap.xml",
  "lastSubmitted": "2026-09-12T06:30:07.930Z",
  "isPending": true,
  "isSitemapsIndex": false,
  "warnings": "0",
  "errors": "0"
}
```

§4.2.1 기준선과의 대조. **단, 행을 삭제·재생성했으므로 오른쪽 열은 같은 행의 추이가 아니라 8분 된
새 행의 값이다** — "변화 없음" 은 동일 객체의 무변화가 아니라 **비교가 성립하지 않는다**는 뜻이다:

| 필드 | 2026-09-10 (30일 된 행) | 2026-09-12 (8분 된 새 행) | 판정 |
|---|---|---|---|
| `lastSubmitted` | `2026-08-12T23:52:31.587Z` | `2026-09-12T06:30:07.930Z` | **갱신**(= 2026-09-12 15:30:07.930 KST). 새 제출이 기록됐다 |
| `isPending` | `true` | `true` | 제출 8분 시점 — `true` 가 당연, 정보량 없음 |
| `lastDownloaded` | 없음 | 없음 | 제출 8분 시점 — 값 없음이 당연, 정보량 없음 |
| `type`·`contents` | 없음 | 없음 | 제출 8분 시점 — 값 없음이 당연, 정보량 없음 |
| `warnings`·`errors` | `"0"`·`"0"` | `"0"`·`"0"` | 새 행의 초기값. 대조 대상이 아님 |

**이 관측은 §4.2.1 표의 네 갈래 중 어느 쪽도 가르지 못한다.** 네 갈래는 배타적이지 않아서, 이번 값은
첫째("`lastSubmitted` 만 갱신")와 넷째("계속 `isPending: true`, 다운로드·형식·내용 필드 없음")를
**동시에 충족**한다. 게다가 **행을 삭제한 이상 `lastSubmitted` 갱신은 필연이어서 첫째 갈래는 판별력을
잃었다.** 따라서 "첫째 갈래에 해당한다" 를 소득으로 기록하지 않는다 — **판별은 며칠 뒤 재조회로
미뤄진 상태이고, 오늘 진행된 판별은 하나도 없다.**

확인된 사실은 **삭제·재제출이 Google 쪽에 기록됐다는 것까지**다. 목록 화면의 제출일도
`2026. 8. 13.` → `2026. 9. 12.` 로 바뀌었다.

**재제출이 처리를 앞당겼는지는 이 시점에 알 수 없다.** 제출 8분 뒤 조회라 위 표의 빈 필드들은 전부
당연한 값이다. **이 응답을 "재제출도 실패했다" 로 읽지 않는다.**

**화면 상태와 API 의 불일치에 주의한다.** 재제출 후에도 목록의 상태 칸은 `가져올 수 없음` 인데 API 는
`errors: "0"` 이다. 모순이 아니다 — `errors: "0"` 은 기록된 오류 수이지 XML 검증 통과가 아니고(§4.2.1),
미처리 상태에서 화면 문구는 직전 판정을 그대로 보여줄 수 있다. **삭제 후 새로 만든 행인데 이전과 같은
문구가 뜬다는 사실만으로 재제출분이 즉시 실패했다고 단정하지 않는다.** 양쪽 모두 성패의 근거가 아니다.

**다음 확인:** 같은 `sitemaps.get` 을 며칠 뒤 한 번 더 조회한다. **판별점은 `lastDownloaded` 가
생기는지와 `isPending` 이 풀리는지 둘이다**(§4.2.1 표의 둘째·셋째 갈래). 첫째 갈래는 위 이유로
판별력이 없고, 넷째("미처리 지속")는 시간이 충분히 지난 뒤에야 의미를 갖는다. 자동 점검은 설정하지
않았다. **재제출을 다시 하지 않는다** — 8-24 금지의 해제는 2026-09-12 지시에 한한 1회분이다(§7).

**미확인으로 남은 것 — OAuth 범위.** §4.2.1 은 `webmasters.readonly` 만 선택했다고 기록했으나, 이번에는
`Show scopes` 를 UIA 로 열지 못해(아래 §6 2026-09-12 절) **선택된 범위를 확인하지 못한 채 실행했다.**
동의는 사용자가 직접 처리했다. 조회 자체는 읽기 전용 `get` 이지만, **"이번에도 readonly 였다" 는 기록은
남기지 않는다** — 확인하지 않은 것을 확인한 것처럼 적지 않는다.

#### 4.2.3 2026-09-16 — 재제출 4일 뒤 재조회, 값이 9-12 와 동일하다

§4.2.2 가 "며칠 뒤 재조회로 미뤄진 상태" 로 남긴 판별을 수행했다. **조회 경로는 이번부터 CDP 다**
(§19.4). 입력은 §4.2.1 과 동일하고, 쓰기 scope 를 해제해 `webmasters.readonly` 만 남겼다.

```json
{
  "path": "https://droidactor.github.io/sitemap.xml",
  "lastSubmitted": "2026-09-12T06:30:07.930Z",
  "isPending": true,
  "isSitemapsIndex": false,
  "warnings": "0",
  "errors": "0"
}
```

**9-12 응답과 한 글자도 다르지 않다.**

| 필드 | 2026-09-12 (제출 8분) | 2026-09-16 (제출 4일) | 판정 |
|---|---|---|---|
| `lastSubmitted` | `2026-09-12T06:30:07.930Z` | 동일 | 그 사이 새 제출이 없었다는 뜻이다(재제출하지 않았다) |
| `isPending` | `true` | `true` | **4일째 미처리.** 8분 시점과 달리 이번 값은 판별력이 있다 |
| `lastDownloaded` | 없음 | **없음** | 다운로드 기록이 생기지 않았다 |
| `type`·`contents` | 없음 | 없음 | 처리 결과가 없다 |
| `warnings`·`errors` | `"0"`·`"0"` | 동일 | 미처리 상태의 초기값이며 XML 검증 통과를 뜻하지 않는다 |

**§4.2.1 표의 넷째 갈래("계속 `isPending: true`, 다운로드·형식·내용 필드 없음 → 미처리 상태 지속,
원인·대기 순서는 알 수 없음")에 해당한다.** 9-12 는 시점 때문에 어느 갈래도 가르지 못했으므로,
**행 삭제 후 재제출이 처리를 앞당겼다는 증거는 아직 없다.** 30일 된 옛 행과 4일 된 새 행을 같은
지표로 비교할 수는 없으므로 "재제출이 효과가 없었다" 도 단정하지 않는다.

**화면 쪽 값도 함께 읽었다(같은 시점).** 목록: 유형 `알 수 없음`, 제출 `2026. 9. 12.`,
**마지막으로 읽은 날짜 (빈칸)**, 상태 **`가져올 수 없음`**, 발견된 페이지 `0`. 세부 화면:
**마지막으로 읽은 날짜 `26. 9. 12.`**, 발견된 페이지 `0`, `사이트맵을 읽을 수 없음`.
**목록과 세부가 또 어긋난다** — 9-10 에도 목록 `(없음)` 대 세부 `2026-08-13` 이었다(§4.2 의 경고).
**API 에 `lastDownloaded` 가 없는 이상 세부 화면의 그 날짜를 "읽은 날" 로 읽지 않는다** — 값이
제출일과 같다는 점도 그 방향이다.

### 4.3 URL 검사 → 색인 생성 요청

새 URL을 한꺼번에 수동 요청하지 않는다. Sitemap이 기본 경로이고, URL 검사는 중요한 URL의 발견을
앞당기거나 비색인 원인을 진단하는 보조 수단이다. Google은 속성별 일일 요청 제한이 있다고 명시한다.

이 사이트에서 2026-08-10~11에 확인한 실행 규칙을 그대로 적용한다.

- KST 09:00 이후 시작한다.
- 하루 10개를 기준으로 진행한다.
- 할당량 초과 메시지가 나오면 그날은 즉시 중단한다.
- 같은 URL을 반복 요청하지 않는다.
- 요청 전 반드시 `실제 URL 테스트`를 통과시킨다.

**2026-08-14 에 이 "하루 10개" 가 실제 한도임이 확인됐다.** 우선순위 0의 10개를 요청한 뒤 같은 날
Tech Notes 허브 2개를 이어서 요청했고, **12번째(`/ko/tech-notes/`)에서 "할당량 초과 — 일일 할당량을
초과하여 이 요청을 처리할 수 없습니다. 내일 다시 제출해 주세요." 가 떴다.** 11개까지는 통과했다.
하루 10개는 여유 있는 목표치가 아니라 한도 바로 아래 값이므로, **10개를 채웠으면 그날은 끝낸다.**

**2026-08-17 에 확인한 GSC 화면 동작 3가지.** 8-14 에 사람이 손으로 할 때는 드러나지 않았던 것들이고,
셋 다 "요청이 실패한 것처럼 보이지만 실제로는 아닌" 오판을 만든다.

1. **`색인 생성 요청` 버튼이 실시간 테스트를 자동으로 수행한다.** 누르면 `실제 URL의 색인을 생성할 수
   있는지 테스트 중(1~2분 걸릴 수 있습니다)` → `요청 제출 중` → `색인 생성 요청됨` 순으로 진행된다.
   따라서 §4.3 의 "요청 전 실제 URL 테스트 통과" 규칙은 이 경로로 충족된다. 반대로 **`실제 URL 테스트`
   버튼을 먼저 눌러 그 탭에서 요청하면 흐름이 꼬여 모달이 멈춘다**(실측 5분 이상 `데이터 가져오는 중`
   에서 정지). 요청은 결과 화면에서 바로 누르는 편이 안전하다.
2. **성공 대화상자 위에 `Google 색인에서 데이터 가져오는 중` 모달이 겹쳐 뜬다.** 요청 직후 GSC 가
   결과를 재조회하기 때문이다. 위에 덮인 것만 보고 "요청이 안 끝났다" 고 읽으면 오판이다 — 아래 깔린
   `색인 생성 요청됨` 을 함께 확인한다.
3. **검사창에 이력이 쌓이면 입력 즉시 자동완성 목록이 열리고 `Enter` 가 그 목록에 소비된다.** 조회가
   시작되지 않는데 화면은 직전 URL 의 결과를 그대로 보여주므로, **결과 카드에 지금 검사한 URL 이
   찍혔는지 확인하기 전에는 그 판정을 그 URL 의 것으로 믿지 않는다.** 목록이 열려 있으면 항목을
   직접 클릭한다.

**요청 성공 판정에서 속기 쉬운 지점 — 화면의 "색인 생성 요청됨" 은 그 자체로 성공 근거가 아니다.**
GSC 는 버튼을 누른 직후 성공 대화상자를 먼저 띄우고, 서버가 할당량을 확인해 거절하면 그 대화상자를
닫은 **뒤에** "할당량 초과" 를 새로 띄운다. 결과 카드에 남는 인라인 `✓ 색인 생성 요청됨` 표시도
거절된 요청에 그대로 남는다. 그래서 판정은 **성공 대화상자를 닫은 다음 초과 대화상자가 뒤따르는지까지
확인**해야 한다.

#### 우선순위 0 — 즉시 요청: 허브와 출시 앱의 새 제품 URL, 10개

**2026-08-14 11:15~11:38 KST 에 10개 전부 완료했다.** 각 URL은 `URL 검사` → `실제 URL 테스트`
(전부 "URL을 Google에 등록할 수 있음") → `색인 생성 요청`(전부 "색인 생성 요청됨 — URL이 우선순위
크롤링 대기열에 추가되었습니다") 순으로 처리했다. 할당량 초과 메시지는 나오지 않았다.
검사 시점의 GOOGLE 색인 탭은 모두 "URL이 Google에 등록되어 있지 않음 / 아직 알려지지 않은 URL"이었다
— 신규 경로이므로 예상된 값이다.

- [x] `https://droidactor.github.io/apps/`
- [x] `https://droidactor.github.io/ko/apps/`
- [x] `https://droidactor.github.io/manual/`
- [x] `https://droidactor.github.io/ko/manual/`
- [x] `https://droidactor.github.io/apps/bt-keyboard/`
- [x] `https://droidactor.github.io/ko/apps/bt-keyboard/`
- [x] `https://droidactor.github.io/apps/wifi-scout/`
- [x] `https://droidactor.github.io/ko/apps/wifi-scout/`
- [x] `https://droidactor.github.io/apps/lgtv/`
- [x] `https://droidactor.github.io/ko/apps/lgtv/`

**같은 URL을 다시 요청하지 않는다.** 다음 요청일은 Tech Notes 허브 2개가 대상이다(아래 별도 묶음).

**2026-08-24 — 이 묶음의 `lgtv` 2개를 다시 요청했다. 위 규칙을 어긴 것이므로 경위를 남긴다.**

`/apps/lgtv/`·`/ko/apps/lgtv/` 가 `중복 페이지, Google에서 사용자와 다른 표준을 선택함` 으로 판정된
것을 진단한 뒤(§13) 그 조치로 색인 생성 요청을 골랐고, 두 건 모두 `색인 생성 요청됨 — URL이 우선순위
크롤링 대기열에 추가되었습니다` 를 받았다. 일일 할당량 10개 중 2개를 썼다.

**그런데 이 두 URL 은 바로 위 목록에서 2026-08-14 에 이미 요청 완료(`[x]`)였다.** 조치를 고를 때
이 절의 요청 이력을 먼저 확인하지 않았다. GSC 화면이 그 사실을 두 곳에서 알려주고 있었다 —

- 버튼 라벨이 `색인 생성 요청` 이 아니라 **`색인 생성 요청 / 다시 요청`** 이었다(요청 이력이 있는
  URL 에 GSC 가 붙이는 표시다).
  **2026-09-02 실측이 이 근거를 무너뜨린다 — 단, 무너지는 것은 `UIA 가 읽는 이름` 쪽이다.** 요청
  이력이 전혀 없는 URL(`/tech-notes/lgtv/`, `Google에는 아직 알려지지 않은 URL`)에서도 **UIA 가 읽는
  버튼 이름은 똑같이 `색인 생성 요청다시 요청`** 이고, 같은 순간의 화면 스크린샷에서 **눈에 보이는
  라벨은 `색인 생성 요청` 하나뿐**이다. 버튼 안에 숨은 대체 라벨이 함께 들어 있어 접근성 트리에서만
  이어 붙어 나오는 것이다.
  **2026-09-05 실측이 여기를 한 번 더 좁힌다 — 화면 라벨은 실제로 이력을 따라 바뀐다.** 같은 URL 을
  요청하기 전후로 스크린샷을 찍어 **요청 전 `색인 생성 요청` → 요청 후 `✓ 색인 생성 요청됨` +
  `다시 요청`** 으로 바뀌는 것을 확인했다. **따라서 8-24 가 화면에서 `다시 요청` 을 보고 재요청을
  알아챈 것은 근거로서 유효했다** — 9-02 가 "라벨은 이력의 표시가 아니다" 라고 적은 것은 UIA 이름에
  대해서만 맞다.
  **다만 9-05 가 같은 자리에 덧붙인 "이미 색인된 URL 에서는 문구 자체가
  `페이지가 변경되었나요? 색인 생성 요청` 이다" 는 2026-09-10 에 반증됐다.** 그 문구는 **색인되지 않은
  URL 에도 똑같이 붙는다** — 요청 직후 스크린샷(성공 대화상자가 떠 있고 배경 카드는 아직 요청 전
  렌더인 상태)에서 `/ko/blog/`(`크롤링됨 - 현재 색인이 생성되지 않음`)
  와 `/blog/use-android-phone-as-bluetooth-mouse/`(`아직 알려지지 않은 URL`, 세부값 전부 `해당사항 없음`)
  가 **둘 다** `페이지가 변경되었나요? 색인 생성 요청` 이었다. **즉 이 문구로 색인 여부를 가릴 수 없다**
  (9-05 가 색인된 URL 에서 그 문구를 본 것 자체는 그대로 서지만, 그것이 색인 URL 의 표시는 아니다).
  **그래도 이력 판정은 이 절의 `[x]` 목록으로만 한다** — 화면 라벨은 사람이 스크린샷을 봐야 읽히고
  UIA 이름은 이력을 구분하지 못하므로, 자동화 경로에서 신뢰할 수 있는 것은 이 목록뿐이다.
  (8-24 의 재요청이 실효가 없다는 결론 자체는 성공 모달 본문
  "여러 번 제출해도 대기열 위치나 우선순위가 변경되지 않습니다" 가 따로 뒷받침하므로 그대로 선다.)
- 성공 모달 본문이 **"URL이 우선순위 크롤링 대기열에 추가되었습니다. 페이지를 여러 번 제출해도
  대기열 위치나 우선순위가 변경되지 않습니다."** 였다.

**즉 이 재요청의 실효는 기대하기 어렵다.** Google 이 명시적으로 "여러 번 제출해도 우선순위가 바뀌지
않는다" 고 말하고 있으므로, 8-14 요청이 만든 대기열 위치가 그대로일 뿐이다. 접수 자체는 성공했고
할당량 2개는 소모됐다.

**따라서 색인 요청은 §13 의 canonical 잠식에 대해 이미 써 본 카드다.** 남은 후보와 각각의 정책상
지위는 §13.4 표에 있고, 그 표의 결론은 "문서의 기존 정책을 지키는 한 남는 것은 손대지 않고 기다리는
것뿐" 이다 — **이 절을 근거로 "아직 쓸 수 있는 조치가 있다" 고 출발하지 않는다.**
다음 세션은 조치를 고르기 전에 **이 절의 `[x]` 목록을 먼저 읽는다.**

#### 우선순위 1 — 조건부 요청: 매뉴얼 3종과 나머지 Bluetooth 제품 URL, 10개

먼저 sitemap에 맡긴다. 7~14일 뒤에도 URL Inspection에서 미발견 또는 비색인이면 제외 사유와 live test를
확인하고, 기술 문제를 고친 뒤 필요한 URL만 요청한다.

**우선순위 1·2 를 가른 기준은 "출시 앱 먼저" 였는데 그 기준은 2026-08-14 오후에 소멸했다** —
`bt-ppt`·`bt-mouse`·`ssh-scout` 가 프로덕션에 올라 6종이 모두 출시 상태가 됐고, 미출시는
`yt-downloader`(우선순위 2) 하나뿐이다. 위 우선순위 0 의 "출시 앱" 은 8-14 오전 시점 기록이므로
그대로 둔다. 실제 요청일에는 두 묶음의 순서를 출시 여부가 아니라 **색인 미도달 여부**로 다시 정한다.

**2026-08-15 표본 스캔 — 3개 전부 미발견이다.** URL 검사는 색인 생성 요청과 달리 일일 할당량을
소모하지 않으므로 요청 대상 선별에 자유롭게 쓴다.

| URL | 판정 | Sitemaps | 참조 페이지 |
|---|---|---|---|
| `/manual/bt-keyboard/` | URL이 Google에 등록되어 있지 않음 | 감지된 참조 사이트맵 없음 | 감지된 페이지 없음 |
| `/ko/apps/bt-ppt/` | URL이 Google에 등록되어 있지 않음 | 감지된 참조 사이트맵 없음 | 감지된 페이지 없음 |
| `/apps/bt-mouse/` | URL이 Google에 등록되어 있지 않음 | 감지된 참조 사이트맵 없음 | 감지된 페이지 없음 |

`/apps/bt-ppt/` 도 같은 값이며 "Google에는 아직 알려지지 않은 URL" 로 표시된다.

**2026-08-17 전수 스캔 — 18개 전부 미색인이다.** 표본이 아니라 우선순위 1의 10개 + bt-keyboard
상세 노트 6개 + 그 부모 허브 2개를 모두 검사했다. 18/18 이 `URL이 Google에 등록되어 있지 않음` +
`참조 페이지: 감지된 페이지 없음` + `최근 크롤링: 해당사항 없음` 이다.

| 묶음 | 대상 | 결과 |
|---|---|---|
| 우선순위 1 | 매뉴얼 6 + `(ko/)apps/{bt-ppt, bt-mouse}` 4 | 10/10 미색인 |
| bt-keyboard 상세 노트 | §9 2개 + §10 4개 | 6/6 미색인 |
| bt-keyboard Tech Notes 허브 | `(ko/)tech-notes/bt-keyboard/` | 2/2 미색인 |

**허브 2개가 미색인이라는 사실이 상세 노트의 처지를 설명한다.** 지금까지 상세 노트를 후순위로 둔
근거는 "허브가 색인되면 내부 링크로 딸려 발견된다" 였는데, 그 허브 자체가 색인되지 않았다.
현재 링크 사슬은 `/tech-notes/`(색인됨) → `/tech-notes/bt-keyboard/`(**미색인**) → 상세(미색인)
로 중간이 끊겨 있다. 상세 노트가 스스로 발견될 경로는 지금 없다.

**2026-08-17 에 이 10개를 전부 요청했다.** 원래 계획일은 8-21~28 이었으나 앞당겼다 — 판단 근거는
아래 §6 의 2026-08-17 절에 적었다. 10/10 이 `색인 생성 요청됨 — URL이 우선순위 크롤링 대기열에
추가되었습니다` 이고 할당량 초과는 나오지 않았다. 오늘 예산 10개를 정확히 소진했다.

- [x] `https://droidactor.github.io/manual/bt-keyboard/`
- [x] `https://droidactor.github.io/ko/manual/bt-keyboard/`
- [x] `https://droidactor.github.io/manual/wifi-scout/`
- [x] `https://droidactor.github.io/ko/manual/wifi-scout/`
- [x] `https://droidactor.github.io/manual/lgtv/`
- [x] `https://droidactor.github.io/ko/manual/lgtv/`
- [x] `https://droidactor.github.io/apps/bt-ppt/`
- [x] `https://droidactor.github.io/ko/apps/bt-ppt/`
- [x] `https://droidactor.github.io/apps/bt-mouse/`
- [x] `https://droidactor.github.io/ko/apps/bt-mouse/`

**같은 URL을 다시 요청하지 않는다.** 다음 요청일의 대상은 우선순위 2(§4.3)와 Tech Notes 허브 12개다.

#### 우선순위 2 — 조건부 요청: 나머지 제품·매뉴얼 URL, 10개

우선순위 1과 같은 기준을 적용한다. 이미 발견·crawl됐거나 정상적으로 색인 대기 중인 URL은 요청하지 않는다.

- [x] `https://droidactor.github.io/apps/ssh-scout/` — 2026-09-02 요청 접수
- [x] `https://droidactor.github.io/ko/apps/ssh-scout/` — 2026-09-02 요청 접수
- [x] `https://droidactor.github.io/apps/yt-downloader/` — 2026-09-05 요청 접수 (2026-09-10 검사: 여전히 미발견)
- [x] `https://droidactor.github.io/ko/apps/yt-downloader/` — 2026-09-05 요청 접수 (2026-09-10 검사: 여전히 미발견)
- [x] `https://droidactor.github.io/manual/bt-ppt/` — 2026-09-02 요청 접수
- [x] `https://droidactor.github.io/ko/manual/bt-ppt/` — 2026-09-02 요청 접수
- [x] `https://droidactor.github.io/manual/bt-mouse/` — 2026-09-02 요청 접수
- [x] `https://droidactor.github.io/ko/manual/bt-mouse/` — 2026-09-02 요청 접수
- [x] `https://droidactor.github.io/manual/ssh-scout/` — 2026-09-02 요청 접수
- [x] `https://droidactor.github.io/ko/manual/ssh-scout/` — 2026-09-02 요청 접수

**2026-09-02 — 이 10개 중 8개를 요청했고 전부 접수됐다.** 남긴 것은 `(ko/)apps/yt-downloader/` 2개다.
같은 날 1순위였던 `(ko/)tech-notes/lgtv/` 2개를 먼저 쓰고 남은 8칸을 여기에 배정했는데, 그 8칸을
**출시 앱 기준으로 갈랐다** — `yt-downloader` 는 6종 중 유일한 미출시 앱이다(§4.3 우선순위 1 의
"출시 앱 먼저" 가 8-14 오후에 소멸했다는 기록은 그때 5종이 한꺼번에 프로덕션에 오른 상황을 가리키고,
`yt-downloader` 하나가 남은 것은 그 뒤로 바뀌지 않았다). **다음 요청일 1순위는 이 2개다.**
8개 전부 검사 시점에 `URL이 Google에 등록되어 있지 않음` + `Google에는 아직 알려지지 않은 URL입니다`
였다 — 8-31 의 Tech Notes 10개와 같은 단계이고, 8-24 가 다룬 `크롤링됨 - 현재 색인이 생성되지 않음`
과는 다른 단계다.

**2026-09-05 — 남은 `(ko/)apps/yt-downloader/` 2개를 요청해 접수됐고, 이 묶음 10개가 전부 닫혔다.**
검사 시점에 2개 모두 `URL이 Google에 등록되어 있지 않음` + `Google에는 아직 알려지지 않은 URL입니다`
였다. **같은 날 이 묶음의 나머지 8개를 다시 검사한 결과 8/8 이 `URL이 Google에 등록되어 있음` 이다**
— 9-02 요청이 3일 만에 전량 색인으로 이어졌다(아래 2026-09-05 절).

#### Tech Notes — 별도 요청 묶음: 허브 2개 우선, 상세 12개 조건부

Tech Notes 배포 다음 요청일에는 허브 2개를 실제 URL 테스트한 뒤 요청한다. 상세 문서는 먼저 sitemap과
앱·매뉴얼·Tech Notes 허브의 내부 링크에 맡기고, 7~14일 뒤에도 미발견 또는 비색인이면 원인을 확인한 뒤
필요한 URL만 선별 요청한다.

**2026-08-14 에 이 2개를 우선순위 0의 10개와 같은 날 이어서 요청했다(원래 계획은 "다음 요청일"이었다).
그 결과 하루 12개가 되어 마지막 1개가 일일 할당량에 걸렸다.**

- [x] `https://droidactor.github.io/tech-notes/` — 우선 요청
      2026-08-14 11:47 KST 요청 접수(11번째). 실시간 테스트 통과.
- [x] `https://droidactor.github.io/ko/tech-notes/` — 우선 요청 **(요청 없이 종결. 같은 묶음의 위
      항목 `[x]` 가 "요청 접수됨" 인 것과 뜻이 다르다)**
      2026-08-14 11:49 KST 요청 실패 — "할당량 초과 / 내일 다시 제출해 주세요"(12번째).
      실시간 테스트는 "URL을 Google에 등록할 수 있음" 으로 통과했으므로 페이지 문제가 아니었다.
      **2026-08-15 에 재요청하려고 URL 검사한 결과 이미 `URL이 Google에 등록되어 있음`(색인 완료)
      이어서 색인 생성 요청 없이 종결했다.** 세부값:

      | 항목 | 값 |
      |---|---|
      | Sitemaps | 감지된 참조 사이트맵이 없습니다 |
      | 참조 페이지 | `https://droidactor.github.io/tech-notes/` |
      | 최근 크롤링 | 2026-08-14 11:50:38 |
      | 크롤링에 사용된 에이전트 | Googlebot 스마트폰 |
      | 크롤링 허용 / 페이지 가져오기 / 색인 생성 허용 | 예 / 성공 / 예 |
      | 사용자 선언 표준 URL | `https://droidactor.github.io/ko/tech-notes/` |
      | Google에서 선택한 표준 URL | 검사된 URL (self canonical 일치) |

      **어제의 할당량 초과는 결과적으로 손실이 아니었다.** 8-14 11:47 에 통과한 `/tech-notes/`
      요청이 3분 뒤 크롤을 유발했고, Googlebot이 그 페이지의 `hreflang`·내부 링크를 따라
      `/ko/tech-notes/` 까지 스스로 발견·색인했다. 참조 페이지 칸이 그 경로를 그대로 보여준다.
      **같은 URL을 다시 요청하지 않는다** — 이미 색인된 URL의 요청은 할당량만 소모한다.
- [x] `https://droidactor.github.io/tech-notes/bt-keyboard/` — 2026-08-31 요청 접수
- [x] `https://droidactor.github.io/ko/tech-notes/bt-keyboard/` — 2026-08-31 요청 접수
- [x] `https://droidactor.github.io/tech-notes/bt-ppt/` — 2026-08-31 요청 접수
- [x] `https://droidactor.github.io/ko/tech-notes/bt-ppt/` — 2026-08-31 요청 접수
- [x] `https://droidactor.github.io/tech-notes/bt-mouse/` — 2026-08-31 요청 접수
- [x] `https://droidactor.github.io/ko/tech-notes/bt-mouse/` — 2026-08-31 요청 접수
- [x] `https://droidactor.github.io/tech-notes/wifi-scout/` — 2026-08-31 요청 접수
- [x] `https://droidactor.github.io/ko/tech-notes/wifi-scout/` — 2026-08-31 요청 접수
- [x] `https://droidactor.github.io/tech-notes/ssh-scout/` — 2026-08-31 요청 접수
- [x] `https://droidactor.github.io/ko/tech-notes/ssh-scout/` — 2026-08-31 요청 접수
- [x] `https://droidactor.github.io/tech-notes/lgtv/` — 2026-09-02 요청 접수
- [x] `https://droidactor.github.io/ko/tech-notes/lgtv/` — 2026-09-02 요청 접수

**2026-08-31 — 위 12개 중 10개를 요청했고 전부 접수됐다. 일일 할당량 10개를 정확히 소진했다.**
`lgtv` 한·영 2개만 남겼다 — 같은 제품의 `/apps/lgtv/` 가 §13 의 중복 판정을 받고 있어 결과 해석이
오염될 수 있으므로 뒤로 미뤘다. 다음 요청일 1순위다.

**10개 전부가 검사 시점에 `Google에는 아직 알려지지 않은 URL입니다` 였다.** `크롤링 허용 여부`·
`페이지 가져오기`·`최근 크롤링`·`사용자 선언 표준 URL` 이 모두 `해당사항 없음` 이고
`Sitemaps: 감지된 참조 사이트맵이 없습니다` / `참조 페이지: 감지된 페이지 없음` 이다.
**즉 색인이 안 된 것이 아니라 Google 이 이 URL 들의 존재 자체를 모른다.** 8-24 가 다룬
`크롤링됨 - 현재 색인이 생성되지 않음`(크롤은 됐고 색인에서 멈춤)과는 **다른 단계**다.

**한 건은 8-24 보고서와 어긋난다.** `/ko/tech-notes/bt-keyboard/` 는 8-24 의 `크롤링됨 - 현재 색인이
생성되지 않음` 8건 목록에 들어 있었는데(§6 2026-08-24 성과 4번), 8-31 URL 검사는 같은 URL 을
`아직 알려지지 않은 URL` 이라고 답한다. **보고서(데이터 기준 8-21)와 실시간 검사가 다른 값을 준다는
사실만 기록한다** — 어느 쪽이 맞는지는 GSC 가 말하지 않으므로 다음 세션이 추정을 사실로 인용하지 않는다.

`ko/tech-notes/bt-ppt/` 는 첫 시도에서 `색인 생성 요청을 제출하는 중에 문제가 발생했습니다. 나중에
다시 시도해 주세요` 를 받았고 **간격을 두고 재시도해 접수됐다.** 8-14 의 `할당량 초과 / 내일 다시
제출해 주세요` 와는 **다른 메시지이고 다른 원인**이다(그때는 12번째였고 오늘은 4번째였다).
**연속 제출이 빠르면 이 일시적 오류가 난다** — 요청 사이에 20초 이상 두면 재현되지 않았다.

**2026-09-05 — 위 12개를 전수 재검사했다. 색인 3, 크롤됨 1, 미발견 8 이다.**
색인된 것은 `/tech-notes/bt-keyboard/`(8-31 요청)와 `(ko/)tech-notes/lgtv/` 2개(9-02 요청)이고,
`/ko/tech-notes/bt-keyboard/` 는 `크롤링됨 - 현재 색인이 생성되지 않음`(참조 페이지가 영문 허브다),
나머지 8개(`(ko/)tech-notes/{bt-ppt, bt-mouse, wifi-scout, ssh-scout}/`)는 `아직 알려지지 않은 URL`
에 `최근 크롤링: 해당사항 없음` 이다. **8-31 요청 10건 중 크롤을 부른 것은 영문 bt-keyboard 1건뿐인데
9-02 요청 10건은 전부 색인됐다** — 자세한 대조는 2026-09-05 절 2·3번에 있다. **9-02 묶음에서 크롤
시각을 실제로 읽은 것은 표본 `/apps/ssh-scout/` 1건(요청 당일)이고 나머지 9건은 색인 여부만 확인했다**
— "요청 당일 크롤" 을 10건 전체의 실측으로 인용하지 않는다.
**이 8개를 다시 요청하지 않는다**(§4.3 의 반복 요청 금지).

**2026-09-10 — 이 8개가 요청 10일째에도 8/8 `아직 알려지지 않은 URL` 이다.** 같은 날 보고서 CSV 도
같은 값을 준다. 9-05 다음 작업 3번이 걸어 둔 조건(§2.5 의 5개를 처리한 뒤에도 미발견이면 재요청
여부를 사용자 결정으로 올린다)이 충족돼 **사용자 결정 대기 상태로 올렸다**(§6 2026-09-10 절 7번).
**결정이 내려지기 전에는 계속 요청하지 않는다.** 참고로 같은 실패 모양이 이 묶음보다 2주 앞선
`(ko/)manual/bt-keyboard/`(8-17 요청, 9-10 현재 24일째 미발견)에도 있으므로, 이 8개를 8-31 묶음만의
이례로 보지 않는다.

**bt-keyboard 상세 노트 6개는 위 12개보다 후순위였다.** §9 의 2개(Google TV HID, 8-14 배포)와 §10 의
4개(각인 드리프트·HID 세션 공유, 8-16 배포)를 말한다. 허브·앱별 Tech Notes 가 먼저 색인되면 그
내부 링크로 발견될 수 있으므로, 상세 노트를 수동 요청에 먼저 태우지 않는다는 것이 원래 방침이었다.

**2026-09-05 — 이 후순위가 해제됐고 6개 전부 요청 접수됐다(사용자 지시).**

- [x] `https://droidactor.github.io/tech-notes/bt-keyboard/why-google-tv-does-not-detect-bluetooth-hid.html` — 2026-09-05 요청 접수
- [x] `https://droidactor.github.io/ko/tech-notes/bt-keyboard/why-google-tv-does-not-detect-bluetooth-hid.html` — 2026-09-05 요청 접수
- [x] `https://droidactor.github.io/tech-notes/bt-keyboard/why-korean-english-key-labels-drift.html` — 2026-09-05 요청 접수
- [x] `https://droidactor.github.io/ko/tech-notes/bt-keyboard/why-korean-english-key-labels-drift.html` — 2026-09-05 요청 접수
- [x] `https://droidactor.github.io/tech-notes/bt-keyboard/sharing-one-bluetooth-hid-session.html` — 2026-09-05 요청 접수
- [x] `https://droidactor.github.io/ko/tech-notes/bt-keyboard/sharing-one-bluetooth-hid-session.html` — 2026-09-05 요청 접수

**"허브가 먼저 색인되면 내부 링크로 딸려 온다" 는 기대는 5일치 반증을 받고 접혔다** — 영문 허브
`/tech-notes/bt-keyboard/` 가 8-31 에 색인됐는데도 9-05 검사에서 상세 6개가 전부 `아직 알려지지 않은
URL` 이었다. 경위와 판단 근거는 §6 2026-09-05 절 4번에 있다.

**2026-09-10 — 이 6개가 5일 만에 전부 단계를 올렸고, 언어로 정확히 갈렸다.**

| URL | 9-05 요청 시점 | 9-10 검사 |
|---|---|---|
| `/tech-notes/bt-keyboard/{why-google-tv-does-not-detect-bluetooth-hid, why-korean-english-key-labels-drift, sharing-one-bluetooth-hid-session}.html` 3 | 아직 알려지지 않은 URL | **색인 생성됨** |
| `/ko/tech-notes/bt-keyboard/` 같은 3편 | 아직 알려지지 않은 URL(1건은 크롤링됨) | **크롤링됨 - 현재 색인이 생성되지 않음** |

**영문 3/3 이 색인되고 국문 3/3 이 크롤 단계에서 멈췄다.** 요청 후 6/6 이 크롤 단계에 도달했고
(1건은 요청 전부터 이미 `크롤링됨` 이었다 — §6 2026-09-05 절 1번), 그 도달률이 9-02 묶음과 같고
8-31 묶음과 다르다. **국문이 색인에서 멈추는 것은 이 사이트에서 반복되는 비대칭이다**
— 부모 허브도 `/tech-notes/bt-keyboard/`(색인) 대 `/ko/tech-notes/bt-keyboard/`(크롤링됨) 로 같은 모양이고,
`/blog/`(색인) 대 `/ko/blog/`(크롤링됨) 도 같다. **원인은 확인하지 않았다** — GSC 는 `크롤링됨 - 현재
색인이 생성되지 않음` 의 이유를 URL 단위로 말하지 않는다. **이 6개를 다시 요청하지 않는다**(§4.3).

수동 URL 검사 대상으로 선택한 URL에서 확인할 값 (아래는 우선순위 0의 10개에 대한 2026-08-14 결과):

- [x] Page fetch: 성공 — 실시간 테스트에서 10/10 "페이지 색인을 생성할 수 있음".
- [x] Crawl allowed: 예 — `robots.txt`가 `Allow: /`이고 실시간 테스트가 전부 통과했다.
- [x] Indexing allowed: 예 — 새 페이지 44개에 `noindex`가 없다(§3).
- [x] User-declared canonical: 검사한 새 URL과 동일 — 44/44 self canonical(§3).
- [x] 실제 URL 테스트: 성공 — 10/10 "URL을 Google에 등록할 수 있음".
- [x] 색인 요청 대상으로 선별한 URL만 `색인 생성 요청` 성공을 확인 — 10/10 "색인 생성 요청됨".

### 4.4 옛 URL 확인

옛 URL은 sitemap에 다시 넣거나 색인 생성 요청하지 않는다. Google이 기존 URL을 다시 crawl하면서 이동
신호를 읽도록 계속 제공한다.

- [x] 영문 대표 `/bt-keyboard/`를 URL 검사하여 새 `/apps/bt-keyboard/`로 이동하는지 확인한다.
- [x] 국문 대표 `/ko/bt-keyboard/`를 URL 검사하여 새 `/ko/apps/bt-keyboard/`로 이동하는지 확인한다.
- [x] 기존에 색인됐던 나머지 옛 제품 URL도 대응하는 새 URL로 1:1 연결되는지 표본 확인한다.
- [ ] 옛 URL에 대한 삭제 요청을 만들지 않는다.
- [ ] 옛 URL 호환 페이지를 최소 2027-08-14까지 삭제하지 않는다.

**2026-08-15 URL 검사 실측 — 옛 URL 4개(`/bt-keyboard/`, `/ko/bt-keyboard/`, `/wifi-scout/`,
`/ko/lgtv/`)가 전부 `URL이 Google에 등록되어 있음`(색인 유지) 상태다.** 아직 이동이 반영되지 않았고,
그것이 정상이다. 이유는 세부값이 설명한다 — 표본 `/ko/lgtv/`:

| 항목 | GSC 값 | live 실측(2026-08-15) |
|---|---|---|
| 최근 크롤링 | **2026-08-13 08:56:16** | — |
| 참조 페이지 | `/lgtv/`, `/ko/blog/control-lg-webos-tv-over-wifi/` | — |
| 사용자 선언 표준 URL | `https://droidactor.github.io/ko/lgtv/` (옛 URL 자신) | `https://droidactor.github.io/ko/apps/lgtv/` |
| Google에서 선택한 표준 URL | 검사된 URL | — |
| `meta refresh` | (크롤 시점에 없었음) | `0; url=/ko/apps/lgtv/` |

**GSC가 보여주는 canonical 은 2026-08-13 크롤 시점의 캐시이고, 이동 신호를 넣은 배포는 그 뒤인
2026-08-14 이다.** 같은 날 `/bt-keyboard/` 도 live 는 canonical `/apps/bt-keyboard/` +
`meta refresh 0; url=/apps/bt-keyboard/` 로 정확하다. 즉 사이트 쪽 신호는 갖춰졌고, 남은 것은
**Googlebot이 옛 URL 14개를 재크롤하는 일**뿐이다. 재크롤 전까지 GSC 값이 옛 상태로 남아 있는 것을
결함으로 읽지 않는다. 옛 URL에는 색인 생성 요청을 하지 않는다(§7).

URL 이동은 Googlebot이 옛 URL과 새 URL을 각각 crawl해야 URL 단위로 처리된다. Google 공식 문서는 redirect를
일반적으로 최소 1년 유지하라고 권장한다.

## 5. 외부 링크 갱신

내부 링크와 sitemap은 이미 새 URL로 바뀌었다. 외부에서 옛 제품 URL을 직접 가리키는 링크가 있으면 새
canonical URL로 바꾸면 redirect 의존도를 줄일 수 있다.

- [x] Search Console의 `링크` 보고서에서 옛 제품 URL로 들어오는 외부 링크를 확인한다.
      **2026-08-15 실측: 외부 링크 총 `0`개, 내부 링크 총 `0`개, 상위 링크된 페이지·상위 링크 사이트
      모두 `데이터 없음`.** 따라서 이 절에서 바꿔야 할 외부 링크는 현재 없다. 내부 링크까지 0인 것은
      GSC가 아직 이 사이트의 링크 그래프를 거의 수집하지 못했다는 뜻이며(색인된 페이지가 한 자릿수),
      sitemap 미수집·새 URL 미발견과 같은 원인을 공유한다. 링크 그래프는 크롤이 진행되면 채워진다.

      **2026-08-17 정정 — "색인된 페이지가 한 자릿수" 라는 근거가 무너졌다.** 같은 날 확인한
      Page indexing 보고서의 색인 수는 22 다(§6). 색인 22 인데 내부 링크 그래프가 0 이면 원인은
      "크롤이 부족해서" 가 아니라 다른 데 있다. 링크 보고서는 반영이 특히 느린 보고서이므로 일단
      그 지연으로 보되, **8-20 판정 때 §5 를 한 번 다시 읽어 여전히 0 인지 확인한다.**

      **2026-08-31 재확인 — 여전히 0 이다. "보고서 지연" 으로는 더 이상 설명되지 않는다.**
      외부 링크 `총 0개`(상위 링크된 페이지·상위 링크 사이트·상위 링크 텍스트 전부 `데이터 없음`),
      내부 링크 `총 0개`(상위 링크된 페이지 `데이터 없음`). **8-15 실측과 한 글자도 다르지 않다.**
      그 사이 색인은 6 → 22 → 38 로 늘었고 16일이 지났다.

      **결정적인 것은 같은 날 URL 검사가 보여준 값과의 충돌이다.** `/apps/lgtv/` 의 `참조 페이지` 는
      `https://droidactor.github.io/lgtv/` 로 **잡혀 있다**(§13.5). 즉 **Google 은 내부 링크를 인지하고
      있는데 링크 보고서에는 그것이 한 건도 나타나지 않는다.** 두 화면이 같은 사실에 대해 다른 답을
      준다.
      **따라서 "내부 링크가 없다" 를 사이트의 결함으로 읽지 않는다** — 저장소 전수 검색에서 내부 링크는
      실재하고(§13.1 3번), URL 검사도 그것을 본다. **이 속성에서 링크 보고서가 채워지지 않는 것으로
      보되, 왜인지는 GSC 가 말하지 않으므로 확정하지 않는다.** 다음 세션은 이 항목을 근거로 사이트를
      고치지 않는다.
- [x] 직접 수정 가능한 Blogger·Naver·GitHub README·프로필 링크가 있으면 `/apps/<app>/`로 바꾼다.
      **저장소 실측(2026-08-15): 배포되는 문서에는 옛 제품 URL 참조가 없다.** 유일한 적중은
      `doit.txt` 17~19행인데, **이 파일은 `.gitignore` 의 `/doit.txt` 로 애초에 배포되지 않는다**
      (2026-08-17 live 실측 `404`). 검색 영향은 0이므로 이 항목은 닫는다. 로컬 메모의 내용이
      개편 이전 구조를 설명하는 것은 검색과 무관한 별개 사안이다.
- [x] Google Play Console의 스토어 등록정보가 제품 상세 URL을 직접 사용한다면 새 `/apps/<app>/`로 바꾼다.
      **2026-08-19 Console 실측 — 바꿀 것이 없다.** 6개 앱(`bt_keyboard`, `bt_mouse2`, `bt_ppt`,
      `lgtv`, `ssh_scout`, `wifi_scout`)의 `스토어 설정 > 웹사이트` 가 전부
      `https://droidactor.github.io`(루트)다. `SimpleGasMileage` 는 웹사이트 미설정.
      **옛 제품 URL(`/bt-keyboard/` 류)을 쓰는 앱은 0개**이므로 이 항목은 닫는다. 루트를 쓰는 것은
      아래 항목이 "변경하지 않는다" 고 못박은 상태 그대로다.
- [ ] 개발자 웹사이트가 root `https://droidactor.github.io/`이면 변경하지 않는다.
- [ ] 개인정보 처리방침 URL `/#privacy-*`, `/ko/#privacy-*`는 변경하지 않는다.

## 6. 관찰 일정과 완료 조건

### 배포 직후 — 2026-08-14 완료

- [x] live `200`, redirect, canonical, sitemap 64개를 확인한다. (§3, §4.2)
- [x] Search Console sitemap의 현재 상태를 기록한다. — `가져올 수 없음` / 0 페이지 (§4.2)
- [x] 우선순위 0 URL 검사를 수행한다. — 10/10 색인 생성 요청 완료 (§4.3)

2026-08-14 에 처리 완료된 것: 우선순위 0 URL 10개 색인 요청, `/tech-notes/` 색인 요청,
옛 호환 페이지 14개 `noindex` 제거 배포(commit `d6e04f0`).

### 2026-08-15 — 확인 작업 완료, 색인 생성 요청은 0건

이날 예정돼 있던 "다음 작업 2건" 을 둘 다 처리했고, **일일 할당량 10개를 한 건도 쓰지 않았다.**

1. **`/ko/tech-notes/` 재요청 → 불필요로 종결.** URL 검사 결과 이미 색인돼 있었다. 8-14 에
   통과한 `/tech-notes/` 요청이 유발한 크롤이 내부 링크를 따라 국문판까지 도달했다 (§4.3).
2. **sitemap `가져올 수 없음` 미해소.** 세부 화면의 마지막 읽기 표시는 2026-08-13 이었다.
   당시 이를 실패 시각으로 해석한 단정은 §4.2.1 에서 철회했다. 당시 재제출 판정일은 8-20 이었다.

추가로 확인한 것:

- 옛 제품 URL 4개 표본 검사 — 전부 색인 유지, GSC 값은 8-13 크롤 캐시 (§4.4).
- 새 URL 표본 4개(`/manual/bt-keyboard/`, `/apps/bt-ppt/`, `/ko/apps/bt-ppt/`, `/apps/bt-mouse/`)
  — 전부 미발견 (§4.3 우선순위 1).
- Google TV HID 기술 노트 한·영 2개 live 확인, sitemap `<loc>` 66개 (§9).
- 링크 보고서 — 외부 링크 0, 내부 링크 0, 상위 링크된 페이지·사이트 모두 데이터 없음 (§5).

**Page indexing 보고서 현재값(최종 업데이트 2026-08-07 — 이번 개편 이전 데이터다):**

| 항목 | 값 |
|---|---|
| 색인 생성됨 | 6 |
| 색인이 생성되지 않음 | 3 (사유 1종) |
| 사유 | `크롤링됨 - 현재 색인이 생성되지 않음` / 소스 `Google 시스템` / 3 페이지 |

이 수치는 8-14 개편과 8-14~15 크롤을 아직 반영하지 않았다. §6 "7일 후" 의 Page indexing 항목은
보고서 최종 업데이트 날짜가 2026-08-14 이후로 넘어간 뒤에 다시 읽는다.

**다음 작업 → 최신 목록은 §6 의 가장 마지막 날짜 절에만 둔다.** 같은 목록을 날짜 절마다 복제하면
계획이 바뀔 때 한쪽만 고쳐져 두 판본이 어긋난다(실제로 이 절과 8-17 절의 문구가 갈리기 시작했다).
날짜를 박아 두면 절이 하나 늘 때마다 이 문장도 고쳐야 하므로 "가장 마지막 절" 로 적는다.

### 2026-08-17 — 우선순위 1의 10개 색인 요청 완료, 예산 소진

**색인 생성 요청 10건을 접수했다**(§4.3 우선순위 1 전량). 원래 계획일은 8-21~28 이었으나 앞당겼고,
그 판단 근거는 이 절 5번에 적었다. 함께 Page indexing 보고서의 색인 대상 전량을 처음으로 확보했다.

1. **sitemap 목록 화면 값이 8-15 와 같다.** `가져올 수 없음` / 발견 0. **세부 화면은 열지 않았으므로
   "마지막 읽기가 8-13 그대로" 라고 단정하지 않는다** — 8-20 판정 전에 세부 화면을 확인한다 (§4.2).
2. **Page indexing 보고서가 2026-08-10 자로 갱신됐다.** 8-15 확인 때의 최종 업데이트는 8-07 이었다.

   | 항목 | 2026-08-15 확인(8-07 자) | 2026-08-17 확인(8-10 자) |
   |---|---|---|
   | 색인 생성됨 | 6 | **22** |
   | 색인이 생성되지 않음 | 3 | **5** |
   | 사유 | `크롤링됨 - 현재 색인이 생성되지 않음` 1종 | 같음(1종, 소스 `Google 시스템`, 유효성 검사 `시작되지 않음`) |

3. **색인된 22개의 전량 목록을 CSV 로 내려받아 확인했다**(보고서 → `색인이 생성된 페이지에 대한
   데이터 보기` → `내보내기` → `CSV 다운로드`). 결론은 하나다 — **개편 이후의 새 구조 URL
   (`/apps/`, `/manual/`, `/tech-notes/`)은 22개 중 0개다.**

   | 분류 | 개수 | 최종 크롤링 |
   |---|---|---|
   | 홈 `/` | 1 | 2026-08-07 |
   | 블로그 글(한·영) | 12 | 2026-08-10 ~ 08-11 |
   | 옛 제품 URL | 9 (`/bt-keyboard/`, `/bt-mouse/`, `/wifi-scout/`, `/ssh-scout/`, `/lgtv/`, `/ko/bt-keyboard/`, `/ko/bt-mouse/`, `/ko/wifi-scout/`, `/ko/ssh-scout/`) | 2026-08-08 ~ 08-11 |

   **이 0개를 실패 신호로 읽지 않는다.** 이 보고서의 데이터 기준일은 2026-08-10 이고 목록의 최종
   크롤링도 8-11 이 최신인데, 개편 배포와 색인 요청은 8-14 다. 즉 **보고서가 아직 개편 이후 기간에
   도달하지 않았다.** 실제로 8-15 에 URL 검사(실시간)로 확인한 `/ko/tech-notes/` 는 이미 색인 완료였고
   그 URL 역시 이 CSV 에는 없다. 보고서와 URL 검사의 시차를 혼동하지 않는다.
4. **§10 의 새 URL 4개 live 검증 완료**, sitemap `<loc>` 70개 확인, `lastmod` 26건 정정(§10.1).
5. **우선순위 1의 10개를 요청했다 — 계획일(8-21~28)보다 앞당긴 근거.**

   - sitemap 이 제출 후 나흘째 한 번도 성공 수집되지 않았다. "먼저 sitemap 에 맡긴다" 는 §4.3 의
     대기 전제가 성립하지 않는다.
   - 위 3번대로 **새 구조 URL 중 색인된 것이 하나도 없고**, 이날 전수 검사한 18개도 전부 미색인에
     `참조 페이지: 감지된 페이지 없음` 이다. 내부 링크를 통한 발견도 일어나지 않고 있다.
   - 일일 할당량 10개는 **이월되지 않는다.** 8-15·16 의 20개는 이미 소멸했다. 기다림의 비용이 0 이 아니다.
   - 색인 요청과 sitemap 수집은 GSC 에서 독립된 경로다. 8-14 에 sitemap 이 실패 상태인 채로 11건이
     접수됐고 `/tech-notes/` 는 3분 뒤 실제로 크롤됐다. **오늘 요청이 8-20 재제출 판정을 오염시키지
     않는다** — 그 판정 기준은 sitemap 행의 상태·읽은 날짜이지 요청 이력이 아니다.

**다음 작업 → §6 의 가장 마지막 날짜 절에 있다.** 이 절의 목록은 그 뒤 갱신됐으므로 여기서 지웠다
(같은 목록을 날짜 절마다 복제하지 않는다는 §6 8-15 절의 규칙). 8-17 이 남긴 6개 항목 중 2번
(`lastmod` 26건 배포)은 8-18 에 완료됐고, 나머지는 8-18 절의 목록이 이어받았다.

### 2026-08-18 — 8-18 배포 검증과 날짜 신호 정정, 색인 요청 0건

이날은 사이트 쪽 작업만 했다. **색인 생성 요청은 하지 않았고 일일 할당량 10개를 한 건도 쓰지 않았다** —
GSC 접속이 필요한 항목은 아래 다음 작업 1~3번으로 남긴다.

1. **8-17 의 다음 작업 2번(§10.1 `lastmod` 26건 배포)이 완료됐다.** 별건 작업 커밋
   `9a397f5`(2026-08-18 18:05 KST, "블로그 앱 카드와 스크린샷을 Google Play 링크로 전환")가 그
   미커밋 변경을 함께 담아 배포했다. live `sitemap.xml` 이 저장소 파일과 완전히 동일함을 확인했다.
   **8-20 재제출의 선결 조건이 해소됐다.**
2. **같은 커밋이 기존 22개 URL 의 본문을 바꿨고 새 URL 은 없다.** sitemap `<loc>` 수는 70 그대로다.
   범위와 검증은 §11 에 적었다.
3. **live 전수 검증 — 70/70 정상.** sitemap 의 70개 URL 전부 `200` + self canonical 일치 +
   `noindex` 없음 + `hreflang` 2개 이상. 옛 제품 URL 14개도 14/14 가 `200` +
   `meta refresh 0; url=<대응 새 URL>` + 새 URL canonical + `noindex` 없음이다.
4. **어긋남 34건을 찾아 고쳤다(§11.1)** — `lastmod` 10 + `dateModified` 20 + 매뉴얼 화면 표기 4.
   §4.2 가 "배포마다 반복하는 점검" 으로 격상한 항목이 그 배포에서 실제로 또 걸렸다. 정정 작업은
   8-18 에 했고 **배포는 2026-08-19 에 이 정정 커밋으로 했다**(§11.1 머리말). 매뉴얼 화면 표기 4건은
   **diff 검열이 잡아낸 것**이다 — 구조화 데이터만 고치고 화면
   표기를 두면 Google 이 보이는 날짜를 채택해 정정이 무효화된다(§11.1 B-2).
5. **`bt-mouse` 스토어 링크 22곳이 현재 Google Play 에서 `404` 다(§11.2).** 배포 커밋이 재등록 예정
   패키지명 `com.droidactor.bt_mouse2` 를 게시 전에 선반영했기 때문이다. 사이트 쪽 결함은 아니지만
   검색·사용자 양쪽에 영향이 있어 별도 항목으로 남긴다.

**다음 작업 → §6 의 가장 마지막 날짜 절에 있다.** 이 절의 목록은 그 뒤 갱신됐으므로 여기서 지웠다
(같은 목록을 날짜 절마다 복제하지 않는다는 §6 8-15 절의 규칙). 8-18 이 남긴 9개 항목 중 1번
(§11.1 정정 배포)은 8-19 에 완료됐고, 나머지는 8-19 절의 목록이 이어받았다.

### 2026-08-19 — 처리방침 앵커 배포, `lastmod` 정정, Console·GSC 조회 5건

**색인 생성 요청은 하지 않았고 일일 할당량 10개를 한 건도 쓰지 않았다.** 오후에 전용 CDP 프로필
Chrome 으로 Play Console·Search Console 을 **읽기 전용**으로 조회해, 오래 열려 있던 미결 항목을 닫았다
(사용자가 쓰던 브라우저 창은 건드리지 않았고, 조회가 끝난 뒤 디버깅 포트를 닫았다).

1. **8-18 의 다음 작업 1번이 완료됐다.** §11.1 정정 34건을 커밋 `5cc2907`(06:42 KST)로 배포하고 live
   를 확인했다. `sitemap.xml` 이 저장소 파일과 동일(`2026-08-17` 0건, `2026-08-18` 22건), 매뉴얼 4개
   화면 표기가 `Updated 15 August 2026` / `2026년 8월 15일 갱신`, 70개 URL 재검증 70/70 정상.
2. **같은 날 배포가 하나 더 있었다 — 커밋 `a806fbd`(07:07 KST).** 홈 2개에 Simple Gas Mileage
   Calculator 개인정보 처리방침 블록을 추가했다. 새 URL 은 없고 sitemap `<loc>` 는 70 그대로다.
   범위와 검증은 §12 에 적었다.
3. **그 배포가 만든 `lastmod` 어긋남 2건을 같은 날 찾아 고쳤다.** `/`·`/ko/` 가 `2026-08-18` 로
   남아 있었다. §4.2 가 "배포마다 반복하는 점검" 으로 격상한 항목이 **8-17·8-18 에 이어 세 번째로
   적중**했다. 정정 후 분포는 `2026-08-14` 32 + `2026-08-15` 8 + `2026-08-16` 8 + `2026-08-18` 20 +
   `2026-08-19` 2 = **70** 이고 XML 은 well-formed 다.
4. **정정을 커밋 `54af9c8`(08:30 KST)로 배포하고 live 를 확인했다.** 08:32 KST 실측에서 live
   `sitemap.xml` 이 저장소 파일과 70개 전부 일치했다. 같은 커밋이 아래 5번의 문서 정정도 담았다.
5. **sitemap 미수집을 live 실측으로 재진단했다(§4.2).** 검사한 응답에서 결함은 없었고,
   개편을 원인으로 볼 근거도 없었다. 당시 백오프로 단정한 설명은 §4.2.1 에서 철회했다.
6. **개인정보 처리방침 시행일을 `19 August 2026` 로 갱신했다** — 커밋 `fb4a330`(11:19 KST).
   미결로 두었던 판단인데 저장소 선례(`8f3317d`)가 답을 갖고 있었다(§12.1). 홈 2개의 본문 변경이지만
   같은 날이라 `lastmod` 추가 정정은 없다.
7. **GSC 조회 3건 — 8-20 판정의 선결 확인이 전부 끝났다.**
   - **sitemap 세부 화면**: 마지막으로 읽은 날짜 `2026-08-13`, 발견 0, `사이트맵을 읽을 수 없음`.
     **8-15 값과 완전히 같다.** 이 값만으로 재시도가 없었다고 확정한 해석은 철회한다(§4.2.1).
   - **소유권 인증**: 확인 방법 `HTML 파일`, **사용자(2) 둘 다 소유자**, **미사용 소유권 토큰 0**.
     루트의 Google 확인 파일 두 개가 각각 살아 있는 인증 수단이다(§12.2).
   - **속성 정보**: `robots.txt` 유효, 크롤링 요청 213개(지난 90일), 속성 추가 2026-08-06.
     일반 크롤링이 진행된다는 관찰이며, 사이트맵 미처리의 원인을 확정하는 근거는 아니다(§4.2.1).
8. **Play Console 조회 2건 — 미결 2개를 닫고, 8-20 판정에 필요한 사실 하나를 얻었다.**
   - **처리방침 URL 7/7 정확**(§12). `#privacy-gas`·`#privacy-lgtv` 포함 전부 루트 앵커다.
   - **스토어 웹사이트 6/6 이 루트**(§5 의 미결 항목을 닫았다). 옛 제품 URL 사용 0건.
   - **`com.droidactor.bt_mouse2` 는 내부 테스트 트랙에서 검토 중(8-17~)이다**(§11.2). 프로덕션이
     아니므로 스토어 링크 `404` 는 승격 전까지 계속된다. 8-20 되돌림 판정에 직결된다.

**다음 작업 → §6 의 가장 마지막 날짜 절에 있다.** 이 절의 목록은 그 뒤 갱신됐으므로 여기서 지웠다
(같은 목록을 날짜 절마다 복제하지 않는다는 §6 8-15 절의 규칙). **항목을 번호로 가리키지 않고 내용으로
적는다** — 이 목록은 8-19 세션이 커밋 이후에도 두 차례 손봐서 커밋본(9개)과 지워질 때의 상태(7개)가
다르고, 번호로 적으면 `git show` 로 원본을 열어 본 다음 세션이 다른 항목을 집게 된다.

- **Page indexing 보고서를 최종 업데이트가 2026-08-14 이후로 넘어간 뒤에 읽기** → 8-24 에 완료
  (8-21 자 데이터를 읽었다).
- **2026-08-20 sitemap 재제출 판정** → **사용자 결정으로 취소**됐다(§4.2·§7).
- **GSC sitemap 세부 화면을 열어 마지막으로 읽은 날짜 기록** → 8-19 당일에 이행됐다(§4.2).
- **홈 2개의 처리방침 `Effective date` 를 어떻게 할지 정하기** → 8-19 당일에 결정됐다(§12.1).
- 나머지(색인 요청 계획, `bt-mouse` 판정, §5 링크 보고서, 옛 URL 대기, §11.1 재현 절차)는 8-24 절의
  목록이 이어받았다.

### 2026-08-24 — Page indexing 보고서 첫 판독, canonical 잠식 확정, sitemap 재제출 취소

**사이트 파일은 한 줄도 바꾸지 않았다.** 사용자가 쓰던 Chrome 창으로 GSC 를 조회·조작했고, 색인
생성 요청 2건(§4.3)만 상태를 바꾸는 행동이었다. 일일 할당량 10개 중 2개를 썼다.

1. **sitemap 은 세 번째 실측에서도 같다** — 목록·세부 화면 값이 8-15·8-19 와 한 글자도 다르지 않고,
   live sitemap·robots·Content-Type·Googlebot 접근·XML 유효성도 전부 정상이다(§4.2). 제출 후 11일째
   성공한 사이트맵 읽기가 보고되지 않았다. 크롤링 요청은 90일 302개로 8-19 의 213개보다 늘었다.
   재시도 유무·원인에 대한 당시 단정은 §4.2.1 에서 철회했다.
2. **8-20 sitemap 재제출은 실행하지 않고 종결했다. 사용자 결정이다.** 규칙대로라면 재제출할
   차례였으나 사용자가 **"sitemap 재제출은 하지말것. 해 봐도 늘 동일했고, 몇번째 반복 중."** 이라고
   지시했다.
   §4.2 의 해당 항목을 닫고 §7 금지 목록에 올렸다. **다음 세션은 이 항목을 미결로 되살리지 않는다.**
3. **Page indexing 보고서를 처음으로 개편 이후 데이터로 읽었다** — 8-19 의 다음 작업 4번이 걸어 둔
   조건("최종 업데이트가 2026-08-14 이후로 넘어간 뒤")이 충족됐다. 최종 업데이트 **2026-08-21**.

   | 항목 | 2026-08-17 확인(8-10 자) | 2026-08-24 확인(8-21 자) |
   |---|---|---|
   | 색인 생성됨 | 22 | **38** |
   | 색인이 생성되지 않음 | 5 | **10** |
   | 사유 종류 | 1종 | **2종** |

   | 사유 | 소스 | 유효성 검사 | 페이지 | 처음 감지 |
   |---|---|---|---|---|
   | `크롤링됨 - 현재 색인이 생성되지 않음` | Google 시스템 | 시작되지 않음 | **8** | 2026-08-08 |
   | `중복 페이지, Google에서 사용자와 다른 표준을 선택함` | Google 시스템 | 시작되지 않음 | **2** | 2026-08-15 |

   **색인 38 은 개편이 실제로 진척되고 있다는 첫 신호다.** 8-17 의 22 는 옛 URL·블로그·홈만이었고
   새 구조 URL 이 0개였다(그 절 3번). 16개가 늘어난 자리에 새 구조 URL 이 얼마나 들어갔는지는 이날
   확인하지 않았다 — **아래 다음 작업 1번이 그것이다.**
4. **`크롤링됨 - 현재 색인이 생성되지 않음` 8건의 전량 목록을 확보했다.**
   `/ko/` · `/blog/` · `/ko/blog/` · `/apps/` · `/ko/apps/` · `/manual/` ·
   `/ko/tech-notes/bt-keyboard/` · `/ko/bt-ppt/`

   **8개 중 7개가 허브·목록 페이지 또는 국문 홈이다**(허브 6 + `/ko/` 1). 이것은 목록으로 검산되는
   사실이다. **그 다음 한 문단은 추정이다** — 이 사유가 "오류가 아니라 우선순위 보류" 이고, 고유 본문이
   적고 링크 목록이 대부분인 페이지가 전형적으로 받는 판정이며, 외부 링크 0 · 내부 링크 그래프 0(§5)이
   겹쳐 신뢰 신호가 없다는 해석까지가 그것이다. **GSC 는 왜 보류했는지 알려주지 않으므로 다음 세션이
   사실로 인용하지 않는다**(§13.2 와 같은 규율).
   `/ko/bt-ppt/` 는 옛 URL 이므로 색인되지 않는 편이 §2.1 의 의도에 맞다.
   **`/apps/`·`/ko/apps/`·`/manual/` 은 8-14 에 색인 요청한 우선순위 0 의 URL 이다**(§4.3) — 요청이
   크롤까지는 끌어왔고 색인에서 멈췄다는 뜻이므로, 이 셋은 "요청했는데 실패" 가 아니라 "요청이 절반은
   먹혔다" 로 읽는다(이 읽기도 해석이다).
5. **`중복 페이지` 2건은 옛 URL 이 새 URL 의 표준을 잠식한 것으로 확정됐다 — 새 절 §13.**
   `/apps/lgtv/` 의 Google 선택 표준이 `/lgtv/`, `/ko/apps/lgtv/` 의 것이 `/ko/lgtv/` 다. 한·영 대칭.
   본문 중복·언어 쌍·내부 링크 잔존 세 가설은 실측으로 배제했고, 사이트 선언에는 고칠 잘못이 없다.
   **구조가 같은 옛 URL 이 9개이므로 나머지 8개도 같은 판정을 받을 수 있다**(§13.3).
6. **그 조치로 `lgtv` 2개에 색인 생성 요청을 넣었으나, 두 URL 은 8-14 에 이미 요청한 것이었다**(§4.3).
   GSC 버튼이 `다시 요청` 이었고 모달이 "여러 번 제출해도 우선순위가 변경되지 않습니다" 를 명시했다.
   **접수는 성공했으나 실효는 기대하기 어렵고 할당량 2개가 소모됐다.** 조치를 고르기 전에 §4.3 의
   `[x]` 목록을 먼저 읽는다는 것이 이날 얻은 실무 규칙이다.
7. **이 문서 자신의 누락 3건을 찾았다 — 8-19 세션이 남긴 것이고, 셋 다 배포·기록 정합에 관한 것이다.**

   - **2026-08-19 17:38 의 배포 커밋 `5beaf34` 가 이 문서에 한 줄도 없다.** 제목은
     "처리방침에 apartment·calendar·unit 앵커 추가와 §2~§4 사실 정정" 이고 `index.html` 99+/28-,
     `ko/index.html` 91+, `README.md` 2+ 를 바꿨다. **홈 2개의 처리방침 앵커가 8개에서 11개로 늘어난
     배포**다(`#privacy-apartment`·`#privacy-calendar`·`#privacy-unit` 추가, live 실측 11개 = HEAD 와
     일치하므로 배포까지 끝났다). §12 는 같은 날 오전의 `a806fbd`(앵커 1개 추가)만 다루고 이 배포를
     모른다. **`lastmod` 영향은 없다** — 실측상 `/`·`/ko/` 의 `lastmod` 가 `2026-08-19` 이고 이 배포도
     8-19 이므로 값은 어긋나지 않는다. **누락은 기록의 누락이고 값의 오류가 아니다.**
   - **§12 의 `Play Console 처리방침 URL 등록 확인 — 7/7 정확` 은 분모가 바뀐 뒤에 닫혔다.** 그 실측은
     8-19 오전(앵커 8개 / Console 앱 7개) 것인데 같은 날 17:38 에 앵커가 3개 늘었다. 새 앵커 3개에
     대응하는 앱이 Console 에 있는지, 있다면 처리방침 URL 이 그 앵커를 가리키는지는 **이날 확인하지
     않았다**(GSC 화면만 봤다). `index.html` 상단 주석이 "앵커 이름을 바꾸지 마라 — 스토어에 등록돼
     있다" 고 못박은 그 대조가 새 3개에 대해 비어 있다.
   - **작업 트리에 미배포 상태의 홈 2개 본문 변경이 있다.** `index.html` 57+/…, `ko/index.html` 53+/…,
     mtime 은 둘 다 **2026-08-19 17:44**(위 `5beaf34` 커밋 6분 뒤)다. 처리방침 §2·§4 권한 표·§6·§7 의
     사실 정정이고, **"One exception. Lunar anniversaries …" 단락이 새로 생겼다** — 캘린더 앱이 저장한
     항목은 앱을 지워도 기기의 캘린더에 남는다는 예외를 명시한 실질 변경이다. live 는 HEAD 와 같으므로
     **아직 배포되지 않았다.** 8-24 에는 이 변경을 손대지 않았다(§6 이 절 머리말의 "사이트 파일은 한
     줄도 바꾸지 않았다" 는 8-24 작업에 대한 서술이고, 이 대기 변경은 8-19 것이다).
     **배포하면 `/`·`/ko/` 의 `lastmod` 를 그 배포일로 올려야 하고**(§11.1), 처리방침 본문이 실질적으로
     바뀌었으므로 **§12.1 의 선례에 따라 `Effective date` 도 그 배포일로 올릴 대상이다.**

**다음 작업:**

1. **색인된 38개의 전량 목록을 CSV 로 내려받아 새 구조 URL 이 몇 개 들어갔는지 센다**
   (보고서 → `색인이 생성된 페이지에 대한 데이터 보기` → `내보내기` → `CSV 다운로드`). 8-17 에 같은
   경로로 22개를 받았고 그때는 새 구조 URL 이 0개였다. **이 수치가 개편의 성패를 처음으로 정량화한다.**
   16개 증가분의 정체를 모르는 채로 다른 판단을 쌓지 않는다.
2. **§13.5 의 세 항목 — `lgtv` 2개의 `Google에서 선택한 표준 URL`·`최근 크롤링` 재확인, 그리고
   `/apps/bt-keyboard/`·`/apps/wifi-scout/` 표본으로 확산 가설 검증.** URL 검사는 할당량을
   소모하지 않으므로 요청과 무관하게 자유롭게 쓴다.
3. **색인 생성 요청 — 8-19 가 계획한 `(ko/)tech-notes/bt-keyboard/` 허브 2개와 나머지 Tech Notes
   앱별 허브 8개가 그대로 남아 있다.** 8-24 는 이 계획 대신 `lgtv` 재요청에 2개를 썼다.
   요청 전에 **§4.3 의 `[x]` 목록으로 이미 요청한 URL 인지 확인**하고, 이 절 성과 4번(`크롤링됨` 8건이
   대부분 허브)이 밝힌 대로 허브가 미색인이면 상세의 발견 사슬이 끊긴다는 근거(§4.3)는 그대로 유효하다.
4. **`bt-mouse` 트랙·상태를 Play Console 에서 다시 읽고 판정한다** (§11.2). 8-19 실측이 **내부 테스트 /
   검토 중**이었고 8-20 판정일은 지났다. 프로덕션 승격이 확인되면 스토어 링크 22곳이 `200` 인지
   재확인하고, 아니면 배지·상태 문구를 게시 전 표기로 되돌린다.
5. **§5 링크 보고서를 다시 읽어 내부 링크가 여전히 0 인지 확인한다.** 색인이 38 로 늘었으므로 링크
   그래프가 0 인 상태의 설명이 "보고서 지연" 뿐인지 이제 판정할 수 있다.
6. **sitemap 은 관찰만 한다** — 재제출하지 않는다(이 절 성과 2번, §4.2, §7). 상태가 스스로 바뀌면 그때
   기록한다.
7. 옛 URL 은 계속 요청하지 않고 Googlebot 의 재크롤을 기다린다 (§4.4, §7, §13.4).
8. **다음 배포 때도 §11.1 재현 절차를 그대로 돌린다.** `git log -1` 을 기계적으로 쓰지 않는다.
9. **작업 트리의 미배포 홈 2개 변경을 어떻게 할지 정한다**(위 성과 7번 세 번째 항목). 배포하면
   `lastmod` 와 `Effective date` 를 그 배포일로 함께 올리고(§11.1·§12.1), 버릴 것이면 버린다.
   **어느 쪽이든 결정 전에 이 변경이 존재한다는 사실을 잊지 않는다** — 8-19 에 만들어져 닷새째 대기
   중이고, 문서에 없었기 때문에 8-24 세션도 뒤늦게 알았다.
10. **새 처리방침 앵커 3개(`#privacy-apartment`·`#privacy-calendar`·`#privacy-unit`)에 대응하는 앱이
   Play Console 에 있는지, 있다면 처리방침 URL 이 그 앵커를 가리키는지 확인한다**(위 성과 7번 두 번째
   항목). §12 의 `7/7` 은 앵커 8개 시절 값이므로 이 확인이 끝나면 그 항목의 분모를 갱신한다.

### 2026-08-31 — 색인 38 의 정체 확정, §13.5 판정 완료, 배포 1건

**배포 `4bb277f` 1건**("Play 스토어 링크에 hl 파라미터 부착과 bt-keyboard·bt-ppt 광고 표기 정정",
45개 파일), **GSC 조회**, **색인 생성 요청 10건**(전부 접수, 일일 할당량 정확히 소진)을 했다.
세션 중반까지는 `foreground` 확보가 막혀 요청이 불가능했고(성과 6번), 사람이 Chrome 창을 활성화한
뒤에 풀렸다.

1. **8-24 다음 작업 1번의 답 — 색인 38 개의 전량 목록을 CSV 로 확보했고, 증가 16 의 정체가 정확히
   갈렸다.** 8-17 CSV(데이터 기준 8-10, 22개)와 같은 축으로 비교한다.

   | 분류 | 2026-08-17 확인(8-10 자) | 2026-08-31 확인(8-21 자) |
   |---|---|---|
   | 홈 `/` | 1 | 1 |
   | 블로그 글(한·영) | 12 | 12 |
   | **새 구조 URL**(`/apps/`·`/manual/`·`/tech-notes/`) | **0** | **15** |
   | 옛 제품 스텁 | 9 | **10** |
   | 합계 | 22 | **38** |

   **증가 16 = 새 구조 URL 15 + 옛 스텁 `/ko/lgtv/` 1.** 이것이 개편의 첫 정량적 성과다 — 8-24 가
   "새 구조 URL 이 얼마나 들어갔는지는 확인하지 않았다" 며 남긴 자리에 15 가 들어간다.
   **`/ko/` 홈은 여전히 색인되지 않았다**(색인된 홈은 영문 `/` 하나뿐이다).

   새 구조 15개의 내역은 이렇다.

   | 묶음 | URL | 최종 크롤링 |
   |---|---|---|
   | `apps` 8 | `(ko/)apps/{bt-mouse, bt-ppt}/` | 2026-08-17 |
   | | `(ko/)apps/{wifi-scout, bt-keyboard}/` | 2026-08-14 |
   | `manual` 5 | `(ko/)manual/{lgtv, wifi-scout}/` | 2026-08-17 |
   | | `/ko/manual/` | 2026-08-14 |
   | `tech-notes` 2 | `(ko/)tech-notes/` | 2026-08-14 |

   옛 스텁 10개는 8-17 의 9개에 **`/ko/lgtv/` 가 더해진 것**이다. §13.3 이 "9개를 모집단으로 삼으면
   과소평가" 라며 `/ko/lgtv/` 를 지목했던 그 URL 이 실제로 CSV 에 나타났다.

2. **sitemap 70 개 중 색인은 28 개, 미색인이 42 개다.** 색인 38 에서 sitemap 밖의 옛 스텁 10 을 빼면
   28 이다. 미색인 42 의 전량 목록은 이렇다.

   | 묶음 | 개수 | URL |
   |---|---|---|
   | Tech Notes 전량 | 18 | `(ko/)tech-notes/{bt-keyboard, bt-mouse, bt-ppt, lgtv, ssh-scout, wifi-scout}/` 12 + `(ko/)tech-notes/bt-keyboard/` 상세 3편 × 한·영 6 |
   | 매뉴얼 | 8 | `(ko/)manual/{bt-keyboard, bt-mouse, bt-ppt, ssh-scout}/` |
   | 허브 | 5 | `/apps/`, `/ko/apps/`, `/manual/`, `/blog/`, `/ko/blog/` |
   | 제품 | 6 | `(ko/)apps/{lgtv, ssh-scout, yt-downloader}/` |
   | **블로그** | **4** | `(ko/)blog/{use-android-phone-as-bluetooth-mouse, use-android-phone-as-powerpoint-remote}/` |
   | 국문 홈 | 1 | `/ko/` |

   **블로그 4개가 미색인이라는 것은 이 문서에 없던 사실이다.** 블로그 16개 중 12개는 색인됐는데
   `bt-mouse`·`bt-ppt` 두 제품의 글만 한·영 모두 빠졌다. **`/ko/manual/` 은 색인됐는데 `/manual/` 은
   미색인**인 것도 같은 종류의 비대칭이다. **원인은 확인하지 않았다** — 다음 세션이 추정을 사실로
   인용하지 않는다.

3. **§13.5 세 항목이 전부 판정됐다.** `/apps/lgtv/` 를 URL 검사로 다시 읽었다(할당량 소모 없음).

   | 항목 | 2026-08-24 | 2026-08-31 |
   |---|---|---|
   | Google에서 선택한 표준 URL | `/lgtv/` | **`/lgtv/`**(그대로) |
   | 사용자 선언 표준 URL | `/apps/lgtv/` | `/apps/lgtv/` |
   | 최근 크롤링 | 2026-08-14 11:36:35 | **2026-08-24 09:51:21** |
   | Sitemaps | 감지된 참조 사이트맵이 없습니다 | **일시적인 처리 오류** |
   | 참조 페이지 | 감지된 페이지 없음 | **`https://droidactor.github.io/lgtv/`** |
   | 크롤링 허용 / 페이지 가져오기 / 색인 생성 허용 | — | 예 / 성공 / 예 |

   - **표준 URL 은 self 로 바뀌지 않았다.** §13.4 의 마지막 줄(손대지 않고 재크롤을 기다린다)이 유지된다.
   - **최근 크롤링은 움직였다.** 8-24 의 재요청이 크롤은 불러냈고 canonical 판정은 바꾸지 못했다.
     **즉 "크롤이 없어서 재평가가 안 된다" 는 선행 조건은 이제 해소됐는데도 결과가 같다** — §13.2 가
     추정으로 남겨 둔 "신호가 하나도 없다" 쪽 설명이 상대적으로 힘을 얻지만, **GSC 는 여전히 이유를
     말하지 않으므로 확정된 것은 위 표까지다.**
   - **참조 페이지가 처음으로 잡혔고 그것이 옛 스텁 `/lgtv/` 다.** 새 URL 을 가리키는 발견 경로가
     `meta refresh` 스텁 하나뿐이라는 뜻이다. 이것은 실측이다.
   - **확산 가설(§13.3)은 반증 쪽이다.** 표본 `(ko/)apps/{bt-keyboard, wifi-scout}/` 4개가 **전부 색인
     목록에 있고**(위 1번 표), `중복 페이지` 사유의 페이지 수도 8-24 와 같은 **2** 로 늘지 않았다.
     **lgtv 한·영 2개에 머물러 있다.** 다만 `/ko/lgtv/` 가 새로 색인된 것은 옛 스텁 쪽이 여전히
     강해지고 있다는 신호이므로, 나머지 12개 스텁 감시는 계속한다.

4. **§11.2 가 해소됐다 — `com.droidactor.bt_mouse2` 가 `200` 이다.** 8-19 실측(내부 테스트 / 검토 중)
   이후 프로덕션 승격이 끝났다. 6개 패키지 전부 `hl=en`·`hl=ko` 양쪽 `200` 이고, 대조군
   `com.droidactor.bt_mouse`(옛 이름)와 존재하지 않는 패키지는 `404` 라 검사가 실제로 구별함을
   확인했다. **되돌림 판정 자체가 소멸했으므로 배지·상태 문구는 그대로 둔다.** 8-20 기준일을 열하루
   넘겨 닫혔다.

5. **배포 `4bb277f` — 44개 페이지의 Play 링크에 `&hl=` 을 붙이고 광고 표기 2건을 정정했다(§14).**
   `lastmod` 44개를 `2026-08-31` 로 올리면서 8-24 배포분(`/`·`/ko/`)과 8-29 배포분(`/apps/lgtv/`)의
   밀린 값도 함께 흡수했다. `Effective date` 도 §12.1 규칙에 따라 올렸다.

6. **GSC 조작이 중간에 막혔다 — `foreground` 확보 실패.** `uia` lease 가 Chrome 창을 foreground 로
   만들지 못해(`The target window could not be made foreground for the desktop lease`) **키 입력과
   클립보드 붙여넣기가 필요한 작업이 전부 불가능했다.** 읽기와 `invoke`(UIA 패턴 호출)는 정상이므로
   위 1~3번은 그 경로만으로 얻었다 — CSV 내보내기는 메뉴 `invoke` 로, URL 검사는 검색창에 남아 있던
   **최근 검사 기록 항목을 `invoke`** 해서 열었다.
   URL 검사 입력창은 웹 콘텐츠라 `ValuePattern.SetValue` 가 거부되고(실측: read-back 이 빈 문자열)
   남는 경로인 `-ViaClipboard` 는 물리 입력이라 foreground 를 요구한다.
   **§5 링크 보고서는 우회로 뚫었고**(주소창 → 자동완성 `invoke`, 아래 7번) **색인 생성 요청만 남아
   있다가, 사람이 Chrome 창을 활성화한 뒤 풀려 10건을 접수했다**(§4.3).
   **다음 세션은 조작을 시작하기 전에 Chrome 창이 활성화돼 있는지 먼저 확인한다** — 읽기만 하다가
   중간에 막히면 그때까지의 절차를 다시 밟아야 한다.
   덧붙여 **GSC URL 검사 딥링크로 임의의 URL 을 검사할 수 없다 — `id` 는 URL 이 아니라 불투명한
   해시다.** `?resource_id=…&id=https%3A%2F%2F…%2Fapps%2Flgtv%2F` 로 만들어 넣으면 `404` 가 오고,
   실제로 열린 검사 화면의 주소는 `?resource_id=https%3A%2F%2Fdroidactor.github.io%2F&id=`
   **`qO3GZ4aMLVGhgul2Nqtttw`** 였다. 해시를 알 수 없으므로 주소창 우회는 원리적으로 닫혀 있다 —
   **다음 세션이 다시 시도하지 않는다.**
   반면 **`/search-console/links?resource_id=…`·`/search-console/index?resource_id=…` 같은 보고서
   딥링크는 정상 동작한다**(실측). 즉 막힌 것은 URL 검사 하나이고, 보고서 사이 이동은 주소창으로
   자유롭게 된다 — 주소창은 chrome 표면이라 UIA `set-text` 가 먹고, 이동은 자동완성 항목을 `invoke`
   하면 되므로 **foreground 없이도 보고서 열람은 전부 가능하다.**

7. **§5 링크 보고서를 읽었다 — 외부 0 / 내부 0 으로 8-15 와 동일하다.** 색인이 6 → 22 → 38 로 늘고
   16일이 지났는데도 그대로다. **같은 날 URL 검사는 `/apps/lgtv/` 의 참조 페이지를 `/lgtv/` 로 잡고
   있으므로 두 화면이 충돌한다** — Google 은 내부 링크를 보는데 링크 보고서만 비어 있다. §5 에
   적었고, **이 항목을 근거로 사이트를 고치지 않는다.**

9. **색인 생성 요청 10건을 접수했다 — 전부 Tech Notes 앱별 허브이고, 전부 `Google에 알려지지 않은
   URL` 이었다**(§4.3). `(ko/)tech-notes/{bt-keyboard, bt-ppt, bt-mouse, wifi-scout, ssh-scout}/` 10개다.
   **이 상태가 8-24 가 다룬 것과 다른 단계라는 점이 이 세션의 세 번째 발견이다** — `크롤링됨 - 현재
   색인이 생성되지 않음` 은 크롤은 됐고 색인에서 멈춘 것이지만, 이 10개는 `최근 크롤링`·`페이지
   가져오기`·`사용자 선언 표준 URL` 이 전부 `해당사항 없음` 이다. **Google 이 존재 자체를 모른다.**
   sitemap 미수집(§4.2)과 링크 보고서 0(§5)이 겹친 결과가 이 형태로 드러난 것으로 보이나,
   **인과는 GSC 가 말하지 않으므로 추정이다.**
   `lgtv` 한·영 2개는 일부러 남겼다 — 같은 제품의 `/apps/lgtv/` 가 §13 의 중복 판정을 받고 있어
   결과 해석이 오염된다.

8. **8-24 다음 작업 10번도 닫았다 — 처리방침 앵커 ↔ 스토어 등록 대조가 `9/9` 정확이다**(§12).
   **Play Console 에 들어갈 필요가 없었다** — 스토어 등록정보의 처리방침 URL 은 공개 값이라
   `details?id=<pkg>&hl=en` 의 HTML 에서 그대로 읽힌다. 8-19 세션이 Console UI 로 하던 확인을
   `curl` 한 번으로 대체할 수 있다는 뜻이므로, **다음 세션은 이 경로를 먼저 쓴다.**
   새 앵커 3개 중 `#privacy-apartment`(`com.app.apartment`)·`#privacy-calendar`
   (`com.droidactor.DrCalendar`)는 앱이 게시돼 있고 둘 다 정확한 앵커를 가리킨다.
   `#privacy-unit`(`com.app.unitconverter`)은 앱이 미게시라 대조 대상이 아니다.

**다음 작업:**

1. ~~`4bb277f` 의 live 배포를 검증한다.~~ **같은 날 완료했다 — §14.5.** 44개 URL 의 `hl=` 44/44,
   live `sitemap.xml` 이 저장소 파일과 완전 동일, `lastmod` 분포 70, `Effective date` 양쪽 정상,
   옛 광고 주장 잔존 0건.
2. ~~§5 링크 보고서를 읽어 내부 링크가 여전히 0 인지 확인한다.~~ **같은 날 완료했다 — §5.**
   외부 0 / 내부 0 으로 **8-15 와 동일**하다. 색인이 38 로 늘고 16일이 지났는데도 그대로이고,
   같은 날 URL 검사는 `/apps/lgtv/` 의 참조 페이지를 `/lgtv/` 로 **잡고 있다** — 두 화면이 충돌한다.
   **이 속성에서 링크 보고서가 채워지지 않는 것으로 보고, 이 항목을 근거로 사이트를 고치지 않는다.**
3. ~~색인 생성 요청 — §4.3 의 미체크 22개가 그대로다.~~ **10건을 요청해 전부 접수됐다(§4.3).**
   `(ko/)tech-notes/{bt-keyboard, bt-ppt, bt-mouse, wifi-scout, ssh-scout}/` 10개다.
   **남은 것은 12개** — `(ko/)tech-notes/lgtv/` 2개(오늘 일부러 미룬 것, 다음 요청일 1순위)와
   §4.3 우선순위 2 의 10개(`(ko/)apps/{ssh-scout, yt-downloader}/`, `(ko/)manual/{bt-ppt, bt-mouse,
   ssh-scout}/`). 전부 위 2번의 미색인 42 안에 있다.
   **요청 사이에 20초 이상 둔다** — 간격이 짧으면 `제출하는 중에 문제가 발생했습니다` 가 난다(§4.3).
   **2026-09-02 에 이 12개 중 10개를 요청해 전부 접수됐다** — 남은 것은 `(ko/)apps/yt-downloader/`
   2개뿐이다(아래 2026-09-02 절). 간격 규칙은 그대로 지켰고 일시적 오류는 한 번도 나지 않았다.
4. ~~새 처리방침 앵커 3개에 대응하는 앱의 처리방침 URL 을 확인한다.~~ **같은 날 완료했다 — §12.**
   **게시된 앱 기준 `9/9` 정확**이고 `#privacy-unit`·`#privacy-ytdl` 은 앱이 미게시라 대조 대상이
   아니다. **Play Console 에 들어갈 필요가 없었다** — 스토어 등록정보의 처리방침 URL 은 공개 값이라
   `details?id=<pkg>&hl=en` HTML 에서 읽힌다. 다음에도 이 경로를 먼저 쓴다.
5. **sitemap 이미지 확장(`<image:image>`)은 보류한다 — 2026-08-31 사용자 결정이다.**
   8-27~8-29 에 스크린샷 57장이 늘었는데(webp 55 + png 2) sitemap 에 이미지 항목이 0개다.
   `robots.txt` 는 `Allow: /` 이므로 크롤 자체는 막히지 않아 **넣지 않아도 결함이 아니다.**
   **판단 시점을 미색인 42개가 줄어드는 것을 본 뒤로 정했다** — 지금 이미지 항목을 더하면 sitemap 이
   커지는데 그 수집이 아직 성공한 적이 없다(§4.2). **다음 세션이 이 항목을 미결로 되살려 임의로
   넣지 않는다.** 미색인 수가 의미 있게 줄면 그때 다시 꺼낸다.
6. **sitemap 은 관찰만 한다** — 재제출하지 않는다(§4.2, §7). 위 3번 표의 `Sitemaps: 일시적인 처리 오류`
   는 8-24 의 `감지된 참조 사이트맵이 없습니다` 에서 바뀐 값이므로, 상태가 스스로 움직이고 있다는
   기록으로만 남긴다.
7. 옛 URL 은 계속 요청하지 않고 Googlebot 의 재크롤을 기다린다 (§4.4, §7, §13.4).
8. **다음 배포 때도 §11.1 재현 절차를 그대로 돌린다.** `git log -1` 을 기계적으로 쓰지 않는다.
   특히 `4bb277f` 는 **40개 파일이 링크 부착뿐이고 4개만 본문이 바뀐 혼합 커밋**이므로, 이 커밋을
   근거로 블로그 16개·매뉴얼 12개의 `dateModified` 를 당기지 않는다(§14).


### 2026-09-02 — 색인 생성 요청 10건 접수, 배포 0건

**색인 생성 요청 10건을 전부 접수시켜 일일 할당량을 정확히 소진했다.** 배포는 없었고 sitemap 은
건드리지 않았다(§7). 8-31 이 남긴 "다음 작업 3번"(남은 12개)을 10개까지 밀어낸 것이 이 세션의 전부다.

1. **요청 10건 — 8-31 이 남긴 12개 중 10개다.** 1순위였던 `(ko/)tech-notes/lgtv/` 2개를 먼저 쓰고,
   §4.3 우선순위 2 의 10개 중 8개를 이었다. **10개 전부 `색인 생성 요청됨` 을 받았고, 대화상자를 닫은
   뒤 할당량 초과가 뒤따른 건은 0건이다**(§4.3 의 성공 판정 규칙대로 닫은 다음까지 확인했다).

   | 묶음 | URL |
   |---|---|
   | Tech Notes(1순위) | `(ko/)tech-notes/lgtv/` 2 |
   | 제품 | `(ko/)apps/ssh-scout/` 2 |
   | 매뉴얼 | `(ko/)manual/{bt-ppt, bt-mouse, ssh-scout}/` 6 |

   **10개 전부 검사 시점에 `URL이 Google에 등록되어 있지 않음` + `Google에는 아직 알려지지 않은
   URL입니다` 였다.** `Sitemaps: 감지된 참조 사이트맵이 없습니다` / `참조 페이지: 감지된 페이지 없음` /
   `최근 크롤링`·`크롤링 허용 여부`·`페이지 가져오기` 전부 `해당사항 없음` 이다. **8-31 의 Tech Notes
   10개와 같은 단계**이고, 8-24 가 다룬 `크롤링됨 - 현재 색인이 생성되지 않음` 과는 다른 단계다.
   **오늘 요청한 10개는 전부 2026-08-14 배포분**(§2.2 매뉴얼 6 + §2.3 Tech Notes 2 + §2.1 제품 2)
   **이고 그날 이후 계속 sitemap 에 실려 있었다 — 19일이 지나도록 Google 은 존재 자체를 모른다.**

2. **남긴 2개는 `(ko/)apps/yt-downloader/` 다 — 미출시 앱이라 뒤로 미뤘다.** 8칸을 나눌 때 출시 여부로
   갈랐고, 6종 중 미출시는 `yt-downloader` 하나뿐이다. **다음 요청일 1순위다.**

3. **요청 전 live 사전 점검 — 12개 전부 통과했다.** `curl` 로 12/12 가 `200` + self canonical +
   `noindex` 없음이다. GSC 의 실시간 테스트가 요청 버튼에 딸려 도는 것(§4.3)과 별개로, **요청 대상이
   페이지 쪽 결함이 아님을 요청 전에 사이트 쪽에서 먼저 확정**해 두면 접수 실패의 해석이 갈리지 않는다.

4. **8-31 성과 6번의 `foreground` 차단은 재현되지 않았다 — 사람 개입 없이 10건 전부 자동으로 넣었다.**
   시작 시점의 foreground 는 Chrome 이 아니라 WezTerm(이 세션의 터미널)이었는데도 `uia` lease 가
   Chrome 창을 정상적으로 앞으로 끌어왔다. **8-31 의 실패 원인은 확인되지 않았고 이번에 재현되지도
   않았으므로, "Chrome 을 미리 활성화해 둬야 한다" 를 전제로 삼지 않는다** — 다만 실패하면 사람에게
   창 활성화를 요청하는 8-31 의 우회로는 그대로 유효하다.

5. **조작 경로는 8-31 기록 그대로 재현됐다.** 검사창(`ComboBox`, class `Ax4B8 ZAGvjd`)은 웹 콘텐츠라
   `ValuePattern.SetValue` 가 거부된다(실측: read-back 이 빈 문자열). 남는 경로는
   `set-text -ViaClipboard` 뿐이고 물리 입력이라 foreground 를 요구한다. **`-ViaClipboard` + `key enter`
   를 `uia batch` 하나로 묶으면 URL 당 foreground 점유가 1회로 준다** — 10개를 개별 실행하면 20회다.
   요청 버튼 클릭(`invoke`)과 대화상자 닫기는 UIA 패턴 호출이라 foreground 가 필요 없다.

6. **결과 카드 갱신이 60초를 넘는 경우가 있다.** 10번째(`/ko/manual/ssh-scout/`)에서 60초 폴링이
   직전 URL 의 카드만 보고 시간 초과했는데, 그 직후 스크린샷에는 새 URL 의 카드가 정상으로 떠 있었다.
   **"카드에 새 URL 이 안 뜬다" 를 곧바로 실패로 읽지 않는다** — 화면을 한 번 확인한 뒤 판정한다.

7. **요청 버튼의 UIA 이름 `색인 생성 요청다시 요청` 은 요청 이력의 표시가 아니다**(§4.3 에 적었다).
   요청 이력이 없는 URL 에서도 같은 이름이 읽히고, 같은 순간 화면의 라벨은 `색인 생성 요청` 하나다.
   **8-24 가 재요청 사실을 뒤늦게 알아챈 근거 두 개 중 하나가 이것으로 무효가 된다** — 남는 근거는
   성공 모달 본문("여러 번 제출해도 대기열 위치나 우선순위가 변경되지 않습니다") 하나이고, 이력 판정은
   §4.3 의 `[x]` 목록으로만 한다.

8. **§4.3 의 우선순위 목록이 미색인 42개를 다 덮지 않는다는 것이 이 세션에서 분명해졌다.**
   오늘까지의 요청으로 목록에 남은 미요청은 `(ko/)apps/yt-downloader/` 2 + bt-keyboard 상세 노트 6
   (§9 2 + §10 4) 뿐인데, 8-31 이 확정한 미색인 42 안에는 **§4.3 의 어느 우선순위에도 들어 있지 않은
   URL 이 7개** 더 있다 — `/blog/`, `/ko/blog/`(허브 2), `(ko/)blog/use-android-phone-as-bluetooth-mouse/`
   와 `(ko/)blog/use-android-phone-as-powerpoint-remote/`(블로그 글 4), `/ko/`(국문 홈 1).
   **이 7개는 경로 개편으로 생긴 URL 이 아니라 §2.4 의 "변경되지 않은 URL" 쪽**이라 이 문서의 요청
   계획이 처음부터 대상으로 잡지 않았다. 블로그 16개 중 12개가 색인된 것과 대조하면 **같은 묶음 안에서
   4개만 빠진 것**이고, `/ko/` 홈은 영문 `/` 가 색인된 것과 대조된다. **원인은 확인하지 않았다** —
   다음 세션이 추정을 사실로 인용하지 않는다.

**다음 작업:**

1. **`(ko/)apps/yt-downloader/` 2개를 요청한다** — 다음 요청일 1순위다. 오늘 미출시라는 이유로만
   미룬 것이고 다른 결격 사유는 없다(live `200` + self canonical + `noindex` 없음을 오늘 확인했다).
2. **bt-keyboard 상세 노트 6개**(§9 2 + §10 4)를 그 다음에 둔다. 다만 **8-31·9-02 에 요청한 앱별
   Tech Notes 허브 12개가 색인되는지 먼저 본다** — 허브가 색인되면 내부 링크로 딸려 발견될 수 있어
   할당량을 아낀다(§4.3 의 같은 원칙). 허브가 2주 안에 색인되지 않으면 상세를 직접 요청한다.
3. **8-31·9-02 요청분 20개의 색인 반영 여부를 확인한다.** 8-31 의 Tech Notes 허브 10 + 오늘의 10 이다.
   URL 검사는 할당량을 소모하지 않으므로 요청일과 무관하게 돌린다. **`아직 알려지지 않은 URL` →
   `크롤링됨 - 현재 색인이 생성되지 않음` 으로 단계가 올라갔는지**가 이번 요청이 먹혔는지의 첫 신호다.
4. **위 8번의 7개(`/blog/`·`/ko/blog/`·블로그 글 4·`/ko/`)를 어떻게 할지 정한다.** 요청 계획에 넣을지
   여부가 아직 결정된 적이 없다. **임의로 요청에 태우지 않는다** — 할당량이 위 1·2 번과 경합한다.
5. sitemap 이미지 확장은 계속 보류한다 — 8-31 사용자 결정이고 조건(미색인 42 가 줄어드는 것)이 아직
   충족되지 않았다.
6. sitemap 은 관찰만 한다 — 재제출하지 않는다(§4.2, §7).
7. 옛 URL 은 계속 요청하지 않고 Googlebot 의 재크롤을 기다린다(§4.4, §7, §13.4).

### 2026-09-05 — 색인 생성 요청 10건 접수(할당량 소진), 9-02 요청분 10/10 색인 확인, 배포 0건

**일일 할당량 10개를 정확히 소진했다.** 세션 전반에 `(ko/)apps/yt-downloader/` 2개를 요청해 §4.3 의
체크박스 목록을 닫았고, 8-31·9-02 요청분 20개를 실시간 URL 검사로 전수 확인했다. **그 뒤 사용자가
"지금 다 색인 요청" 을 지시해**, 남아 있던 후보 13개(bt-keyboard 상세 6 + §4.3 밖 7)를 전부 요청
대상으로 올리고 **오늘 가능한 8개를 이어서 요청했다.** 배포는 없었고 sitemap 은 건드리지 않았다(§7).

**이 지시로 이 문서의 두 가지 대기 상태가 해제됐다** — ① bt-keyboard 상세 6개의 "허브 색인을 기다린다"
(§4.3, 9-02 다음 작업 2번) ② §4.3 밖 7개의 "요청 계획에 넣을지 결정된 적이 없다"(9-02 다음 작업 4번).
**둘 다 사용자 결정으로 닫혔고, 이 절이 그 근거다.** 남은 5개는 할당량 때문에 다음 요청일로 넘어간
것이지 보류가 아니다.

1. **색인 생성 요청 10건 — 전부 접수됐고 할당량 초과는 0건이다.** 순서와 검사 시점 상태는 이렇다.

   | # | URL | 요청 시점 상태 |
   |---|---|---|
   | 1 | `/apps/yt-downloader/` | 아직 알려지지 않은 URL |
   | 2 | `/ko/apps/yt-downloader/` | 아직 알려지지 않은 URL |
   | 3 | `/tech-notes/bt-keyboard/why-google-tv-does-not-detect-bluetooth-hid.html` | 아직 알려지지 않은 URL |
   | 4 | `/ko/tech-notes/bt-keyboard/why-google-tv-does-not-detect-bluetooth-hid.html` | 아직 알려지지 않은 URL |
   | 5 | `/tech-notes/bt-keyboard/why-korean-english-key-labels-drift.html` | 아직 알려지지 않은 URL |
   | 6 | `/ko/tech-notes/bt-keyboard/why-korean-english-key-labels-drift.html` | 아직 알려지지 않은 URL |
   | 7 | `/ko/tech-notes/bt-keyboard/sharing-one-bluetooth-hid-session.html` | **크롤링됨 - 현재 색인이 생성되지 않음** |
   | 8 | `/tech-notes/bt-keyboard/sharing-one-bluetooth-hid-session.html` | 아직 알려지지 않은 URL |
   | 9 | `/ko/` | **크롤링됨 - 현재 색인이 생성되지 않음** |
   | 10 | `/blog/` | **크롤링됨 - 현재 색인이 생성되지 않음** |

   10건 모두 `색인 생성 요청됨 — URL이 우선순위 크롤링 대기열에 추가되었습니다` 를 받았고,
   **성공 대화상자를 닫은 뒤 할당량 초과가 뒤따른 건은 0건**이다(§4.3 의 성공 판정 규칙대로 닫은
   다음까지 확인했다). 요청 사이에는 25초를 뒀다(§4.3 의 20초 규칙).
   요청 전 live 사전 점검은 **후보 13개 전부**에 대해 돌렸고 **13/13 이 `200` + self canonical +
   `noindex` 없음 + live `sitemap.xml` 의 `<loc>` 에 포함**이다.

   **7번이 오늘 아침 스캔과 다르다.** 같은 URL 을 아침에 검사했을 때는 `아직 알려지지 않은 URL` 이었는데
   (아래 4번), 몇 시간 뒤 요청 직전에는 `크롤링됨 - 현재 색인이 생성되지 않음` 이다. 그 사이에 같은
   폴더의 형제 문서 3건을 요청했으므로 **그것이 이 페이지의 크롤을 끌어왔을 가능성이 있으나, GSC 가
   인과를 말하지 않으므로 추정이다.** 아래 9번(같은 URL 에서 검사 값이 흔들린 8-31 사례)과 함께 읽는다.

   **남은 5개는 다음 요청일로 넘어간다** — `/ko/blog/` 와
   `(ko/)blog/{use-android-phone-as-bluetooth-mouse, use-android-phone-as-powerpoint-remote}/` 4개다.
   **오늘 못 한 이유는 할당량뿐이고 다른 결격 사유는 없다**(위 사전 점검 13/13 통과).

2. **9-02 요청분 10개가 전량 색인됐다 — 10/10 이 `URL이 Google에 등록되어 있음` 이다.**

   | 묶음 | URL | 9-02 검사 | 9-05 검사 |
   |---|---|---|---|
   | Tech Notes | `(ko/)tech-notes/lgtv/` 2 | 아직 알려지지 않은 URL | **색인 생성됨** |
   | 제품 | `(ko/)apps/ssh-scout/` 2 | 아직 알려지지 않은 URL | **색인 생성됨** |
   | 매뉴얼 | `(ko/)manual/{bt-ppt, bt-mouse, ssh-scout}/` 6 | 아직 알려지지 않은 URL | **색인 생성됨** |

   표본으로 `/apps/ssh-scout/` 의 세부값을 읽었다 — **최근 크롤링 `2026. 9. 2. 오후 3:53:49`**
   (요청 당일이다), 크롤링 허용 / 페이지 가져오기 / 색인 생성 허용 = 예 / 성공 / 예,
   사용자 선언 표준 URL = `/apps/ssh-scout/`, **Google에서 선택한 표준 URL = `검사된 URL`**(self 일치).
   **§6 완료 조건의 "표본 검사한 새 URL의 canonical이 Google-selected canonical과 일치한다" 가 이
   표본에서 충족된다.**

3. **그런데 8-31 요청분 10개는 대부분 그대로다 — 색인 1, 크롤됨 1, 미발견 8 이다.**

   | URL | 9-05 판정 | 최근 크롤링 |
   |---|---|---|
   | `/tech-notes/bt-keyboard/` | **색인 생성됨** | 2026. 8. 31. 오후 9:06:36 |
   | `/ko/tech-notes/bt-keyboard/` | **크롤링됨 - 현재 색인이 생성되지 않음** | 2026. 8. 17. 오전 8:25:16 |
   | `(ko/)tech-notes/{bt-ppt, bt-mouse, wifi-scout, ssh-scout}/` 8 | 아직 알려지지 않은 URL | 해당사항 없음 |

   `/tech-notes/bt-keyboard/` 는 사용자 선언 표준 URL = self, Google 선택 표준 = `검사된 URL` 이고
   **크롤 시각이 요청 당일**이다. `/ko/tech-notes/bt-keyboard/` 는 **참조 페이지가
   `https://droidactor.github.io/tech-notes/bt-keyboard/`**(영문 허브)로 잡혀 있다. **다만 이 URL 의
   크롤은 `2026. 8. 17.` 로 영문 허브의 색인(8-31)보다 2주 앞서고, 8-24 보고서도 이미 이 URL 을
   `크롤링됨` 8건에 넣고 있었다**(§6 2026-08-24 성과 4번). **즉 이 참조 관계가 언제 생겼는지는 GSC 가
   말하지 않으며, "영문 허브가 색인돼서 국문이 발견됐다" 고 읽을 근거는 없다.**
   미발견 8개는 표본 `/tech-notes/bt-ppt/` 에서 `최근 크롤링`·
   `크롤링 허용 여부`·`페이지 가져오기`·`색인 생성 허용 여부`·`사용자 선언 표준 URL`·
   `Google에서 선택한 표준 URL` 이 **전부 `해당사항 없음`** 이다.

   **즉 순서가 역전됐다 — 먼저 요청한 8-31 묶음은 5일이 지나도 8개가 미발견인데, 나중에 요청한 9-02
   묶음은 3일 만에 10개 전부 색인됐다.** 8-31 요청 10건 중 크롤을 유발한 것은 영문 bt-keyboard
   1건뿐이다. **원인은 확인하지 않았다** — GSC 는 접수된 요청이 왜 크롤로 이어지지 않았는지 말하지
   않는다. 다음 세션이 추정을 사실로 인용하지 않는다. **다만 "요청 접수 = 크롤 보장" 이 아니라는 것은
   이 두 묶음의 대조로 실측됐다.**

4. **bt-keyboard 상세 노트 6개를 전부 요청했다 — 기다리기로 했던 것을 사용자 지시로 뒤집었다.**
   세션 전반의 판단은 "기다린다" 였고 그 근거는 이랬다. 9-02 다음 작업 2번의 원문은 **"허브가 2주
   안에 색인되지 않으면 상세를 직접 요청한다"** 로 **2주 시한의 주어가 허브**인데, 영문 허브
   `/tech-notes/bt-keyboard/` 가 8-31 에 색인돼 **그 트리거가 소멸했고 원 규칙은 그 다음의 시한을
   주지 않는다.** 그래서 원 규칙 전반부("허브가 색인되면 내부 링크로 딸려 발견될 수 있으므로
   기다린다")를 따라 대기로 두고, 기한 **2026-09-14** 는 이번 세션이 새로 정했다고 명시했다.
   **그 뒤 사용자가 "지금 다 색인 요청" 을 지시해 6개를 즉시 요청했다** — 위 1번 표의 3~8번이다.

   **기다림의 근거는 애초에 약했고, 그것이 이 지시를 정당화한다.**
   - **오늘 아침 6개 검사에서 6/6 이 `Google에는 아직 알려지지 않은 URL입니다` 였다** — 영문 허브가
     색인된 지 5일이 지났는데 상세로는 내려오지 않았다.
   - **"허브 색인 → 상세 발견" 이 이 사이트에서 일어난다는 증거가 이 문서에 없다.** 내부 링크 발견의
     유일한 실측 선례는 8-14 `/tech-notes/` 요청이 다음날 `/ko/tech-notes/` 를 끌어온 건 하나뿐이고
     (§4.3 Tech Notes 묶음), 위 3번의 `/ko/tech-notes/bt-keyboard/` 는 크롤이 8-17 이라 그 사례가 아니다.
   - **기다림의 근거는 영문 3개에만 성립했다.** 국문 3개의 부모 허브 `/ko/tech-notes/bt-keyboard/` 는
     오늘도 색인이 아니라 `크롤링됨 - 현재 색인이 생성되지 않음` 이다(위 3번).
   - 반면 **요청이 크롤을 부른다는 것은 오늘 9-02 묶음 10/10 으로 확인됐다**(위 2번).

   **이로써 §4.3 이 계획으로 명시한 URL 중 미요청은 0 이 됐다.**

5. **§4.3 의 어느 우선순위에도 없는 7개가 사용자 결정으로 요청 계획에 편입됐다.** `/blog/`·`/ko/blog/`
   (허브 2), `(ko/)blog/{use-android-phone-as-bluetooth-mouse, use-android-phone-as-powerpoint-remote}/`
   (블로그 글 4), `/ko/`(국문 홈 1)이다. 8-31 이 미색인 42 안에서 찾아내고 9-02 가 "요청 계획에 넣을지
   여부가 아직 결정된 적이 없다" 고 남긴 뒤로 **처음 내려진 결정**이다.

   **오늘 요청한 것은 2개다** — `/ko/`(국문 홈)와 `/blog/`(영문 블로그 허브). 할당량 10 중 8번째·
   9번째·10번째 자리에 이 둘과 상세 1개가 들어갔다. **고른 기준은 두 가지다.**
   - `/ko/` 는 **영문 `/` 가 색인된 것과 대조되는 국문 홈**이라 이 묶음에서 단일 가치가 가장 크다.
   - `/blog/` 는 허브이고, **8-14 `/tech-notes/` → 다음날 `/ko/tech-notes/` 선례**대로 `hreflang` 을
     타고 `/ko/blog/` 를 끌어올 가능성이 있다. 그렇게 되면 다음 요청일에 한 칸을 아낀다.
     **다만 그 선례는 1건뿐이므로 기대일 뿐이고, `/ko/blog/` 는 다음 요청일 목록에 그대로 남겨 둔다.**

   **둘 다 요청 시점 상태가 `크롤링됨 - 현재 색인이 생성되지 않음` 이었다**(위 1번 표) — 오늘 요청한
   나머지 8개 대부분이 `아직 알려지지 않은 URL` 인 것과 다른, 한 단계 앞선 상태다. 8-24 보고서가
   `크롤링됨` 8건을 보고한 것과 같은 계열로 보이나 **대조는 하지 않았다.**

   **남은 5개**(`/ko/blog/` + 블로그 글 4)는 다음 요청일 1순위다.

6. **§13 은 움직이지 않았다 — `(ko/)apps/lgtv/` 둘 다 여전히 `중복 페이지, Google에서 사용자와 다른
   표준을 선택함` 이다.** 둘 다 오늘 URL 검사로 판정을 읽었고, 세부값까지 읽은 것은 국문 쪽이다
   (영문 `/apps/lgtv/` 의 세부값은 8-31 판독이 최신이다).

   **§13.5 첫 항목의 추론이 오늘 닫혔다.** 8-31 은 검색창 입력이 막혀 `/ko/apps/lgtv/` 를 **직접 읽지
   못했고**, "색인 38 목록에 없고 `중복 페이지` 사유가 2 로 유지되므로 같은 상태로 본다 — **이 한 줄은
   추론이다**" 라고 남겼다. **오늘 처음 직접 읽었고 그 추론이 맞았다.**

   | 항목 | 값 | 비고 |
   |---|---|---|
   | Google에서 선택한 표준 URL | `https://droidactor.github.io/ko/lgtv/` | 옛 스텁. self 아님 |
   | 사용자 선언 표준 URL | `https://droidactor.github.io/ko/apps/lgtv/` | |
   | 최근 크롤링 | **2026. 8. 24. 오전 9:55:22** | §13 이 기록한 `2026-08-14 11:36:35` 에서 전진 |
   | 참조 페이지 | `https://droidactor.github.io/ko/lgtv/` | 옛 스텁 하나 |
   | Sitemaps | 감지된 참조 사이트맵이 없습니다 | |

   **최근 크롤링은 이 URL 에서 처음 읽은 값이고, 8-24 재요청이 국문 쪽에서도 재크롤을 불러냈다는
   실측이다** — 영문 `/apps/lgtv/` 의 `2026-08-24 09:51:21`(§13.5)보다 4분 뒤다. **그런데도 표준 URL
   판정은 영문과 똑같이 바뀌지 않았다.**

   **대조는 8-31 보다 넓어졌다** — 8-31 성과 1번이 이미 `(ko/)manual/lgtv/` 가 색인됐음을 기록했고,
   오늘 `(ko/)tech-notes/lgtv/` 2개가 여기에 더해졌다(위 2번). 즉 lgtv 의 canonical 잠식은 **제품
   URL 에만** 걸려 있고 같은 제품의 manual·tech-notes 경로는 정상으로 색인된다.
   §13.4 의 마지막 줄(손대지 않고 재크롤을 기다린다)을 유지한다.

7. **Page indexing 보고서의 데이터 기준일은 `8-21` 에서 `26. 8. 28.` 로 일주일 전진했는데 수치는
   그대로다.** 8-31 세션이 판독한 것은 8-21 자였고(§6 2026-08-31 성과 1번 표 머리), 오늘은 8-28 자다.
   그런데 `알려진 모든 페이지` 기준 **색인 생성됨 38 / 색인이 생성되지 않음 10**(이유 2개 —
   `크롤링됨 - 현재 색인이 생성되지 않음` 8, `중복 페이지, Google에서 사용자와 다른 표준을 선택함` 2)
   으로 8-31 판독과 같다. 새로고침해도 같은 값이다.
   **즉 8-28 까지의 데이터에도 8-31 요청분이 아직 반영되지 않았다.** 9-02·9-05 요청분은 데이터 기준일
   자체가 그 뒤이므로 애초에 들어올 수 없다.
   **보고서 수치가 그대로인 것을 "요청이 안 먹혔다" 로 읽지 않는다** — 오늘 실시간 검사는 9-02 요청분
   10/10 색인을 보여준다. 두 화면의 시차를 수치 차이로 오해하지 않는다.

8. **sitemap 은 색인된 URL 에서도 발견 경로가 아니다.** 오늘 세부값을 읽은 4개
   (`/apps/ssh-scout/`, `/tech-notes/bt-keyboard/`, `/ko/tech-notes/bt-keyboard/`, `/tech-notes/bt-ppt/`)
   가 **전부 `Sitemaps: 감지된 참조 사이트맵이 없습니다`** 이고, 그중 둘은 색인된 URL 이다.
   §4.2 가 기록해 온 sitemap 미수집이 색인 여부와 무관하게 유지되고 있다는 뜻이다. **재제출하지
   않는다**(§4.2, §7).

9. **8-31 의 `/ko/tech-notes/bt-keyboard/` 판독이 틀렸다는 것이 오늘 확정됐다 — 다만 원인은
   확인하지 않았다.** 8-31 성과 9번(§6)은 이 URL 을 `아직 알려지지 않은 URL`(최근 크롤링
   `해당사항 없음`)로 기록했고, 같은 날 **§4.3 Tech Notes 묶음**이 "한 건은 8-24 보고서와 어긋난다 …
   어느 쪽이 맞는지는 GSC 가 말하지 않으므로 다음 세션이 추정을 사실로 인용하지 않는다" 고 남겼다.
   **오늘 같은 URL 의 검사는 `최근 크롤링 2026. 8. 17.` 을 보여준다 — 8-24 보고서 쪽이 맞았다.**

   **어느 쪽이 틀렸는지까지는 갈리지 않는다.** 후보는 둘이고 이 문서는 어느 쪽도 확정하지 못한다 —
   ① GSC 실시간 검사가 그날 틀린 값을 줬다, ② 8-31 세션이 카드를 잘못 읽었다(그날은 foreground 가
   막혀 "최근 검사 기록 항목을 `invoke`" 하는 우회로를 쓰고 있었고 — §6 2026-08-31 성과 6번 —
   §4.3 8-17 실측 3번이 바로 그 경로에서 "직전 URL 의 결과를 그대로 보여준다" 고 경고한다.
   **오늘 이 세션이 정확히 같은 오독을 한 번 저질렀다** — 아래 조작 실측 마지막 항목).
   **다음 세션이 둘 중 하나를 사실로 인용하지 않는다.** 어느 쪽이든 따라오는 규칙은 같다 —
   **단일 검사 결과를 근거로 페이지를 고치지 않는다.**

**조작 실측 — 다음 세션이 다시 부딪히지 않도록.**

- **검사창은 검사할 때마다 돋보기 버튼으로 접힌다.** URL 검사 결과 화면에서는 `ComboBox`
  (class `Ax4B8 ZAGvjd`)가 트리에서 사라지고 `Button`(name `검색`, class `gb_0e`)만 남는다. 그
  버튼을 `invoke` 하면 다시 열린다. **열려 있을 때 또 `invoke` 하면 닫히므로**, 매번 ComboBox 존재를
  먼저 확인하고 없을 때만 연다.
- **요청 버튼의 화면 라벨은 실제로 바뀐다 — 바뀌지 않는 것은 UIA 이름 쪽이다.** 9-02 는 UIA 가 읽는
  이름이 항상 `색인 생성 요청다시 요청` 이라 이력 판정에 못 쓴다고 기록했는데, **오늘 스크린샷으로
  화면 라벨이 요청 전 `색인 생성 요청` → 요청 후 `✓ 색인 생성 요청됨` + `다시 요청` 으로 바뀌는 것을
  확인했다.** 색인된 URL 에서는 `페이지가 변경되었나요? 색인 생성 요청` 으로 문구 자체가 다르다.
  (**이 마지막 문장은 2026-09-10 에 반증됐다** — 색인되지 않은 URL 에도 같은 문구가 붙는다.
  §6 2026-09-10 절 조작 실측 첫 항목.)
  **이력 판정은 계속 §4.3 의 `[x]` 목록으로 한다.** 화면 라벨이 이력과 무관하다고까지 적은 것은
  과했으므로 **§4.3 우선순위 0 절의 해당 bullet 을 직접 고쳐 두었다** — 세션 로그에만 남기면 §4.3 을
  먼저 읽는 다음 세션이 틀린 문장을 그대로 만난다.
- **KakaoTalk 알림 토스트가 lease 를 막는다.** 토스트 창(class `EVA_Window_Dblclk`)이 foreground 를
  쥐면 `uia` 가 `The foreground window ... has no usable virtual-desktop GUID to return to` 로
  거부한다. **실측 약 90초 뒤 스스로 사라지고 그 다음에는 정상 동작한다** — 강제로 뚫지 말고 그
  클래스가 foreground 에서 빠질 때까지 기다린 뒤 재시도한다. 8-31 성과 6번의 `foreground` 차단과
  같은 계열일 수 있으나 **그때의 원인은 확인된 적이 없으므로 같다고 단정하지 않는다.**
  오늘도 한 번은 `The target window could not be made foreground for the desktop lease` 가 났고,
  재시도로 풀렸다.
- **`capture -NoSwitch` 는 다른 창이 위에 있으면 그 창의 픽셀을 찍는다.** 오늘 실제로 Chrome 대신
  PowerPoint 화면이 찍힌 스크린샷이 나왔다(`CopyFromScreen` 이라 창 좌표의 현재 화면을 그대로 뜬다).
  **스크린샷을 증거로 남길 때는 `-NoSwitch` 를 빼서 lease 가 대상 창을 앞으로 끌어오게 한다.**
  읽기 판정 자체는 UIA 트리로 하는 편이 안전하다 — 트리는 가려져도 정확하다.
- **`-is [pscustomobject]` 로 파이프라인을 거르면 문자열도 통과한다 — 오늘 안전장치 하나가 조용히
  죽어 있었다.** 요청 스크립트가 검사 결과 객체를 `Where-Object { $_ -is [pscustomobject] }` 로 골라
  "이미 색인된 URL 이면 요청하지 않는다" 를 판정하게 해 뒀는데, `[pscustomobject]` 는
  `System.Management.Automation.PSObject` 이고 **PowerShell 의 모든 파이프라인 객체가 이 래퍼를
  만족한다.** 그래서 helper 가 출력한 **문자열**이 객체로 통과했고, `$row.Status` 가 `$null` 이 되어
  **가드 조건이 항상 거짓**이 됐다. 화면에는 `검사:` 뒤가 비어 나오는 것으로만 드러났다.
  실제 피해는 없었다 — 그 3건은 같은 날 아침에 미색인임을 이미 확인한 URL 이었고, 스크린샷 3장으로
  카드 URL 이 요청 대상과 일치함을 사후 확인했다. **판정 결과가 비어 있으면 통과가 아니라 실패로
  다루도록 고쳤고, 버튼을 누르기 직전에 카드 URL 을 한 번 더 대조하는 단계를 넣었다.**
- **§4.3 의 "카드에 지금 검사한 URL 이 찍혔는지 먼저 확인" 을 오늘 한 번 어겨서 잘못된 값을 읽었다.**
  검사 요청이 lease 실패로 아예 실행되지 않았는데 세부값을 그대로 읽어, **직전 URL 의 표준 URL·크롤
  시각을 다음 URL 의 것으로 판독**했다. 곧바로 발견해 폐기하고 다시 읽었다(위 3번 표는 재검사 값이다).
  **세부값을 읽기 전에 카드의 URL 을 대조하는 절차를 코드 쪽에 넣어야 한다** — 사람이 눈으로 지키는
  규칙으로는 새는 지점이다.

**다음 작업:**

1. **남은 5개를 다음 요청일에 요청한다 — 1순위다.** `/ko/blog/` 와
   `(ko/)blog/{use-android-phone-as-bluetooth-mouse, use-android-phone-as-powerpoint-remote}/` 4개다.
   **사용자 결정("지금 다 색인 요청")의 잔여분이므로 다시 묻지 않는다** — 오늘 못 한 이유는 할당량뿐이다.
   요청 전 `/ko/blog/` 를 먼저 검사한다 — `/blog/` 를 오늘 요청했으므로 `hreflang` 으로 딸려 색인됐으면
   요청하지 않고 한 칸을 아낀다(§4.3 의 8-15 선례).
2. **9-05 요청분 10개의 단계 이동을 확인한다.** 9-02 묶음이 3일 만에 색인됐으므로 **9-08 전후**가 첫
   확인 시점이다. 특히 **`아직 알려지지 않은 URL` 이었던 7개가 크롤 단계로 올라갔는지**가 8-31 묶음의
   실패(요청은 접수됐는데 크롤이 안 붙은 8건)와 갈리는 지점이다.
3. **8-31 요청분 중 미발견 8개**(`(ko/)tech-notes/{bt-ppt, bt-mouse, wifi-scout, ssh-scout}/`)를
   계속 검사한다. **같은 URL 을 다시 요청하지 않는다**(§4.3 — 성공 모달 본문이 "여러 번 제출해도
   대기열 위치나 우선순위가 변경되지 않습니다" 다). 위 1번을 처리한 뒤에도 이 8개가 `해당사항 없음`
   이면 그때 재요청 여부를 **사용자 결정으로 올린다** — 문서의 기존 금지 규칙을 뒤집는 일이라 임의로
   하지 않는다.
4. **sitemap 밖에서 새로 생기는 미색인 URL 이 없는지만 본다.** 오늘로 이 문서가 알고 있는 미요청
   URL 은 위 1번의 5개뿐이다.
5. **Page indexing 보고서가 8-28 자를 넘어가면 다시 판독한다.** 8-31·9-02·9-05 요청분 22개가
   보고서에 어떻게 잡히는지가 실시간 검사와 대조되는 첫 지점이다. 색인 38 이 얼마나 오르는지,
   `크롤링됨 - 현재 색인이 생성되지 않음` 8 이 어떻게 움직이는지를 본다.
6. sitemap 이미지 확장은 계속 보류한다 — 8-31 사용자 결정이고, **조건은 "미색인 42 가 줄어드는 것"
   이며 그 42 는 sitemap 필터 기준 보고서 수치다.** 오늘 실시간 검사에서 색인이 늘어난 것은 맞지만
   **보고서 수치는 여전히 8-28 자의 색인 38 / 미색인 10 이라 이 조건은 아직 확인되지 않았다.**
   **실시간 검사만으로 조건 충족을 선언하지 않는다** — 위 5번 뒤에 보고서 숫자로 다시 판단한다.
7. sitemap 은 관찰만 한다 — 재제출하지 않는다(§4.2, §7).
8. 옛 URL 은 계속 요청하지 않고 Googlebot 의 재크롤을 기다린다(§4.4, §7, §13.4).
   `(ko/)apps/lgtv/` 는 8-24 이후 재크롤이 없다. **§13.5 첫 항목의 추론은 위 6번으로 닫혔으므로
   그 항목을 갱신해 뒀다** — 다음 세션이 그 자리를 다시 미결로 읽지 않는다.

### 2026-09-10 — §2.5 종결(요청 5건), 보고서 첫 전진(색인 38→54 / sitemap 미색인 42→26), 배포 0건

**요청 계획에 남아 있던 URL 이 0 이 됐다.** §2.5 의 미요청 5개를 전부 요청해 접수됐고, 할당량 10 중
5칸만 썼다 — **남은 5칸을 쓰지 않은 이유는 요청할 URL 이 더 없기 때문이다**(문서가 아는 미색인 URL 중
미요청은 0 이고, 나머지는 전부 반복 요청 금지 대상이다). 배포는 없고 sitemap 도 건드리지 않았다(§7).
**그리고 Page indexing 보고서에서 8-24 이래 처음으로 움직인 수치를 봤다** — 다만 아래 2번대로
**수치가 바뀐 날은 데이터 기준 `8-29` 이고 오늘 요청과는 무관하다.**

1. **색인 생성 요청 5건 — 전부 접수, 할당량 초과 0건.** 요청 사이 25초(§4.3 의 20초 규칙).

   | # | URL | 요청 시점 상태 |
   |---|---|---|
   | 1 | `/ko/blog/` | **크롤링됨 - 현재 색인이 생성되지 않음** |
   | 2 | `/blog/use-android-phone-as-bluetooth-mouse/` | 아직 알려지지 않은 URL |
   | 3 | `/ko/blog/use-android-phone-as-bluetooth-mouse/` | 아직 알려지지 않은 URL |
   | 4 | `/blog/use-android-phone-as-powerpoint-remote/` | 아직 알려지지 않은 URL |
   | 5 | `/ko/blog/use-android-phone-as-powerpoint-remote/` | 아직 알려지지 않은 URL |

   5/5 가 `색인 생성 요청됨 — URL이 우선순위 크롤링 대기열에 추가되었습니다` 이고, **성공 대화상자를
   닫은 뒤 할당량 초과가 뒤따른 건은 0건**이다(§4.3 성공 판정 규칙). 요청 전 live 사전 점검 5/5 통과
   (`200` + self canonical + `noindex` 없음 + live `sitemap.xml` 의 `<loc>` 포함). 요청 직후 스크린샷
   5장(성공 대화상자 + 아직 갱신되지 않은 배경 카드)으로 카드 URL 이 요청 대상과 일치함을 사후 대조했다.

   **9-05 가 기대한 "`/blog/` 를 요청하면 `hreflang` 을 타고 `/ko/blog/` 가 딸려 색인된다" 는 일어나지
   않았다** — 오늘 `/blog/` 는 색인됐는데 `/ko/blog/` 는 `크롤링됨` 그대로였고, 그래서 계획대로 요청했다.
   8-14 `/tech-notes/` → 다음날 `/ko/tech-notes/` 선례는 **여전히 1건뿐이고 재현되지 않았다.**

2. **Page indexing 보고서가 처음 움직였다 — 데이터 기준 `26. 8. 28.` → `26. 9. 4.`, 색인 38 → 54.**

   | 항목 | 8-21·8-28 자(8-31·9-05 판독) | **9-04 자(오늘)** |
   |---|---|---|
   | 색인 생성됨 | 38 | **54** |
   | 색인이 생성되지 않음 | 10 | **12** |
   | └ `크롤링됨 - 현재 색인이 생성되지 않음` | 8 | **10** |
   | └ `중복 페이지, Google에서 사용자와 다른 표준을 선택함` | 2 | **2** |
   | 알려진 페이지 합계 **(계산)** | 48 | **66** |

   **사유는 여전히 2개뿐이다 — redirect 오류·soft 404·`noindex` 는 이 속성에 없다.** §6 7일 후
   체크리스트의 "redirect 오류·soft 404·canonical 불일치가 늘지 않았는지" 가 이 표로 충족된다.
   **추이 CSV 두 개가 수치가 바뀐 날을 정확히 짚어 준다 — 둘 다 `2026-08-29` 다.**
   색인은 `8-16`~`8-28` 이 **38** 로 고정이고 **`8-29` 에 54** 로 뛴 뒤 `9-04` 까지 그대로다.
   `크롤링됨` 은 `8-05` 0 → `8-06` 3 → `8-08` 5 → `8-11` 7 → `8-15` 8 → **`8-29` 10** → `9-04` 10 이다.
   **즉 색인 +16 과 크롤됨 +2 가 같은 날 한꺼번에 일어났고, 그 날은 9-02·9-05 요청보다 앞선다.**
   **따라서 이 전진을 9-02·9-05 요청의 효과로 읽지 않는다** — 8-31 세션은 8-21 자를, 9-05 세션은 8-28 자를
   판독했으므로 그때 38 이었던 것이 맞고, **오늘 처음 8-29 이후 데이터를 본 것이다.** 무엇이 늘었는지는
   8-31 성과 1번의 색인 38 분류와 오늘 54 목록을 대조해야 갈리며, **이 세션은 그 대조를 하지 않았다.**

   **CSV 로 색인 54 의 전량 목록을 확보했다**(내보내기 → CSV 다운로드, 필터 `알려진 모든 페이지`).
   **표 목록의 최신 크롤 값이 보고서 기준일보다 앞선다** — `/blog/`·`/ko/` 의 최종 크롤링이
   `2026-09-05` 다. 즉 **"최종 업데이트: 26. 9. 4." 는 집계 그래프의 기준일이고 URL 표는 더 최신을
   담는다.** 다음 세션이 이 차이를 수치 불일치로 오해하지 않는다.

3. **sitemap 기준으로는 색인 28 → 44, 미색인 42 → 26 이다. §6 이 이미지 확장 보류 조건으로 걸어 둔
   "미색인 42 가 줄어드는 것" 이 충족됐다.**

   계산은 8-31 과 같은 방법이되 근거가 더 강하다 — 8-31 은 `색인 38 - 옛 스텁 10` 으로 뺐고, 오늘은
   **색인 54 CSV 와 live sitemap 70 의 실제 교집합**을 구했다. 교집합 44, sitemap 밖 색인 10(옛 제품
   스텁), sitemap 안 미색인 26. **8-31 의 옛 스텁 10 과 오늘의 10 은 구성까지 같다** —
   `(ko/)bt-keyboard`, `(ko/)bt-mouse`, `(ko/)wifi-scout`, `(ko/)ssh-scout`, `(ko/)lgtv` 다.

   묶음별 색인 상태(sitemap 70 기준):

   | 묶음 | 색인 / 전체 | 미색인 내역 |
   |---|---|---|
   | 홈 | **2 / 2** | — (**`/ko/` 가 오늘 처음 색인됐다**) |
   | 블로그 허브 | 1 / 2 | `/ko/blog/` |
   | 블로그 글 | 12 / 16 | `(ko/)blog/{use-android-phone-as-bluetooth-mouse, use-android-phone-as-powerpoint-remote}/` 4 |
   | 제품 `/apps/<app>/` | 10 / 14 | `(ko/)apps/lgtv/` 2 + `(ko/)apps/yt-downloader/` 2 |
   | 허브 `/apps/`·`/manual/` | **1 / 4** | `/apps/`, `/ko/apps/`, `/manual/` (색인된 것은 `/ko/manual/` 하나) |
   | 매뉴얼 | 10 / 12 | `(ko/)manual/bt-keyboard/` 2 |
   | Tech Notes | 8 / 20 | 아래 4번 |
   | **합계** | **44 / 70** | **26** |

4. **미색인 26 의 전량이 단계별로 갈렸다 — 알려진 미색인 10 + 미발견 16 이다.** 보고서의 미색인 12 와
   숫자가 다른 이유는 그 12 에 sitemap 밖 2건이 섞여 있고, 미발견 16 은 "알려진 페이지" 가 아니라
   보고서 집계에 애초에 들어오지 않기 때문이다. 검산이 맞는다 — 알려진 66 = (sitemap 안 색인 44 +
   sitemap 안 알려진 미색인 10) + (sitemap 밖 색인 10 + sitemap 밖 크롤됨 2).

   **`크롤링됨 - 현재 색인이 생성되지 않음` 10건 전량(CSV, 최종 크롤링 내림차순):**

   | URL | 최종 크롤링 | 비고 |
   |---|---|---|
   | `/ko/tech-notes/bt-keyboard/why-korean-english-key-labels-drift.html` | 2026-09-05 | 9-05 요청 당일 크롤 |
   | `/ko/tech-notes/bt-keyboard/why-google-tv-does-not-detect-bluetooth-hid.html` | 2026-09-05 | 같음 |
   | `/ko/tech-notes/bt-keyboard/sharing-one-bluetooth-hid-session.html` | 2026-09-05 | 같음 |
   | **`/sitemap.xml`** | **2026-08-31** | sitemap 밖. §4.2 의 2026-09-10 문단 |
   | `/ko/tech-notes/bt-keyboard/` | 2026-08-17 | 9-05 판정과 같음 |
   | **`/manual/`** | **2026-08-14** | **우선순위 0 요청분** |
   | **`/ko/apps/`** | **2026-08-14** | **우선순위 0 요청분** |
   | **`/apps/`** | **2026-08-14** | **우선순위 0 요청분** |
   | `/ko/bt-ppt/` | 2026-08-11 | 옛 스텁. sitemap 밖 |
   | `/ko/blog/` | 2026-08-08 | 오늘 요청 |

   **8-24 의 같은 사유 8건과 대조하면 이 목록의 in/out 이 갈린다**(§6 2026-08-24 성과 4번: `/ko/`,
   `/blog/`, `/ko/blog/`, `/apps/`, `/ko/apps/`, `/manual/`, `/ko/tech-notes/bt-keyboard/`, `/ko/bt-ppt/`).
   **신규 진입 4** = `/sitemap.xml` + 국문 상세 3편, **이탈 2** = `/ko/`·`/blog/`(색인으로 옮겨갔다).
   8 − 2 + 4 = 10 으로 검산된다. **`/sitemap.xml` 이 이 사유 목록에 들어온 것은 이번이 처음이다** —
   9-05 다음 작업 4번("sitemap 밖에서 새로 생기는 미색인 URL 이 없는지만 본다")의 답이 이것이고,
   **sitemap 밖 2건 중 `/ko/bt-ppt/` 는 8-24 에도 있었고 `/sitemap.xml` 은 새로 생긴 것이다.**

   **`중복 페이지…` 2건은 목록으로 확인했다 — `/apps/lgtv/`·`/ko/apps/lgtv/` 정확히 그 둘이다.**
   8-31·9-05 는 사유의 페이지 수가 `2` 로 유지되는 것을 보고 "lgtv 2개에 머물러 있다" 고 **추론**했는데,
   오늘 드릴다운 목록으로 그 추론이 확정됐다. **§13.3 의 확산 가설은 이 시점에도 증거가 없다** —
   다만 그 가설은 "나머지 스텁도 순차로 같은 판정을 받을 수 있다" 는 시계열 진술이므로 한 시점의
   목록으로 반증되지 않고 미확인에 머문다.

   **미발견 16개**(미색인 26 에서 위 8건과 중복 2건을 뺀 차집합. 아래 4개 묶음 전부 실시간 검사로도
   `Google에는 아직 알려지지 않은 URL입니다` 를 확인했다):

   | 묶음 | 개수 | 요청일 | 경과 |
   |---|---|---|---|
   | `(ko/)manual/bt-keyboard/` | 2 | **2026-08-17** | **24일** |
   | `(ko/)tech-notes/{bt-ppt, bt-mouse, wifi-scout, ssh-scout}/` | 8 | 2026-08-31 | 10일 |
   | `(ko/)apps/yt-downloader/` | 2 | 2026-09-05 | 5일 |
   | `(ko/)blog/{…bluetooth-mouse, …powerpoint-remote}/` | 4 | **2026-09-10(오늘)** | 0일 |

   **`(ko/)manual/bt-keyboard/` 가 이 문서에서 가장 오래된 "요청은 접수됐는데 크롤이 붙지 않은" 사례다.**
   8-17 우선순위 1 요청 10개 중 나머지 8개는 색인됐고 이 2개만 24일째 미발견이다. 8-31 묶음의 8개와
   같은 실패 모양이 그보다 2주 앞서 이미 있었다는 뜻이므로, **"8-31 묶음만의 이례" 로 읽지 않는다.**
   **원인은 확인하지 않았다.**

5. **`/apps/`·`/ko/apps/`·`/manual/` 세 허브가 8-14 크롤 이후 27일째 색인되지 않고 있다.**
   **우선순위 0 의 10개는 색인 5 / 색인 밖 5 다** — 색인 밖은 이 허브 3개와 `(ko/)apps/lgtv/` 2개
   (§13 의 중복 판정)이고, 색인된 5개는 `/ko/manual/` + `(ko/)apps/bt-keyboard/` + `(ko/)apps/wifi-scout/`
   다. **허브 4개만 떼어 보면** 요청 → 크롤은 4/4 성공했고 색인은 `/ko/manual/` 하나뿐이다. **원인은 확인하지 않았다.** 이 3개는
   §4.3 의 반복 요청 금지 대상이며(8-14 `[x]`), 8-24 의 lgtv 재요청이 "여러 번 제출해도 대기열
   위치·우선순위는 바뀌지 않는다" 를 이미 실측했으므로 **재요청을 후속 조치 후보로 올리지 않는다.**

6. **9-05 요청분 10개는 5일 만에 색인 5 / 크롤됨 3 / 미발견 2 로 갈렸고, 갈린 축이 언어다.**

   | URL | 9-05 요청 시점 | 9-10 |
   |---|---|---|
   | `/ko/` | 크롤링됨 - 현재 색인이 생성되지 않음 | **색인 생성됨** |
   | `/blog/` | 크롤링됨 - 현재 색인이 생성되지 않음 | **색인 생성됨** |
   | `/tech-notes/bt-keyboard/` 상세 3편(영문) | 아직 알려지지 않은 URL | **색인 생성됨** |
   | `/ko/tech-notes/bt-keyboard/` 상세 3편(국문) | 아직 알려지지 않은 URL(1건은 크롤링됨) | 크롤링됨 - 현재 색인이 생성되지 않음 |
   | `(ko/)apps/yt-downloader/` 2 | 아직 알려지지 않은 URL | **아직 알려지지 않은 URL**(그대로) |

   - **`/ko/` 국문 홈이 색인됐다.** 이 문서가 8-31 이래 세 번 기록해 온 비대칭(`/` 는 색인, `/ko/` 는
     아니다)이 닫혔다. **9-05 요청 이후 5일 안에 색인됐다** — 정확한 색인일은 관측되지 않았다(보고서 집계 그래프는
     `8-29` 이후 54 로 고정이고 URL 표와 스냅샷이 다르다. 위 2번).
   - **영문 상세 3/3 색인, 국문 상세 3/3 크롤 정지.** **요청 후 6/6 이 크롤 단계에 도달했다** —
     5건은 최종 크롤링이 요청일 `2026-09-05` 이고, 1건
     (`/ko/tech-notes/bt-keyboard/sharing-one-bluetooth-hid-session.html`)은 **요청 전부터 이미
     `크롤링됨`** 이었다(9-05 절 1번). **"요청이 크롤을 유발했다" 는 인과는 GSC 가 말하지 않는다.**
     이 도달률이 9-02 묶음과 같고 8-31 묶음과 다르다는 대조까지가 실측이다. 같은 모양이 부모에서도 반복된다 — `/tech-notes/bt-keyboard/`(색인) 대
     `/ko/tech-notes/bt-keyboard/`(크롤링됨), `/blog/`(색인) 대 `/ko/blog/`(크롤링됨).
     **원인은 확인하지 않았다** — GSC 는 `크롤링됨 - 현재 색인이 생성되지 않음` 의 이유를 URL 단위로
     말하지 않는다. **다음 세션이 "국문이라서" 를 원인으로 인용하지 않는다.** 관찰된 것은 상관뿐이다.
   - **`yt-downloader` 한·영 2개는 요청 5일이 지나도 크롤조차 붙지 않았다** — 8-17·8-31 묶음의 실패와
     같은 모양이다. 이 앱이 6종 중 유일한 미출시 앱인 것은 대조로만 적는다(인과는 확인하지 않았다).

7. **8-31 요청분 미발견 8개는 10일이 지나도 그대로다 — `(ko/)tech-notes/{bt-ppt, bt-mouse,
   wifi-scout, ssh-scout}/` 8/8 이 `아직 알려지지 않은 URL` 이다.** 9-05 다음 작업 3번이 걸어 둔
   조건("§2.5 의 5개를 처리한 뒤에도 이 8개가 `해당사항 없음` 이면 재요청 여부를 사용자 결정으로
   올린다")이 **오늘 충족됐다.** 보고서 CSV 와도 어긋나지 않는다 — **미발견 URL 은 두 CSV 어디에도
   나타나지 않으며**(= 알려진 페이지가 아니다) 위 4번의 차집합으로만 잡힌다.
   **문서의 반복 요청 금지를 뒤집는 일이므로 이 세션이 임의로 재요청하지 않았고, 사용자 결정 대기로
   남긴다.** 오늘 할당량은 5칸이 남아 있었으므로 결정만 있으면 즉시 가능하다.

8. **Sitemaps 보고서는 28일째 그대로인데, 같은 날 드릴다운은 `/sitemap.xml` 이 8-31 에 크롤됐다고
   말한다.** 목록 화면은 마지막으로 읽은 날짜 (없음) / 상태 `가져올 수 없음` / 발견된 페이지 0 이다.
   후속 진단에서 세부 화면의 날짜 `8-13`, 8-31 Googlebot 가져오기 `성공`, 오늘 `10:03:02 KST`
   실제 URL 테스트 `성공` 을 확인했다. **공식 API 는 `isPending: true`, 오류·경고 0, 다운로드 시각·
   형식·내용 필드 없음으로 응답했다.** 미처리 상태는 확인했으나 원인은 미확인이다. 응답 원문과
   과거 단정의 정정, 재제출 효과의 한계는 §4.2.1 에 기록했다. 재제출은 실행하지 않았다(§7).

9. **보고서와 실시간 검사가 오늘은 어긋나지 않았다.** 8-24·8-31 은 같은 URL 에 다른 값을 줘서 §4.3 과
   §6 이 "어느 쪽이 맞는지 GSC 가 말하지 않는다" 를 여러 번 적어야 했는데, 오늘 대조한 20개 URL 과
   보고서 CSV 는 전부 일치한다(색인/크롤됨/미발견 판정이 URL 단위로 동일). **이것은 두 화면이 항상
   일치한다는 뜻이 아니라 오늘의 대조 결과다** — 9-05 가 기록한 판독 불일치 사례를 무효로 만들지 않는다.

**조작 실측 — 다음 세션이 다시 부딪히지 않도록.**

- **9-05 가 남긴 "이미 색인된 URL 에서는 버튼 문구 자체가 `페이지가 변경되었나요? 색인 생성 요청` 이다"
  는 틀렸다.** 오늘 요청 직후 스크린샷(성공 대화상자가 떠 있고 배경 카드는 아직 요청 전 렌더다 —
  **요청 스크립트는 버튼을 누른 뒤에만 캡처하므로 "요청 직전" 화면은 이 경로로 재현되지 않는다**)에서 `/ko/blog/`(`크롤링됨`)와
  `/blog/use-android-phone-as-bluetooth-mouse/`(`아직 알려지지 않은 URL`, 세부값 전부 `해당사항 없음`)
  **둘 다** 같은 문구였다. **이 문구로 색인 여부를 가릴 수 없다.** §4.3 우선순위 0 절의 해당 문단을
  직접 고쳐 뒀다 — 세션 로그에만 남기면 §4.3 을 먼저 읽는 다음 세션이 틀린 문장을 그대로 만난다.
- **Chrome 의 top-level 창이 4개라 selector 가 엉뚱한 HWND 를 고를 수 있다.** `windows` 로 보면 제목
  있는 창 2개(`페이지 색인 생성`, 다른 탭 창) + 제목 없는 `Chrome_WidgetWin_1` 2개다. 한 번은 lease 가
  제목 없는 창(`3739720`)을 잡아 `GetId` 가 `TYPE_E_ELEMENTNOTFOUND` 로 죽고 그 뒤 단계가 연쇄
  실패했다. **`-Window <hwnd>` 로 대상 창을 고정하면 재현되지 않는다** — 보고서 조작처럼 여러 단계를
  잇는 작업에서는 처음부터 박는다.
- **보고서 표의 행(`DataItem`)은 `invoke` 가 불가능하다** — `supports no invocable pattern` 이 뜬다.
  미색인 사유 드릴다운은 **`click`**(합성 클릭)으로 열린다. lease 수준이 `required` 로 올라가므로
  전환·foreground 를 동반한다.
- **sitemap 필터는 UIA 로 열지 못했다.** 필터 값 `알려진 모든 페이지` 는 `ListItem` 인데
  `is disabled` 로 거부되고(선택된 항목이라 그렇다), 드롭다운 트리거가 트리에서 따로 잡히지 않는다.
  **그래서 sitemap 기준 수치는 화면 필터가 아니라 CSV 교집합으로 구했다**(위 3번). 다음 세션도 이
  경로를 쓰면 된다 — 목록까지 얻으므로 필터 화면보다 오히려 낫다.
- **CSV 내보내기는 `내보내기` Button `invoke` → `CSV 다운로드` MenuItem `invoke` 로 열린다**(전환
  없음). 파일은 `~/Downloads` 에 `https___droidactor.github.io_-Coverage-{Valid,Drilldown}-<날짜>.zip`
  으로 떨어지고, 안의 CSV 파일명은 한글이라 셸에서 깨져 보이지만 내용은 UTF-8 로 정상이다.
  드릴다운 zip 에는 `속성,값` 시트가 함께 들어 있어 **어떤 필터·어떤 사유의 목록인지 파일 안에서
  확인된다**(`Sitemap,알려진 모든 페이지` / `문제,크롤링됨 - 현재 색인이 생성되지 않음`).
- 오늘 lease 실패는 위 HWND 건 1회뿐이었다. KakaoTalk 토스트(`EVA_Window_Dblclk`) 대기는 발동하지
  않았다(스크립트의 `Wait-ForegroundSane` 은 그대로 유지했다).

**다음 작업:**

1. **8-31 요청분 미발견 8개의 재요청 여부는 사용자 결정을 받는다**(위 7번). 결정 없이 재요청하지 않는다.
2. **오늘 요청한 5개의 단계 이동을 확인한다.** 9-02 묶음은 3일, 9-05 묶음은 5일에 절반이 색인됐으므로
   **9-13 전후**가 첫 확인 시점이다. `/ko/blog/` 는 이미 `크롤링됨` 이었으므로 **색인으로 올라가는지**가,
   블로그 글 4개는 **크롤이 붙는지**가 갈리는 지점이다.
3. **9-05 요청분 중 크롤에서 멈춘 국문 상세 3개와 `/ko/tech-notes/bt-keyboard/` 를 계속 검사한다.**
   재요청하지 않는다(§4.3).
4. **`/apps/`·`/ko/apps/`·`/manual/` 3개 허브를 계속 검사한다**(위 5번). 재요청은 하지 않는다 —
   조치가 아니라 관찰 대상이다.
5. **Page indexing 보고서가 9-04 자를 넘어가면 다시 판독한다.** 오늘 요청분 5개가 보고서에 어떻게
   잡히는지가 다음 대조점이다. **표 목록이 기준일보다 최신일 수 있음을 기억한다**(위 2번).
6. **sitemap 이미지 확장의 보류 조건이 충족됐다 — 재개 여부는 사용자 결정이다.** 8-31 결정의 조건은
   "미색인 42 가 줄어드는 것" 이었고 오늘 보고서 기준으로 26 으로 줄었다(위 3번). **조건이 풀렸다는
   사실만 올리고 이 세션이 착수하지 않는다.**
7. sitemap 의 읽기 전용 API 조회는 완료했다(`isPending: true`, §4.2.1). 9-17 관찰 시점과
   지원 문의는 제안 단계이며 확정 일정·게시 승인이 아니다. 재제출하지 않는다(§7).
8. 옛 URL 은 계속 요청하지 않고 재크롤을 기다린다(§4.4, §7, §13.4). **`(ko/)apps/lgtv/` 의 최근
   크롤링은 오늘 읽지 않았다** — 최신 기록은 여전히 `2026-08-24 09:51:21`(영문)·`09:55:22`(국문)이고
   (§13.5), 오늘 확인한 것은 두 URL 이 `중복 페이지` 사유 목록에 그대로 있다는 것까지다. **다음 세션은
   그 크롤 시각을 직접 읽어 8-24 이후 재크롤이 있었는지 갱신한다.**
   **옛 스텁은 §2.1 의 14개가 모집단이고 그 안에서 색인 10 · `크롤링됨` 1 · Google 이 모르는 URL 3
   (`/bt-ppt/`, `(ko/)yt-downloader/` — 보고서 어느 사유에도 없다)으로 갈린다. 색인 10개가 그대로여서
   검색 결과 교체는 시작되지 않았다.** `/ko/bt-ppt/` 하나만 `크롤링됨 - 현재 색인이
   생성되지 않음` 인데 **그 크롤은 `2026-08-11` 로 개편 배포(8-14)보다 앞서므로 이동 신호를 읽은 결과가
   아니다** — "옛 URL 이 색인에서 빠지기 시작했다" 로 읽지 않는다.

### 2026-09-12 — sitemap 행 삭제 후 재제출(8-24 금지 해제), API 기준선 갱신, 배포 0건

1. **사용자가 "sitemap.xml 삭제 후 재제출" 을 지시했다.** 이는 §7 에 박제된 2026-08-24 결정
   ("행을 삭제했다가 다시 만들지 않는다", "재제출하지 않는다")과 정면으로 어긋난다. **착수 전에 그
   충돌을 인용해 보고하고, 아니라면 멈춰 달라고 적었다.** 동시에 §4.2.1 이 "향후 사용자가 별도로
   재제출을 지시하는 경우" 를 위한 비교 기준을 이미 남겨둔 점을 들어, 금지를 이유로 막지 않고 그 기준을
   쓰기로 했다. §7 의 해당 조항은 오늘 자로 갱신했다(해제는 이번 1회분).

2. **삭제 여부를 선택지로 제시해 사용자가 골랐고, 삭제를 포함한 경로였다.** 다만 **그때 제시한 근거
   일부가 틀렸다** — "삭제를 생략하면 `lastSubmitted` 기준선이 보존된다" 고 설명했으나 재제출하면
   삭제 여부와 무관하게 그 값은 갱신된다. 보존되는 것은 행의 누적 처리 이력이다(§4.2.2).
   **잘못된 이점 설명 위에서 선택이 이뤄졌다는 사실을 남긴다.**

   **승인 단위도 정확히 적는다 — 사용자는 "삭제 → 재입력 → 제출" 경로를 한 번에 승인했고, 그 승인
   하나로 두 비가역 단계를 실행했다. 단계별 개별 확인은 받지 않았다.** 각 단계 직전에 한 것은
   "승인하신 경로대로 누르겠습니다" 라는 고지이지 재확인이 아니다. **다음 세션이 이 절을 "비가역
   단계마다 직전 확인을 받는다" 는 선례로 인용하지 않는다.**

3. **실행은 성공했다.** 토스트 `사이트맵이 삭제되었습니다` → 목록 `총 0행 중 0~0` → 재입력 →
   대화상자 `사이트맵이 제출됨` 까지 확인했다. 목록의 제출일이 `2026. 8. 13.` → `2026. 9. 12.` 로
   바뀌었다. **상태 칸은 여전히 `가져올 수 없음`, 발견된 페이지 `0` 이다.**

4. **API 는 `lastSubmitted` 만 갱신됐다**(`2026-09-12T06:30:07.930Z`, `isPending: true` 유지,
   `lastDownloaded` 없음). **조회 시점은 사용자가 "지금 바로" 를 골라 정해졌고, 제출 8분 뒤였다.**
   응답 전문과 대조표는 §4.2.2 에 있다. **이 관측은 §4.2.1 표의 네 갈래 중 어느 쪽도 가르지 못한다** —
   네 갈래가 배타적이지 않아 첫째·넷째를 동시에 충족하고, 행을 삭제한 이상 첫째는 판별력 자체가 없다.
   **오늘 진행된 판별은 없다.** 이 값으로 성패를 판정할 수 없다 — **"재제출도 실패" 로 읽지 않는다.**

5. **OAuth 범위는 확인하지 못했다.** `Show scopes` 를 UIA 로 열지 못해 §4.2.1 의 `webmasters.readonly`
   선례가 이번에도 지켜졌는지 미확인이다. 동의는 사용자가 직접 처리했다. 확인하지 못한 것을 확인한 것처럼
   기록하지 않는다(§4.2.2).

**UI 자동화 실측 — 다음 세션이 같은 곳에서 막히지 않도록:**

- **Search Console 사이트맵 입력창에서는 `ValuePattern.SetValue` 가 "거부" 가 아니라 "쓰기는 되고
  read-back 만 빈 문자열" 이었다.** `set-text` 가 `Text read-back mismatch ... control now reads ''` 로
  실패를 던지는데 **화면에는 값이 실제로 들어가 있고 제출 버튼도 활성화된다**(이 입력창에서 2회 재현).
  스킬이 조용한 오작동을 막으려 보수적으로 실패 처리하는 것이므로, **이 오류를 보면 포기하지 말고
  스크린샷으로 실제 반영 여부를 확인한다.** `-ViaClipboard` 나 CDP 로 우회할 필요가 없었다.
  **다만 이것을 "Chromium 웹 콘텐츠 `<input>` 일반" 으로 일반화하지 않는다** — 관찰은 입력창 하나,
  2회뿐이다. `~/CLAUDE.md` 의 기존 실측(Chrome realbox `<input>` 은 거부)과는 다른 결과이므로,
  **요소마다 갈릴 수 있다고 보고 매번 화면으로 확인한다.**
- 반면 **주소창(`OmniboxViewViews`)은 read-back 까지 정상 통과**한다(`TEXT_SET`). chrome 표면과 웹
  콘텐츠의 차이가 그대로 재현됐다.
- **APIs Explorer 는 파라미터를 URL 로 미리 채울 수 있다** —
  `.../sitemaps/get?apix=true&apix_params=<URL 인코딩된 JSON>`. 웹 콘텐츠 입력 단계를 통째로 건너뛰므로
  `sitemaps.get` 재조회는 주소창 `set-text` + `key enter` 한 번이면 된다.
- **목록 화면의 행 ⋮ 메뉴에는 삭제가 없다**(`페이지 색인 생성 보기` 등 2개뿐, 둘 다 비활성). 삭제는
  **행을 눌러 세부 화면으로 들어간 뒤 우상단 `옵션 더보기...` → `사이트맵 삭제`** 다. 세부 화면 URL 은
  `.../sitemaps/info-drilldown?resource_id=<속성>&sitemap=<전체 URL>` 이다.
- 행(`DataItem`)이 `invoke` 불가라 `click` 이 필요한 것은 9-10 기록 그대로다. **`-Window <hwnd>` 미지정
  시 다른 Chrome 창이 잡히는 현상도 재발했다** — 처음부터 박는 원칙을 지킨다.
- **클래스명 앞에 공백이 붙는 요소가 있다**(` devsite-concierge-panel-close ...`). 정확 매칭이 실패하므로
  `-ClassName '*조각*' -Like` 로 잡는다.
- **`find` 는 찾는데 `invoke` 셀렉터는 못 찾는 불일치가 있다**(API 탐색기 패널의 `Show scopes`).
  `Execute` 는 같은 패널에서 `invoke` 가 됐으므로 요소별로 갈린다. 막히면 다른 요소로 우회하거나
  `click` 으로 내려간다.
- **`find -All` 결과는 `-Json` 없이 파이프하면 속성 접근이 안 된다**(`Count` 는 나오는데 `Name` 이 빈다).
  필터링하려면 `-Json | ConvertFrom-Json` 을 쓴다.
- **RDP 로 접속 중 원격 창이 비활성이면 `GetForegroundWindow()` 가 `0` 이고 lease 가 물리 입력을 거부한다**
  (`The target window could not be made foreground`). 세션은 `rdp-tcp#0` Active, 콘솔은 `LogonUI` 로 잠금
  상태였다. **그 상태에서도 읽기와 `invoke` 는 정상이고, `set-text` 도 값은 들어간다**(단 위 read-back
  예외는 그대로 난다). **막히는 것은 `click`·`key` 뿐이다.** 사용자가 RDP 창을 다시 포커스하면 해소된다.
  **이 관찰도 1세션 1회분이다.**

**다음 작업:**

1. **`sitemaps.get` 을 며칠 뒤 한 번 더 조회한다.** §4.2.1 표의 나머지 세 갈래 중 어디로 가는지가
   유일한 판별점이다. 자동 점검은 설정하지 않았다.
2. **재제출·삭제를 다시 하지 않는다.** 해제는 9-12 지시에 한한 1회분이다(§7).
3. **9-10 절 다음 작업 1~6·8 이 전부 그대로 유효하다**(7번만 오늘 작업으로 대체됐다). 오늘 작업은
   sitemap 제출 건 하나만 건드렸고 색인 요청·배포는 0건이다. **아래 열거는 닫힌 목록이 아니라 9-10 절을
   가리키는 포인터다 — 인계할 때는 그 절을 직접 읽는다.** 특히 놓치기 쉬운 둘을 다시 적는다:
   - **9-10 다음 작업 3번** — 9-05 요청분 중 크롤에서 멈춘 국문 상세 3개와 `/ko/tech-notes/bt-keyboard/`
     를 계속 검사한다(재요청하지 않는다).
   - **9-10 다음 작업 8번** — `(ko/)apps/lgtv/` 의 최근 크롤 시각을 **직접 읽어** 8-24 이후 재크롤이
     있었는지 갱신한다. §13.5 를 닫으려면 이 값이 필요하다.

4. **9-10 다음 작업 2번의 첫 확인 시점이 "9-13 전후" 다 — 내일이다.** 9-10 에 요청한 5개(`/ko/blog/` +
   블로그 글 4개)의 단계 이동을 본다.

### 2026-09-14 — 로마 숫자 변환기 URL 4개(9-13 배포) 확인, sitemap 70→74, 색인 요청 0건

1. **최근 2일(9-13·9-14) 커밋 8개 중 새 URL 을 만든 것은 `160b5c3` 하나다.** 나머지 7개는 URL 을
   늘리지 않았다 — `fdf7231`(`todo-roman-launch.md` 신설), `edc76fc`·`c27d773`·`d2eeb6c`(홈 2개의
   처리방침 앵커 `#privacy-roman`·`#privacy-currency`·넘패드 추가), `a392c49`(roman 4개 본문의 변환
   범위 갱신), `a45d3d6`·`a4a3538`(배너 `*.webp` 7개 추가, HTML 변경 없음 — 현재 어디에서도 참조되지 않는다, §15.5). **처리방침 앵커는 홈
   페이지 안의 `id` 이지 별도 URL 이 아니다** — sitemap 에 넣지 않는다(§12 의 `#privacy-gas` 선례).

2. **새 URL 4개는 `(ko/)apps/roman/`·`(ko/)manual/roman/` 이고 전량 live 검증을 통과했다** —
   4/4 `200` + self canonical + `hreflang` 3개 + `noindex` 없음, live sitemap `<loc>` **74** 에 포함,
   live ↔ 저장소 HEAD `a4a3538` diff 0. 허브·홈에서 들어가는 내부 링크도 갖춰져 있다. 상세는 §15.

3. **`lastmod` 점검이 네 번째로 적중했다 — 배포본 기준 20건이 어긋나 있다**(§15.1). roman 4개는
   `2026-09-13` 인데 본문은 `a392c49`(9-14)가 바꿨고, `160b5c3` 이 역방향 링크를 더한 16개
   (`(ko/)apps/` 제품 7쌍 + `(ko/)blog/`)는 `2026-08-31`·`2026-08-14` 에 멈춰 있다. **미커밋 working
   tree 가 그중 19건을 이미 `2026-09-14` 로 올려 두었으나 `manual/roman/` 한 행만 `2026-09-13` 으로
   남아 국문 짝과 어긋난다** — 커밋 전에 맞춘다.

4. **`(ko/)manual/roman/` 2개에는 JSON-LD 가 아예 없다**(§15.2). 나머지 매뉴얼 12개는 `TechArticle` +
   `BreadcrumbList` + `SoftwareApplication` 과 `dateModified` 를 갖고 있다. **색인 차단 사유는
   아니므로** 비색인 원인으로 쓰지 않는다. 제품 페이지 2개는 `lgtv` 와 구조가 같아 어긋나지 않는다.

5. **색인 생성 요청은 0건이고, 요청할지 자체가 미결이다**(§15.4). sitemap 이 한 달째 미처리라 "먼저
   sitemap 에 맡긴다" 는 기본 방침의 전제가 성립하지 않는 상태이고, 남은 발견 경로인 부모 허브는
   4개 중 1개만 색인이다. 선택지 3개를 §15.4 에 적었다 — **사용자 결정 대기.**

6. **`apps/currency/`·`apps/numpad/` 와 국문 짝 4개는 아직 커밋되지 않았다**(§15.5). 작업 중
   `sitemap.xml` 은 78 이지만 live 는 74 다. **배포 전에는 이 문서의 목록에 넣지 않는다.**

7. **이 세션은 Search Console 화면도 API 도 열지 않았다.** 수행한 것은 live HTTP 실측과 저장소 대조,
   그리고 이 문서 갱신뿐이다. **따라서 9-12 절이 남긴 다음 작업은 하나도 진행되지 않았다** — 아래에
   그대로 옮겨 둔다.

**다음 작업:**

1. **§15.1 의 `lastmod` 정정을 배포한다.** 미커밋 묶음에 19건이 들어 있고 `manual/roman/` 1건이
   빠져 있다. 배포 후 live 분포를 다시 읽어 이 절 아래에 기록한다.
2. **§15.4 의 색인 요청 여부를 사용자와 정한다.** 관찰을 택하면 재판단 시점은 9-20~9-27 이다.
3. **9-12 다음 작업 1번 — `sitemaps.get` 재조회가 지금 그 "며칠 뒤" 다**(재제출 9-12, 오늘 9-14).
   판별점은 `lastDownloaded` 가 생기는지와 `isPending` 이 풀리는지 둘뿐이다(§4.2.1 둘째·셋째 갈래).
   **재제출·삭제는 다시 하지 않는다**(§7).
4. **9-12 다음 작업 4번이 기한을 지났다** — 9-10 에 요청한 5개(`/ko/blog/` + 블로그 글 4개)의 단계
   이동을 9-13 전후에 보기로 했으나 확인하지 않았다.
5. **9-10 다음 작업 1~6·8 이 여전히 유효하다.** 특히 국문 상세 3개와 `/ko/tech-notes/bt-keyboard/`
   관찰(재요청 금지), `(ko/)apps/lgtv/` 최근 크롤 시각 직접 읽기(§13.5 를 닫는 데 필요). **인계할
   때는 9-10 절을 직접 읽는다.**
6. **`roman` 출시 시점에 `badge soon` 을 스토어 배지로 바꾸면 4개 URL 의 본문이 바뀐다** — 그
   배포에서 `lastmod` 를 다시 올린다(§15.3).

### 2026-09-14 (2) — 색인 생성 요청 착수, `/apps/roman/` 접수(+중복 1건), 화면 잠금으로 3건 미완

**사용자 지시로 §15.4 의 미결이 닫혔다 — "신규 페이지 색인 요청 수행"(4개 전부, Chrome 창을 새로
띄워 작업)이다.** 착수 시각 2026-09-14 23:13 KST(§4.3 의 "KST 09:00 이후" 충족).

| URL | 결과 |
|---|---|
| `https://droidactor.github.io/apps/roman/` | **요청 접수**(23:2x KST). 그 뒤 **중복 1건이 더 들어갔다**(아래 3번) |
| `https://droidactor.github.io/ko/apps/roman/` | **미실행** — 검사창 입력이 막혔다 |
| `https://droidactor.github.io/manual/roman/` | 미실행 |
| `https://droidactor.github.io/ko/manual/roman/` | 미실행 |

1. **요청 전 상태(기록).** `/apps/roman/` 은 `URL이 Google에 등록되어 있지 않음` /
   `페이지 색인이 생성되지 않음: Google에는 아직 알려지지 않은 URL입니다.` / 참조 사이트맵
   `감지된 참조 사이트맵이 없습니다` / 참조 페이지 `감지된 페이지 없음` / 최근 크롤링 `해당사항 없음`
   이었다. **§4.2 의 sitemap 미처리가 URL 단위로도 확인된 셈이다** — 배포 1일이 지난 URL 인데 참조
   사이트맵이 잡히지 않는다. (단 이 한 URL 의 관측이므로 sitemap 상태 판정을 여기서 바꾸지 않는다.)

2. **요청은 접수됐다.** `색인 생성 요청` → 대화상자 `색인 생성 요청됨` + 본문 `URL이 우선순위 크롤링
   대기열에 추가되었습니다. 페이지를 여러 번 제출해도 대기열 위치나 우선순위가 변경되지 않습니다.`
   **§4.3 의 판정 절차대로 대화상자를 닫은 뒤 할당량 초과 대화상자가 뒤따르는지까지 확인했고 없었다.**

3. **같은 URL 에 중복 요청 1건이 들어갔다 — §4.3 의 "같은 URL 을 반복 요청하지 않는다" 를 어겼다.**
   경위를 남긴다. 두 번째 URL(`/ko/apps/roman/`)을 처리하려고 검사창 붙여넣기 → Enter → 요청 버튼을
   한 스크립트로 묶어 실행했는데, **붙여넣기 단계가 화면 잠금 때문에 실패**했고(아래 4번) 스크립트가
   그대로 다음 단계로 내려가 **`/apps/roman/` 결과 화면에 남아 있던 요청 버튼을 다시 눌렀다.**
   화면에 표시된 URL 을 사후 확인해 중복 대상이 `/apps/roman/` 임을 확정했다.
   - **실효는 없다** — GSC 성공 모달이 "여러 번 제출해도 대기열 위치나 우선순위가 변경되지 않습니다"
     라고 명시한다(8-24 재요청 때와 같은 근거, §4.3 우선순위 0 절). 소모된 것은 일일 할당량 1건이다.
   - **원인은 스크립트 설계다.** 입력 단계 실패를 확인하지 않고 버튼 단계로 내려갔다. 고쳐서
     ① 붙여넣기·Enter 가 성공하지 않으면 중단 ② **결과 카드에 대상 URL 이 찍혔는지 확인한 뒤에만**
     버튼을 누르도록 게이트를 넣었다. **다음 세션은 이 게이트 없이 요청 루프를 돌리지 않는다.**
   - 8-24 의 재요청과 성격이 다르다 — 그때는 이력 확인을 빠뜨린 판단 착오였고, 이번은 자동화가
     실패를 삼킨 것이다. **공통점은 "요청 직전에 대상이 무엇인지 확인하지 않았다" 이다.**

4. **막힌 이유 — 원격 세션이 끊기고 화면이 잠겼다.** `GetForegroundWindow()` 가 `0`,
   `LogonUI` 2개 실행 중, `query session` 기준 사용자 세션 `dualk`(ID 1)가 `Disc`, 콘솔 세션만 연결.
   **이 상태에서도 UIA 읽기와 `invoke` 는 정상 동작하고**(그래서 요청 버튼은 눌렸다) **물리 입력
   (`click`·`key`·`set-text -ViaClipboard`)만 거부된다** — 9-12 절이 RDP 비활성 때 기록한 것과 같은
   증상이며 이번에 잠금 상태에서도 재현됐다. **잠금을 풀면 즉시 이어서 할 수 있다.**

**UI 자동화 실측 — 다음 세션이 같은 곳에서 막히지 않도록:**

- **URL 검사 딥링크는 없다.** `search-console/inspect?resource_id=<속성>&id=<검사할 URL>` 은 **404**
  다(2회 시도). 검사 후 주소창을 보면 `id=` 값이 URL 이 아니라 **불투명 토큰**(예
  `IuDZeKEgL5gYsksDDfaINA`)이다. 즉 URL 을 파라미터로 넣어 검사 화면을 여는 경로는 없고, **검사창에
  직접 입력하는 수밖에 없다.** (9-12 이 APIs Explorer 에서 쓴 URL 프리필 요령은 여기 적용되지 않는다.)
- **검사창(`ComboBox`, 클래스 `Ax4B8 ZAGvjd`)에 `ValuePattern` 으로 값을 넣으면 화면에는 보이지만
  Enter 가 먹지 않는다.** `set-text` 는 `Text read-back mismatch ... control now reads ''` 로 실패를
  던지는데 **스크린샷에는 값이 그대로 찍혀 있고**(9-12 사이트맵 입력창과 같은 현상), 그 상태에서
  Enter 를 보내면 **검사가 시작되지 않는다.** 페이지가 입력 이벤트를 못 받아 내부 상태가 빈 채로
  남기 때문으로 보인다(추정). **`set-text -ViaClipboard` 로 붙여넣으면 정상 동작한다** — 이 경로는
  물리 입력이라 잠금·비활성 세션에서 막힌다(위 4번).
- **속성 홈은 URL 로 열 수 있다** — `search.google.com/search-console?resource_id=<URL 인코딩 속성>`.
  주소창(`OmniboxViewViews`)은 `set-text` read-back 까지 정상 통과하므로 여기까지는 잠금과 무관하다.
- **요청 버튼의 UIA 이름은 `색인 생성 요청다시 요청`** 이다(요청 이력과 무관, 9-02 기록 그대로).
  성공 대화상자는 `Window` + `Text` 로 `색인 생성 요청됨`, 닫기 버튼은 클래스에 `mnPd5d` 가 붙는다.
- **창이 다른 모니터에 있으면 `capture` 가 빈 화면을 찍는다**(실측: 본문이 통째로 흰 이미지). 잠금
  상태에서는 더더욱 믿을 수 없으므로 **판정은 스크린샷이 아니라 UIA 요소 조회로 한다.**

**다음 작업:**

1. **화면 잠금을 풀고 남은 3개를 요청한다** — `/ko/apps/roman/`, `/manual/roman/`, `/ko/manual/roman/`.
   오늘 소모한 할당량은 2건(요청 1 + 중복 1)이라 3건은 한도 안이다.
2. **`/apps/roman/` 은 다시 요청하지 않는다** — 오늘 2회 접수됐다.
3. 요청분의 단계 이동은 7~14일 뒤에 본다(§4.3). **재요청하지 않는다.**
4. **9-14 (1) 절의 다음 작업 1~6 은 하나도 진행되지 않았다** — 특히 `lastmod` 정정 배포와
   9-12 재제출분 `sitemaps.get` 재조회가 그대로 남아 있다.

### 2026-09-15 — 신규 URL 8개 색인 생성 요청 완료(할당량 초과 0), `lastmod` 정정 배포 확인

전날 밤 잠금으로 멈춘 작업(§6 2026-09-14 (2) 절)을 사용자의 "계속" 지시로 06:24 KST 에 재개했다.
`GetForegroundWindow()` 가 정상값이고 `LogonUI` 가 없으며 세션이 `Active` 임을 먼저 확인했다.

1. **roman 3개를 이어서 요청해 §15 의 4개가 닫혔다** — `/ko/apps/roman/`, `/manual/roman/`,
   `/ko/manual/roman/`. 셋 다 요청 전 판정이 `Google에는 아직 알려지지 않은 URL입니다` 였고,
   `색인 생성 요청됨` + `URL이 우선순위 크롤링 대기열에 추가되었습니다` 를 받았다.

2. **사용자가 "더 있으면 시도해 봐" 라고 해 확인했더니 실제로 더 있었다** — 전날 22:45 KST 커밋
   `2c23df2` 가 `currency`·`numpad` 제품 페이지 4개를 배포했고 sitemap 이 74 → **78** 이 됐다.
   **§15.5 가 "미커밋이라 범위 밖" 으로 분류한 그 묶음이다.** 사전 점검(4/4 `200` + self canonical +
   `hreflang` 3 + `noindex` 없음 + sitemap 포함)을 거쳐 **4개 전부 요청해 접수시켰다**(§16).

3. **오늘까지 이 속성에 들어간 요청은 9건이고 할당량 초과 대화상자는 한 번도 뜨지 않았다.**
   내역은 9-14 2건(`/apps/roman/` + 그 중복) + 9-15 7건이다. §4.3 의 "하루 10개" 한도 바로 아래이므로
   **오늘은 여기서 끝낸다.** (구글의 일일 경계가 KST 기준이 아닐 수 있어 9-14 23시대의 2건을 같은
   창으로 보고 세었다. 경계 자체는 확인하지 않았다 — 보수적으로 합산한 것이다.)

4. **§15.1 의 `lastmod` 어긋남 20건이 배포로 해소됐다.** `2c23df2` 가 함께 정정했고, HEAD 의 sitemap
   78행을 파일별 마지막 본문 변경 커밋과 기계 대조한 결과 **어긋남 0건**이다(날짜 필드만 고친
   메타데이터 커밋은 §4.2 규칙대로 제외). §15.1 이 "빠져 있다" 고 적은 `manual/roman/` 한 행도
   `2026-09-14` 로 맞았다. 현재 분포는 `08-14` 12 + `08-16` 8 + `08-31` 28 + `09-14` 30 = **78**.

5. **§4.3 의 "KST 09:00 이후 시작한다" 를 지키지 않았다** — 재개 시각이 06:24 KST 다. 사용자의 "계속"
   지시에 따른 것이고 할당량 초과는 발생하지 않았지만, **규칙을 벗어난 실행이라는 사실은 남긴다.**
   이 1회를 근거로 규칙을 바꾸지 않는다.

**UI 자동화 실측 — 요청 루프를 돌릴 다음 세션을 위해:**

- **요청 버튼을 누른 뒤 성공 대화상자가 뜨기까지 25초로는 부족할 때가 있다.** 화면에는
  `실제 URL의 색인을 생성할 수 있는지 테스트 중 / 1~2분 정도 걸릴 수 있습니다` 모달이 떠 있다.
  **9-15 에 실행한 7건 중 3건**이 그랬다. **없다고 해서 실패로 읽지 말고, 버튼을 다시 누르지도 말고, `대기열` 문구가
  나타날 때까지 폴링한다**(실측: 최대 150초 폴링으로 전부 확인됨).
- **판정은 스크린샷이 아니라 UIA 요소 조회로 한다.** 성공 = `Window`/`Text` 이름 `색인 생성 요청됨`
  + 본문 `URL이 우선순위 크롤링 대기열에 추가되었습니다…`, 실패 = 이름에 `할당량` 을 가진 요소.
  대화상자를 닫은 뒤 한 번 더 `할당량` 을 조회하는 것까지가 §4.3 의 판정 절차다.
- **자동화 스크립트에는 게이트 두 개가 필수다**(9-14 (2) 절 3번의 중복 요청이 그 부재로 생겼다):
  ① 붙여넣기·Enter 가 성공하지 않으면 버튼을 누르지 않는다 ② **결과 카드에 대상 URL 이 찍힌 것을
  확인한 뒤에만** 누른다. **9-15 의 7건은 이 게이트를 통과한 뒤 요청했고 중복은 0건이다**(게이트가
  없던 9-14 의 1건이 중복을 낳았다).
- 대화상자가 떠 있으면 배경의 URL 텍스트 조회가 실패한다 — 그 실패를 "페이지가 바뀌었다" 로 읽지 않는다.

**다음 작업:**

1. **이번 묶음 8개의 단계 이동을 7~14일 뒤에 본다**(9-20~9-29) — roman 4 + currency 2 + numpad 2.
   9-10 요청분 5개(§2.5)와 9-05 요청분은 **별개의 창**이며 각각 그 절의 일정으로 본다.
   **재요청하지 않는다.**
2. **9-12 재제출분 `sitemaps.get` 재조회가 사흘째 밀려 있다.** 판별점은 `lastDownloaded` 생성과
   `isPending` 해제 둘이다(§4.2.1). **`감지된 참조 사이트맵이 없습니다` 를 화면에서 직접 읽은 것은
   `/apps/roman/`(9-14)와 `/apps/numpad/`(9-15) 2개**이고, 나머지 요청분은 색인 단계만 기록해 그 필드를
   확인하지 않았다. **그 2개에 한해 sitemap 미처리가 URL 단위로도 관측된다** — 전량으로 확대하지 않는다.
3. **9-10 다음 작업 1~6·8 과 9-14 (1) 절의 다음 작업 3·5·6 이 그대로 남아 있다.** (같은 절 1번은
   `2c23df2` 배포로, **2번은 9-14 사용자 지시로 닫혔다** — §15.4 를 "결정 대기" 로 되살리지 않는다.
   4번은 위 1번과 9-10 절이 이중으로 덮는다.) 특히
   `(ko/)apps/lgtv/` 최근 크롤 시각(§13.5 가 남긴 관찰 — §13.3·§13.4)과
   `(ko/)manual/roman/` 의 JSON-LD 누락(§15.2).
4. **`roman`·`currency`·`numpad` 출시 배포 때 `badge soon` 교체와 함께 `lastmod` 를 올린다**(§15.3, §16.2).

### 2026-09-15 (2) — 단위 변환기 URL 2개 배포 확인(sitemap 78→80), `lastmod` 28건 어긋남, 요청 0건

오전 요청 세션(위 절)이 끝난 뒤 **07:17 KST 커밋 `166e55e` 로 `(ko/)apps/unit/` 2개가 배포됐다.**
§16.4 가 미배포로 추적하던 그 묶음이며, 이 절은 배포 직후의 실측이다. **색인 생성 요청은 하지 않았다.**

1. **live 2/2 통과** — `200` + self canonical + `hreflang` 3 + `noindex` 없음 + sitemap `<loc>` **80**
   에 포함. JSON-LD 3종도 다른 제품 페이지와 같다. 상세는 §17.

2. **`lastmod` 점검이 다섯 번째로 적중했다 — 28건이다**(§17.1). `166e55e` 이 홈 2 + 허브 2 + 제품 22 +
   블로그 허브 2 의 본문을 바꾸면서 sitemap 을 `2026-09-14` 로 둔 채 배포했고, **새로 만든
   `(ko/)apps/unit/` 2개조차 `2026-09-14`** 다. **아직 정정되지 않았다.**
   **앱 추가 배포는 거의 항상 20여 개 URL 의 `lastmod` 를 동반한다** — 이 점검을 배포 체크리스트에
   넣지 않으면 계속 반복된다(8-17·8-18·8-19·9-13·9-15 다섯 번).

3. **요청을 미룬 이유** — 오늘 이 속성에 이미 9건이 들어갔고 2건을 더하면 11건이다. 8-14 실측에서
   11건까지 통과하고 12번째가 거절됐으므로 **경계에 붙는다.** §4.3 의 "10개를 채웠으면 그날은 끝낸다"
   를 따른다. 다음 요청일에 §17 의 2개를 처리한다.

**다음 작업:**

1. **§17.1 의 `lastmod` 28건을 정정해 배포한다.** 이 묶음에는 `dateModified` 짝을 가진 URL 이 없어
   sitemap 만 고치면 된다.
2. **`(ko/)apps/unit/` 2개를 다음 요청일에 요청한다**(§17.2). 게이트 2개와 폴링은 위 절의 실측대로.
3. 위 절의 다음 작업 1~4 는 그대로 유효하다.

### 2026-09-15 (3) — 신규 앱 4종 출시 확인, `lastmod` 잔여 16건·매뉴얼 JSON-LD 정정(미배포), 요청 0건

21시대 세션이다. 위 (2) 절 이후 같은 날 커밋 5개가 더 쌓여 있었고(`8d07570`·`ae26187`·`cc7e605`·
`ac934ce`·`42de779`), **이 문서가 기록하지 않은 배포가 셋 있었다.** 전량을 §18 로 정리했다.

1. **신규 앱 4종이 Play 에 출시됐다** — `cc7e605`(14:51)가 `badge soon` → 스토어 배지, 사양표
   `Status` → `Published on Google Play` 로 바꿨다. `roman`·`currency`·`unit`·`bt_numpad` 4종의
   Play 등재를 실측 확인했다(`og:title` 존재 + 응답 ~1.21 MB, 대조군 2개는 `404` + 1,673 B).
   **§15.3·§16.2·§17 의 "미출시" 기술이 닫혔다**(§18.1).

2. **§17.1 의 `lastmod` 28건이 28/28 정정됐다.** `cc7e605` 가 자기가 손댄 14건을 올렸고, **남은
   16건을 이 세션이 정정했다**(§18.2). HEAD sitemap 80행을 파일별 마지막 *본문* 변경 커밋과 기계
   대조한 결과 **어긋남 0건**이다. 현재 분포는 `08-14` 12 + `08-16` 8 + `08-31` 28 + `09-14` 2 +
   `09-15` 30 = 80. **정정분은 미배포다.**

3. **§15.2 의 매뉴얼 JSON-LD 누락을 메웠다** — `(ko/)manual/roman/` 에 `TechArticle` +
   `BreadcrumbList` 를 넣어 매뉴얼 14/14 가 같은 모양이 됐다(§18.3). `dateModified` 는 `2026-09-14`
   다(산문을 마지막으로 바꾼 `a392c49` 기준) — `lastmod` 09-15 와 다른 것이 §11.1 C 의 정상 적용이다.
   **미배포다.**

4. **새로 발견 — 같은 매뉴얼 2개에 화면 갱신 날짜(`manual-meta`)도 없다**(§18.4). 다른 12개는 12/12 가
   갖고 있다. **일부러 넣지 않았다** — 그 문구가 "해당 앱 버전으로 설명서를 점검했다" 는 검증 주장이고
   점검을 수행하지 않았다. **사용자 결정 대기.**

5. **`dateModified` 28건이 `lastmod` 와 다른 것은 결함이 아니다**(§18.5). §14.2 의 의도이며, 일괄
   정정하면 모든 글에 거짓 개정 신호를 보낸다. **다음 세션이 이것을 "어긋남" 으로 고치지 않도록
   절을 따로 뒀다.**

6. **색인 생성 요청은 0건이다.** **2026-09-15 자로** 이 속성에 들어간 요청은 **9건 그대로**이고, §17.2 의
   `(ko/)apps/unit/` 2개가 유일한 미요청분이다. 미루는 이유는 (2) 절 3번과 같다 — 2건을 더하면
   11건으로 8-14 실측 경계(11 통과 / 12 거절)에 붙고, §4.3 은 "10개를 채웠으면 그날은 끝낸다" 다.
   **세션 시각이 21:35 KST 라는 점도 같은 방향이다.** 출시는 재요청 사유가 되지 않는다(§18.7).

7. **GSC 조회는 하지 않았다** — `sitemaps.get` 재조회(9-12 재제출분)와 `(ko/)apps/lgtv/` 최근 크롤
   시각은 사용자의 로그인 브라우저 화면을 빌려야 하는 작업이고, 야간에 임의로 점유하지 않았다.
   **둘 다 미결로 그대로 남아 있다.**

**미배포 정정 3묶음 — 다음 배포에 함께 실어야 한다:**

| 파일 | 변경 | 절 |
|---|---|---|
| `sitemap.xml` | `lastmod` 16건 `2026-09-14` → `2026-09-15` | §18.2 |
| `manual/roman/index.html` | JSON-LD 13줄 추가 **+ 화면 빵부스러기 1행(`Manuals` → `User manuals`)** | §18.3, §18.3.1 |
| `ko/manual/roman/index.html` | JSON-LD 13줄 추가 | §18.3 |

**JSON-LD 추가는 그 2개 URL 의 본문 변경이다.** 다만 `lastmod` 는 이미 `2026-09-15` 이고 배포도 같은
날이면 값이 그대로 맞으므로 추가 정정이 필요 없다 — **날짜가 바뀐 뒤에 배포하면 `(ko/)manual/roman/`
2행을 그 배포일로 올려야 한다.** 이 문서가 다섯 번 반복해 적힌 함정이 여기서 한 번 더 성립한다.

**다음 작업:**

1. **위 미배포 3묶음을 배포하고 live 대조한다** — `sitemap.xml` 의 `<loc>` 80·`lastmod` 분포와
   매뉴얼 2개의 JSON-LD 가 live 에 반영됐는지. 배포일이 09-15 를 넘기면 위 단서대로 `lastmod` 를
   먼저 조정한다.
   **→ 2026-09-16 `5e998bd` 로 완료됐다**(§18.8). 배포일이 09-15 를 넘겨 `(ko/)manual/roman/` 2행을
   `2026-09-16` 으로 먼저 조정했다. **이 항목과 아래 2번은 닫혔다 — 다시 수행하지 않는다.**
2. **`(ko/)apps/unit/` 2개를 다음 요청일에 요청한다**(§17.2). 게이트 2개와 폴링은 (1) 절 실측대로.
   **→ 2026-09-16 에 2건 다 접수됐다**(§6 2026-09-16 절 2번).
3. **§18.4 의 `manual-meta` 삽입 여부를 결정한다** — 넣으려면 앱 버전 점검이 선행이다.
4. **(1)·(2) 절의 다음 작업 중 아래 둘은 닫혔으므로 이월하지 않는다.**
   **(1) 절 4번**(`roman`·`currency`·`numpad` 출시 배포 때 `badge soon` 교체 + `lastmod` 올리기)은
   `cc7e605` 와 이 세션의 16건으로 완결이다 — `(ko/)apps/{roman,currency,numpad,unit}/` 8행과
   `(ko/)manual/roman/` 2행이 모두 `2026-09-15` 로 맞다. **(2) 절 1번**(§17.1 `lastmod` 28건 정정)도
   28/28 정정으로 닫혔다 — **남은 것은 배포뿐이며 위 1번이 그것을 들고 있다.**
   **그 밖은 그대로 유효하다** — 9-12 재제출분 `sitemaps.get` 재조회, `(ko/)apps/lgtv/` 최근 크롤
   시각(§13.5), 9-15 요청분 8개의 단계 이동 관찰(9-20~9-29), 9-10 절 다음 작업 1~6·8.
5. **`assets/blog/windirstat-treemap.webp` 이 untracked 로 남아 있다** — 저장소 HTML 어디에서도
   참조되지 않는다(0건). 향후 블로그 글용으로 보이며, **sitemap·색인과 무관하다.** 이 세션은
   추가·삭제 어느 쪽도 하지 않았다.

### 2026-09-16 — 미배포 3묶음 배포(`5e998bd`), `(ko/)apps/unit/` 2건 요청 접수, GSC 조작을 CDP 로 전환

08시대 세션이다. 위 (3) 절이 "다음 작업" 으로 남긴 1·2 와 9-10·9-15 가 이월한 관찰을 한 번에 처리했다.
**사용자 지시는 "지금 다 수행해 봐. uia 말고 크롬 CDP 인가? 새로운 크롬 창 띄워서 해 봐" 였다** —
GSC 조작 경로가 `/uia` 에서 Chrome CDP 로 바뀐 것이 이 세션의 구조적 변화다(§19).

1. **미배포 3묶음을 배포했다 — `5e998bd`.** `sitemap.xml` 18행 + `(ko/)manual/roman/index.html` 2개다.
   문서 갱신은 선례(`ddef57e`)대로 별도 커밋으로 분리했다.

   **배포 전 전수 감사에서 80행 중 78행이 일치했고, 어긋난 2행은 미커밋 본문 변경이 있는
   `(ko/)manual/roman/` 뿐이었다.** 오늘이 09-16 이므로 §18.3 이 남긴 단서가 그대로 성립해 그 2행을
   `2026-09-15` → **`2026-09-16`**(배포일)으로 올렸다. **§18.2 의 함정이 일곱 번째로 성립한 것이며,
   이번에는 배포 전에 잡혔다** — 앞의 여섯 번은 배포 뒤에 발견했다.

   **live 대조 전부 통과:** `<loc>` **80**, 분포 `08-14` 12 + `08-16` 8 + `08-31` 28 + `09-14` 2 +
   `09-15` 28 + `09-16` 2 = 80(로컬과 동일), `(ko/)manual/roman/` 2행 `2026-09-16`, 매뉴얼 2개의
   `ld+json` 블록 1개씩 + `dateModified` `2026-09-14`(§18.3 대로 `lastmod` 와 다르다), 영문 crumb
   `User manuals`. 매뉴얼 14/14 가 ①~⑥ 검증(§18.3)을 통과한다.

2. **`(ko/)apps/unit/` 2개를 요청해 접수시켰다 — §17.2 가 닫혔다.** 요청 전 판정은 둘 다
   `Google에는 아직 알려지지 않은 URL입니다` 이고 참조 사이트맵·참조 페이지·최근 크롤링이 전부
   `해당사항 없음`/`감지되지 않음` 이었다. 사전 점검도 2/2 통과했다(`200` + self canonical +
   head `hreflang` 3종 + `robots` meta 0개 + live sitemap 포함).
   **게이트 2개를 지켰다** — ① 검사창 입력값이 대상 URL 과 문자열 일치 ② 결과 화면에 대상 URL 이
   찍힌 것을 확인한 뒤에만 버튼. 둘 다 `색인 생성 요청됨` + `URL이 우선순위 크롤링 대기열에
   추가되었습니다` 를 받았고, 대화상자를 닫은 뒤 재조회에서도 **`할당량` 문구는 나타나지 않았다.**

   **§4.3 의 "KST 09:00 이후 시작" 을 또 지키지 않았다** — 시작이 08:1x KST 다. 9-15 의 06:24 에 이어
   두 번째이며, 이번에도 사용자의 명시 지시("지금 다 수행해 봐")에 따른 것이고 할당량 초과는 없었다.
   **1회로 규칙을 바꾸지 않는다.** **2026-09-16 자로** 이 속성에 들어간 요청은 **2건**이다.

3. **`sitemaps.get` 을 재조회했다 — 9-12 재제출 후 4일째다**(§4.2.3). 응답이 9-12 와 **완전히 동일**하다.
   `lastSubmitted` 는 그대로 `2026-09-12T06:30:07.930Z`, `isPending: true`, `lastDownloaded`·`type`·
   `contents` 없음. **이번 관측은 §4.2.1 표의 넷째 갈래("미처리 상태 지속")를 가리키며, 9-12 와 달리
   판별력이 있다** — 그때는 제출 8분 시점이라 어느 값도 정보량이 없었고, 이번은 4일이다.

4. **`(ko/)apps/lgtv/` 최근 크롤 시각을 읽었다 — 8-24 그대로다**(§13.5 갱신). 영문
   `2026-08-24 09:51:21`, 국문 `2026-08-24 09:55:22` 로 **9-05 에 읽은 값과 초 단위까지 같다.
   23일째 재크롤이 없다.** 판정도 그대로 `중복 페이지, Google에서 사용자와 다른 표준을 선택함` 이고,
   Google 선택 표준 URL 은 여전히 옛 스텁(`/lgtv/`·`/ko/lgtv/`), 참조 페이지도 그 스텁이다.

5. **이월된 관찰 대상 12개를 판독했다.** 전부 재요청하지 않고 읽기만 했다(§4.3).

   | URL | 오늘 판정 | 최근 크롤링 |
   |---|---|---|
   | `/ko/blog/` | 크롤링됨 - 현재 색인이 생성되지 않음 | `2026-09-10 09:30:27` |
   | `(ko/)blog/use-android-phone-as-bluetooth-mouse/` 2 | 아직 알려지지 않은 URL | 해당사항 없음 |
   | `(ko/)blog/use-android-phone-as-powerpoint-remote/` 2 | 아직 알려지지 않은 URL | 해당사항 없음 |
   | `/ko/tech-notes/bt-keyboard/` | 크롤링됨 - 현재 색인이 생성되지 않음 | `2026-08-17 08:25:16` |
   | `/ko/tech-notes/bt-keyboard/why-google-tv-does-not-detect-bluetooth-hid.html` | 크롤링됨 | `2026-09-05 08:27:07` |
   | `/ko/tech-notes/bt-keyboard/sharing-one-bluetooth-hid-session.html` | 크롤링됨 | `2026-09-05 08:19:04` |
   | `/ko/tech-notes/bt-keyboard/why-korean-english-key-labels-drift.html` | 크롤링됨 | `2026-09-05 08:31:08` |
   | `/apps/` | 크롤링됨 - 현재 색인이 생성되지 않음 | `2026-08-14 11:18:31` |
   | `/ko/apps/` | 크롤링됨 - 현재 색인이 생성되지 않음 | `2026-08-14 11:22:32` |
   | `/manual/` | 크롤링됨 - 현재 색인이 생성되지 않음 | `2026-08-14 11:24:32` |

   - **9-10 요청분 5개는 6일이 지나도 단계가 오르지 않았다.** `/ko/blog/` 는 요청일에 크롤이 붙었고
     그대로 색인 전 단계에 머물러 있으며, 블로그 글 4개는 **크롤조차 붙지 않았다.** 9-02·9-05 묶음이
     3~5일에 절반이 색인된 것과 다른 모양이다. **원인은 확인하지 않았다.**
   - **9-05 요청분 국문 상세 3개는 9-05 크롤 이후 11일째 그대로다.** 재크롤도 색인도 없다.
   - **허브 3개는 8-14 크롤 이후 33일째 그대로다.** 9-10 절 5번이 27일째로 적은 그 상태가 이어진다.

6. **Page indexing 보고서는 `최종 업데이트: 2026-09-04` 그대로다** — 12일째 전진하지 않았다.
   색인 생성됨 **54**, 색인되지 않음 **12**(`크롤링됨 - 현재 색인이 생성되지 않음` 10 +
   `중복 페이지, Google에서 사용자와 다른 표준을 선택함` 2), 알려진 페이지 합계 **66** 으로 9-10 과
   같다. **9-10 다음 작업 5번("보고서가 9-04 자를 넘어가면 다시 판독한다")의 조건은 아직 충족되지 않았다.**

7. **§18.4 의 `manual-meta` 는 넣지 않았다.** 메인이 권장안으로 "넣지 않고 그대로 둔다" 를 제시했고
   사용자가 "지금 다 수행해 봐" 로 답한 뒤 이 세션이 그 권장안대로 진행했다. **사용자가 세 선택지 중
   하나를 명시적으로 고른 것은 아니므로, 결정으로 박제하지 않고 §18.4 를 그대로 열어 둔다.**
   근거는 §18.4 그대로다 — 문구가 "그 앱 버전으로 설명서를 점검했다" 는 검증 주장인데 점검을 하지 않았다.

8. **URL 검사 화면의 `Sitemaps` 칸은 시점·URL 마다 흔들린다 — 신호로 읽지 말 것.** 오늘 같은 세션
   안에서 `/apps/lgtv/` 는 `감지된 참조 사이트맵이 없습니다`, `/ko/apps/lgtv/` 는 `일시적인 처리 오류`
   였다. **§13.5 가 8-31 에 영문에서 `일시적인 처리 오류` 를 관측한 것과 방향이 반대다.**
   `/ko/tech-notes/bt-keyboard/` 계열 4개에서도 두 문구가 섞여 나왔다. 사이트맵이 `isPending` 인 이상
   **두 문구 어느 쪽도 URL 단위의 사실로 쓰지 않는다.**

**다음 작업:**

1. **9-15 요청분 8개의 단계 이동을 9-20~9-29 에 본다**(roman 4 + currency 2 + numpad 2). 오늘 요청한
   `(ko/)apps/unit/` 2개는 **9-21~9-30** 이 관찰 창이다. **재요청하지 않는다.**
2. **`sitemaps.get` 을 다시 조회한다.** 오늘로 재제출 4일째이고 여전히 미처리다. 판별점은 그대로
   `lastDownloaded` 생성과 `isPending` 해제 둘이다(§4.2.1). 조회 절차는 §19.4 에 있다.
3. **Page indexing 보고서가 `2026-09-04` 를 넘어가면 다시 판독한다**(9-10 다음 작업 5번 그대로).
4. **§18.4 의 `manual-meta` 삽입 여부는 여전히 사용자 결정이다**(위 7번).
5. **8-31 요청분 미발견 8개의 재요청 여부도 여전히 사용자 결정이다**(9-10 다음 작업 1번). 결정 없이
   재요청하지 않는다.
6. **sitemap 이미지 확장 재개 여부도 사용자 결정으로 남아 있다**(9-10 다음 작업 6번).
7. 9-10 다음 작업 2·3·4·8 은 오늘 위 4·5번으로 한 번 읽었다. **닫힌 것은 8번의 lgtv 크롤 시각 확인뿐**
   이고 나머지는 계속 관찰 대상이다.
8. **작업 트리에 이 세션 소관이 아닌 변경이 셋 있다 — 전부 `--ga`/`--gc` 때 휩쓸리지 않게 적어 둔다.**
   셋 다 저장소 HTML 에서 참조 0건이고 **sitemap·색인과 무관**하다. 이 세션은 어느 것도 추가·삭제하지 않았다.

   | 대상 | 상태 | 비고 |
   |---|---|---|
   | `assets/blog/windirstat-treemap.webp` | untracked | 9-15 (3) 절이 처음 기록. 향후 블로그 글용으로 보인다 |
   | `assets/banner/*.webp` 8개 | **modified** | 파일 시각 `2026-09-16 08:25`. 배포 커밋(08:00) 이후에 생긴 별개 작업이다 |
   | `assets/banner/en/` (8개) | untracked | 파일 시각 `2026-09-16 08:24~08:26`. 위와 같은 묶음으로 보인다 |

   **배너 두 묶음은 이 세션이 만든 것이 아니다** — 그 시각에 이 세션은 GSC 조회 중이었다. **출처를
   확인하지 않았으므로 "어느 세션이 만들었다" 를 단정하지 않는다.** 배너는 HTML 이 참조하지 않으므로
   교체되더라도 어떤 URL 의 `lastmod` 도 올리지 않는다(§18 머리의 `8d07570`·`ae26187` 판정과 같다).

### 7일 후

**아래 5개 항목은 2026-09-10 에 전부 확인을 마쳤다(배포 후 27일째). `[x]` 는 "확인했다" 이고, 결과가
목표를 충족했다는 뜻이 아니다 — 앞의 두 항목은 확인 결과가 "여전히 아니다" 다.**

- [x] sitemap의 마지막 읽은 날짜가 이동 이후로 갱신됐는지 확인한다.
      **갱신되지 않았다.** 목록 화면 기준 **마지막으로 읽은 날짜 `(없음)`** · 상태 `가져올 수 없음` 이며,
      후속 진단에서 연 세부 화면은 **`2026-08-13`** 그대로였다. API 에는 `lastDownloaded` 가 없고
      `isPending: true` 다. **날짜 표시만으로 접근 시도·실패·재시도 유무를 확정하지 않는다**(§4.2.1).
- [x] 발견된 페이지 수가 70으로 바뀌었는지 확인한다. — **`0` 그대로다.**
- [x] Page indexing 보고서를 해당 sitemap으로 필터링해 색인됨·비색인 수와 제외 사유를 기록한다.
      **sitemap 필터가 UIA 로 열리지 않아 색인 54 CSV 와 live sitemap 70 의 교집합으로 구했다** —
      **색인 44 / 미색인 26**, 사유는 `크롤링됨 - 현재 색인이 생성되지 않음` 8 +
      `중복 페이지, Google에서 사용자와 다른 표준을 선택함` 2 + **미발견 16**(§6 2026-09-10 절 3·4번).
- [x] 우선순위 1·2 URL의 발견 여부·색인 상태·마지막 crawl 날짜를 확인하고 요청 대상을 선별한다.
      **20개 중 16개 색인, 미색인 4개는 전부 `아직 알려지지 않은 URL` 이다** —
      `(ko/)manual/bt-keyboard/`(8-17 요청, 24일 경과)와 `(ko/)apps/yt-downloader/`(9-05 요청, 5일).
      **요청 대상 선별 결과는 "없음" 이다** — 넷 다 이미 요청 이력이 있어 반복 요청 금지 대상이다.
- [x] Page indexing에서 redirect 오류·soft 404·canonical 불일치가 늘지 않았는지 확인한다.
      **늘지 않았다.** 9-04 자 보고서의 비색인 사유는 여전히 2개뿐이고 redirect 오류·soft 404·
      `noindex`·`robots.txt` 차단 계열은 0 이다.

### 14일 후

**2026-09-10 에 확인했다. "발견" 은 색인 + 크롤됨 + 중복(= Google 이 URL 을 아는 상태) 이고,
"미발견" 은 `Google에는 아직 알려지지 않은 URL입니다` 다.**

- [x] 새 제품 URL 14개 중 Google이 발견한 수를 기록한다.
      **12/14 발견**(색인 10 + `중복 페이지` 2 = `(ko/)apps/lgtv/`). 미발견 2 = `(ko/)apps/yt-downloader/`.
- [x] 새 매뉴얼 URL 12개와 허브 4개 중 Google이 발견한 수를 기록한다.
      **매뉴얼 10/12 발견**(전부 색인). 미발견 2 = `(ko/)manual/bt-keyboard/`.
      **허브 4/4 발견**이지만 **색인은 `/ko/manual/` 하나뿐**이고 `/apps/`·`/ko/apps/`·`/manual/` 3개가
      `크롤링됨 - 현재 색인이 생성되지 않음` 이다(§6 2026-09-10 절 5번).
- [x] 새 Tech Notes URL 20개(§2.3 14 + §9 2 + §10 4) 중 Google이 발견한 수를 기록한다.
      **12/20 발견** — 색인 8(`(ko/)tech-notes/` 2, `/tech-notes/bt-keyboard/`, `(ko/)tech-notes/lgtv/` 2,
      영문 상세 3) + 크롤됨 4(`/ko/tech-notes/bt-keyboard/` + 국문 상세 3). 미발견 8 =
      `(ko/)tech-notes/{bt-ppt, bt-mouse, wifi-scout, ssh-scout}/`.
- [x] 비색인 URL은 `발견됨 - 현재 색인이 생성되지 않음`, duplicate, soft 404 등 정확한 제외 사유를 기록한다.
      **전량을 URL 단위로 기록했다**(§6 2026-09-10 절 4번). 이 속성에 실제로 나타난 사유는
      `크롤링됨 - 현재 색인이 생성되지 않음` 과 `중복 페이지, Google에서 사용자와 다른 표준을 선택함`
      둘뿐이며, **`발견됨 - 현재 색인이 생성되지 않음`·soft 404 는 한 번도 나타나지 않았다.**
- [x] 새 URL의 Google-selected canonical이 self canonical인지 표본 검사한다.
      **표본 2건이 self 일치다** — `/apps/ssh-scout/`(9-05)과 `/ko/blog/`(9-10, 색인되지 않은 URL 인데도
      Google 선택 표준 URL 이 `검사된 URL` 이다). **잠식은 `(ko/)apps/lgtv/` 2개에 한정된다**(§13).
- [x] 옛 제품 URL 검색 결과가 새 `/apps/` URL로 교체되기 시작했는지 확인한다.
      **시작되지 않았다.** 옛 스텁의 모집단은 §2.1 의 14개이고 그중 **10개가 여전히 색인을 유지**하며
      (색인 54 중 sitemap 밖 10), 3개(`/bt-ppt/`, `(ko/)yt-downloader/`)는 Google 이 아직 모르는 URL 이다.
      나머지 하나인
      `/ko/bt-ppt/` 는 `크롤링됨 - 현재 색인이 생성되지 않음` 인데 **그 크롤은 `2026-08-11` 로 개편
      배포보다 앞서므로 이동 신호를 읽은 결과가 아니다.** Performance 보고서 대조는 아래 30일 후 항목이다.

### 30일 후

- [ ] Performance 보고서에서 `페이지` 기준으로 옛 제품 URL과 새 제품 URL의 노출을 비교한다.
- [ ] 옛 URL 노출은 감소하고 새 URL 노출은 증가하는지 확인한다.
- [ ] 새 URL이 발견되지 않았다면 해당 URL의 referring sitemap·referring page·last crawl을 점검한다.
- [x] sitemap 상태가 해소되지 않아 live fetch·XML parsing·robots 접근을 다시 진단했다.
      **2026-09-10 완료** — 실제 URL 테스트·로컬 검사 정상, 직접 조치 없음, API 는 미처리(§4.2.1).
      상태 변화나 새 오류가 생기면 추가 진단한다. 이 완료 표시는 사이트맵 처리가 성공했다는 뜻이 아니다.

완료 조건은 sitemap 수집과 실제 색인을 구분한다.

Sitemap 수집 완료:

- [ ] sitemap 상태가 성공이고 발견된 페이지가 **80** 다.
      **2026-09-15 갱신** — 조건 문면의 70 은 §10 까지, 74 는 §15 까지, 78 은 §16 까지의 값이었고,
      §17 의 2개가 더해져 현재 live sitemap 은 80 이다. **이 수치는 배포마다 바뀐다** — 닫을 때는
      그 시점의 live `<loc>` 수와 맞춘다.
- [ ] 이 80은 sitemap에서 파싱한 고유 URL 수이며, crawl 또는 index 완료 수가 아님을 확인했다.

색인 전환 완료:

- [x] Page indexing 보고서를 sitemap으로 필터링한 색인됨·비색인 수와 각 제외 사유를 기록했다.
      **2026-09-10 대체 방법으로 충족** — 조건 문면의 "sitemap 으로 필터링" 은 하지 못했다(필터
      드롭다운이 UIA 로 열리지 않는다). **색인 CSV ∩ sitemap 70 교집합**으로 구한 값이
      **색인 44 / 미색인 26**(사유: 크롤됨 8 + 중복 2 + 미발견 16)이다(§6 2026-09-10 절 3·4번).
- [ ] 허브 4개, 출시 앱 제품 6개, 출시 앱 대표 매뉴얼 6개를 URL 검사해 `URL이 Google에 등록되어 있음`과
      Google-selected canonical을 확인했다.
      **2026-09-10 부분 충족.** 출시 앱 제품 6종 중 5종(`bt-keyboard`·`bt-ppt`·`bt-mouse`·`wifi-scout`·
      `ssh-scout`)은 한·영 모두 색인이고 `lgtv` 한·영은 §13 의 중복 판정이다. 대표 매뉴얼은
      `bt-keyboard` 한·영이 미발견이고 나머지는 색인이다. **허브 4개 중 색인은 `/ko/manual/` 하나뿐**
      (`/apps/`·`/ko/apps/`·`/manual/` 은 크롤됨) 이므로 이 항목은 아직 닫히지 않는다.
- [ ] Tech Notes 허브 2개를 URL 검사하고, 상세 문서 12개의 발견·색인 상태 또는 비색인 사유를 기록했다.
      **2026-09-10 부분 충족 — 상세 12개는 기록했으나 허브 2개는 URL 검사하지 않았다.**
      허브 2개(`(ko/)tech-notes/`)는 **색인 CSV 로만** 색인 유지를 확인했고(최종 크롤링 `2026-08-14`),
      실시간 판정·canonical·참조 사이트맵은 읽지 않았다. **canonical 잠식은 이 문서에서 실시간 검사로만
      잡힌 사안이므로**(§13) 다음 세션이 이 2개를 URL 검사해 이 항목을 닫는다. 앱별 12개는 색인 3(`/tech-notes/bt-keyboard/`,
      `(ko/)tech-notes/lgtv/`) + 크롤됨 1(`/ko/tech-notes/bt-keyboard/`) + 미발견 8. §10·§9 의 상세
      6편은 영문 3 색인 / 국문 3 크롤됨이다.
- [ ] 제품 결과가 새 `/apps/<app>/`, `/ko/apps/<app>/` URL로 전환된다.
      **2026-09-10 미충족** — 옛 스텁 10개가 색인을 유지하고 있어 교체가 시작되지 않았다.
- [x] 나머지 제품·매뉴얼 URL은 색인 상태가 확인되거나, 비색인 사유와 후속 조치가 기록된다.
      2026-09-10 — sitemap 70 전량의 단계를 URL 단위로 기록했다(§6 2026-09-10 절 3·4번). 후속 조치는
      "재요청하지 않고 관찰" 이며, 예외로 올린 8건은 사용자 결정 대기다(같은 절 7번).
- [x] 표본 검사한 새 URL의 canonical이 Google-selected canonical과 일치한다.
      2026-09-05 `/apps/ssh-scout/`, 2026-09-10 `/ko/blog/` 둘 다 `Google에서 선택한 표준 URL = 검사된 URL`.
- [x] 옛 URL에서 잘못된 대상·redirect chain·soft 404가 발생하지 않는다.
      2026-09-10 — 보고서 비색인 사유에 redirect 오류·soft 404 가 0 이다. **다만 근거의 절반은
      추론이다** — 옛 URL 14개의 `meta refresh` 1:1 정확성은 8-14 실측 이후 **그 파일을 바꾼 배포가
      없다**는 것에서 나오며 오늘 재실측하지 않았다. 또 스텁 3개(`/bt-ppt/`, `(ko/)yt-downloader/`)는
      Google 이 아직 모르는 URL 이라 애초에 보고서가 soft 404 를 말할 수 없는 상태다.

Google은 소규모 사이트도 URL 이동 처리가 수 주 걸릴 수 있으며, 색인 요청은 색인을 보장하지 않는다고
안내한다. 요청 직후 수치가 바뀌지 않아도 반복 제출하거나 redirect를 제거하지 않는다.

## 7. 하지 말아야 할 것

- 옛 제품 URL을 `404`로 바꾸지 않는다.
- 옛 제품 URL을 모두 홈이나 앱 허브 하나로 몰아 보내지 않는다.
- redirect를 여러 단계로 연결하지 않는다.
- 옛 제품 URL을 sitemap에 다시 넣지 않는다.
- **Search Console sitemap 행 삭제와 `sitemap.xml` 재제출 — 2026-08-24 의 금지는 2026-09-12 사용자
  지시로 해제됐고, 그날 삭제 후 재제출을 실행했다(§4.2.2).** 아래 8-24 결정의 문면은 그 시점의 판단으로
  보존하되 **현재 유효한 지침이 아니다.** 다만 금지의 근거였던 관찰("재제출을 반복해도 상태가 바뀌지
  않았다")은 그대로다. **9-12 재제출의 효과는 아직 판별되지 않았다**(§4.2.2 — 제출 8분 뒤 값이라
  판별력이 없다. **"효과가 없었다" 로 읽지 않는다**). 그러므로 **다음 재제출·삭제는 사용자의 명시
  지시가 있을 때만 한다. 해제는 9-12 지시에 한한 1회분이며, 이 문서가 자동으로 반복하지 않는다.**
  - (8-24 원문 두 항목) **Search Console sitemap 행을 삭제했다가 다시 만들지 않는다.**
  - (8-24 원문 두 항목) **`sitemap.xml` 을 재제출하지 않는다.** 2026-08-24 사용자 결정이다 — 재제출은
    이미 여러 번 반복됐고 상태가 한 번도 바뀌지 않았다(§4.2). 판정 기준일 8-20 규칙보다 이 결정이
    우선한다.
  - **2026-09-10 에는 효과만 논의한 뒤 사용자가 "문서만 update" 를 지시했다.** 재제출 승인이 아니었으며,
    읽기 전용 API 조회만 수행했다. 그때 남긴 비교 기준은 §4.2.1 에, 그 기준으로 판정한 9-12 실행 결과는
    §4.2.2 에 있다.
- 주소 변경 도구를 사용하지 않는다.
- 삭제 도구로 옛 URL을 숨기지 않는다.
- 새 URL 색인 요청을 같은 날 반복하지 않는다.
- 소유권 확인 파일 **3개를 모두** 삭제하지 않는다 — `googlec8773d84a9d25688.html`,
  `google6e10b6b19ade6d42.html`(Google), `naver291cd179e909bd205a8a0bf7179d3588.html`(네이버).
  이 목록은 2026-08-19 저장소 스윕으로 확정했다(§12.2). 그전에는 첫 번째 것만 적혀 있었다.

## 8. 공식 참고 자료

- [Site Moves and Migrations](https://developers.google.com/search/docs/crawling-indexing/site-move-with-url-changes)
- [Redirects and Google Search](https://developers.google.com/search/docs/crawling-indexing/301-redirects)
- [Change of Address tool](https://support.google.com/webmasters/answer/9370220)
- [URL Inspection tool](https://support.google.com/webmasters/answer/9012289)
- [Build and submit a sitemap](https://developers.google.com/search/docs/crawling-indexing/sitemaps/build-sitemap)
- [Sitemaps report](https://support.google.com/webmasters/answer/7451001)
- [Canonicalization](https://developers.google.com/search/docs/crawling-indexing/consolidate-duplicate-urls)
- [Removals tool](https://support.google.com/webmasters/answer/9689846)

## 9. Google TV Bluetooth HID 조사 글 추가 URL

2026-08-14에 bt-mouse의 Bluetooth HID가 Google TV 페어링 목록에서 보이지 않는 원인을 분석한 한·영 글을
추가했다. 기존 64 URL에 더해 sitemap의 예상 URL 수는 66개다. 배포 뒤 live 응답·self canonical·상호
`hreflang`을 확인하고, 먼저 sitemap에 맡긴다. 7~14일 뒤에도 미발견 또는 비색인이면 필요한 URL만 검사한다.

- [x] `https://droidactor.github.io/tech-notes/bt-keyboard/why-google-tv-does-not-detect-bluetooth-hid.html`
- [x] `https://droidactor.github.io/ko/tech-notes/bt-keyboard/why-google-tv-does-not-detect-bluetooth-hid.html`

**2026-08-15 live 실측 — 2개 모두 배포·확인 완료다.**

- 둘 다 `200`.
- self canonical 정확(각자 자기 URL).
- `hreflang` 3개(`en`, `ko`, `x-default`)가 서로를 정확히 가리키며 `x-default` 는 영문판이다.
- `noindex` 없음.
- sitemap 의 `<loc>` 수가 **66개**로 예상과 일치하고, 이 2개가 실제로 포함돼 있다.

색인 요청은 하지 않았다. 계획대로 sitemap·내부 링크에 맡기고, 7~14일 뒤 미발견이면 그때 선별한다.

**2026-08-17 URL 검사 — 2개 모두 여전히 미색인이다**(`참조 페이지: 감지된 페이지 없음`,
`최근 크롤링: 해당사항 없음`). 부모 허브 `(ko/)tech-notes/bt-keyboard/` 도 함께 미색인이라
"허브를 통한 발견" 경로가 성립하지 않는다. 상세는 §4.3 의 8-17 전수 스캔 표를 본다.

**2026-09-05 URL 검사 — 2개 모두 여전히 `Google에는 아직 알려지지 않은 URL입니다` 였고, 같은 날
2개 다 색인 생성 요청을 접수시켰다.**
검사 시점에 **부모 허브 `/tech-notes/bt-keyboard/` 는 8-31 요청으로 이미 색인돼 있었는데도**
(2026-09-05 절 3번) 상세로는 내려오지 않았다. 8-17 이 적은 "허브를 통한 발견 경로가 성립하지 않는다"
는 전제는 영문 허브에 한해 해소됐지만, 그 뒤 5일이 지나도 이 2개는 그대로였다.
**그래서 sitemap·내부 링크에 맡기던 방침을 접고 직접 요청했다**(사용자 지시). 경위는 2026-09-05 절
4번, 요청 기록은 §4.3 Tech Notes 묶음의 체크리스트에 있다.

## 10. bt-keyboard 상세 기술 노트 추가 URL — 4개

2026-08-16 에 bt-keyboard 기술 노트 2건을 한·영으로 추가했다. 커밋은 `842eb3d`(한/영 각인이 어긋나는
이유)와 `ec0f0cf`(HID 세션 공유) 두 개이며, 둘 다 같은 날 배포됐다. 이 4개로 sitemap `<loc>` 수는
66 → **70** 이 됐다.

- [x] `https://droidactor.github.io/tech-notes/bt-keyboard/why-korean-english-key-labels-drift.html`
- [x] `https://droidactor.github.io/ko/tech-notes/bt-keyboard/why-korean-english-key-labels-drift.html`
- [x] `https://droidactor.github.io/tech-notes/bt-keyboard/sharing-one-bluetooth-hid-session.html`
- [x] `https://droidactor.github.io/ko/tech-notes/bt-keyboard/sharing-one-bluetooth-hid-session.html`

**2026-08-17 live 실측 — 4개 모두 배포·확인 완료다.**

- 4개 전부 `200`.
- self canonical 정확(각자 자기 URL).
- `hreflang` 3개(`en`, `ko`, `x-default`)가 서로를 정확히 가리키며 `x-default` 는 영문판이다.
- `noindex` 없음(`<meta name="robots">` 자체가 없다).
- live sitemap 의 `<loc>` 수가 **70개**이고 이 4개가 실제로 포함돼 있다.
- **URL 검사(2026-08-17): 4개 전부 미색인.** `참조 페이지: 감지된 페이지 없음`,
  `최근 크롤링: 해당사항 없음`. 색인 생성 요청은 하지 않았다 — 부모 허브가 먼저다(§4.3).
- **URL 검사(2026-09-05): 4개 중 3개가 `Google에는 아직 알려지지 않은 URL입니다`,
  `/ko/…/sharing-one-bluetooth-hid-session.html` 1개는 `크롤링됨 - 현재 색인이 생성되지 않음` 이었다.**
  이 1개는 **같은 날 아침 검사에서는 `아직 알려지지 않은 URL` 이었다가 몇 시간 뒤 단계가 올라간
  것**이고, 그 사이에 형제 문서 3건을 요청했다 — 인과는 확인되지 않았다(2026-09-05 절 1번).
  부모 허브 `/tech-notes/bt-keyboard/` 는 8-31 요청으로 색인됐다(2026-09-05 절 3번).
- **2026-09-05 — 4개 전부 색인 생성 요청 접수**(사용자 지시). §9 의 2개와 한 묶음으로 처리했고,
  요청 기록은 §4.3 Tech Notes 묶음의 체크리스트에 있다.

### 10.1 sitemap `lastmod` 정정 — 26개 (2026-08-18 `9a397f5` 로 배포 완료)

§4.2 의 "`lastmod` 가 실제 최종 변경일인지" 점검에서 어긋난 값을 찾아 2026-08-17 에 고쳤다.
**작성 시점에는 미배포였고, 2026-08-18 커밋 `9a397f5` 가 이 변경을 함께 담아 배포했다** — live
`sitemap.xml` 이 저장소 파일과 동일함을 그날 확인했다(§11). 8-20 재제출의 선결 조건이던 항목이므로
그 제약은 해소됐다.

정정은 두 묶음이다. **처음에는 아래 A 6개만 고쳤는데, 그 결과 sitemap 에서 `2026-08-15` 가 완전히
사라져 "8-15 에는 아무것도 바뀌지 않았다" 는 잘못된 상태가 됐다.** 검토에서 이 범위 누락을 잡아
B 20개를 함께 고쳤다.

#### A. `2026-08-15` → `2026-08-16` — 6개

| URL | 고치기 전 | 고친 뒤 | 사유 |
|---|---|---|---|
| `/tech-notes/bt-keyboard/` | 2026-08-15 | 2026-08-16 | `ec0f0cf` 로 sharing 노트 링크가 실제로 추가됐는데 `lastmod` 가 그대로였다 |
| `/ko/tech-notes/bt-keyboard/` | 2026-08-15 | 2026-08-16 | 위와 같다 |
| `/tech-notes/bt-keyboard/why-google-tv-does-not-detect-bluetooth-hid.html` | 2026-08-15 | 2026-08-16 | `842eb3d` 에서 수정됐고 그 커밋·배포일이 8-16 이다 |
| `/ko/tech-notes/bt-keyboard/why-google-tv-does-not-detect-bluetooth-hid.html` | 2026-08-15 | 2026-08-16 | 위와 같다 |
| `/tech-notes/bt-keyboard/why-korean-english-key-labels-drift.html` | 2026-08-15 | 2026-08-16 | 신규 추가 커밋 `842eb3d` 의 날짜가 8-16 이다 |
| `/ko/tech-notes/bt-keyboard/why-korean-english-key-labels-drift.html` | 2026-08-15 | 2026-08-16 | 위와 같다 |

판단 근거는 **`lastmod` 의 기준이 로컬 작성일이 아니라 콘텐츠가 공개적으로 바뀐 배포일**이라는 점이다.
두 커밋 모두 `2026-08-16`(`842eb3d` 08:42 KST, `ec0f0cf` 19:59 KST)이므로 8-15 는 어느 파일에도 맞지
않는다. 특히 허브 2개는 `ec0f0cf` 에서 본문이 바뀌었는데 sitemap 은 새 URL 2개만 넣고 허브의 `lastmod`
갱신을 빠뜨렸다 — sitemap 자신의 머리주석이 "When a page is added or its content changes, update
`<lastmod>` for that pair." 라고 적어 둔 규약을 어긴 상태였다.

#### B. `2026-08-14` → `2026-08-15` — 20개

커밋 `a795df6`(2026-08-15 07:27 KST)이 실제로 본문을 바꾼 URL 인데 `lastmod` 가 `2026-08-14` 로
남아 있던 것들이다. 변경 내용은 사소하지 않다 — `<span class="badge soon">Coming soon</span>` 이
Google Play 배지 링크로 바뀌었고, 사양표 `Status` 행이 `Not yet published on Google Play` 에서
`Published on Google Play — store listing` 으로, 푸터에 상표 고지 `<p class="tm">` 가 추가됐다.
**A 의 "허브에 링크 한 줄 추가" 보다 훨씬 큰 변경이고, 상업적으로도 가장 중요한 변경(스토어 링크)이다.**

| 묶음 | URL |
|---|---|
| 홈 2개 | `/`, `/ko/` |
| 제품 6개 | `(ko/)apps/{bt-ppt, bt-mouse, ssh-scout}/` |
| 매뉴얼 6개 | `(ko/)manual/{bt-ppt, bt-mouse, ssh-scout}/` |
| 블로그 6개 | `(ko/)blog/{use-android-phone-as-powerpoint-remote, use-android-phone-as-bluetooth-mouse, find-and-connect-to-ssh-hosts-from-android}/` |

`README.md` 도 같은 커밋에서 바뀌었지만 sitemap 대상이 아니므로 제외했다.

#### 정정 결과

`lastmod` 분포는 `2026-08-14` 42개 + `2026-08-15` 20개 + `2026-08-16` 8개 = **70개**이고 XML 은
well-formed 다. **이 수치는 A·B 두 묶음까지만 검증한 값이며 "sitemap 의 모든 `lastmod` 가 옳다" 는
뜻이 아니다** — 앞으로 부분 배포를 할 때마다 같은 대조(`git log -1 --date=iso-local -- <path>` ↔
sitemap `lastmod`)를 반복한다.

**아직 Google 이 이 sitemap 을 한 번도 성공적으로 읽지 못했으므로(§4.2) 이 정정에는 부작용이 없다** —
이미 수집된 값을 되돌리는 것이 아니라 첫 수집 전에 맞추는 일이다. **단 그 무해함은 배포를 마쳤을 때만
성립한다.** 미배포 상태로 8-20 재제출을 하면 첫 성공 수집이 틀린 `lastmod` 로 이뤄진다.

**2026-08-18 후속.** 배포는 끝났고(`9a397f5`), 위 "앞으로 부분 배포를 할 때마다 반복한다" 는 약속을
그 배포에 적용한 결과가 §11.1 이다. 같은 커밋이 `lastmod` 10건과 `dateModified` 20건을 새로 어긋나게
만들었고 그 자리에서 고쳤다. 위 분포 수치(42+20+8)는 8-17 시점 기록이므로 그대로 두고, 현재 분포는
§11.1 에서 본다.

## 11. 2026-08-18 배포 — 블로그 앱 카드·스크린샷의 Google Play 링크 전환

커밋 `9a397f5`(2026-08-18 18:05 KST). **새 URL 은 없고 기존 22개 URL 의 본문이 바뀐 배포다.** sitemap
`<loc>` 수는 70 그대로이므로 이동 처리나 신규 URL 발견 절차는 대상이 아니고, `lastmod`·`dateModified`
갱신과 live 검증만 대상이다. 커밋이 손댄 배포 파일은 24개(22개 페이지 + `sitemap.xml` + `assets/site.css`)다.

본문이 바뀐 22개 URL:

| 묶음 | 개수 | URL |
|---|---|---|
| 홈 | 2 | `/`, `/ko/` |
| 앱 허브 | 2 | `/apps/`, `/ko/apps/` |
| 매뉴얼 허브 | 2 | `/manual/`, `/ko/manual/` |
| bt-mouse 제품 | 2 | `(ko/)apps/bt-mouse/` |
| bt-mouse 매뉴얼 | 2 | `(ko/)manual/bt-mouse/` |
| 블로그 앱 소개 글 | 12 | `(ko/)blog/{use-android-phone-as-bluetooth-mouse, use-android-phone-as-powerpoint-remote, find-and-connect-to-ssh-hosts-from-android, find-devices-on-wifi-from-android, control-lg-webos-tv-over-wifi, type-korean-over-bluetooth-hid}/` |

검증 결과:

- [x] **live 전수 `200`** — sitemap 의 70개 URL 전부 `200` + self canonical 일치 + `noindex` 없음 +
      `hreflang` 2개 이상. 70/70.
- [x] **옛 제품 URL 14개** — 14/14 가 `200` + `meta refresh 0; url=<대응 새 URL>` 1:1 + 대응 새 URL
      canonical + `noindex` 없음. §3·§4.4 의 점검을 배포 단위로 반복한 것이다.
- [x] **live `sitemap.xml` = 저장소 파일** — `<loc>` 70개, XML well-formed, `application/xml`.
      즉 §10.1 의 26건 정정이 live 에 반영됐다.
- [x] **`lastmod`·`dateModified`·화면 표기 대조** — 어긋남 34건을 찾아 고쳤다(§11.1, 8-19 배포).
      `lastmod` 10 + `dateModified` 20 + 매뉴얼 화면 표기 4 다.
- [ ] **`bt-mouse` 스토어 링크** — 22곳이 현재 `404` 다(§11.2). 게시 후 재확인이 남았다.

### 11.1 날짜 신호 정정 34건 — `lastmod` 10 + `dateModified` 20 + 화면 표기 4

**정정은 2026-08-18 에 하고 배포는 2026-08-19 에 했다** — 커밋 `5cc2907`
("날짜 신호 34건 정정 — lastmod 10, dateModified 20, 화면 표기 4").
**이 커밋의 날짜(8-19)는 어떤 `lastmod` 의 기준일도 아니다** — 날짜 필드와 화면 표기만 고쳤고 독자가
읽는 내용은 한 글자도 바뀌지 않았으므로, 아래 재현 절차에서 건너뛸 대상이다. 이 구분이 없으면 정정할
때마다 날짜가 하루씩 밀린다.

대조 방법은 두 축이다.

1. sitemap 의 `<lastmod>` ↔ 그 URL 파일의 마지막 **본문 변경** 배포일
2. 페이지 JSON-LD 의 `dateModified` ↔ 같은 기준일

**"본문 변경" 이 기준이라는 점이 중요하다.** 날짜 필드만 고치는 이번 정정 커밋은 콘텐츠 변경이
아니므로 그 커밋 날짜로 `lastmod` 를 다시 당기지 않는다. 이 구분이 없으면 정정할 때마다 날짜가 하루씩
밀려 영구히 수렴하지 않는다. 반대로 `git log -1` 만 기계적으로 믿어도 같은 함정에 빠진다.

#### A. `lastmod` `2026-08-17` → `2026-08-18` — 10개

| 묶음 | URL |
|---|---|
| 블로그 5쌍 | `(ko/)blog/{type-korean-over-bluetooth-hid, use-android-phone-as-powerpoint-remote, find-devices-on-wifi-from-android, find-and-connect-to-ssh-hosts-from-android, control-lg-webos-tv-over-wifi}/` |

**2026-08-17 에는 배포가 없었다.** 그날 자 커밋이 아예 없고(`ec0f0cf` 8-16 → `9a397f5` 8-18), 이 10개
페이지가 마지막으로 바뀐 커밋은 `9a397f5` 하나다. 즉 이 콘텐츠가 공개적으로 바뀐 날은 8-18 이다.
같은 커밋이 손댄 블로그 12개 중 `use-android-phone-as-bluetooth-mouse` 쌍만 8-18 로 적혀 있었으므로,
한 커밋의 `lastmod` 가 두 날짜로 갈려 있던 상태였다. (그 커밋 시점의 8-18 은 12개였고 — 홈 2, 허브 4,
bt-mouse 제품·매뉴얼 4, mouse 블로그 2 — 나머지 블로그 10개만 8-17 이었다.)

정정 후 분포: `2026-08-14` 32 + `2026-08-15` 8 + `2026-08-16` 8 + `2026-08-18` 22 = **70**, XML
well-formed. `2026-08-17` 은 사라진다 — 그날 공개된 변경이 없으므로 맞다.

#### B. `dateModified` 20개

| 대상 | 개수 | 고치기 전 | 고친 뒤 | 근거 커밋(배포일) |
|---|---|---|---|---|
| 블로그 5쌍 | 10 | 2026-08-17 | 2026-08-18 | `9a397f5` (8-18) — 도입부 앱 카드·스크린샷 링크 전환 |
| `(ko/)manual/{bt-ppt, ssh-scout}/` | 4 | 2026-08-14 | 2026-08-15 | `a795df6` (8-15) — Play 배지와 상표 고지 추가 |
| `(ko/)tech-notes/bt-keyboard/` | 2 | 2026-08-15 | 2026-08-16 | `ec0f0cf` (8-16) — 허브 목록에 sharing 노트 링크 추가 |
| `(ko/)…/why-google-tv-does-not-detect-bluetooth-hid.html` | 2 | 2026-08-15 | 2026-08-16 | `842eb3d` (8-16) — article 내 관련 노트 링크 1개 추가 |
| `(ko/)…/why-korean-english-key-labels-drift.html` | 2 | 2026-08-15 | 2026-08-16 | `842eb3d` (8-16) — 신규 추가 |

**갱신/유지를 가르는 규칙은 이렇게 읽는다** — `dateModified` 는 **article 안에서 독자가 보는 것이
늘거나 바뀌었을 때** 갱신하고, **전역 푸터·탐색 링크와 `href` 대상 교체만**이면 유지한다. 위 5줄과
아래 C 4개가 이 규칙으로 전부 설명된다. 애매한 사례가 실제로 있었다 —
`why-google-tv-does-not-detect-bluetooth-hid.html` 은 산문이 한 글자도 안 바뀌고 article 안
`manual-actions` 에 관련 노트 링크 하나가 늘었다. **독자에게 보이는 것이 늘었으므로 갱신 쪽이다.**
반대로 C 의 4개는 같은 앵커 텍스트의 `href` 대상만 바뀌었다.

`why-korean-english-key-labels-drift.html` 2개는 `datePublished` 도 `2026-08-15` → `2026-08-16` 으로
같이 고쳤다. 로컬 작성일이 8-15 였을 뿐 공개된 날은 8-16 이다. §10.1 A 가 같은 파일의 sitemap
`lastmod` 에 이미 적용한 판단을 페이지 안 구조화 데이터에도 맞춘 것이다.

#### B-2. 화면에 보이는 갱신 날짜 4개 — `2026-08-14` → `2026-08-15`

`(ko/)manual/{bt-ppt, ssh-scout}/` 의 `<p class="manual-meta">` 표기를 `Updated 14 August 2026` /
`2026년 8월 14일 갱신` → `15 August 2026` / `8월 15일 갱신` 으로 함께 고쳤다. **구조화 데이터만 올리고
화면 표기를 두면 둘이 어긋난다** — Google 은 Article 계열에서 보이는 날짜를 채택하거나 구조화 날짜를
무시하므로, 그대로 두면 방금 고친 8-15 신호가 버려진다. `9a397f5` 가 `manual/bt-mouse` 에서
`dateModified` 와 `Updated … 2026` 을 함께 고친 선례가 이 저장소의 관례다.

같은 점검에서 **`why-korean-english-key-labels-drift.html` 의 화면 표기는 일부러 두었다.** 그 줄은
`Source baseline: app 1.0.0 (2) · 15 August 2026` / `소스 기준: 앱 1.0.0 (2) · 2026년 8월 15일` 로
**분석에 사용한 소스 스냅샷 날짜**이지 발행일 표기가 아니다. `datePublished` 8-16(공개된 날)과 다른
사실을 말하므로 어긋남이 아니다 — 다음 세션이 "동기화" 하지 않도록 여기 적어 둔다.

#### C. 일부러 고치지 않은 것 — `dateModified` `2026-08-03` 4개

`(ko/)blog/{find-ls-plc-ip-address, identify-industrial-devices-by-port}/` 는 `dateModified` 가
`2026-08-03` 인데 sitemap `lastmod` 는 `2026-08-14` 다. **이 불일치는 정상이다.**

- `lastmod` 는 **페이지**가 바뀐 날이다. 8-14 개편이 이 페이지들의 링크 대상(`/wifi-scout/` →
  `/apps/wifi-scout/`)과 푸터 탐색을 바꿨으므로 8-14 가 맞다.
- `dateModified` 는 **글**이 바뀐 날이다. 8-14 diff 를 확인한 결과 본문 문장은 그대로이고 링크
  대상과 푸터만 바뀌었으므로 8-03 을 유지한다.

이 구분을 잃으면 링크 정리 배포마다 모든 글의 `dateModified` 가 갱신되어 "글이 개정됐다" 는 잘못된
신호를 보낸다.

#### 재현 절차

배포마다 아래 대조를 반복한다. 결과가 0건이어야 정상이며, 어긋남이 나오면 그 배포가 실제로 무엇을
바꿨는지 diff 로 확인한 뒤 위 규칙으로 방향을 정한다.

```bash
# 이 파일을 바꾼 커밋 목록 — 맨 위가 최신
git log --format='%h %ad %s' --date=short -- <path>
# 그중 "날짜 필드만 고친 정정 커밋" 은 건너뛰고, 그 아래 첫 실질 변경 커밋의 날짜를 기준으로 쓴다
git show <hash> -- <path>          # article 내 변경인지 푸터·href 뿐인지 확인
grep -o '"dateModified"[^,]*' <path>                # ↔ sitemap <lastmod>
grep -o '<p class="manual-meta">[^<]*' <path>       # ↔ 화면에 보이는 날짜(매뉴얼)
```

**`git log -1` 을 그대로 쓰면 안 된다.** 이 정정 자체가 커밋되는 순간 매뉴얼 4개와 Tech Notes 6개의
`git log -1` 은 8-18 을 돌려주는데 `lastmod` 는 8-15·8-16 이므로, 어긋남 10건이 거짓으로 뜬다. 그대로
믿고 날짜를 당기면 정정할 때마다 하루씩 밀리는 드리프트가 실제로 생긴다. **날짜 필드·화면 표기만 고친
커밋은 기준일이 아니다.**

#### 남겨 둔 오기 — `assets/site.css?v=20260817-blog-play-link`

`9a397f5` 가 12개 페이지에 붙인 CSS 캐시 토큰의 날짜가 `20260817` 인데 실제 배포일은 8-18 이다.
**고치지 않았다** — 토큰을 바꾸면 그 12개 페이지 본문이 또 바뀌어 `lastmod` 를 다시 갱신해야 하고,
`Cache-Control: max-age=600` 이라 캐시 문제도 없다. 하이픈 없는 표기라 `2026-08-17` grep 에도 걸리지
않으므로, **이 토큰을 "8-17 에 배포가 있었다" 는 증거로 읽지 말 것.** 8-17 자 커밋은 없다.

### 11.2 `bt-mouse` 스토어 링크 22곳이 현재 `404` — 게시 후 재확인 필요

`9a397f5` 는 재등록 예정 패키지명 `com.droidactor.bt_mouse2` 를 **게시 전에** 사이트 전역으로
선반영했다(커밋 메시지가 그렇게 밝히고 있다). 2026-08-18 실측 결과는 다음과 같다.

| 패키지 | 링크 수 | Google Play 응답 |
|---|---|---|
| `com.droidactor.wifi_scout` | 32 | `200` |
| `com.droidactor.ssh_scout` | 24 | `200` |
| `com.droidactor.lgtv` | 24 | `200` |
| `com.droidactor.bt_ppt` | 22 | `200` |
| `com.droidactor.bt_keyboard` | 20 | `200` |
| **`com.droidactor.bt_mouse2`** | **22** | **`404`** |
| `com.droidactor.bt_mouse` (옛 이름) | 0 | `404` |

`/apps/bt-mouse/` 사양표는 `Published on Google Play —` 로 적혀 있어, 현재는 페이지의 주장과 스토어
실제 상태가 어긋난다. **의도된 선반영이므로 사이트를 되돌리지 않았다.** 다만 게시가 늦어지는 동안
① 방문자가 `404` 에 도달하고 ② 색인된 페이지가 사실과 다른 공개 상태를 주장한다.

**타이밍이 나쁘게 겹친다.** 404 링크를 품은 페이지는 8개이고 그중 **홈 2개**가 들어 있다
(`/`, `/ko/`, `(ko/)apps/bt-mouse/`, `(ko/)manual/bt-mouse/`, `(ko/)blog/use-android-phone-as-bluetooth-mouse/`).
이 8개가 8-18 배포로 가장 신선한 `lastmod` 코호트에 들어갔고(**그중 홈 2개는 8-19 배포로
`2026-08-19` 로 다시 올라갔다 — §12.** 나머지 6개는 `2026-08-18` 그대로다), 다음 작업은 색인 요청
10건으로 크롤을 더 부른다. 즉 **크롤을 적극적으로 부르는 기간과 CTA 가 죽은 기간이 겹친다.**

**되돌림 기준일 = 2026-08-20**(sitemap 재제출 판정일과 같은 날). 그날까지 게시가 안 되면 배지·상태
문구를 게시 전 표기로 되돌린다. 게시되면 위 표를 재확인해 22곳이 `200` 인지 본다.

**⚠ 이 판정은 8-20 에도 8-24 에도 수행되지 않았다.** 8-20 에는 세션이 없었고, 8-24 세션은 GSC 만
열고 Play Console 은 열지 않았다. **기준일이 나흘 지난 채로 이월 중**이며 §6 2026-08-24 의 다음 작업
4번이 그것을 들고 있다. 함께 묶여 있던 sitemap 재제출 판정은 같은 날 **취소**됐으므로(§4.2·§7)
"같은 날" 이라는 이 절의 결속은 이제 성립하지 않는다 — 이 판정은 독립적으로 남아 있다.

**2026-08-19 Play Console 실측 — 게시되지 않았고, 트랙조차 프로덕션이 아니다.**

| 항목 | 값 |
|---|---|
| 패키지 | `com.droidactor.bt_mouse2` |
| 트랙 | **내부 테스트**(프로덕션 아님) |
| 상태 | **검토 중**(`schedule`) |
| 마지막 변경 | 2026-08-17 |
| 옛 패키지 `com.droidactor.bt_mouse` | 앱 이름이 `Obsolete Bluetooth Mouse` 로 바뀌었고 **출시 안됨** |

**이 값이 8-20 판정을 사실상 결정한다.** 내부 테스트 트랙에서 검토 중인 앱은 프로덕션 스토어
페이지가 없으므로, 심사가 통과하고 **프로덕션으로 승격**되기 전에는 `play.google.com/store/apps/
details?id=com.droidactor.bt_mouse2` 가 계속 `404` 다. 8-17 부터 검토 중이므로 8-20 까지 프로덕션
승격이 끝날 것으로 기대할 근거가 없다. 되돌림 쪽으로 기울어 있으나, **판정 자체는 8-20 에 상태를
다시 읽고 한다** — 심사는 하루 만에도 끝난다.

**2026-08-31 실측 — 해소됐다. 이 항목은 닫는다.**

| 패키지 | `hl=en` | `hl=ko` |
|---|---|---|
| `com.droidactor.bt_mouse2` | **`200`** | **`200`** |
| `bt_keyboard`, `bt_ppt`, `lgtv`, `ssh_scout`, `wifi_scout` | `200` | `200` |
| `com.droidactor.bt_mouse`(옛 이름, 대조군) | `404` | `404` |

프로덕션 승격이 끝나 **되돌림 판정 자체가 소멸했다.** 배지·상태 문구는 그대로 둔다 — 선반영이
사실과 일치하게 됐다. 대조군이 `404` 를 돌려주므로 이 검사가 실제로 구별한다는 것도 확인했다.
링크 수는 8-18 의 22곳에서 **26곳**으로 늘었다(8-27 스크린샷 절 추가분). 8-20 기준일을 열하루
넘겨 닫혔고, 그동안 사이트를 되돌리지 않은 판단은 결과적으로 옳았다.

## 12. 2026-08-19 배포 — Simple Gas Mileage Calculator 개인정보 처리방침 앵커 추가

커밋 `a806fbd`(2026-08-19 07:07 KST). **새 URL 은 없고 홈 2개의 본문이 바뀐 배포다.** sitemap `<loc>`
수는 70 그대로이므로 이동 처리나 신규 URL 발견 절차는 대상이 아니고, `lastmod` 갱신과 live 검증만
대상이다. 커밋이 손댄 배포 파일은 `index.html`·`ko/index.html` 2개다.

**같은 날 커밋 `5cc2907`(06:42 KST)도 배포됐지만 그것은 §11.1 의 날짜 신호 정정이고 콘텐츠 변경이
아니므로 어떤 `lastmod` 의 기준일도 아니다**(§11.1 머리말). 하루에 두 커밋이 배포된 날이므로 이
구분을 특히 놓치기 쉽다 — 기준일을 주는 것은 `a806fbd` 하나뿐이다.

변경 내용은 개인정보 처리방침 7장 뒤에 `<div class="policy" id="privacy-gas">` 블록을 넣은 것이다 —
`<h3>` 제목 + `<span class="pkg-inline">com.droidactor.SimpleGasMileage</span>` + 본문 두 단락(입력한
숫자로 계산만 하고 기기에 머문다 / 배너 광고 1개). **독자가 보는 것이 늘어난 본문 변경**이므로
§11.1 B 가 세운 **판정 기준**("독자가 보는 것이 늘거나 바뀌었는가")으로 갱신 쪽이다 — 다만 여기서
갱신하는 대상은 `dateModified` 가 아니라 §11.1 의 대조축 1 인 sitemap `lastmod` 다. 파일 상단 주석의
앵커 목록도 갱신하며 누락돼 있던 `#privacy-lgtv` 를 함께 반영했다(주석이므로 그 자체는 화면 변화가
아니다).

**제품 페이지를 만들지 않은 것은 의도다** — 커밋 메시지가 "Dotori 브랜드 앱이 아니므로 dotori span 과
제품 페이지 링크는 두지 않았다" 고 밝히고 있다. 그래서 이 배포에는 `/apps/…` 계열 신규 URL 이 없고
sitemap 도 늘지 않는다. 다음 세션이 "앱이 늘었는데 제품 페이지가 없다" 를 누락으로 오판하지 않도록
적어 둔다.

검증 결과 (2026-08-19 07:55 KST live 실측):

- [x] **홈 2개 live 반영** — `/`·`/ko/` 둘 다 `200` + `id="privacy-gas"` 1개 + `#privacy-lgtv` 1개 +
      `com.droidactor.SimpleGasMileage` 존재 + self canonical 일치 + `noindex` 없음 + `hreflang` 7개.
- [x] **`lastmod` 대조 — 어긋남 2건을 찾아 고쳤다.** `/`·`/ko/` 가 `2026-08-18` 로 남아 있어
      `2026-08-19` 로 정정했다(`sitemap.xml` 의 `<loc>` 첫 두 블록). 정정 후 `<loc>` 70개,
      XML well-formed, 분포 `2026-08-14` 32 + `2026-08-15` 8 + `2026-08-16` 8 + `2026-08-18` 20 +
      `2026-08-19` 2 = 70.
- [x] **`dateModified`·화면 표기는 이 배포의 대조 대상이 아니다.** 홈 2개에는 JSON-LD 날짜 필드가
      없고(`"date…"` grep 0건), 매뉴얼의 `<p class="manual-meta">` 같은 갱신일 표기도 없다. 따라서
      §11.1 B·B-2 의 **대상 필드가 이 배포에 존재하지 않는다**(위 판정 기준과 혼동하지 않는다).
      대조축은 sitemap `lastmod` 하나뿐이다. **홈에만 있는 날짜인 `Effective date` 는 성격이 달라
      아래 §12.1 로 분리한다.**
- [x] **저장소 전수 스윕 — sitemap 누락 URL 0건.** HTML 89개를 훑어 sitemap `<loc>` 70개와 대조했다.
      sitemap 밖 19개는 전부 의도된 제외다 — 옛 제품 URL 호환 페이지 14개(§2.1·§7 이 sitemap 재등록을
      금지), `404.html`·`blog/_post-template.html`(둘 다 `noindex`), 소유권 확인 파일 3개
      (`googlec8773d84a9d25688.html`, `google6e10b6b19ade6d42.html`, `naver291cd179…html`).
      **역방향도 0건** — sitemap 에만 있고 대응 파일이 없는 URL 은 없다.
      (`google6e10b6b19ade6d42.html` 은 `113d6ea`(8-07)로 추가된 **두 번째** Google 확인 파일인데
      §3·§7 은 `googlec8773d84a9d25688.html` 하나만 적고 있다 — 아래 §12.2.)
- [x] **push 후 live 재확인 — 완료.** 정정은 커밋 `54af9c8`(2026-08-19 08:30 KST)로 배포했고
      08:32 KST 실측에서 live `sitemap.xml` 이 저장소 파일과 **70개 전부 완전 일치**했다
      (`/`·`/ko/` = `2026-08-19`, 분포 `2026-08-14` 32 + `2026-08-15` 8 + `2026-08-16` 8 +
      `2026-08-18` 20 + `2026-08-19` 2). **8-20 재제출의 선결 조건이 해소됐다.**
      (첫 조회에서는 아직 `2026-08-18` 이 내려왔고 약 20초 뒤 반영됐다 — Pages 빌드 지연이므로
      push 직후 1회 조회로 판정하지 않는다.)
- [x] **Play Console 처리방침 URL 등록 확인 — 7/7 정확.** 2026-08-19 Console 실측이다.
      `index.html` 상단 주석이 `Do not rename the #privacy-* anchors — they are registered on the
      store listings` 라고 못박은 그 등록 상태를 처음으로 대조했다.

      **⚠ 이 `7/7` 은 앵커가 8개였던 8-19 오전 시점의 값이다.** 같은 날 17:38 의 커밋 `5beaf34` 가
      `#privacy-apartment`·`#privacy-calendar`·`#privacy-unit` 3개를 더해 앵커가 11개가 됐고(live 실측
      11개), 그 3개에 대응하는 앱의 등록은 8-19·8-24 에 대조하지 못했다
      (§6 2026-08-24 성과 7번, 같은 절 다음 작업 10번).

      **2026-08-31 재대조 — 게시된 앱 기준 `9/9` 정확이다. 분모를 갱신하고 이 항목을 닫는다.**
      **Console 에 들어가지 않고 공개 스토어 페이지에서 확인했다** — 스토어 등록정보의 처리방침 URL 은
      공개 값이므로 `details?id=<pkg>&hl=en` 의 HTML 에서 그대로 읽힌다. 로그인도 UI 조작도 필요 없다.

      | 앵커 | 패키지 | 스토어 | 스토어에 등록된 처리방침 URL |
      |---|---|---|---|
      | `#privacy-keyboard` | `com.droidactor.bt_keyboard` | `200` | `/#privacy-keyboard` |
      | `#privacy-ppt` | `com.droidactor.bt_ppt` | `200` | `/#privacy-ppt` |
      | `#privacy-mouse` | `com.droidactor.bt_mouse2` | `200` | `/#privacy-mouse` |
      | `#privacy-wifi` | `com.droidactor.wifi_scout` | `200` | `/#privacy-wifi` |
      | `#privacy-ssh` | `com.droidactor.ssh_scout` | `200` | `/#privacy-ssh` |
      | `#privacy-lgtv` | `com.droidactor.lgtv` | `200` | `/#privacy-lgtv` |
      | `#privacy-gas` | `com.droidactor.SimpleGasMileage` | `200` | `/#privacy-gas` |
      | **`#privacy-apartment`** | `com.app.apartment` | `200` | **`/#privacy-apartment`** |
      | **`#privacy-calendar`** | `com.droidactor.DrCalendar` | `200` | **`/#privacy-calendar`** |
      | `#privacy-ytdl` | `com.droidactor.ytdownloader` | `404` | — (미게시) |
      | **`#privacy-unit`** | `com.app.unitconverter` | `404` | — (미게시) |

      **새 앵커 3개 중 2개는 앱이 게시돼 있고 둘 다 정확한 앵커를 가리킨다.** 나머지 `#privacy-unit`
      은 앱이 아직 스토어에 없으므로 대조 대상이 아니다 — `#privacy-ytdl` 과 같은 상태다.
      스토어가 가리키는 앵커 9개가 **live 홈에 전부 실재함**도 확인했다(live 앵커 11개 전수).
      각 스토어 페이지에는 루트 `https://droidactor.github.io`(웹사이트)와 앵커 URL(처리방침)이 **둘 다**
      들어 있다 — §5 가 "웹사이트 = 루트" 라고 적은 것과 모순이 아니라 서로 다른 칸이다.
      **`com.app.*` 두 앱은 패키지 접두사가 `com.droidactor.*` 가 아니다** — 앱 목록을 패키지명으로
      필터링하면 놓친다.

      | 앱 패키지 | Console `개인정보처리방침 URL` |
      |---|---|
      | `com.droidactor.SimpleGasMileage` | `https://droidactor.github.io/#privacy-gas` |
      | `com.droidactor.bt_keyboard` | `https://droidactor.github.io/#privacy-keyboard` |
      | `com.droidactor.bt_mouse2` | `https://droidactor.github.io/#privacy-mouse` |
      | `com.droidactor.bt_ppt` | `https://droidactor.github.io/#privacy-ppt` |
      | `com.droidactor.lgtv` | `https://droidactor.github.io/#privacy-lgtv` |
      | `com.droidactor.ssh_scout` | `https://droidactor.github.io/#privacy-ssh` |
      | `com.droidactor.wifi_scout` | `https://droidactor.github.io/#privacy-wifi` |

      **`#privacy-gas` 는 앵커를 만든 그 커밋 시점에 이미 등록돼 있었다** — 별개 작업이 남아 있지
      않았다. `#privacy-lgtv` 도 정확하다(주석 목록에서만 빠져 있었을 뿐 등록은 되어 있었다).
      **Play Console 은 앱당 처리방침 URL 이 하나**라 `/ko/#privacy-gas` 를 따로 넣을 칸은 없다 —
      국문 독자는 영문 페이지의 `hreflang`·상단 링크로 이동한다. 7개 전부 **루트 앵커**이므로
      §5 의 옛 제품 URL 문제와도 무관하다.
- [x] **개인정보 처리방침 `Effective date` 판단 — 올리는 것으로 결정.** 저장소 선례가 있었다(§12.1).

### 12.1 홈 2개의 `Effective date` — 저장소 선례에 따라 `19 August 2026` 으로 올렸다

처음에는 "기계적으로 정할 수 없다" 며 미뤄 뒀는데, **`index.html` 자신의 이력이 답을 갖고 있었다.**
`Effective date` 가 바뀐 지점을 전 이력에서 추적한 결과다.

| 커밋 | 날짜 | 개인정보 처리방침에 한 일 | `Effective date` |
|---|---|---|---|
| `13e41d2` | 8-06 09:11 | LG TV Remote 절 추가 | `28 July` 유지 |
| **`8f3317d`** | **8-06 14:22** | **Bluetooth Mouse 절 추가**(`<h3>` + `pkg-inline`) | `28 July` → **`6 August`** |
| `e01e36d` | 8-08 | 하우스 광고 공개 정책 보강 | `6 August` → **`8 August`** |
| `e0cac58` ~ `a806fbd` | 8-10 ~ 8-19 | 링크·배지·URL 개편 등 정책 무관 | `8 August` 유지 |

`8f3317d` 는 `a806fbd` 와 **정확히 같은 모양의 변경**(새 앱의 `<h3>…<span class="pkg-inline">` 처리방침
절 추가)이고 그때 시행일을 올렸다. `13e41d2` 만 올리지 않았으나 같은 날 5시간 뒤 `8f3317d` 가 덮었다.
반대로 정책과 무관한 변경에서는 **한 번도 올리지 않았다.** 규칙이 이미 서 있었던 것이다 —
**처리방침 본문이 실질적으로 늘거나 바뀌면 그 배포일로 올리고, 그 외에는 유지한다.**

문서의 자기 약속도 같은 방향이다. 8장이 영문은 "If this policy changes, the updated version is posted
on this page with a new date above", 국문은 "이 방침이 변경되면 갱신된 내용을 이 페이지에 게시하고
**위의 날짜를 함께 고칩니다**" 라고 적고 있다. 앱 하나의 공개 범위가 늘어난 것은 이 문서의 변경이다.

그래서 `index.html:250` 을 `19 August 2026`, `ko/index.html:222` 를 `2026년 8월 19일` 로 고쳤다.

**`lastmod` 영향은 없다.** 이 정정도 홈 2개의 본문 변경이지만 같은 날(8-19) 안에서 일어났고 두 URL 의
`lastmod` 는 이미 `2026-08-19` 다. 하루를 넘겨서 고쳤다면 `lastmod` 를 다시 올려야 했고, 그것이
§11.1 이 경계한 "정정할 때마다 날짜가 밀리는" 상황을 부른다 — **날짜 성격의 정정은 같은 날 안에
끝내는 편이 싸다**는 것이 이번에 얻은 실무 규칙이다.

`dateModified` 는 여전히 대상이 아니다(홈 2개에 JSON-LD 날짜 필드가 없다, §12).

### 12.2 소유권 확인 파일이 3개였다 — 문서는 하나만 보호하고 있었다

이 배포와 직접 관계는 없고, 위 전수 스윕에서 드러난 것이다. 루트에 site verification 파일이 셋 있다.

| 파일 | 추가 커밋 | 용도 |
|---|---|---|
| `googlec8773d84a9d25688.html` | `5206b25` (2026-08-02, "Search Console 소유권 확인 파일 추가") | Google |
| `google6e10b6b19ade6d42.html` | `113d6ea` (2026-08-07, "권한 확인 파일: 필수. 삭제하지 말것.") | Google — 별개 토큰 |
| `naver291cd179e909bd205a8a0bf7179d3588.html` | `687f2cd` (2026-08-10) | 네이버 서치어드바이저 |

그런데 §3 의 확인 항목과 §7 의 금지 목록은 **첫 번째 것만** 적고 있었다. 나머지 둘은 문서상 보호되지
않아 정리 작업에서 지워질 수 있었고, 그러면 소유권 확인이 풀린다. §7 에 셋을 모두 적었다.

**두 번째 Google 토큰의 정체는 미확인이다.** 같은 속성에 추가한 확인 방법일 수도, 다른 계정·다른
Google 제품의 확인일 수도 있다. 커밋 제목이 "필수. 삭제하지 말것." 뿐이라 저장소만으로는 갈리지 않는다.
8-19 시점에는 이것을 "8-20 에 GSC 를 열 때 함께 확인한다" 로 남겼다 — 재제출 판정에서 ②번 갈래
(재시도했는데 또 실패)로 갔을 때의 후보 원인이 "제출 위치와 보고 있는 속성이 어긋남" 이었기 때문이다.
**그 지시는 8-19 당일에 이미 이행됐고(바로 아래 `[x]`), 재제출 자체가 2026-08-24 에 취소됐으므로
(§4.2·§7) 이 대기 항목은 소멸했다.** ②번 갈래를 배제했던 판단도 §4.2.1 에서 철회했다.

- [x] **2026-08-19 GSC 실측 — 두 번째 토큰은 두 번째 소유자의 것이다. 미사용 토큰은 0개다.**

      | 화면 | 값 |
      |---|---|
      | 설정 > 소유권 인증 | 인증된 소유자입니다 / 기존에 사용한 확인 방법 = **HTML 파일** |
      | 사용자 및 권한 | **사용자(2)** — `droidactor@gmail.com`(나, **소유자**) + 별도 Google 계정 1개(**소유자**, 확인됨) |
      | 미사용 소유권 토큰 | **0** |

      **"미사용 토큰 0" 이 결론을 확정한다** — 루트의 Google 확인 파일 두 개가 각각 두 소유자의
      살아 있는 확인 수단이고, 어느 쪽도 지워도 되는 잔재가 아니다. §7 이 셋을 모두 보호 목록에
      올린 것이 맞았다. **하나라도 지우면 그 소유자의 인증이 풀린다.**

      (두 번째 소유자의 이메일 주소는 여기 적지 않는다 — 이 저장소는 공개이고, 개인 주소를 커밋하면
      되돌릴 수 없다. 값이 필요하면 GSC `설정 > 사용자 및 권한` 에서 직접 본다.)

      **부수 소득**: 이 속성의 소유권이 확인된 상태다. 이것만으로 제출 속성 불일치 여부나 백오프를
      판정할 수는 없다. 정확한 `siteUrl`·`feedpath` 로 수행한 API 실측은 §4.2.1(2026-09-10 기준선)과
      **§4.2.2(2026-09-12 최신)** 을 참조한다.

## 13. 옛 URL 이 새 URL 의 표준을 잠식하고 있다 — 2026-08-24 확정

`페이지 색인 생성` 보고서의 `중복 페이지, Google에서 사용자와 다른 표준을 선택함` 2건을 URL 검사로
파고든 결과다. **추정이 아니라 GSC 가 값으로 답한 사실이다.**

| URL | 사용자 선언 표준 URL | Google에서 선택한 표준 URL |
|---|---|---|
| `https://droidactor.github.io/apps/lgtv/` | `/apps/lgtv/` | **`https://droidactor.github.io/lgtv/`** |
| `https://droidactor.github.io/ko/apps/lgtv/` | `/ko/apps/lgtv/` | **`https://droidactor.github.io/ko/lgtv/`** |

**Google 이 개편 이전 URL 을 표준으로 붙잡고 있고, 그래서 새 URL 이 그 중복으로 밀려났다.** 한·영이
완전히 대칭이다. 둘 다 `URL이 Google에 등록되어 있지 않음` 이고, 처음 감지된 날짜는 **2026-08-15** —
개편 배포(8-14) 바로 다음날이다. `/ko/apps/lgtv/` 의 **최근 크롤링은 2026. 8. 14. 오전 11:36:35** 로
그 뒤 열흘간 재크롤이 없었고, 같은 화면의 `검색` 칸은 `Sitemaps: 감지된 참조 사이트맵이 없습니다` /
`참조 페이지: 감지된 페이지 없음` 이었다. **이 URL 에는 sitemap 경로도 내부 링크 경로도 살아 있지
않다**(§4.2 의 8-15 실측이 말한 상태가 열흘째 그대로다).

### 13.1 배제한 가설 — 셋 다 실측으로 닫았다

1. **"같은 제품의 다른 새 URL 과 본문이 겹쳐서" 는 반증됐다.** 6-gram shingle 중복도(작은 쪽 기준)를
   재면 `apps/lgtv` ↔ `manual/lgtv` = **0.037**, `apps/lgtv` ↔ `tech-notes/lgtv` = **0.003**,
   `manual/lgtv` ↔ `tech-notes/lgtv` = **0.003** 이다. 대조군 `bt-mouse` 가 오히려 더 높다
   (`apps` ↔ `manual` = **0.063**). 국문도 같다(`0.038` / `0.007` / `0.007`). **lgtv 만 특별한 중복이 없다.**
2. **"한·영이 서로를 중복 판정" 도 성립하지 않는다.** 두 URL 이 **모두** 중복으로 판정됐으므로 표준은
   제3의 URL 이다(둘 중 하나가 표준이면 그 하나는 목록에 없어야 한다). 위 표가 그것을 확인했다.
3. **"내부 링크에 옛 URL 이 남아서" 도 아니다.** 옛 제품 URL 6종(`lgtv`, `bt-keyboard`, `bt-mouse`,
   `bt-ppt`, `wifi-scout`, `ssh-scout`)을 가리키는 링크를 저장소 `*.html`·`*.xml` 전수 검색한 결과
   **호환 스텁 자신을 제외하면 0건**이다. sitemap 에도 없다(§2.1·§7 이 금지한 그대로다).

또한 **사이트 선언은 전부 정확하다** — `apps/lgtv/index.html:8` self canonical,
`ko/apps/lgtv/index.html:8` self canonical, `lgtv/index.html:7` → `/apps/lgtv/`,
`ko/lgtv/index.html:7` → `/ko/apps/lgtv/`. 옛 스텁 둘 다 `<meta http-equiv="refresh" content="0; url=…">`
를 갖는다. **고칠 잘못된 태그가 없다.**

### 13.2 왜 canonical 이 무시되는가 — 추정이며, 확정된 것은 위 표까지다

Google 은 canonical 로 묶인 두 페이지가 실질적으로 중복이 아니면 그 선언을 무시한다. 옛 스텁
`/lgtv/` 는 본문이 `This page has moved` 한 줄(17행 파일)이고 새 페이지는 10,336 bytes 의 완전한
제품 페이지다 — **내용이 전혀 다른 두 URL 을 canonical 로 묶은 형태**다. GitHub Pages 는 서버 301 이
불가능해 `meta refresh` 가 유일한 수단인데 그 신호는 301 보다 약하고, Googlebot 이 refresh 를
따라가지 않고 스텁을 색인하면 canonical 이 무력화된다. 여기에 이 속성의 조건이 겹친다 — 외부 링크 0,
내부 링크 그래프 0(§5), sitemap 미수집(§4.2). **새 URL 쪽에 신호가 하나도 없다.**

**GSC 는 "왜 무시했는지" 를 알려주지 않는다.** 이 문단은 그 공백을 메우는 추론이므로, 다음 세션이
사실로 인용하지 않는다.

### 13.3 함의 — lgtv 만의 문제가 아니다

8-17 CSV 에서 색인 유지 중인 옛 제품 URL 이 **9개**였다(`/bt-keyboard/`, `/bt-mouse/`, `/wifi-scout/`,
`/ssh-scout/`, `/lgtv/`, `/ko/bt-keyboard/`, `/ko/bt-mouse/`, `/ko/wifi-scout/`, `/ko/ssh-scout/`).
sitemap 상 제품별 URL 구성도 사실상 동일하다 — `apps`/`manual`/`tech-notes` × 한·영 = 6개씩이고
`yt-downloader` 만 2개, `bt-keyboard` 만 tech-note 상세 3편 × 한·영이 더해져 12개다.

**다만 이 9개를 모집단으로 삼으면 과소평가다.** `/ko/lgtv/` 가 그 목록에 **없는데도** §13 머리 표에서
`/ko/apps/lgtv/` 의 표준으로 선택됐다. 9개는 8-17 에 받은 CSV 이고 그 **데이터 기준일이 2026-08-10**
이므로(§6 2026-08-17 3번) 그 뒤 색인된 옛 URL 이 빠져 있다 — 실제로 §4.4 는 8-15 URL 검사에서
`/ko/lgtv/` 가 `URL이 Google에 등록되어 있음` 이었다고 적고 있다. **따라서 위험 모집단은 저장소에
존재하는 옛 스텁 14개 전체로 본다**(7제품 × 한·영, §2.1). **구조가 같으므로 lgtv 를 뺀 12개도
순차로 같은 판정을 받을 수 있고, lgtv 는 먼저 도달한 첫 사례로 본다.**

### 13.4 조치 후보와 이 문서의 기존 정책

| 후보 | 이 문서의 정책상 지위 |
|---|---|
| 색인 생성 요청 | **이미 써 봤다.** 8-14 에 요청했고 8-24 에 다시 요청했으나 GSC 가 "여러 번 제출해도 우선순위가 바뀌지 않는다" 를 명시했다(§4.3). |
| 옛 스텁에 `noindex` 재도입 | **§1 이 명시적으로 되돌린 결정이다** — "옛 제품 URL 14개는 최소 1년, 가능하면 계속 유지하되 현재 호환 페이지의 `noindex,follow`는 제거한다". Google 도 사이트 이동 시 옛 URL 의 `noindex` 를 권하지 않는다(리디렉션 신호를 죽인다). 되살리려면 §1 을 뒤집는 결정이 필요하다. |
| 옛 URL 을 `404` 로 | **§7 이 금지한다.** |
| 삭제(Removals) 도구로 옛 URL 숨김 | **§1·§7 이 둘 다 금지한다.** |
| 손대지 않고 재크롤을 기다린다 | **§4.4·§7 의 기존 방침과 일치한다.** 현 구성이 정적 호스팅에서 가능한 정석이다. |

**즉 문서의 기존 정책을 지키는 한 남는 것은 마지막 줄뿐이다.** 8-24 시점의 판단은 그것이고, 다른
후보로 가려면 §1 또는 §7 을 고치는 결정이 선행해야 한다 — 그 결정 없이 손대지 않는다.

### 13.5 판정 — 2026-08-31 완료

- [x] **`Google에서 선택한 표준 URL` 은 self 로 바뀌지 않았다.** `/apps/lgtv/` 를 URL 검사로 다시
      읽은 결과 여전히 `https://droidactor.github.io/lgtv/` 다. §13.4 의 마지막 줄대로 계속 기다린다.
      (`/ko/apps/lgtv/` 는 검색창 입력이 막혀 직접 읽지 못했으나 — §6 2026-08-31 성과 6번 — 색인 38
      목록에 없고 `중복 페이지` 사유가 2 로 유지되므로 같은 상태로 본다. **이 한 줄은 추론이다.**)
      **2026-09-05 에 `/ko/apps/lgtv/` 를 직접 읽어 이 추론이 닫혔다** — Google 선택 표준 URL 이
      `https://droidactor.github.io/ko/lgtv/` 로 영문과 같고, 최근 크롤링은 `2026-08-24 09:55:22` 다
      (§6 2026-09-05 절 6번). 추론이 맞았고 이제 실측이다.
- [x] **`최근 크롤링` 은 움직였다 — `2026-08-14 11:36:35` → `2026-08-24 09:51:21`.** 8-24 의 재요청이
      크롤을 불러냈다. **그런데도 표준 URL 판정은 같다.** 즉 "재크롤이 없어서 재평가가 안 된다" 는
      선행 조건은 해소됐는데 결과가 바뀌지 않았다 — §13.2 의 추정(새 URL 쪽에 신호가 없다)을 배제하지
      못하게 하는 관찰이지만, **GSC 가 이유를 말하지 않으므로 여전히 추정이다.**
      같은 검사에서 **참조 페이지가 처음으로 잡혔고 그것이 옛 스텁 `/lgtv/` 다**(8-24 에는 `감지된
      페이지 없음`). 새 URL 을 가리키는 발견 경로가 `meta refresh` 스텁 하나뿐이라는 실측이다.
      `Sitemaps` 칸도 `감지된 참조 사이트맵이 없습니다` → `일시적인 처리 오류` 로 바뀌었다.

      **2026-09-16 재확인 — 두 값 모두 그대로다.** 영문 `2026-08-24 09:51:21`, 국문
      `2026-08-24 09:55:22` 로 9-05 에 읽은 값과 초 단위까지 같다. **8-24 이후 23일째 재크롤이 없고**,
      판정(`중복 페이지…`)·Google 선택 표준 URL(옛 스텁)·참조 페이지(옛 스텁)도 변하지 않았다.
      9-10 다음 작업 8번이 "다음 세션은 그 크롤 시각을 직접 읽어 8-24 이후 재크롤이 있었는지
      갱신한다" 로 걸어 둔 항목이 이것으로 닫힌다. **`Sitemaps` 칸은 이번엔 영문이 `감지된 참조
      사이트맵이 없습니다`, 국문이 `일시적인 처리 오류` 로 8-31 과 반대였다 — 이 칸은 신호가
      아니다**(§6 2026-09-16 절 8번).
- [x] **확산 가설은 반증 쪽이다.** 표본 `(ko/)apps/{bt-keyboard, wifi-scout}/` **4개가 전부 색인**돼
      있고(§6 2026-08-31 성과 1번), `중복 페이지` 사유의 페이지 수도 8-24 와 같은 **2** 로 늘지 않았다.
      **lgtv 한·영 2개에 머물러 있다.**
      **다만 §13.3 을 닫지는 않는다** — 같은 기간에 옛 스텁 `/ko/lgtv/` 가 새로 색인돼 색인 유지 중인
      옛 스텁이 9개에서 **10개**로 늘었다. 잠식의 재료는 줄지 않았으므로 나머지 12개 스텁 감시는
      계속한다.

## 14. 2026-08-31 배포 — Play 링크 `hl` 파라미터와 광고 표기 정정

커밋 `4bb277f`(45개 파일 = HTML 44 + `sitemap.xml`). **새 URL 은 없고 sitemap `<loc>` 는 70 그대로다.**

### 14.1 이 커밋은 성격이 섞여 있다 — `git log -1` 로 읽으면 안 된다

**40개 파일은 링크 부착뿐이고, 본문이 실제로 바뀐 것은 4개다.** 판정은 추정이 아니라 기계적으로 했다 —
44개 각각에서 `&amp;hl=en`·`&amp;hl=ko` 를 제거한 뒤 `HEAD` 와 diff 해서 잔여 차이를 셌다.

| 성격 | 개수 | 파일 |
|---|---|---|
| Play 링크에 `&hl=` 부착만 | 40 | `(ko/)apps/*` 10, `(ko/)manual/*` 12, `(ko/)blog/*` 16, `(ko/)apps/` 허브 2 |
| 링크 부착 + **본문 실질 변경** | 4 | `index.html`, `ko/index.html`, `(ko/)apps/bt-ppt/index.html` |

**그래서 이 커밋을 근거로 블로그 16개·매뉴얼 12개의 `dateModified` 를 당기지 않는다.** 그 28개는
`href` 대상조차 바뀌지 않았고(같은 Play 페이지에 쿼리 파라미터만 붙었다) §11.1 C 가 유지하라고 한
경우보다도 약한 변경이다. **이 구분을 잃으면 다음 점검에서 어긋남 28건이 거짓으로 뜬다** — §11.1
머리말이 경고한 바로 그 함정이고, 이번에는 `9a397f5` 같은 날짜-필드-전용 커밋이 아니라 **혼합 커밋**이
그 함정을 만든다는 점만 다르다.

`lastmod` 는 반대다 — **44개 전부 올린다.** `lastmod` 는 페이지가 바뀐 날이고 `href` 변경도 페이지
변경이기 때문이다(§11.1 C 의 앞줄).

### 14.2 날짜 신호

- **`lastmod` 44개 → `2026-08-31`.** 정정 후 분포: `2026-08-14` 16 + `2026-08-16` 8 + `2026-08-18` 2 +
  `2026-08-31` 44 = **70**, XML well-formed.
  이 배포가 **밀려 있던 두 건을 함께 흡수했다** — `/`·`/ko/`(8-24 `4e591fa` 가 홈 본문을 바꿨는데
  `lastmod` 가 `2026-08-19` 로 남아 있었다)와 `/apps/lgtv/`(8-29 `c9ab677` 가 한국어 글 링크를 바꿨는데
  `2026-08-27` 로 남아 있었다). **둘 다 이 배포로 값이 맞아떨어지므로 별도 정정 커밋이 필요 없다.**
- **`dateModified` 갱신 0건.** 위 14.1 의 이유로 블로그·매뉴얼은 유지한다. 본문이 바뀐 4개
  (홈 2 + `apps/bt-ppt` 2)는 **그 파일들에 `dateModified` 필드 자체가 없다** — 홈은 §12 가 이미
  적었고, `apps/**` 14개 전부 `dateModified`·`datePublished` 가 0건이다(제품 페이지는 article 날짜를
  갖지 않는 것이 이 저장소의 구조다).
- **매뉴얼 화면 표기 12개 유지.** `<p class="manual-meta">` ↔ `dateModified` 대조 결과 12개 전부
  일치하고(bt-keyboard 08-14, bt-mouse 08-18, bt-ppt 08-15, lgtv 08-27, ssh-scout 08-15,
  wifi-scout 08-14 — 한·영 동일) 이 배포는 그 값을 건드리지 않았다.
- **`Effective date` → `31 August 2026` / `2026년 8월 31일`.** 아래 14.3 의 정정이 처리방침 절
  (`#privacy-keyboard`·`#privacy-ppt`) **안**의 본문을 실질적으로 바꿨으므로 §12.1 규칙에 해당한다.
  **8-24 `4e591fa` 의 이월분도 함께 해소된다** — 그 커밋도 처리방침 §2·§4·§6·§7 을 고치고 시행일을
  올리지 않았다. §12.1 이 얻은 실무 규칙("날짜 성격의 정정은 같은 날 안에 끝내는 편이 싸다")대로 이
  배포 안에서 끝냈다.

### 14.3 광고 표기 정정 — 앱 구현에 맞춘 것이다

`bt-keyboard`·`bt-ppt` 두 앱의 광고 서술이 실제 동작과 어긋나 있었다. 정정 근거는 전부
`F:\Git\kwak\mobile\master` 의 실제 소스다.

| 앱 | 고치기 전 | 고친 뒤 | 근거 |
|---|---|---|---|
| `bt-keyboard` | "유휴·연결됨은 허용, 연결 중·복구 중·격리 상태는 불허" | "홈·기록·설정·진단 결과·연결된 입력 화면 허용, 페어링·연결 중·가이드 테스트·복구 중 불허" | `ads/AdGate.kt` 의 `whitelist` 5개와 그 여집합 |
| `bt-ppt` | "발표 화면과 연결 중에는 광고 없음" | "발표 화면 포함 모든 화면 상단에 배너 1개, 높이 사전 예약, 전면 광고 없음" | `ui/nav/AppNavHost.kt` 가 `NavAdSlot` 을 route 바깥 최상단에 고정, `ui/AdSlot.kt` 이 미로드 시 `Spacer(height)` 유지, `Interstitial|Rewarded|AppOpenAd` grep 0건 |

**리드 절도 함께 고쳤다 — diff 검열이 잡은 자기모순이다.** 정정 초안은 열거만 바꾸고 그 앞의
`Ad slots are gated on session state rather than on which screen you are viewing` /
`광고 슬롯은 보고 있는 화면이 아니라 세션 상태로 판정합니다` 를 남겨, **"화면으로 판정하지 않는다"
고 선언한 직후에 화면 목록을 나열**하고 있었다. 옛 열거는 세션 상태 목록이라 리드 절이 맞았지만 새
열거는 화면 이름이라 성립하지 않는다. 실제 구현도 리드 절 쪽이 아니다 —
`AdGate.routeUiState(currentRoute, sessionPhase)` 는 **`currentRoute` 로 분기**하고
`sessionPhase.isRecovering()` 일 때만 그것을 앞질러 덮는다. 그래서 리드 절을 **"화면 단위로 판정하고,
복구 중 같은 세션 상태가 그 판정을 덮는다"** 로 바꿨다.
**이 모순을 놓쳤다면 다음 세션이 "열거 쪽이 잘못됐다" 로 오독해 방금 바로잡은 사실을 되돌릴 수
있었다.** 처리방침은 Play Console 에 등록된 문서이므로(§12) 자기모순의 비용이 특히 크다.

같은 검열에서 영문 `The slot sits above the navigation area` 도 고쳤다. 구현상으로는 맞지만
(`Column { AdSlot; Box(weight=1f){ NavHost } }`) 영문 독자에게 "navigation area" 는 Android 하단
내비게이션 바로 읽혀 **배너가 화면 아래에 있다는 정반대 인상**을 준다. 국문 "조작 영역 위쪽" 과 같은
뜻이 되도록 `above the control area` 로 맞췄다.

### 14.4 `&hl=` 규약을 `index.html` 머리 주석에 박제했다

신규 앱 출시 절차의 SSOT 인 그 주석에 링크 형식을 적고, **이스케이프 규칙을 함께 적었다.**

```
play.google.com/store/apps/details?id=<package>&hl=en (hl=ko under /ko/).
Write the separator as &amp;hl= inside an HTML attribute; never &amp; inside a
<script type="application/ld+json"> block, which is not entity-decoded.
```

**뒷줄이 중요하다.** JSON-LD 블록은 HTML 엔티티가 디코딩되지 않으므로 거기에 `&amp;` 를 쓰면 URL 이
문자 그대로 `&amp;hl=en` 이 되어 깨진다. 이번 배포는 **JSON-LD 안에 Play URL 이 하나도 없어서** 그
사고를 피했고(44개 파일의 JSON-LD 를 전부 파싱해 확인했다), 그 사실이 우연에 기대고 있다는 것이
주석을 남긴 이유다.

### 14.5 배포 후 검증 — 통과

저장소 기준 실측: Play 링크 **168개 전부가 `hl=` 을 갖고**, `/ko/` 아래에 `hl=en` 이 섞인 것과 그
반대가 각각 0건이며, 패키지별 EN/KO 대칭(`wifi_scout` 18/18, `ssh_scout` 14/14, `lgtv` 14/14,
`bt_ppt` 13/13, `bt_mouse2` 13/13, `bt_keyboard` 12/12)도 맞고, 미이스케이프 `&` 는 0건이다.

**live 검증도 같은 날 끝냈다**(§4.2 가 "배포마다 반복하는 점검" 으로 격상한 항목).

| 검증 | 결과 |
|---|---|
| 배포 44개 URL 의 `hl=` (`/ko/` 는 `hl=ko`) | **44/44 통과, 실패 0** |
| live `sitemap.xml` ↔ 저장소 파일 | **완전 동일**(줄 끝 정규화 후 diff 0) |
| live `lastmod` 분포 | `08-14` 16 + `08-16` 8 + `08-18` 2 + `08-31` 44 = **70** |
| 홈 2개 `Effective date` | `31 August 2026` / `2026년 8월 31일` |
| 리드 절 정정 | `Ad slots are gated per screen, …` / `광고 슬롯은 화면 단위로 판정하고, …` |
| 옛 주장 잔존(`gated on session state`, `보고 있는 화면이 아니라`, `No ad while you present`, `발표 화면에 광고 없음`, `navigation area`) | `/`·`/ko/`·`(ko/)apps/bt-ppt/` 전부 **0건** |

**따라서 §6 2026-08-31 의 다음 작업 1번은 이 절로 닫힌다.**

## 15. 2026-09-13 배포 — 로마 숫자 변환기 제품·설명서 URL 4개 추가

커밋 `160b5c3`(2026-09-13 19:23 KST, "로마 숫자 변환기 제품·설명서 페이지와 사이트 연결 일습").
이 4개로 sitemap `<loc>` 수는 70 → **74** 가 됐다. §9·§10 과 달리 Tech Notes 가 아니라 **제품·매뉴얼
계열**이고, 대상 앱 `com.droidactor.roman` 은 **아직 Play 에 출시되지 않았다**(§15.3). 사이트 쪽 작업
목록 전체는 별도 문서 `todo-roman-launch.md` 에 있으며 이 절은 검색 관련만 다룬다.

- [x] `https://droidactor.github.io/apps/roman/` — 2026-09-14 요청 접수(그날 중복 1건 포함, §6 9-14 (2) 절)
- [x] `https://droidactor.github.io/ko/apps/roman/` — 2026-09-15 요청 접수
- [x] `https://droidactor.github.io/manual/roman/` — 2026-09-15 요청 접수
- [x] `https://droidactor.github.io/ko/manual/roman/` — 2026-09-15 요청 접수

체크박스는 §9·§10 과 같은 뜻이다 — **색인 생성 요청 접수 여부**이며 **4개 전부 접수됐다**
(9-14 에 `/apps/roman/` 1건, 9-15 에 나머지 3건. 경위는 §6 의 2026-09-14 (2)·2026-09-15 절).

**2026-09-14 live 실측 — 4개 모두 배포·확인 완료다.**

- 4/4 `200`.
- self canonical 정확(각자 자기 URL).
- `hreflang` 3개(`en`, `ko`, `x-default`)가 서로를 정확히 가리키며 `x-default` 는 영문판이다.
- `noindex` 없음(`<meta name="robots">` 자체가 없다).
- live `sitemap.xml` 의 `<loc>` 수가 **74개**이고 이 4개가 실제로 포함돼 있다. live 파일은 저장소
  HEAD `a4a3538` 와 **완전 동일**(줄 끝 정규화 후 diff 0).
- **내부 링크 경로가 갖춰져 있다.** 세는 단위를 섞지 않도록 둘로 나눠 적는다.
  - **카드 기준** — 허브 4개(`(ko/)apps/`, `(ko/)manual/`)와 홈 2개에 각각 `roman` 카드 1개.
  - **`href` 기준**(HEAD 실측) — `(ko/)apps/` 허브는 3개씩(제품 2 + 매뉴얼 1), `(ko/)manual/` 허브는
    2개씩, 홈 2개는 3개씩(제품 2 + `#privacy-roman` 1)이다.
  - **파일 수** — `(ko/)apps/roman/` 로 가는 `href` 를 가진 HTML 이 **25개**다: 역방향
    `<div class="also">` 를 받은 제품 14 + 앱 허브 2 + 홈 2 + 블로그 허브 2 + `404.html` 1 +
    매뉴얼 `roman` 2 + 제품 `roman` 자신 2(언어 전환 링크). `(ko/)manual/roman/` 쪽은 **8개**다.
  - `todo-roman-launch.md` 7번("가장 빠뜨리기 쉬운 항목")이 실제로 지켜졌다.

**이 절의 실측은 HTTP 응답과 저장소 대조까지다.** 이 세션은 Search Console 화면도 API 도 열지 않았다
— GSC 상의 발견·색인 단계는 **확인되지 않았다.**

### 15.1 sitemap `lastmod` 어긋남 20건 — 네 번째 적중, 배포본은 아직 그대로다

§4.2 가 "한 번 끝낸 점검이 아니라 배포마다 반복하는 점검" 으로 격상한 항목이 8-17·8-18·8-19 에 이어
**네 번째로 적중했다.** (**셈법을 적어 둔다** — 8-31 배포도 밀려 있던 2건을 잡았지만 그 배포 자체가
값을 흡수해 별도 정정 커밋이 필요 없었으므로 §14.2 는 적중으로 세지 않았다. 그 건까지 넣으면 이번이
다섯 번째다. 서수 자체보다 **세 배포 연속 이후로도 계속 걸린다**는 사실이 요점이다.) 대조 규칙은 §4.2·§11.1 그대로다 — sitemap `lastmod` ↔ 그 파일의 마지막
**"본문 변경"** 배포일이며, 날짜 필드만 고친 메타데이터 커밋은 날짜를 당기지 않는다.

**A. roman 4개 — `2026-09-13` 인데 본문은 `2026-09-14` 에 바뀌었다.**
`a392c49`(2026-09-14 15:00 KST, "로마 숫자 변환기 범위를 3,999,999,999 로 갱신")가 이 4개 파일의
본문을 바꿨는데 sitemap 4행은 `160b5c3` 이 넣은 `2026-09-13` 그대로다.

**B. `160b5c3` 이 역방향 링크를 더한 16개 — `2026-09-13` 로 올라가지 않았다.**
roman 링크를 넣느라 본문이 바뀐 파일인데 `lastmod` 는 그 이전 값이다.

| 대상 | HEAD `lastmod` | 실제 마지막 본문 변경 |
|---|---|---|
| `(ko/)apps/bt-keyboard/`·`bt-mouse/`·`bt-ppt/`·`lgtv/`·`ssh-scout/`·`wifi-scout/` 12개 | `2026-08-31` | `160b5c3` 2026-09-13 |
| `(ko/)apps/yt-downloader/` 2개 | `2026-08-14` | `160b5c3` 2026-09-13 |
| `(ko/)blog/` 2개 | `2026-08-14` | `160b5c3` 2026-09-13 |

**어긋나지 않은 것도 적어 둔다** — 허브 4개(`(ko/)apps/`, `(ko/)manual/`)는 `2026-09-13`, 홈 2개는
`d2eeb6c` 가 `2026-09-14` 로 올려 맞다. 배너 커밋 2개(`a45d3d6`·`a4a3538`)는 `assets/banner/*.webp`
만 추가했고 HTML 을 건드리지 않았으므로 `lastmod` 대상이 아니다.

**2026-09-14 live `lastmod` 분포:** `2026-08-14` 16 + `2026-08-16` 8 + `2026-08-31` 40 +
`2026-09-13` 8 + `2026-09-14` 2 = **74**. (분포는 배포마다 낡는다 — §4.2 의 규약대로 최신값은 §6 의
마지막 날짜 절과 마지막 배포 절에서 본다.)

**정정은 2026-09-14 22:45 KST 커밋 `2c23df2` 로 배포됐다 — 20건 전부 해소됐다.**
위에서 "빠져 있다" 고 적은 `manual/roman/` 한 행도 `2026-09-14` 로 맞았다. HEAD 의 sitemap 78행을
파일별 마지막 본문 변경 커밋과 기계 대조한 결과 **어긋남 0건**이고(§4.2 규칙대로 날짜 필드만 고친
메타데이터 커밋은 제외), 분포는 `2026-08-14` 12 + `2026-08-16` 8 + `2026-08-31` 28 + `2026-09-14` 30
= **78** 이다(§6 2026-09-15 절 4번). **아래 미배포 서술은 9-14 시점의 기록이다.**

### 15.2 매뉴얼 2개에 JSON-LD 가 아예 없다

**2026-09-15 해소 — §18.3 이 이 2개에 JSON-LD 를 넣었다. 매뉴얼 14/14 가 같은 모양이 됐다.**
**다만 같은 2개에 화면 갱신 날짜(`manual-meta`)가 없는 것은 그대로 남아 있다**(§18.4, 사용자 결정 대기).
아래는 발견 당시의 기록이다.

`manual/roman/index.html`·`ko/manual/roman/index.html` 은 `application/ld+json` 블록이 **0개**다.
나머지 매뉴얼 12개는 전부 `TechArticle` + `BreadcrumbList` + `SoftwareApplication` 3종과
`dateModified`(`2026-08-14`~`2026-08-27`)를 갖고 있다.

- **제품 페이지 쪽은 어긋나지 않는다** — `(ko/)apps/roman/` 은 `(ko/)apps/lgtv/` 와 같은
  `SoftwareApplication` + `Offer` + `Organization` 3종이고, 제품 계열은 원래 `dateModified` 를 쓰지 않는다.
- 함의 1 — §11.1 이 세운 "`lastmod` 를 고칠 때 같은 URL 의 `dateModified` 를 짝으로 함께 본다" 규칙이
  **이 2개에는 맞춰 볼 대상이 없다.** §15.1 A 를 고칠 때 매뉴얼 2개는 sitemap 쪽만 고치면 된다.
- 함의 2 — 구조화 데이터 누락은 **색인 차단 사유가 아니다.** 이 사이트에서 실제로 기록된 비색인
  사유는 `크롤링됨 - 현재 색인이 생성되지 않음`·`중복 페이지, Google에서 사용자와 다른 표준을 선택함`·
  미발견 셋뿐이고 구조화 데이터 계열은 없다(§6 2026-09-10 절 3·4번, 30일 후 절).
  즉 이것을 "색인이 안 되는 원인" 으로 쓰지 않는다. 깨진 것은 매뉴얼 계열의 일관성이다.

### 15.3 미출시 앱의 URL 이라는 점 — 2026-09-15 출시로 닫혔다

**`com.droidactor.roman` 은 2026-09-15 `cc7e605` 로 출시 반영됐고 Play 등재를 실측 확인했다**(§18.1).
`badge soon` 은 배지로 바뀌었고 `lastmod` 도 그 배포가 올렸다 — 이 절이 경고한 함정은 발생하지 않았다.
아래는 미출시 시점의 기록이다.

저장소 HEAD 기준 `com.droidactor.roman` 의 Play 링크는 **0건**이고, 제품 페이지 2개와 홈 카드에
`badge soon` 이 남아 있다. 같은 상태의 선례가 `yt-downloader` 다.

- 출시 시점에 `badge soon` → 스토어 배지 교체가 예정돼 있고(`todo-roman-launch.md` 8번), 그 배포는
  이 4개 URL 의 본문 변경이다 — **그때 `lastmod` 를 다시 올려야 한다**(§15.1 과 같은 함정이다).
- 선례 수치: `(ko/)apps/yt-downloader/` 는 9-05 에 색인 요청했고 9-10 기준 여전히
  `Google에는 아직 알려지지 않은 URL입니다` 다(§6 7일 후 절). **"미출시 앱 페이지라서 색인이 안 된다"
  는 근거로 쓰지 않는다** — 표본 2개이고 요청 5일 뒤의 값이라 인과를 말할 수 없다.

### 15.4 색인 생성 요청 여부 — 2026-09-14 사용자 지시로 "4개 전부 요청" 으로 닫혔다

**아래 미결 서술은 그 지시 이전의 상태 기록이다.** 실제 실행과 결과(1건 접수 + 중복 1건, 나머지 3건은
화면 잠금으로 미완)는 §6 의 2026-09-14 (2) 절에 있다.

문서의 기본 방침은 "먼저 sitemap 에 맡기고 7~14일 뒤 미발견이면 선별 요청" 이다(§4.3, §9, §10).
**그런데 이 사이트에서는 그 전제가 한 달째 성립하지 않는다** — sitemap 행은 8-13 제출 이래 미처리이고
9-12 재제출분도 아직 판별 전이다(§4.2.2). 실제로 남아 있는 발견 경로는 내부 링크뿐이다.

- 그 경로의 상태는 반반이다 — 부모 허브 4개 중 색인은 `/ko/manual/` 하나뿐이고 `(ko/)apps/`·`/manual/`
  은 `크롤링됨 - 현재 색인이 생성되지 않음` 이다(§6 30일 후 절). 홈은 `/`·`/ko/` 둘 다 색인이다(§2.5).
- 선택지는 셋이고 **결정은 사용자 몫이다.** ① 방침대로 7~14일 관찰 후 재판단(9-20~9-27) ② 4개 전부
  즉시 요청 ③ 제품 2개만 요청하고 매뉴얼 2개는 관찰.
- 어느 쪽이든 §4.3 의 실행 규칙은 그대로다 — KST 09:00 이후, 하루 10개 한도, 할당량 초과 시 즉시 중단,
  **같은 URL 반복 요청 금지**(요청 이력 판정은 이 문서의 `[x]` 목록으로만 한다).

### 15.5 아직 커밋되지 않은 URL 4개 — 2026-09-14 밤에 배포됐다(§16)

**이 절은 9-14 낮 시점의 기록이다.** 여기 적힌 `currency`·`numpad` 4개는 그날 22:45 KST 커밋
`2c23df2` 로 배포됐고, 9-15 에 색인 생성 요청까지 끝냈다. **현재 상태는 §16 에서 본다.**

working tree 에 `apps/currency/`·`apps/numpad/` 와 국문 짝 4개가 untracked 로 있고, 작업 중인
`sitemap.xml` 은 `<loc>` **78개**(= HEAD 74 + 이 4개)였다. **이 수치는 9-14 낮 기준이다** — 지금은
HEAD 가 78 이고 working tree 는 80 이다(§16.4). 홈 2개의 처리방침 앵커
`#privacy-currency`·`#privacy-numpad` 는 **이미 커밋·배포됐다**(`c27d773` 2026-09-14,
`d2eeb6c` 2026-09-14).

- 즉 **9-14 낮 기준 live 에는 두 앱의 처리방침 앵커만 있고 제품 URL 이 없었다**(그날 밤 §16 으로
  해소됐다). roman 이 9-13 직전에 있던
  상태와 같다(`todo-roman-launch.md` 의 "이미 되어 있는 것").
- **이 4개는 배포된 뒤에 절을 더한다.** 지금 목록에 넣으면 live sitemap 74 와 어긋난다.
- **같은 미커밋 묶음의 실제 내용은 배너가 아니라 `currency`·`numpad` 의 사이트 연결이다**(HTML 26개
  + `sitemap.xml` + `README.md`). 두 앱의 역방향 링크 각 31건, 홈 2개의 카드와 처리방침 링크,
  홈 JSON-LD `ItemList` 의 `position` 재배열, 국문 파일의 `dotori-ko` 표기 51건, sitemap `<loc>` +4 와
  §15.1 의 `lastmod` 정정 19건이 들어 있다. **커밋 시점에 §4.2 의 `lastmod` ↔ `dateModified` 짝
  점검을 다시 돌린다.**
- **배너 커밋 2개가 추가한 `assets/banner/*.webp` 7개는 현재 어떤 HTML·CSS 에서도 참조되지 않는다**
  (저장소 전체 `assets/banner` 참조 0건, 미커밋 diff 에도 `banner` 0건). 사이트에 아직 붙지 않은
  자산이라 이 문서의 URL·`lastmod` 계산과는 무관하다. **용도는 이 저장소에서 확인되지 않는다** —
  Play 스토어 게재물로 추정되지만 근거는 없다.

## 16. 2026-09-14 배포 — 환율 계산기·블루투스 넘패드 제품 URL 4개 추가

커밋 `2c23df2`(2026-09-14 22:45 KST, "환율 계산기·넘패드 제품 페이지 신설과 앱 목록 전 자리 등재").
**§15.5 가 "아직 커밋되지 않았다" 고 적어 둔 그 묶음이 그날 밤 배포된 것이다.** 이 4개로 sitemap
`<loc>` 수는 74 → **78** 이 됐다. 두 앱 모두 **아직 Play 에 출시되지 않았다**(§16.2).

- [x] `https://droidactor.github.io/apps/currency/` — 2026-09-15 요청 접수
- [x] `https://droidactor.github.io/ko/apps/currency/` — 2026-09-15 요청 접수
- [x] `https://droidactor.github.io/apps/numpad/` — 2026-09-15 요청 접수
- [x] `https://droidactor.github.io/ko/apps/numpad/` — 2026-09-15 요청 접수

**2026-09-15 live 실측 — 4개 모두 배포·확인 완료다.** 색인 생성 요청 전에 수행한 사전 점검이다.

- 4/4 `200`.
- self canonical 정확(각자 자기 URL).
- `hreflang` 3개(`en`, `ko`, `x-default`).
- `noindex` 없음(`<meta name="robots">` 자체가 없다).
- live `sitemap.xml` 의 `<loc>` 수가 **78개**이고 4개가 실제로 포함돼 있으며 `lastmod` 는 `2026-09-14`.
- 내부 링크: 두 앱 각각 저장소 HTML **27개**에서 링크된다(제품·허브·홈·블로그 허브·404 + 자기 자신).

### 16.1 구조화 데이터는 어긋나지 않았다

`(ko/)apps/currency/`·`(ko/)apps/numpad/` 4개 모두 `SoftwareApplication` + `Offer` + `Organization`
3종으로 `(ko/)apps/lgtv/`·`(ko/)apps/roman/` 과 같다. **§15.2 가 매뉴얼 2개에서 발견한 JSON-LD 누락은
이 묶음에서 재발하지 않았다.** 제품 계열이라 `dateModified` 가 없는 것도 규약대로다(§14.2).

### 16.2 미출시 앱이고 매뉴얼·Tech Notes·블로그가 없다 — 출시 부분은 2026-09-15 에 닫혔다

**`com.droidactor.currency`·`com.droidactor.bt_numpad` 는 2026-09-15 `cc7e605` 로 출시 반영됐고
Play 등재를 실측 확인했다**(§18.1). 매뉴얼·Tech Notes·블로그가 없다는 아래 기술은 그대로 유효하다.

- 저장소 HEAD 기준 `com.droidactor.currency`·`com.droidactor.bt_numpad` 의 Play 링크는 **각 0건**이고,
  (넘패드의 패키지명은 `com.droidactor.numpad` 가 아니라 **`com.droidactor.bt_numpad`** 다 — 틀린
  문자열로 grep 하면 출시 뒤에도 0건이 나와 미출시로 오판한다)
  제품 페이지 4개와 홈 카드에 `badge soon` 이 남아 있다(홈 `index.html` 5곳 · `ko/index.html` 4곳은
  `roman`·`yt-downloader` 를 포함한 수이며 그중 하나는 `todo-roman-launch.md` 가 경고한 HTML 주석
  예시다 — 개수만 세지 않는다).
- 매뉴얼·Tech Notes·블로그 글은 만들지 않았다. `yt-downloader` 와 같은 형태이며, 그래서 이 묶음은
  제품 URL 2쌍뿐이다.
- **출시 배포 때 `badge soon` → 스토어 배지 교체로 이 4개의 본문이 바뀐다 — 그때 `lastmod` 를 함께
  올린다**(§15.3 과 같은 함정).

### 16.3 처리방침 앵커는 이미 배포돼 있었다

`#privacy-currency`(`c27d773`, 2026-09-14)와 넘패드 항목(`d2eeb6c`, 2026-09-14)이 제품 페이지보다
먼저 홈에 올라갔다. **§15.5 가 기록한 "앵커만 있고 제품 URL 은 없는" 상태는 이 배포로 해소됐다.**
앵커는 홈 페이지 안의 `id` 이지 별도 URL 이 아니므로 sitemap 에 넣지 않는다(§12 선례).

### 16.4 아직 커밋되지 않은 URL 2개 — `(ko/)apps/unit/` (2026-09-15 07:17 배포됨, §17)

**§15.5 가 맡던 "미배포 URL 추적" 을 이 절이 이었고, 그 대상은 같은 날 아침에 배포됐다**(커밋
`166e55e`). **아래는 그 직전 시점의 기록이며 현재 상태는 §17 에서 본다.**

2026-09-15 06시대 working tree 에 `apps/unit/`·`ko/apps/unit/` 가 untracked 로 있었고 작업 중인
`sitemap.xml` 은 `<loc>` **80개**(= 당시 HEAD·live 78 + 이 2개)였다. 홈의 처리방침 앵커 `#privacy-unit` 은 `5beaf34`(2026-08-19)로 **이미
배포돼 있다** — `currency`·`numpad` 가 9-14 낮에 있던 상태(§15.5)와 같은 모양이 `unit` 에서 반복된다.

- **배포 전에는 이 문서의 URL 목록·총수에 넣지 않는다.** 지금 넣으면 live 78 과 어긋난다.
- 배포되면 §15·§16 과 같은 형식으로 절을 더하고, 사전 점검(`200` + self canonical + `hreflang` +
  `noindex` 없음 + sitemap 포함) 뒤 색인 생성 요청 여부를 정한다.
- **`lastmod` 짝 점검을 그 배포에서도 돌린다**(§4.2) — 9-13 배포가 20건을 밀리게 한 전례가 있다(§15.1).

## 17. 2026-09-15 배포 — 단위 변환기 제품 URL 2개 추가

커밋 `166e55e`(2026-09-15 07:17 KST, "도토리 단위 변환기(unit) 사이트 등재")와 `02ff540`(배너 webp 1개).
**§16.4 가 "아직 커밋되지 않았다" 고 적은 그 묶음이 같은 날 아침에 배포된 것이다.** 이 2개로 sitemap
`<loc>` 수는 78 → **80** 이 됐다. 앱 `com.droidactor.unit` 은 그날 **`cc7e605`(14:51)로 출시 반영됐다**
— 이 절을 쓴 시점에는 미출시였고, Play 등재 실측은 §18.1 에 있다.

- [ ] `https://droidactor.github.io/apps/unit/`
- [ ] `https://droidactor.github.io/ko/apps/unit/`

체크박스는 **색인 생성 요청 접수 여부**이며 둘 다 아직 요청하지 않았다(§17.2).

**2026-09-15 live 실측 — 2개 모두 배포·확인 완료다.**

- 2/2 `200`, self canonical 정확, `hreflang` 3개(`en`·`ko`·`x-default`), `noindex` 없음.
- live `sitemap.xml` 의 `<loc>` 수가 **80개**이고 2개가 포함돼 있다.
- JSON-LD 는 `SoftwareApplication` + `Offer` + `Organization` 3종으로 다른 제품 페이지와 같다(§16.1).
- 내부 링크: `apps/unit/` 를 링크하는 HTML 이 저장소 기준 **29개**다.
- ~~미출시~~ — 작성 당시 `com.droidactor.unit` 의 Play 링크 0건, 제품 페이지 2개에 `badge soon` 각 1곳.
  **같은 날 14:51 `cc7e605` 가 배지로 교체했다**(§18.1).
  처리방침 앵커 `#privacy-unit` 은 `5beaf34`(2026-08-19)로 훨씬 전에 배포돼 있었다.

### 17.1 `lastmod` 어긋남 28건 — 다섯 번째 적중, 2026-09-15 에 28/28 정정, 2026-09-16 `5e998bd` 로 배포됨(§18.8)

**종결 상태 — `cc7e605`(14:51)가 자기가 건드린 14건을 올리고, 남은 16건을 같은 날 저녁 세션이
정정했다. HEAD 재감사 결과 sitemap 80행 전수 어긋남 0건이다**(§18.2). **정정분은 2026-09-16 에 배포됐다**(`5e998bd`, §18.8).
아래는 발견 당시(28건 전량 어긋남) 의 기록이다.

**`166e55e` 이 28개 URL 의 본문(역방향 링크·홈 카드·허브 목록)을 바꾸면서 `lastmod` 를 올리지
않았다.** 전부 `2026-09-14` 로 남아 있고 실제 마지막 본문 변경은 `2026-09-15` 다. HEAD sitemap 80행을
기계 대조한 결과다(§4.2 규칙대로 날짜 필드만 고친 메타데이터 커밋은 제외).

| 대상 | 개수 |
|---|---|
| 홈 `/`·`/ko/` | 2 |
| 허브 `(ko/)apps/` | 2 |
| 제품 `(ko/)apps/{bt-keyboard, bt-mouse, bt-ppt, currency, lgtv, numpad, roman, ssh-scout, unit, wifi-scout, yt-downloader}` | 22 |
| 블로그 허브 `(ko/)blog/` | 2 |

**새로 추가한 `(ko/)apps/unit/` 2개도 여기 포함된다** — 9-15 에 만든 URL 인데 `lastmod` 가 `2026-09-14`
다. 현재 분포는 `08-14` 12 + `08-16` 8 + `08-31` 28 + `09-14` 32 = **80** 이다.

**이로써 이 점검은 8-17·8-18·8-19·9-13(§15.1)에 이어 다섯 번째로 적중했다**(8-31 §14.2 는 같은
배포가 값을 흡수해 세지 않는 셈법을 그대로 따른다). **배포 절차에 빠져 있는 단계라는 §4.2 의 진단이
그대로 유지된다** — 앱을 하나 추가할 때마다 역방향 링크가 20여 개 파일에 들어가므로, 그 배포는 거의
언제나 `lastmod` 대량 갱신을 동반한다.

**정정은 2026-09-16 에 배포됐다.**(→ 위 종결 상태 참조: 28/28 정정 후 `5e998bd` 로 배포, §18.8.)
`(ko/)manual/roman/` 처럼 `dateModified` 를 짝으로 가진 URL 은 이 묶음에 없다
(제품·허브·홈·블로그 허브뿐이고 블로그 허브는 글 목록이라 article 날짜를 갖지 않는다) — sitemap 쪽만
고치면 된다.

### 17.2 색인 생성 요청 — 2026-09-16 에 2건 접수로 닫혔다

**9-15 오전에 이미 7건을 요청했고 9-14 분까지 합쳐 이 속성에 9건이 들어갔다**(§6 2026-09-15 절 3번).
§4.3 의 하루 10개 기준에 2건을 더하면 11건이 되는데, **8-14 실측에서 11건까지는 통과하고 12번째가
거절됐다.** 경계에 붙는 값이므로 이 2개는 **다음 요청일로 미룬다.**

- 요청할 때는 §6 2026-09-15 절의 실측을 따른다 — 검사창은 `set-text -ViaClipboard` 로만 입력되고,
  성공 대화상자가 25초 안에 안 뜰 수 있으며, 게이트 2개(입력 성공 확인 + 결과 카드 URL 확인)를
  반드시 통과시킨 뒤 버튼을 누른다.
- ~~먼저 §17.1 의 `lastmod` 를 고쳐 배포하는 편이 낫다~~ — **정정은 2026-09-15 에 끝났다**(§18.2).
  **남은 것은 배포뿐이다.** 요청 전에 그 배포를 먼저 하면 sitemap 신호가 정확한 상태에서 요청하게 된다.

**2026-09-16 에 배포(`5e998bd`)를 먼저 하고 그 뒤에 2개를 요청해 접수시켰다** — 순서도 위 줄 그대로다.
요청 전 판정은 둘 다 `Google에는 아직 알려지지 않은 URL입니다` 였고, 게이트 2개를 통과한 뒤 눌러
`색인 생성 요청됨` + `우선순위 크롤링 대기열` 을 받았다. 할당량 초과는 없었다(§6 2026-09-16 절 2번).
**이 절은 닫혔다. 재요청하지 않는다**(§4.3).

## 18. 2026-09-15 배포 — 신규 앱 4종 출시 반영, 신규 URL 0개

`ddef57e`(08:31, 문서 갱신 커밋) 이후 같은 날 커밋 5개가 더 쌓였다. **sitemap `<loc>` 는 80 그대로이고
새 URL 은 0개다** — 이 절은 이동 처리나 발견 절차의 대상이 아니라 본문 변경·날짜 신호·출시 상태 반영이
대상이다. §11(신규 URL 없는 배포)과 같은 성격이다.

| 커밋 | 시각 KST | 성격 | sitemap 대상 파일 |
|---|---|---|---|
| `8d07570` | 09:32 | 배너 8종 글자 깨짐 수정, 미출시 앱 CTA | **0개**(`assets/banner/*.webp` 만) |
| `ae26187` | 13:02 | 신규 앱 4종 스크린샷 추가 | **0개**(`assets/shots/**` 만) |
| `cc7e605` | 14:51 | **신규 앱 4종 출시 반영**, 처리방침 제품 링크 보강 | 12개 + `sitemap.xml` |
| `ac934ce` | 18:05 | Medium import 시험용 페이지 추가 | 0개 |
| `42de779` | 18:10 | 같은 페이지 제거 | 0개 |

**`8d07570`·`ae26187` 은 `assets/` 만 바꿨으므로 어떤 URL 의 `lastmod` 도 올리지 않는다.** 이 저장소의
`lastmod` 기준은 "그 URL 의 HTML 본문이 바뀐 배포일" 이고(§11.1), 이미지 교체는 HTML 을 건드리지
않는다. **단 §11.1 이 남긴 `?v=` 캐시 토큰 선례와는 다르다** — 그때는 토큰이 HTML 안에 있어서 본문이
바뀌었다. 여기서는 파일명이 그대로이므로 HTML diff 가 0줄이다.

### 18.1 신규 앱 4종이 Play 에 출시됐다 — §15.3·§16.2·§17 의 "미출시" 가 닫힌다

`cc7e605` 가 제품 페이지 8개(`(ko/)apps/{roman, currency, numpad, unit}/`)에서 `badge soon` →
스토어 배지로 바꾸고, 사양표 `Status` 를 `Not yet published on Google Play` →
`Published on Google Play — store listing` 으로, 푸터에 상표 고지 `<p class="tm">` 를 더했다.
`(ko/)manual/roman/` 2개에도 `.manual-actions` 배지와 상표 고지가 들어갔다.

**2026-09-15 21시대 실측 — 4종 전부 실제 등재다.**

| 패키지 | status | 응답 크기(참고치) | `og:title` |
|---|---|---|---|
| `com.droidactor.roman` | `200` | ~1.2 MB | `Dotori Roman Numerals - Apps on Google Play` |
| `com.droidactor.currency` | `200` | ~1.2 MB | `Dotori Currency Converter - Apps on Google Play` |
| `com.droidactor.unit` | `200` | ~1.2 MB | `Dotori Unit Converter - Apps on Google Play` |
| `com.droidactor.bt_numpad` | `200` | ~1.2 MB | `Dotori Bluetooth Numpad - Apps on Google Play` |
| `com.droidactor.bt_mouse`(옛 이름, 대조군) | **`404`** | **1,673 B** | **없음** |
| `com.droidactor.nonexistent_xyz`(가짜, 대조군) | **`404`** | **1,673 B** | **없음** |

**응답 크기는 참고치다 — 정확한 값을 판정 기준으로 쓰지 말 것.** 한 세션 안에서는 안정적이지만
(2회 연속 측정에서 ±30 B) **세션 간에는 1.18~1.22 MB 로 흔들린다**(검증 세션이 ~1.18 MB 를 얻어
이 표의 초안값 `~1.21 MB` 가 재현되지 않았다). **판별력은 정확한 수치가 아니라 약 700배의 격차와
대조군의 정확한 `1,673 B`** 에 있다.

**§11.2 가 쓴 status 판별법은 오늘도 유효하다** — 없는 패키지는 `404` 다. 여기에 `og:title`·응답 크기
두 축을 더한 이유는 Play 가 JS 껍데기를 주므로 `<title>` 이 비어 있고, **`<title>` 이 비었다는 것을
"등재 없음" 으로 읽으면 오판**이기 때문이다. 판별은 `og:title` 로 한다.

**§11.2 는 이미 2026-08-31 에 닫혔다** — 오늘 `bt_mouse2` 를 함께 잰 것은 재확인이고, 이 세션이
그 항목을 닫은 것이 아니다.

**`badge soon` 잔존은 `yt-downloader` 뿐이다** — `(ko/)apps/yt-downloader/` 2곳 + 홈 2곳. §16.2 가
경고한 "홈 카드 개수만 세지 말 것" 이 그대로 적용되며, `roman`·`currency`·`numpad`·`unit` 은 홈에서도
전부 배지로 바뀌었다.

**`&hl=` 규약(§14.4) 전수 통과.** Play 링크를 가진 HTML 전량을 대조해 `/ko/` 아래는 `hl=ko`,
그 밖은 `hl=en` 이고 위반 0건이다. **JSON-LD 안의 `&amp;` 사고도 0건**이며, 저장소 HTML 전체의
`application/ld+json` 블록이 100% 파싱된다.

> **grep 함정 하나를 남긴다.** `index.html` 머리주석에 §14.4 규약문이 `<script
> type="application/ld+json">` 이라는 문자열 그대로 들어 있어, HTML 주석을 제거하지 않고
> `<script ... ld+json ...>(.*?)</script>` 로 훑으면 `index.html` 이 **거짓 적중**한다. 실제로 이
> 세션이 한 번 걸렸다. 검사 스크립트는 `<!--.*?-->` 를 먼저 지운다.

### 18.2 `lastmod` — `cc7e605` 가 14건을 고쳤고 16건이 남아 있었다

**`cc7e605` 가 sitemap 14행을 올렸고, 그중 §17.1 의 28건에 해당하는 것은 12행이다**
(`sitemap.xml | 28 ++++----` = 14 insert + 14 delete). 올린 14행은 홈 2 + 앱 허브 2 +
신규 4종 제품 8 + `(ko/)manual/roman/` 2 다.

**`(ko/)manual/roman/` 2행은 §17.1 의 28건 목록 밖이다** — 그 목록은 홈 2 + 허브 2 + 제품 22 +
블로그 허브 2 로 매뉴얼을 담지 않았고, 발견 시점에 그 2행은 `09-14` 로 옳았다(`cc7e605` 가 배지·상표
고지를 넣으면서 정당하게 올린 것이다). **따라서 산술은 12 + 16 = 28 이다** — `28 - 14` 로 계산하면
14 가 나와 잔여 16건과 어긋난다.

**남은 16건을 이 세션이 정정했다 — `2026-09-14` → `2026-09-15`.**

| 대상 | 개수 | 왜 `2026-09-15` 인가 |
|---|---|---|
| 제품 `(ko/)apps/{bt-keyboard, bt-ppt, bt-mouse, wifi-scout, ssh-scout, yt-downloader, lgtv}/` | 14 | `166e55e`(09-15 07:17)가 단위 변환기 교차 링크 한 줄을 넣었다 |
| 블로그 허브 `(ko/)blog/` | 2 | 위와 같은 한 줄 |

실제 diff 는 `<a href="/apps/unit/"><span class="dotori">Dotori</span> Unit Converter</a>` 한 줄
추가다. **작아도 본문 변경이므로 §10.1 A 의 "허브에 링크 한 줄 추가" 와 같은 판정이다.**

**재감사의 제외 규칙을 여기에 박제한다 — 이것이 없으면 재현되지 않는다.** `git log -1 -- <파일>` 을
그대로 쓰면 **어긋남 6건이 오탐으로 잡힌다**(`(ko/)tech-notes/bt-keyboard/` 허브 2 + 상세 4 중 4개,
sitemap `08-16` 대 마지막 커밋 `5cc2907` `08-19`). **`5cc2907` 은 그 파일들에서 `ld+json` 의
`dateModified`·`datePublished` 한 줄만 바꿔 본문 0줄이므로 §4.2 규칙대로 제외 대상이다.** 대조는
"파일을 바꾼 마지막 커밋" 이 아니라 **"본문을 바꾼 마지막 커밋"** 으로 해야 하고, 판정은 그 커밋의
diff 가 날짜 필드 밖을 건드렸는지로 한다. **이 6행을 `08-19` 로 올리면 날짜 신호에 거짓 개정이 들어간다**
— §18.5 가 `dateModified` 쪽에서 막으려는 사고의 `lastmod` 판이다.

**HEAD 재감사 결과 어긋남 0/80 이다.** 현재 분포는 `08-14` 12 + `08-16` 8 + `08-31` 28 + `09-14` 2 +
`09-15` 30 = **80**, XML well-formed. **이 분포는 2026-09-15 시점의 값이다** — 2026-09-16 배포
(`5e998bd`)가 `(ko/)manual/roman/` 2행을 `09-16` 으로 올려 현재(live·로컬)는 `08-14` 12 + `08-16` 8 +
`08-31` 28 + `09-14` 2 + **`09-15` 28 + `09-16` 2** = 80 이다(§18.8). `09-14` 로 남은 2개는 `(ko/)manual/` 이며 마지막 본문 변경이
`2c23df2`(9-14)라 정당하다 — **이 2개를 09-15 로 올리지 않는다.**

**이 정정은 2026-09-16 에 배포됐다**(§18.8). `dateModified` 짝을 가진 URL 이 이 16개에는 없으므로(제품·블로그 허브뿐이고
전수 확인해 0건) sitemap 쪽만 고쳤다.

**이로써 이 점검은 8-17·8-18·8-19·9-13·9-15(§17.1)에 이어 여섯 번째로 적중했다.** 다만 성격이 다르다 —
앞의 다섯 번은 배포가 `lastmod` 를 아예 빠뜨린 것이고, **이번은 배포가 절반만 올린 것이다.** 배포
주체가 "내가 이 커밋에서 손댄 파일" 만 보고 "그 URL 의 본문이 마지막으로 바뀐 날" 을 보지 않으면
이 절반 누락이 반복된다. **점검은 커밋 단위가 아니라 sitemap 80행 전수 대조여야 한다.**

### 18.3 §15.2 의 JSON-LD 누락을 메웠다 — 매뉴얼 14/14 가 같은 모양이 됐다

`(ko/)manual/roman/` 에 `TechArticle` + `BreadcrumbList` 를 한 `@graph` 로 넣었다(다른 매뉴얼 12개와
동일 구조, `about` 에 `SoftwareApplication` 중첩). 삽입 위치도 같다 — `<link rel="stylesheet">` 뒤,
`</head>` 앞.

**`dateModified` 는 `2026-09-14` 로 넣었다. `lastmod` 는 `2026-09-15` 이고, 이 불일치가 맞다.**

- `lastmod`(= 페이지가 바뀐 날) → `cc7e605`(9-15)가 Play 배지·상표 고지를 넣었으므로 09-15.
- `dateModified`(= 글이 바뀐 날) → 산문을 마지막으로 바꾼 것은 `a392c49`(9-14)다. 로마 숫자 범위를
  `3,999,999` → `3,999,999,999` 로, 키 라벨을 `Overline ×1000` → `×1K`/`×1M` 로 고친 커밋이다.
  `cc7e605` 는 `.manual-actions` 와 푸터만 건드려 산문은 한 글자도 바뀌지 않았다.
- **이것이 §11.1 C 규칙의 정상 적용이다.** 처음 넣을 때 `2026-09-15` 로 썼다가 커밋 이력을 읽고
  정정했다. `datePublished` 는 생성 커밋 `160b5c3`(9-13)이다.

검증: 매뉴얼 14/14 가 ① `ld+json` 블록 1개 ② `@type` 이 `['TechArticle','BreadcrumbList']`
③ `TechArticle.url` = `<link rel="canonical">` ④ `BreadcrumbList` position 3 `item` = canonical
⑤ **화면 빵부스러기 2번째 항목 = JSON-LD position 2 `name`** ⑥ 블록 안 `&amp;` 0건 — 전부 통과.

**⑤ 를 맞추는 방향이 갈렸고, 화면 문구를 고치는 쪽으로 정했다 — 아래 18.3.1 이 그 결정이다.**

#### 18.3.1 영문 빵부스러기 `Manuals` → `User manuals` — 화면 문구를 고쳤다

**처음에는 JSON-LD 를 화면에 맞춰 `Manuals` 로 넣었다.** ⑤(화면 = 구조화 데이터) 를 지키는 선택이고
Google 의 `BreadcrumbList.name` 정의("표시되는 제목")에도 맞았다. **그런데 그 화면 문구 자체가
저장소 유일의 outlier 였다.**

**계수 범위는 "`/manual/`·`/ko/manual/` 허브로 가는 `<a>` 의 텍스트" 다** — 파일 전체 문자열로 세면
다른 수가 나온다(영문 31 / 국문 65). 위치 설명도 정정한다: `User manuals` 29 중 **23이 푸터**이고
crumb·nav 은 6이다(`<footer>` 는 `<nav>` 가 아니다). 푸터가 `All manuals` 인 것은 **매뉴얼 7페이지뿐**
이고 나머지 영문 23페이지 푸터는 `User manuals` 다.

| 표기 | 개수 | 위치 |
|---|---|---|
| `User manuals` | 29 | 영문 푸터 23 + crumb·nav 6 |
| `All manuals` | 7 | 영문 **매뉴얼 페이지** 푸터 |
| **`Manuals`** | **1** | **`manual/roman/index.html:48` 뿐** |
| `사용 설명서` | 30 | 국문 (드리프트 없음) |
| `전체 설명서` | 7 | 국문 매뉴얼 페이지 푸터 — `All manuals` 의 짝 |

**목적지 페이지 자신이 `User manuals` 다** — `manual/index.html` 의 `<h1>User manuals</h1>` 과
`<title>User manuals — droidactor</title>`. 즉 roman 의 crumb 은 같은 URL 을 영문 형제 6개와 다르게,
그리고 그 페이지가 스스로를 부르는 이름과도 다르게 부르고 있었다.

**그래서 화면 1단어를 고쳤다** — `manual/roman/index.html:48` 의 crumb 과 같은 파일 JSON-LD 의
position 2 `name` 을 함께 `User manuals` 로. 이것으로 허브 `h1`·`title`, 영문 형제 6개, 구조화 데이터가
한 번에 정합된다. **표기 선택의 문제가 아니라 단일 값 드리프트였다는 판단이다** — 대안이 갈리는
copy 결정이라면 사용자에게 물었겠지만, 정답이 29:1 로 한쪽이었다.

- **`dateModified` 는 그대로 `2026-09-14` 다.** 빵부스러기 라벨은 탐색 chrome 이지 글의 산문이 아니다
  — §11.1 C 가 "링크 대상과 푸터만 바뀌었으므로 `08-03` 을 유지한다" 고 판정한 것과 같은 범주다.
- **`lastmod` 도 그대로 `2026-09-15` 다.** 이 정정이 같은 날 안에 있으므로 값이 바뀌지 않는다.
  **다만 배포가 09-15 를 넘기면 `(ko/)manual/roman/` 2행을 그 배포일로 올려야 한다**(§6 2026-09-15 (3) 절).
- 국문은 손대지 않았다 — `사용 설명서` 가 14/14 일치라 드리프트가 없다.

**결과: 매뉴얼 14/14 가 화면 crumb = JSON-LD position 2 `name` 이고, 영문 7/7 이 `User manuals` 다.**

**같은 종류의 드리프트가 제목에도 있으나 의도적으로 두었다**(2026-09-16 검토). 영문 6개는
`Dotori X user manual` 인데 roman 만 `Dotori Roman Numerals — user manual` 이다(국문도 `— 사용 설명서`).
**고치지 않는다 — 제목은 검색 결과에 노출되는 신호라 라벨 정합을 위해 바꿀 대상이 아니고**, 이 파일
안에서는 `<title>`·`<h1>`·JSON-LD `headline` 셋이 서로 일치해 규약(14/14 headline = title)을 지킨다.
**다음 세션이 §18.3.1 의 29:1 논리를 제목에 확대 적용하지 않는다.**

### 18.4 새로 발견 — `(ko/)manual/roman/` 에는 화면 갱신 날짜(`manual-meta`)도 없다

**§15.2 는 JSON-LD 만 봤는데, 같은 2개 파일에 `<p class="manual-meta">` 도 없다.** 다른 매뉴얼
12개는 **12/12 가 둘 다 갖고 있다.**

| 파일 | `ld+json` | `manual-meta` |
|---|---|---|
| 매뉴얼 12개(roman 제외) | 1 | 1 |
| `(ko/)manual/roman/` | **1**(이 세션이 넣음) | **0** |

**일부러 넣지 않았다.** 다른 12개의 문구는 `Checked with Android app version 1.0.0 (1) · Updated
14 August 2026` / `Android 앱 1.0.0 (1) 기준 · 2026년 8월 14일 갱신` 형식인데, 앞부분이 **"그 앱 버전에
맞춰 이 설명서를 점검했다" 는 검증 주장**이다. 버전 자체는
`mobile/master/apps/roman/Version.txt` = `1.0.0+1` 로 확인했으나, **점검을 수행한 것은
아니므로 그 주장을 대신 쓰지 않는다.**

- 날짜 신호로서의 손실은 없다 — 화면 날짜가 없으면 구조화 날짜와 어긋날 대상도 없다.
  §11.1 B-2 가 경고한 것은 "화면 날짜가 낡은 채로 남는" 상황이고, 여기는 그 상황이 아니다.
- **남은 것은 매뉴얼 계열의 표기 일관성이며, 사용자 결정 사항이다.** 넣는다면 문구는
  `Checked with Android app version 1.0.0 (1) · Updated 14 September 2026` /
  `Android 앱 1.0.0 (1) 기준 · 2026년 9월 14일 갱신` 이 된다(날짜는 `dateModified` 와 같은 09-14).
- roman 매뉴얼의 hero 는 `hero product`(아이콘 + `h1` + `.pkg`)이고 다른 12개는
  `hero manual-hero`(`h1` + 설명 + `manual-meta`)다 — **삽입 위치가 자명하지 않은 것도 이유다.**

### 18.5 `dateModified` 28건이 `lastmod` 와 다르다 — 정상이므로 고치지 말 것

전수 대조에서 `dateModified` 를 가진 URL **48개 중 30개가 `lastmod` 와 다르게 나온다.**
**두 묶음이며 성격이 다르다 — 둘 다 정상이다.**

| 묶음 | 개수 | `lastmod` | `dateModified` | 근거 |
|---|---|---|---|---|
| 8-31 배포 코호트 | **28** | `2026-08-31` | `08-03`~`08-27` | §14.2 가 그 배포에서 `dateModified` 갱신을 0건으로 둔 것이 의도다 |
| `(ko/)manual/roman/` | **2** | `2026-09-16` | `2026-09-14` | §18.3 이 그렇게 넣고 09-16 배포로 올라간 값이다(페이지는 9-16, 산문은 9-14) |

**28 과 30 을 혼동하지 말 것.** 28 은 이 세션 *이전*(HEAD) 기준이고, **30 은 §18.3 이 매뉴얼 2개에
JSON-LD 를 넣은 뒤의 값**이다. 보유 URL 도 46 → 48 로 같이 늘었다. 문서 초안이 보유 수(48, 변경 후)와
어긋남 수(28, 변경 전)를 한 문장에 섞어 적었던 것을 정정한 것이다.

**§14.2 가 그 배포에서 `dateModified` 갱신을 0건으로 둔 것이 의도였다.** `4bb277f`(8-31)는 Play 링크
`hl` 파라미터와 광고 표기를 고쳤고 글의 산문은 바꾸지 않았으므로, §11.1 C 의 구분대로
`dateModified` 를 올리지 않는 것이 맞다.

**따라서 이 30건은 결함이 아니다.** 단순 대조 스크립트는 전부 어긋남으로 보고하므로, **다음 세션이
그 보고를 보고 일괄 정정하면 모든 글에 "개정됐다" 는 거짓 신호를 보낸다.** §11.1 C 가 경고한 바로 그
드리프트다. 대조 결과는 **반드시 그 배포의 diff 가 산문을 바꿨는지 확인한 뒤** 방향을 정한다.

**특히 `(ko/)manual/roman/` 2건을 "새로 생긴 어긋남" 으로 읽지 말 것.** 이 문서가 28 만 적어 두면
다음 세션의 측정값 30 과 차이 2 가 남고, 그 2 를 신규 결함으로 오인해 `dateModified` 를 `09-15` 로
당기는 것이 가장 그럴듯한 오작동 경로다 — **§18.3 이 09-14 로 정한 근거가 커밋 diff 실측이다.**

### 18.6 Medium import 시험용 페이지 — 5분간 존재했고 신호 밖이다

`ac934ce`(18:05)가 `medium-import/roman-test.html` + 이미지 3개를 올리고 `42de779`(18:10)가 지웠다.

- sitemap `<loc>` 에 없었고, 저장소 HTML 어디에서도 링크되지 않았다(참조 0건).
- 그 페이지에는 `rel="canonical"` 도 `<meta name="robots">` 도 없었다. **신호가 없는 페이지였다.**
- 현재 live 는 `404` 이고 저장소에도 없다.
- **발견 위험은 사실상 0 이다** — sitemap·내부 링크 둘 다 없으면 Google 이 5분 창에서 이 URL 을 알게
  될 경로가 없다. 다만 **다음에 시험 페이지를 올릴 때는 `noindex` 를 넣는 편이 싸다** — 이 저장소는
  옛 URL 스텁에서 `noindex` 를 일부러 뺐지만(§2.1) 그것은 canonical 신호를 살리기 위한 것이고,
  시험 페이지는 반대로 색인될 이유가 없다.

### 18.7 이 배포의 색인 관련 작업 — 0건

**새 URL 이 없으므로 색인 생성 요청 대상이 늘지 않았다.** §17.2 의 `(ko/)apps/unit/` 2개가 여전히
유일한 미요청분이고, **2026-09-15 자로** 이 속성에 들어간 요청은 **9건 그대로**다(§6 2026-09-15 절 3번).
**그 2개는 2026-09-16 에 요청돼 닫혔다**(§17.2, §6 2026-09-16 절 2번).

**출시가 색인 요청의 사유가 되지는 않는다.** `(ko/)apps/{roman, currency, numpad}/` 6개는 9-14~9-15 에
이미 요청했고(§15·§16), `badge soon` 이 배지로 바뀐 것은 본문 변경이지만 **§4.3 의 "같은 URL 을 반복
요청하지 않는다" 가 그대로 적용된다.** 8-24 의 `lgtv` 재요청이 남긴 교훈이다.

### 18.8 미배포 3묶음은 2026-09-16 에 배포됐다

§6 2026-09-15 (3) 절의 표가 가리킨 세 묶음(`sitemap.xml` `lastmod` 16건, `(ko/)manual/roman/` 2개의
JSON-LD)이 `5e998bd` 로 배포됐고 live 대조를 통과했다. 실측 값은 §6 2026-09-16 절 1번에 있다.

**배포가 09-15 를 넘겼으므로 §18.3 의 단서대로 `(ko/)manual/roman/` 2행을 `2026-09-16` 으로 올렸다.**
따라서 §18.2·§18.3 본문에 적힌 그 2행의 `2026-09-15` 는 **정정 시점의 값이고 현재 live 값이 아니다.**
`dateModified` 는 `2026-09-14` 그대로다 — 산문이 바뀌지 않았으므로 §11.1 C 대로다.

## 19. 2026-09-16 — GSC 조작 경로를 `/uia` 에서 Chrome CDP 로 바꿨다

**사용자 지시로 이 세션부터 Search Console 조작을 Chrome DevTools Protocol(CDP)로 한다.** 8-14 이래
모든 GSC 작업은 `/uia` lease 로 사용자의 로그인 브라우저 창을 빌려 왔고, 그 경로는 데스크톱 전환 +
foreground 점유 + 클립보드 차용(`set-text -ViaClipboard`)을 동반했다. **CDP 는 그 셋을 전부 쓰지
않는다** — 사용자 화면을 건드리지 않고, 사용자가 타이핑 중이어도 간섭이 없다.

**아래는 전부 이 세션의 실측이다.** `~/.claude/docs/cdp-web-automation.md` 의 일반 함정과 별개로,
**GSC 고유의 것만** 적는다.

### 19.1 띄우는 방법 — 전용 프로필 재사용, 포트는 비어 있는 것으로

```
chrome.exe --remote-debugging-port=9333 \
  --user-data-dir=%LOCALAPPDATA%\claude-play-console \
  --no-first-run --no-default-browser-check
```

- **프로필을 새로 만들지 않고 `claude-play-console` 을 재사용했다** — Play Console·AdMob 작업용으로
  만들어 둔 전용 프로필이고, **거기에 이미 droidactor 계정 로그인이 살아 있어 GSC 가 바로 열렸다.**
  새 프로필을 만들면 로그인을 한 번 해야 한다.
- **포트 9222 는 쓰지 못했다** — 9-15 Medium 작업용 인스턴스(`claude-cdp-chrome\medium` 프로필)가
  점유 중이었다. **점유 프로세스의 프로필을 확인하기 전에 붙지 않는다**(`Win32_Process.CommandLine`
  으로 `--user-data-dir` 을 읽어 소유를 판정한다). 남의 인스턴스에 붙으면 그쪽 탭을 조작하게 된다.
- **작업이 끝나면 그 인스턴스만 닫는다.** `CommandLine` 에 `claude-play-console` 을 가진 프로세스만
  종료했고, 9222 의 medium 인스턴스와 사용자의 기본 프로필 Chrome 은 건드리지 않았다.
- playwright 는 이 PC 에 설치돼 있지 않다. **Node 22 의 내장 `WebSocket` 으로 CDP 를 직접 말했다** —
  `/json` 으로 target 을 얻고 `webSocketDebuggerUrl` 에 붙어 `Runtime.evaluate`·`Input.*`·
  `Page.captureScreenshot` 을 쓴다. 의존성 설치가 필요 없다.

### 19.2 URL 검사 deep link 는 없다 — 404 다

`https://search.google.com/search-console/inspect?resource_id=<속성>&id=<검사할 URL>` 은 **세 가지
변형 전부 `404`** 였다(`id` 인코딩 유무, `hl=ko`, `/u/0/` 접두). **GSC 는 검사 대상을 URL 이 아니라
내부 토큰으로 가리킨다** — 검사창에 입력해 이동하면 주소가
`…/inspect?resource_id=…&id=Oo_giO2J6SrDugE6NZHngA` 처럼 바뀐다. 그 토큰은 검사 때마다 다르다.
**따라서 검사는 반드시 앱 안의 검사창을 거쳐야 한다.**

### 19.3 입력과 Enter — 사다리의 3단이 필요하다

검사창은 `input[role=combobox]` 이고 `aria-label` 이 `https://droidactor.github.io/에 있는 모든 URL 검사` 다.

| 단계 | 결과 |
|---|---|
| `Input.insertText` | **값은 박히는데 Enter 가 먹지 않는다.** `el.value` 를 읽으면 대상 URL 과 일치하지만 앱이 입력을 인식하지 않았다 |
| 문자별 `Input.dispatchKeyEvent`(`type: 'keyDown'` + `text`) | **된다.** 39자(`/apps/unit/`)·42자(`/ko/apps/unit/`)에 1초 미만 |
| `Input.dispatchKeyEvent` `type: 'rawKeyDown'` 으로 Enter | **먹지 않는다** — 화면이 그대로 개요에 머문다 |
| Enter 를 `type: 'keyDown'` + `text: '\r'`(CR) 로 | **된다.** 검사 페이지로 이동한다 |

**`el.value` 일치를 "입력 성공" 으로 읽으면 안 된다**(`~/.claude/docs/cdp-web-automation.md` §6 의
측정 지표 함정이 여기서 그대로 재현됐다). **성공 판정은 `location.href` 가 `/inspect` 로 바뀌는 것**이다.

`색인 생성 요청`·`닫기` 같은 컨트롤은 **좌표 마우스 이벤트**로 눌렀다(`Input.dispatchMouseEvent`
`mouseMoved` → `mousePressed` → `mouseReleased`). `el.click()` 은 Google 앱에서 반응하지 않는다.

### 19.4 `sitemaps.get` — APIs Explorer 는 iframe 안에서만 산다

- `https://developers.google.com/webmaster-tools/v1/sitemaps/get` 의 `사용해 보기` 버튼을 눌러야
  explorer iframe 이 생긴다. **버튼은 화면 밖에 있을 수 있어 `scrollIntoView` 후 좌표를 다시 잰다.**
- **iframe 의 `src` 를 새 탭으로 열면 거부된다** — `API Explorer currently only supports usage in an
  isolated frame.`
- 그래서 **`Page.createIsolatedWorld({frameId})` 로 그 프레임의 격리 월드를 만들어 DOM 을 다뤘다.**
  좌표 클릭은 부모 기준이므로 **iframe 의 `getBoundingClientRect()` 오프셋을 더한다.**
- 파라미터는 `siteUrl`·`feedpath` 두 칸이고, `Show scopes` 를 펼치면 `webmasters`(쓰기)와
  `webmasters.readonly` 가 **둘 다 체크된 기본값**이다. **쓰기 scope 를 해제하고 읽기 전용만 남겼다**
  (§4.2.1 의 선례). 계정에 이미 동의가 남아 있어 **OAuth 동의 팝업은 뜨지 않았다.**
- `Execute` 후 응답은 같은 프레임 본문에 `200` 과 JSON 으로 나온다.

### 19.5 남는 제약

- **`/uia` 를 대체하는 것은 GSC 에 한정된다.** 사용자의 기존 로그인 창이 대상이거나 브라우저
  chrome(주소창·확장) 이 대상이면 CDP 는 답이 아니다. 이미 떠 있는 창에는 사후 부착이 안 된다.
- **이 경로는 사용자 화면을 빌리지 않으므로, 실패해도 사용자가 화면에서 알아채지 못한다.** 그래서
  게이트 2개(입력 성공·결과 카드 URL)와 상태 재조회가 `/uia` 때보다 더 중요하다.
