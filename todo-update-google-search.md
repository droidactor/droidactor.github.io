# Google Search Console 운영 문서 — droidactor.github.io

최초 작성 2026-08-14(`f300f26` 경로 개편 후속) · **마지막 정리 2026-09-17** · **마지막 갱신 2026-09-23**

**이 문서는 "다음에 무엇을 하는가" 를 담는다. 날짜별 작업 로그는 더 이상 담지 않는다.**
2026-08-14 ~ 09-17 의 전체 로그(세션별 판독 내역, 배포별 검증 표, 철회된 해석의 경위)는
**커밋 `cc532b1` 의 이 파일**에 있다 — `git show cc532b1:todo-update-google-search.md`.
아래에 남긴 것은 **① 지금 열려 있는 일 ② 바꾸려면 결정이 필요한 고정 정책 ③ 반복해서 당한 함정
④ 재현에 필요한 조작 절차 ⑤ 미해결 문제 둘** 뿐이다.

---

## 1. 현재 상태 — 2026-09-22 실측 (보고서 기준일이 `9-14` 에서 `9-18` 로 전진했다)

| 항목 | 값 | 비고 |
|---|---|---|
| 속성 | `https://droidactor.github.io/` (URL 접두어) | 새 속성 만들지 않았다 |
| live sitemap `<loc>` | **119** | **9-22 저녁 배포(`7f70c03`) 후.** 로컬 파일과 일치하고 `sitemap-all.xml` 도 바이트까지 같다. **아래 판독은 그 배포 전(108) 시점 값이다** |
| sitemap 처리 상태 | **`가져올 수 없음` · 발견된 페이지 `0`** (제출 2행 모두) | §7 — 미해결 |
| Page indexing 보고서 기준일 | **2026-09-18** | 9-14 에서 전진. 9-14 는 9-17~9-20 나흘 동안 묵었다 |
| 색인 생성됨 | **88** | 9-14 자 62 에서 **+26** |
| 색인되지 않음 | **16** | 사유 2개. 기존 12 + 기술 글 4(§1.1) |
| 알려진 페이지 | **104** | 88 + 16 |
| 색인 ∩ sitemap 108 | **78** | 52 → 78. **분모는 판독 시점의 108 이다** |
| 색인인데 sitemap 밖 | **10** | 옛 스텁. 구성원 불변. §8 잠식 |
| sitemap 중 미색인 | **30** → **41** | 56 → 30(판독 시점, 내역은 §1.2.1). **저녁 배포로 신규 11개가 더해져 지금은 41 이다** — 그 11개는 방금 배포·요청한 것이라 미색인이 당연하고, §1.2.1 의 "새로 미색인이 된 URL 0" 판정을 뒤집지 않는다 |

**검산이 맞는다** — 9-17 자 "sitemap 중 미색인 56" 에서 **26 이 줄어 30** 이 됐고, 그 26은
**기술 글 24**(28개 중 색인된 것, §2.3.2·§2.3.3) **+ `(ko/)apps/unit/` 2** 다. **sitemap 쪽에서 새로
미색인이 된 URL 은 이번에도 0 이다.**

> **9-19 에 이 자리에 적은 예측이 맞았다 — 규칙은 그대로 유효하다.**
> 그때 "보고서가 전진하면 62 가 크게 오를 자리다" 라고 적었고 **실제로 88(+26)이 됐다.**
> 보고서 값은 **기준일 스냅샷**이므로 그 뒤에 한 요청의 성과가 들어올 수 없다. 9-17·9-18·9-19 요청은
> 기준일 `9-14` 뒤였고, 기준일이 `9-18` 로 오르자 비로소 반영됐다.
> **요청 결과는 보고서가 아니라 실시간 URL 검사로 본다**(§6.2). 보고서는 전수 집계용이다.
> **이번 전진으로 그 격차는 해소됐다 — 3일치 요청 28개가 보고서와 실시간 검사에서 일치한다.**
> 색인 88 안에 기술 글 24개가 있고 미색인 16 안에 나머지 4개가 있어 **28개 전부가 반영돼 있다.**
> **9-19 요청분 8개도 전부 들어와 있다**(색인 6 + 미색인 2). 기준일이 `9-18` 인데 `9-19` 크롤 결과가
> 보이는 것은 §2.3.2 가 적어 둔 대로 **드릴다운 표가 집계 기준일보다 최신을 담기 때문**이다.
> **따라서 "다음 전진 때 88 이 자동으로 더 오른다" 고 기대하지 말 것** — 더 오르려면
> **미색인 4개나 얼어 있는 기존 12건이 움직여야 한다.**

### 1.1 색인되지 않음 16건 — 기존 12건은 하나도 빠지지 않았고 기술 글 4건이 더해졌다

| URL | 사유 | 최종 크롤링 |
|---|---|---|
| **`/ko/blog/techs/what-to-delegate-to-ai/`** | 크롤링됨 - 현재 색인이 생성되지 않음 | **2026-09-19** |
| **`/ko/blog/techs/git-bash-windows-symlink/`** | 〃 | **2026-09-19** |
| **`/ko/blog/techs/explorer-vs-terminal/`** | 〃 | **2026-09-18** |
| **`/blog/techs/what-to-delegate-to-ai/`** | 〃 | **2026-09-17** |
| `/ko/blog/` | 〃 | 2026-09-10 |
| `/ko/tech-notes/bt-keyboard/why-korean-english-key-labels-drift.html` | 〃 | 2026-09-05 |
| `/ko/tech-notes/bt-keyboard/why-google-tv-does-not-detect-bluetooth-hid.html` | 〃 | 2026-09-05 |
| `/ko/tech-notes/bt-keyboard/sharing-one-bluetooth-hid-session.html` | 〃 | 2026-09-05 |
| `/sitemap.xml` | 〃 | 2026-08-31 |
| `/ko/tech-notes/bt-keyboard/` | 〃 | 2026-08-17 |
| `/manual/` | 〃 | 2026-08-14 |
| `/ko/apps/` | 〃 | 2026-08-14 |
| `/apps/` | 〃 | 2026-08-14 |
| `/ko/bt-ppt/` | 〃 | 2026-08-11 |
| `/apps/lgtv/` | 중복 페이지, Google에서 사용자와 다른 표준을 선택함 | 2026-08-24 |
| `/ko/apps/lgtv/` | 〃 | 2026-08-24 |

**늘어난 4건은 §2.3.2 가 실시간 검사로 이미 잡아 둔 그 4개이고, 보고서가 뒤늦게 같은 것을 말한 것이다.**
**기존 12건은 in/out 0·0 이다** — 색인이 62→88 로 26 오르는 동안 하나도 해소되지 않았다.
**허브 3개(`/apps/`·`/ko/apps/`·`/manual/`)는 8-14 크롤 이후 39일째 그대로다.**
`중복 페이지` 2건의 최종 크롤링도 `2026-08-24` 로 29일째 같다(§8).

**`/sitemap.xml` 이 이 목록에 있는 것을 "사이트맵이 처리됐다" 로 읽지 않는다** — 그것은 일반 크롤
기록이고, 사이트맵 처리 파이프라인은 §7 대로 `lastDownloaded` 조차 없다.

### 1.2 색인 88 중 sitemap 밖 10개 — 옛 스텁이다

`/bt-keyboard/` `/bt-mouse/` `/ssh-scout/` `/wifi-scout/` `/lgtv/` 와 각 `/ko/` 짝.
**§3.1 의 옛 제품 URL 14개 중 10개이며, 색인이 62→88 로 +26 오르는 동안에도 하나도 빠지지 않았다**(§8).
**2026-09-22 에 색인 88개 전수를 받아 live sitemap 108 과 기계적으로 대조해 확인한 값이다**(§6.6).

**옛 스텁 14개의 분해는 이렇다 — 색인 10 · `크롤링됨` 1 · Google 이 모르는 URL 3**
(`/bt-ppt/`, `(ko/)yt-downloader/` — 보고서 어느 사유에도 없다).
**`크롤링됨` 1개인 `/ko/bt-ppt/` 를 "옛 URL 이 색인에서 빠지기 시작했다" 로 읽지 말 것** —
그 크롤은 `2026-08-11` 로 **개편 배포(8-14)보다 앞서므로 이동 신호를 읽은 결과가 아니다.**

### 1.2.1 sitemap 108 중 미색인 30개 — 전수 내역

**2026-09-22 실측이다. 56 → 30 으로 26 줄었고, 그 26은 기술 글 24 + `(ko/)apps/unit/` 2 다**(§1 검산).
**남은 30은 9-17 자 56의 부분집합이고 새로 들어온 URL 이 0 이다** — 전부 이 문서가 이미 추적하던 것들이다.

| 묶음 | 개수 | 상태 |
|---|---|---|
| 기술 글 미색인 | 4 | `크롤링됨`. §2.3.3 |
| `(ko/)tech-notes/{bt-ppt, bt-mouse, wifi-scout, ssh-scout}/` | 8 | 8-31 요청분 **미발견**. §2.2 2번의 결정 대기 |
| `/ko/tech-notes/bt-keyboard/` + 상세 3편 | 4 | `크롤링됨`. 국문 비대칭 묶음(§2.3) |
| `(ko/)blog/use-android-phone-as-{bluetooth-mouse, powerpoint-remote}/` | 4 | 9-10 요청분. 크롤조차 붙지 않았다 |
| 허브 `/apps/`·`/ko/apps/`·`/manual/` | 3 | `크롤링됨`. §2.6 조건 2 |
| `(ko/)apps/lgtv/` | 2 | `중복 페이지`. §8 잠식 |
| `(ko/)apps/yt-downloader/` | 2 | **미발견**. 미출시 앱 |
| `(ko/)manual/bt-keyboard/` | 2 | **미발견**. 8-17 요청 |
| `/ko/blog/` | 1 | `크롤링됨` |

**`/ko/manual/` 은 여기 없다** — 허브 4개 중 그것만 색인이다(§2.6 조건 2).

### 1.3 반복 관측이 무의미해진 지표

- **외부 링크·내부 링크 그래프 — 링크 보고서를 이 사이트의 신호로 쓰지 않는다.** 8-15 와 8-31 두 번
  읽었고 **외부 0 / 내부 0 으로 한 글자도 다르지 않았다.** 그 사이 색인은 6 → 22 → 38 로 늘었으므로
  "보고서 지연" 으로는 더 이상 설명되지 않는다.
  **결정적인 것은 같은 날 URL 검사와의 충돌이다** — `/apps/lgtv/` 의 `참조 페이지` 는 옛 스텁
  `/lgtv/` 로 **잡혀 있다**. **즉 Google 은 내부 링크를 인지하는데 링크 보고서에는 한 건도 나타나지
  않는다.** 저장소 전수 검색으로도 내부 링크는 실재한다. **따라서 "내부 링크가 없다" 를 사이트의
  결함으로 읽지 않고, 이 항목을 근거로 사이트를 고치지 않는다.** 왜 비어 있는지는 GSC 가 말하지
  않으므로 확정하지 않는다.
- **`bt_mouse2` Play 링크 404**(8-18 발견): 8-31 에 `200` 으로 해소됐고 **2026-09-17 이 문서를 정리한
  세션의 재확인도 `200`** 이다. **닫힌 문제다.**

### 1.4 실적(Performance) 판독 — 2026-09-17 최초 수행

**§2.6 의 완료 조건을 처음 집행한 결과다.** 개편(8-14) 전후를 노출 기준으로 갈라 읽었다.

| 구간 | 정의 | 일수 | 페이지 표 클릭 합 | 페이지 표 노출 합 | 노출/일 |
|---|---|---|---|---|---|
| **A 개편 이전** | 2026-08-01 ~ 08-13 (맞춤 범위) | 13 | 3 | 147 | 11.3 |
| **B 개편 이후** | 2026-08-18 ~ 09-14 (`num_of_days=28`) | 28 | 28 | 932 | 33.3 |

**전환일 8-14~8-17 은 어느 구간에도 넣지 않았다.** 전체(8-01~9-14, 45일) 헤드라인은 클릭 36 ·
노출 약 1,160 · CTR 3.1% · 평균 게재순위 17.6 이고 **8-01 이전 데이터는 존재하지 않는다.**

**위 수치는 `페이지` 표 전 행(A 17행 · B 50행, 둘 다 `총 N행 중 1~N`)의 합이지 GSC 헤드라인 총계가
아니다 — 둘은 일치하지 않는다.** A 구간의 헤드라인은 **클릭 3 · 노출 142** 인데 같은 구간 페이지 표의
합은 **147** 이다(**표 합이 5 더 크다**). **원인은 확정하지 않는다** — 익명화는 차원 합을 총계보다
*낮추는* 쪽이라 이 방향을 설명하지 못하고, GSC 는 사유를 말하지 않는다. **한 판정 안에서 두 기준을
섞지 않는 것으로 충분하다.** 아래 비교는 전부 **페이지 표 합** 기준이다.

| 제품 | 옛 URL 노출 A → B | 새 `/apps/…/` 노출 A → B |
|---|---|---|
| bt-keyboard | 23 → 26 (영 23→20 · 국 0→6) | 0 → **28** (영 14 · 국 14) |
| bt-mouse | 6 → 23 (영 6→21 · 국 0→2) | 0 → **26** (영 23 · 국 3) |
| bt-ppt | 0 → 0 | 0 → **38** (영 29 · 국 9) |
| wifi-scout | 4 → 3 | 0 → **13** (영 10 · 국 3) |
| ssh-scout | 1 → 3 | 0 → **6** (영 3 · 국 3) |
| **lgtv** | **7 → 34** (영 4→16 · 국 3→18) | **0 → 0** |
| yt-downloader | 0 → 0 | 0 → 0 (미출시) |
| **합계** | **41 → 89** (3.15/일 → 3.18/일) | **0 → 111** (0/일 → 3.96/일) |

클릭은 옛 URL 이 A 2 → B 7, 새 URL 이 A 0 → B 7 이다.
**나머지는 A 가 홈 `/` 1, B 가 `/blog/`·`/tech-notes/` 14 다** — `/blog/`·`/tech-notes/` 는 A 에서 클릭 0 이다.

**판정 셋.**

1. **합계로는 "옛 URL 노출 감소" 가 관측되지 않는다 — 그런데 그 평평함은 lgtv 하나가 만든다.**
   합계 일평균은 3.15 → 3.18 로 같지만, **잠식된 lgtv(판정 3)를 빼면 34 → 55, 일평균 2.62 → 1.96
   으로 약 25% 줄어든다.** 제품별로는 방향이 갈린다 — bt-keyboard 1.77 → 0.93(−47%) ·
   wifi-scout 0.31 → 0.11(−65%) 은 줄고, bt-mouse 0.46 → 0.82 · ssh-scout 0.08 → 0.11 은 늘며,
   **lgtv 만 0.54 → 1.21 로 두 배 넘게 뛴다.**
   **따라서 "이동이 통째로 실패했다" 로 읽지 않는다** — 그렇게 읽으면 §3.1·§3.2 를 뒤집는 논의
   (`noindex` 재도입·`404`)로 가는데, §8.4 가 닫아 둔 경로다. 정확한 서술은 **"lgtv 를 뺀 합계는
   25% 줄었고(감소분은 대부분 bt-keyboard 몫이며 bt-mouse·ssh-scout 는 오히려 늘었다), 합계 전체가
   평평해 보이는 것은 lgtv 의 옛 스텁이 노출을 두 배 넘게 늘렸기 때문"** 이다.
   **다만 표본이 매우 작다**(구간 합 41 대 89). 사이트 전체가 11.3 → 33.3/일 로 **2.94배**가 되는 동안
   옛 스텁 비중은 27.9%(41/147) → 9.5%(89/932) 로 희석됐다.
   완료 조건 "옛 스텁의 검색 결과가 새 URL 로 교체된다" 는 **아직 충족되지 않았다** — 새 URL 이 옛
   URL 을 대체한 것이 아니라 **그 옆에 추가됐고**, 옛 스텁 10개는 여전히 색인돼 있다(§1.2).
2. **"새 URL 노출 증가" 는 확인됐다.** 0 → 111. 개편 전이 0인 것은 그 URL 이 8-14 에 생겼으니 당연하고,
   **의미 있는 것은 같은 28일 창에서 옛 스텁을 넘어섰다는 것(옛 89 대 새 111)**이다.
3. **lgtv 만 새 URL 노출이 0이다.** 여섯 제품 중 다섯은 새 URL 이 전부 노출을 받는데,
   `/apps/lgtv/`·`/ko/apps/lgtv/` 는 **45일·28일 어느 표에도 나타나지 않고** 옛 스텁이 34노출을
   가져간다. **§8 의 canonical 잠식이 색인 분류 라벨에 그치지 않고 실제 노출 손실로 나타난 첫
   증거다.** 같은 구간에 `/tech-notes/lgtv/` 가 **156노출로 사이트 최다**이므로 **주제 수요가 없어서가
   아니다.**

**교차 확인 — §2.3 의 미발견 URL 은 실적에서도 전부 0 이다. 다만 역은 성립하지 않는다.**

- `(ko/)tech-notes/{bt-ppt, bt-mouse, wifi-scout, ssh-scout}/` **8개가 어느 구간에도 노출 0** 이다.
  **8-31 요청분 미발견 8개와 같은 집합이다**(§2.2 2번).
- `(ko/)manual/bt-keyboard/` 2개도 노출 0 이다(§2.3). 제품 매뉴얼은 sitemap 기준 **14개**이고, 이 둘을
  뺀 **12개 중 10개에 노출이 있다.** 남는 `(ko/)manual/roman/` 2개는 **9-16 배포라 9-14 기준 데이터
  구간 밖**이다 — 결함이 아니다.
- 허브 `/apps/`·`/ko/apps/`·`/manual/` 도 노출 0 이고, 이 셋은 §1.1 의 `크롤링됨 - 현재 색인이
  생성되지 않음` 과 정합이다.

**"공백 = 미발견" 으로 뒤집어 읽지 말 것.** B 구간에 노출이 있는 sitemap URL 은 **108 중 41** 이고
**67개가 노출 0** 이다(실측). 그 67의 대부분은 **데이터 기준일 이후 배포**이며(기술 글 28개 ·
`(ko/)apps/{roman, currency, numpad, unit}`), **미발견과 무관**하다.

**노출 0 의 사유는 셋이고 섞지 않는다** — ① **Google 이 모른다**(위 8+2개) ② **알고 크롤했는데 색인을
안 했다**(허브 3개, §1.1) ③ **구간 밖 배포**(roman 매뉴얼·기술 글 등). **①만 "기다린다" 가 답이고,
②는 이 판독 시점(9-17)에 34일째 얼어 있던 별개 문제다**(9-22 기준 39일째 — §1.1).

**`/ko/tech-notes/` 는 이 셋 어디에도 해당하지 않아 따로 적는다** — **색인돼 있는데**(§2.6 의 실시간
검사) B 구간 노출이 0 이다. 영문 짝 `/tech-notes/` 는 2노출이라 한·영이 갈린다. **원인 미확인이고,
표본이 2노출이라 유의미한 차이인지도 확정할 수 없다.**

**재판독 조건** — §2.3 의 관찰 창 **셋이 모두** 지난 뒤다. 기술 글 요청이 3일치로 갈려
**영문 10개 9-22~10-01 · 영문4+국문6 9-23~10-02 · 국문 8 9-24~10-03** 이므로
**마지막 창이 기준이다.** 그 전에 같은 수치를 다시 읽지 않는다. 판독 절차는 §6.4.

**재판독할 때 그 사이에 움직인 변수로 3일치 요청 28개를 반드시 병기한다**(§6.4 마지막 규칙).
1일차만 적어 두면 변화를 요청 10개 덕으로 귀속하게 되는데 실제로 움직인 것은 28개다.

---

## 2. 지금 열려 있는 일

**열려 있는 요청 배치는 §2.9(남은 5건)·§2.10(6건, 2026-09-24 배포)·§2.11(6건, 2026-09-24 배포) 셋이다.** §2.7 은 2026-09-23 09:18 에 **11/11 로 끝났고**,
§2.8 은 아침에 국문 1건이 할당량 거절로 남았다가 **같은 날 19:06 에 접수돼 2/2 로 끝났다.**
**그 거절과 19:06 접수가 이 문서의 할당량 이해를 바꿨다 — §4.1 의 '롤링 24시간' 절을 먼저 읽는다.**

### 2.1 집행 완료 — 기술 글 28개 색인 생성 요청, 3일치 (2026-09-19 종료)

**요청은 28/28 이 접수돼 끝났다. 남은 것은 관찰뿐이고 그 창은 §2.3 에 있다(최장 10-03).**

**2026-09-17 사용자 결정으로 순서가 확정됐다**(기본안 채택). 집행 규칙은 §4.

| 일자 | 대상 | 상태 |
|---|---|---|
| 1일차 **2026-09-17** | 영문 14개 중 **상위 10개**(sitemap = `/blog/` 편집 순) | **10/10 접수 완료**(09:03:32~09:11:46 KST). 할당량 초과 0 |
| 2일차 **2026-09-18** | 영문 잔여 4 + **국문 상위 6**(같은 sitemap 순) | **10/10 접수 완료**(09:02:20~09:11:17 KST). 할당량 초과 0 |
| 3일차 **2026-09-19** | 국문 잔여 8 | **8/8 접수 완료**(09:00:34~09:06:06 KST). 할당량 초과 0 |

1일차 10개 — `windirstat-find-disk-hogs` · `windows-package-manager` · `makefile-as-command-list` ·
`explorer-vs-terminal` · `claude-desktop-vs-code-cli` · `alias-function-script` ·
`git-bash-windows-symlink` · `wezterm-unix-domain-persistence` · `wezterm-leader-cheatsheet` ·
`what-to-delegate-to-ai` (전부 `/blog/techs/<slug>/`).
2일차 10개 — 영문 잔여 4(`/blog/techs/<slug>/`) `wezterm-windows-korean-tmux` ·
`git-worktree-linear-merge` · `git-worktree-basics` · `claude-codex-shared-instructions`
+ 국문 상위 6(`/ko/blog/techs/<slug>/`) `windirstat-find-disk-hogs` · `windows-package-manager` ·
`makefile-as-command-list` · `explorer-vs-terminal` · `claude-desktop-vs-code-cli` ·
`alias-function-script`.
3일차 국문 잔여 8 — `git-bash-windows-symlink` · `wezterm-unix-domain-persistence` ·
`wezterm-leader-cheatsheet` · `what-to-delegate-to-ai` · `wezterm-windows-korean-tmux` ·
`git-worktree-linear-merge` · `git-worktree-basics` · `claude-codex-shared-instructions`
(전부 `/ko/blog/techs/<slug>/`).

**"국문 6" 이 어느 6개인지는 문서에 적혀 있지 않아 2026-09-18 에 정했다 — 1일차가 쓴 규칙
(sitemap = `/blog/` 편집 순)을 그대로 이어 국문 상위 6개로 잡았다.** 그래서 3일차 8개는 국문
7~14번이 된다. 한·영이 같은 순서라 짝이 갈리지 않는다.

**2026-09-17 06:5x 사전 점검 10/10 통과**(이 문서를 정리한 세션의 실측) — `200` + self canonical
문자열 일치 + `hreflang` 3종 + `robots` meta 0개 + live sitemap 포함.

**요청 쪽을 고른 근거는 추정이 아니라 실측이다** — 9-14·9-15 요청분 8개가 **8/8 색인**됐고
요청→색인이 **1~2일**이었다. 반면 sitemap 기본 경로는 §7 대로 작동하지 않는다.

**1일차 실행 기록(2026-09-17 09:03:32~09:11:46 KST — 접수 시각 기준이다. `09:02` 는 스크립트 기동
시각이라 쓰지 않는다).** 10/10 이 §5.4 의 게이트 2개를 통과한 뒤 요청됐고
전부 `우선순위 크롤링 대기열` 을 받았다. **요청 전 10/10 이 미색인이었고, 대화상자를 닫은 뒤 재조회에서도
`할당량` 문구는 한 번도 나타나지 않았다.** 요청 간격은 22초로 뒀다(§4.1 의 "20초 이상").
**관찰 창은 9-22~10-01 이다. 재요청하지 않는다.**

**2일차 실행 기록(2026-09-18 09:03:00~09:11:17 KST).** 10/10 이 §5.4 의 게이트 2개를 통과한 뒤
요청됐고 전부 `우선순위 크롤링 대기열` 을 받았다. **요청 전 10/10 이 미색인이었고, 대화상자를 닫은 뒤
재조회와 마지막 화면 재확인 어디에서도 `할당량` 문구가 나타나지 않았다.**
**관찰 창은 9-23~10-02 이다. 재요청하지 않는다.**

**위 범위는 1일차와 같은 기준이다 — 결과 파일에 기록된 시각이고, `09:02:20`(배치 기동)도
`09:02:56`(첫 대기열 문구 확인)도 아니다.** 셋이 40초 안에 몰려 있어 아무거나 적으면 다음 세션이
1일차와 비교할 때 기준이 섞인다(1일차 `09:03:32~09:11:46` 역시 결과 파일 기록 시각이다).

**요청 사이 대기는 22초로 뒀다(§4.1 의 "20초 이상"). 다만 실제 접수 간격은 46~69초다** — 대기보다
검사·요청 자체가 오래 걸린다. **"10건이면 4분" 으로 일정을 잡지 말 것. 실측은 두 번 다 9분 안팎이다.**

**사전 점검 10/10 통과**(2026-09-18 08:2x) — `200` + self canonical 문자열 일치 + `hreflang` 3종
(`en`·`ko`·`x-default`) + `robots` meta 0개 + live sitemap 포함. 같은 시각 live sitemap `<loc>` 108개가
저장소 파일과 완전히 일치했다.

**09:00 정각의 첫 시도는 요청 0건으로 중단됐다 — 할당량은 소모되지 않았고 결과 파일도 생기지 않았다.**
게이트가 아니라 그 앞의 입력 단계에서 터졌다(원인과 수정은 §6.2).

**3일차 실행 기록(2026-09-19 09:00:34~09:06:06 KST).** 8/8 이 §5.4 의 게이트 2개를 통과한 뒤
요청됐고 전부 `우선순위 크롤링 대기열` 을 받았다. **요청 전 8/8 이 미색인이었고, 대화상자를 닫은 뒤
재조회 어디에서도 `할당량` 문구가 나타나지 않았다.**
**관찰 창은 9-24~10-03 이다. 재요청하지 않는다. 이로써 기술 글 28개 요청이 3일치로 전부 끝났다.**

**사전 점검 8/8 통과**(2026-09-19 08:5x) — `200` + self canonical 문자열 일치 + `hreflang` 3종 +
`robots` meta 0개 + live sitemap 포함. 같은 시각 live sitemap `<loc>` 108개가 저장소 파일과 일치했다.

**8건이 6분이다 — 1·2일차의 10건 9분보다 건당 46~49초로 빨랐다.** 접수까지 150초를 쓴 건은 없었다.

**포트를 `9333` 에서 `9334` 로 바꿔야 했다 — §6.1 이 경고한 상황이 실제로 발생했다.**
9333 은 **다른 프로필**(`claude-cdp-chrome\medium`)의 인스턴스가 물고 있었고, 스크립트는 포트를
하드코딩하므로 그대로 돌렸으면 남의 브라우저에서 GSC 를 조작했다. 상세는 §6.1.

**다음에 요청 배치를 돌릴 세션에게.** **2026-09-22 에 조작 경로가 Aside 로 바뀌었고(§6.6), 같은 날
저녁 배치 10건(§2.7)으로 요청까지 검증됐다.** **기본은 Aside 다** — 입력이 `fill()` +
`press('Enter')` 라 스크립트가 짧고, §6.6 에 요청 루프의 함정(검사창이 접힘 · 버튼은 ref 로 ·
라벨 변화)까지 적어 두었다. **아래 CDP 스크립트는 폴백으로 남긴다**(검증된 판이지만 포트·프로필
점유 확인이 매번 필요하다). **어느 쪽이든 §5.4 게이트 2개와 §4.1 의 2단계 성공 판정, 기록 규칙은
같다.**

CDP 판 스크립트는 `%LOCALAPPDATA%/Temp/claude/F--Git-kwak-droidactor-github-io/3cffa77f-…/scratchpad/` 의
`request.mjs`(§5.4 게이트 2개 + §6.2 착지 검증 + 포트 9334) · `day3.txt` · `day3-results.json` ·
`run-day3.sh`(09:00:05 까지 대기 후 기동)다. 한 세대 앞은 `f1b7091b-…/scratchpad/`(포트 9333 판)다.
임시 폴더가 지워졌으면 §6.2 의 규칙만으로 다시 만들 수 있다.

**CDP 판을 그대로 쓰기 전에 두 가지를 먼저 통과시킨다.**

1. **§6.1 의 포트·프로필 점유 확인.** 포트가 **하드코딩**돼 있고 소유 검증 없이 붙는다.
   **`inspect` 로 `NOPORT` 를 보고, 그와 별개로 `Win32_Process.CommandLine` 으로 그 포트를 누가
   물고 있는지까지 본다** — 9-19 에 `inspect` 만으로는 판별되지 않았다(§6.1).
2. **대상 목록 파일과 `RESULTS_FILE` 을 둘 다 새 것으로 준다.**

**결과 파일은 URL 단위라 이미 `REQUESTED` 인 것은 건너뛰므로 중간에 끊겨도 이어 돌릴 수 있다.**
2026-09-18 판부터는 **접수 문구를 확인한 즉시 기록**하고(`acceptedAt`) 할당량 재확인 뒤에 `at` 과
`quotaChecked: true` 를 채운다. **`acceptedAt` 만 있고 `at` 이 없는 레코드는 "GSC 에는 들어갔는데
기록 전에 죽었다" 는 뜻이다 — 재요청하지 말고 §4.2 원장과 대조해 사람이 판정한다.**
(2일차까지의 판은 할당량 재확인 *뒤에* 한 번만 저장해 그 5초 구간에 죽으면 같은 URL 을 다시
요청하게 돼 있었다. §3.2 의 "같은 날 반복 금지" 를 자동화가 깨는 경로였다.)

### 2.2 사용자 결정 대기 — 4건 (+ 미정 1건)

| # | 사안 | 상태 |
|---|---|---|
| 1 | **`(ko/)manual/roman/` 에 `manual-meta` 화면 갱신 날짜를 넣을지** | 넣지 않은 상태. 아래 설명 |
| 2 | **8-31 요청분 미발견 8개를 재요청할지** | `(ko/)tech-notes/{bt-ppt, bt-mouse, wifi-scout, ssh-scout}/`. 9-10 에 요청 10일째 8/8 `아직 알려지지 않은 URL` 이었다. **결정 전에는 재요청하지 않는다** |
| 3 | **sitemap 이미지 확장(`image:image`)을 재개할지** | 9-10 부터 열려 있다. 아래 설명 |
| 4 | **`/blog/techs/`·`/ko/blog/techs/` 가 live `404`** | 아래 설명 |
| — | **`datePublished` 규약과 배포본의 갈림** | **결정 대기가 아니라 미정 사안이다** — 이번 28개는 2026-09-17 "그대로 둔다" 로 닫혔다. 아래 설명 |

**3번 — sitemap 이미지 확장.** 8-27~8-29 에 스크린샷 57장이 들어왔는데 sitemap 의 이미지 항목은
**0개**다. **넣지 않아도 결함이 아니다** — 이미지 sitemap 은 선택 사항이다. 보류의 실질 근거는
**지금 이미지 항목을 더하면 sitemap 이 커지는데 그 수집이 아직 한 번도 성공한 적이 없다**는 것이다
(§7). **다음 세션이 이 항목을 미결로 되살려 임의로 넣지 않는다.**

**1번 — `manual-meta`.** 다른 매뉴얼 12개는 `Checked with Android app version 1.0.0 (1) · Updated
14 August 2026` / `Android 앱 1.0.0 (1) 기준 · …갱신` 을 갖는데 `(ko/)manual/roman/` 2개에만 없다.
**일부러 넣지 않았다** — 그 문구 앞부분이 "그 앱 버전으로 이 설명서를 점검했다" 는 **검증 주장**인데
점검을 하지 않았다. 날짜 신호로서의 손실은 없다(화면 날짜가 없으면 구조화 날짜와 어긋날 대상도 없다).
남은 것은 매뉴얼 계열의 표기 일관성뿐이다. 넣는다면 날짜는 `dateModified` 와 같은 `2026-09-14` 이고,
roman 매뉴얼의 hero 가 `hero product` 라 다른 12개(`hero manual-hero`)와 삽입 위치가 다르다.

**4번 — `/blog/techs/` 404 의 원인.** `blog/techs/index.html` 이 **없고**, GitHub Pages 는 디렉터리
목록을 자동 생성하지 않는다. **설계이지 사고가 아니다** — `blog/_post-template.html:31` 이
`There is no section index page — the one list at /blog/ carries them all.` 로 규정한다.
경로 마디 10개 중 이 둘만 `index.html` 이 없고 나머지 8개(`/blog/`·`/apps/`·`/manual/`·`/tech-notes/`
와 각 `/ko/` 짝)는 전부 `200` 이다. **영향은 좁다** — sitemap 밖이고 저장소 HTML 중 이 허브를
가리키는 링크가 **0건**이라 크롤러가 도달할 경로 자체가 없다. 남는 것은 사람이 URL 을 잘라 올라올 때와,
**옛 URL 을 404 로 만들지 않는다는 원칙(§3)을 세운 저장소에 유일한 404 마디가 생겼다**는 일관성이다.
후보 — ① 그대로 둔다 ② 옛 스텁과 같은 `meta refresh` 로 `/blog/` 로 보낸다 ③ 갈래 목록 페이지를
만든다(템플릿 규약 변경 필요).

**미정 사안 — `datePublished`.** 템플릿은 `datePublished`/`dateModified` 를 **배포일**로 규정하는데
(`5cc2907`, 8-19 이래 불변) 기술 글 28개는 초고 작성일(`2026-08-23`~`09-15`)로 들어갔다. 기존 8편은
규약을 지켰다. **2026-09-17 사용자 결정은 "그대로 둔다" 이고, 본문 56지점·목록 56지점 어느 것도
고치지 않는다.** 근거 셋 — ① 발행일 변경은 되돌리기 어려운 신호 변경 ② 초고일 유지가 의도된 편집
방침일 수 있다 ③ `dateModified`·`lastmod` 는 배포일로 정확해 "언제 갱신됐는가" 신호는 온전하다.
**남는 것은 규약 문면과 배포본이 갈린 상태 자체다** — 다음에 글을 낼 때 템플릿을 고치든 배포본을
맞추든 한 번은 갈라야 한다. 지금 앞당겨 정하지 않았다.

### 2.3 관찰 중 — 재요청하지 않는다

| 대상 | 요청일 | 관찰 창 / 판정 |
|---|---|---|
| **기술 글 영문 10개**(§2.1 1일차) | 2026-09-17 | **9-22~10-01.** 요청 전 10/10 미색인. **9/10 색인이 9-19·9-20·9-22 세 번 같다. 미색인 1개는 `/blog/techs/what-to-delegate-to-ai/`** |
| **기술 글 영문 4 + 국문 6**(§2.1 2일차) | 2026-09-18 | **9-23~10-02.** 요청 전 10/10 미색인. **9/10 색인. 미색인 1개는 `/ko/blog/techs/explorer-vs-terminal/`** |
| **기술 글 국문 8**(§2.1 3일차) | 2026-09-19 | **9-24~10-03.** 요청 전 8/8 미색인. **6/8 색인**(§2.3.2·§2.3.3) |
| **경제 트렌딩 국문 10**(§2.7 1일차) | 2026-09-22 | **9-27~10-06.** 요청 전 10/10 이 `아직 알려지지 않은 URL` 이었다(배포 당일) |
| **경제 트렌딩 1 + voice-input 2 + receipt·namecard 7**(§2.7 2일차 · §2.8 · §2.9 첫 창) | 2026-09-23 | **9-28~10-07.** 요청 전 전부 `아직 알려지지 않은 URL` |
| `(ko/)apps/unit/` 2개 | 2026-09-16 | **9-21~9-30. 2026-09-22 보고서(기준일 9-18)에서 2/2 색인으로 닫혔다** — 최종 크롤링 `2026-09-16` |
| **9-10 요청분 5개** — `/ko/blog/` + `(ko/)blog/use-android-phone-as-{bluetooth-mouse, powerpoint-remote}/` 4 | 2026-09-10 | **9-16 기준 6일이 지나도 단계가 오르지 않았다.** `/ko/blog/` 는 요청일에 크롤이 붙고 색인 전 단계에 머물러 있고, **블로그 글 4개는 크롤조차 붙지 않았다. 원인 미확인** |
| 8-31 요청분 미발견 8개 | 2026-08-31 | 위 §2.2 2번의 결정 대기 |
| `(ko/)manual/bt-keyboard/` | 2026-08-17 | 9-10 기준 24일째 `아직 알려지지 않은 URL` |
| `(ko/)apps/yt-downloader/` | 2026-09-05 | 9-10 기준 여전히 미발견 |
| 국문 비대칭 묶음 | — | `/ko/blog/`, `/ko/tech-notes/bt-keyboard/` 와 그 상세 3편이 `크롤링됨 - 현재 색인이 생성되지 않음` 에서 멈춰 있다. **영문 짝은 전부 색인이다. 원인 미확인** |

**위 블로그 글 4개는 비대칭 사례가 아니다** — 한·영 **둘 다** 미발견이므로 국문 비대칭 묶음과 섞지 않는다.

**9-14·9-15 요청분 8개(roman 4 + currency 2 + numpad 2)는 8/8 색인으로 닫혔다.**

### 2.3.1 2026-09-19 실시간 검사 — 기술 글 20개 중 **18개 색인**

**관찰 창(9-22~, 9-23~)보다 이르지만 사용자 질의에 답하려고 20개 전부를 실시간 URL 검사했다.
요청은 하지 않았다(검사만).**

| 묶음 | 색인 | 크롤링됨 - 현재 색인이 생성되지 않음 |
|---|---|---|
| 1일차 9-17 영문 10 | **9** | 1 — `/blog/techs/what-to-delegate-to-ai/` |
| 2일차 9-18 영문 4 + 국문 6 | **9** | 1 — `/ko/blog/techs/explorer-vs-terminal/` |

**요청이 곧바로 크롤을 불렀다 — 20/20 이 요청 시각의 2~4분 뒤에 크롤됐다.**
1일차는 `9-17 09:04:53`~`09:12:56`, 2일차는 `9-18 09:04:20`~`09:12:22` 이고 **접수 순서와 크롤 순서가
같다.** 크롤 시각이 2~3개씩 같은 초에 몰리는 것으로 보아 Googlebot 이 묶어서 가져갔다.
**`우선순위 크롤링 대기열` 이 문구가 아니라 실제 동작임을 이 속성에서 처음 확인한 것이다.**

**미색인 2개를 "실패" 로 읽지 않는다.** 둘 다 크롤은 붙었고(요청 3~4분 뒤) 색인 전 단계일 뿐이며,
**관찰 창이 시작되기도 전이다.** §4.3 이 보여주듯 이 속성은 편차가 크다. **재요청하지 않는다.**

**참조 사이트맵은 20개 전부 `감지된 참조 사이트맵이 없습니다` 였다** — §7 과 정합이고 새 사실이 아니다.

### 2.3.2 2026-09-20 실시간 검사 — 기술 글 **24/28 색인**, 허브·정체 URL 은 불변

**3일차 8개(첫 검사)와 9-19 에 미색인이던 2개(재확인), §2.6 조건 2 의 허브 4개, 조건 5 의 정체 URL
6개 — 모두 20건을 검사했다. 요청은 하지 않았다.**

| 묶음 | 요청일 | 색인 | 색인 전 단계 |
|---|---|---|---|
| 1일차 영문 10 | 9-17 | 9 | 1 |
| 2일차 영문4+국문6 | 9-18 | 9 | 1 |
| **3일차 국문 8** | 9-19 | **6** | 2 |
| **합계 28** | | **24** | **4** |

**3일차도 요청 2~4분 뒤에 크롤됐다**(`9-19 09:02:02`~`09:08:05`). **세 묶음 28/28 이 같은 패턴이다.**

**미색인 4개는 전부 `크롤링됨 - 현재 색인이 생성되지 않음` 이고, 요청일 크롤 이후 재크롤이 없다.**

| URL | 요청일 | 최종 크롤링 |
|---|---|---|
| `/blog/techs/what-to-delegate-to-ai/` | 9-17 | `9-17 09:12:56` (9-19·9-20 두 번 검사, 변화 없음) |
| `/ko/blog/techs/explorer-vs-terminal/` | 9-18 | `9-18 09:10:22` (9-19·9-20 두 번 검사, 변화 없음) |
| `/ko/blog/techs/git-bash-windows-symlink/` | 9-19 | `9-19 09:02:02` |
| `/ko/blog/techs/what-to-delegate-to-ai/` | 9-19 | `9-19 09:04:03` |

**`what-to-delegate-to-ai` 만 한·영 둘 다 미색인이다.** 나머지 둘은 짝이 색인돼 있다
(`/blog/techs/explorer-vs-terminal/` 색인 · `/blog/techs/git-bash-windows-symlink/` 색인).
**원인은 확정하지 않는다** — 표본이 하나이고 GSC 는 사유를 말하지 않는다.
**관찰 창(최장 10-03) 안이므로 재요청하지 않는다.**

**보고서 기준일과 드릴다운 표의 값은 다를 수 있다** — `최종 업데이트` 는 **집계 그래프의 기준일**이고
URL 표는 더 최신을 담는다(실측: 9-04 자 보고서의 표에 9-05 크롤이 있었다). **이 차이를 수치 불일치로
오해하지 않는다.** 위 `(ko/)apps/unit/` 도 표에서는 먼저 보일 수 있다.
**9-22 실측이 이것을 다시 보여줬다** — 기준일이 `9-18` 인 보고서의 드릴다운 표에 `9-19` 크롤이 두 건 있다.

### 2.3.3 2026-09-22 판독 — 보고서가 전진했고(색인 88) 미색인 4개는 그대로다

**보고서 기준일이 `9-14` → `9-18` 로 전진해 §2.5 의 판독 조건이 충족됐다. 전수 판독했고 요청은 하지
않았다.** 수치는 §1 의 표로 옮겼다. **이번 세션부터 조작 경로를 Aside 로 바꿨다**(§6.6).

**미색인 4개는 실시간 URL 검사로 개별 확인했고 넷 다 요청일 크롤 이후 재크롤이 없다.**

| URL | 요청일 | 최종 크롤링 | 참조 페이지 |
|---|---|---|---|
| `/blog/techs/what-to-delegate-to-ai/` | 9-17 | `9-17 09:12:56` (9-19·9-20·9-22 세 번, 불변) | **감지된 페이지 없음** |
| `/ko/blog/techs/explorer-vs-terminal/` | 9-18 | `9-18 09:10:22` (세 번, 불변) | `/blog/techs/explorer-vs-terminal/` |
| `/ko/blog/techs/git-bash-windows-symlink/` | 9-19 | `9-19 09:02:02` (두 번, 불변) | `/blog/techs/git-bash-windows-symlink/` |
| `/ko/blog/techs/what-to-delegate-to-ai/` | 9-19 | `9-19 09:04:03` (두 번, 불변) | **감지된 페이지 없음** |

**참조 페이지 칸에서 갈린다 — 한·영 둘 다 미색인인 `what-to-delegate-to-ai` 만 참조 페이지가 없고,
나머지 둘은 색인된 영문 짝이 참조로 잡힌다.** **인과로 읽지 말 것** — 색인이 안 돼서 참조로
보고되지 않는 것일 수도 있어 방향을 가릴 수 없다. **대비가 2 대 2 라 방향을 가릴 표본도 못 된다.**
**관찰 창(최장 10-03) 안이므로 재요청하지 않는다.**

**보고서의 미색인 16건과 이 4개가 정확히 맞물린다**(§1.1) — 실시간 검사가 사흘 앞서 말한 것을
보고서가 따라왔다.

### 2.4 실행됨 — sitemap 복사본 제출 실험 (2026-09-20 제출, 결과 대기)

§7.4 에 설계가 있다. **실행 시점은 §2.1 의 3일치가 끝난 뒤**이며, 2026-09-17 사용자 결정
("3일 뒤에 하자")이다. 미룬 이유는 비용이 아니라 **변수 분리**다 — 같은 기간에 색인 요청과
새 sitemap 이 동시에 움직이면 며칠 뒤 색인이 늘었을 때 어느 쪽 덕인지 가를 수 없다.

**2026-09-19 로 선행 조건이 충족됐다 — 3일치 요청이 28/28 로 끝났으므로 9-20 부터 실행할 수 있다.**

**다만 요청이 끝난 것이지 그 효과가 끝난 것이 아니다.** §2.3 의 관찰 창은 **10-03 까지** 열려 있고,
그 창 안에 sitemap 복사본을 제출하면 그 사이의 색인 변화를 어느 쪽에 귀속할지 가를 수 없다.
**이것은 실행을 막는 규칙이 아니라 기록 의무다** — 9-20 에 실행하든 10-03 뒤로 미루든,
실행했다면 §6.4 마지막 규칙대로 **재판독 때 움직인 변수로 반드시 병기한다.**

**2026-09-20 사용자 결정으로 "지금 실행" 을 골랐고 같은 날 제출했다.** 집행 내역은 §7.4 에 있다.
**따라서 지금부터 색인·실적을 재판독할 때 움직인 변수는 둘이다 — ① 기술 글 28개 색인 요청
(9-17·9-18·9-19, 관찰 창 최장 10-03) ② 이 sitemap 복사본 제출(9-20). 둘을 반드시 함께 적는다.**

### 2.5 다음 판독 조건

- **Page indexing 보고서가 `2026-09-18` 를 넘어가면 다시 판독한다.** (기준일은 판독할 때마다 올린다)
  **직전 조건 `9-14` 는 2026-09-22 에 충족돼 판독을 마쳤다**(§2.3.3, 수치는 §1).
  **같은 날 두 번 읽어도 바뀌지 않으므로 하루에 한 번을 넘겨 읽지 않는다** — 9-17 에 두 번 읽어
  같은 값을 받았다.
  **`기준일이 며칠 묵었다` 와 `며칠 연속 확인했다` 를 섞지 말 것** — `9-14` 가 걸려 있던 동안
  보고서를 읽은 것은 9-17(두 번)·9-18·9-19·9-20 이고 9-15·9-16·9-21 은 읽지 않았다.
  **보고서와 실시간 검사는 서로 다른 것을 잰다**(§1 의 경고). 9-20 에는 보고서 62 대 실시간 24개
  색인으로 격차가 벌어져 있었고, **9-22 에 보고서가 88 로 전진하면서 그 격차가 완전히 해소됐다** —
  3일치 요청 28개가 양쪽에서 24 색인 · 4 미색인으로 일치한다. **남은 격차는 없다.**
- **`sitemaps.get` 은 계속 관찰하되 매일 조회하지 않는다** — **API 값이 세 번 연속 같았고**(9-12·
  9-16·9-17) **그 뒤 화면 값도 두 번 같다**(9-20·9-22). 다음 배포나 주 단위 점검에 묶는다.

### 2.6 완료 조건 — 아직 충족되지 않은 것만

**8-14 개편 이후의 완료 조건 중 이미 충족돼 닫힌 항목은 지웠다.** 남은 것은 아래 다섯이고,
**sitemap 수집과 실제 색인을 구분한다.**

| # | 조건 | 현재 |
|---|---|---|
| 1 | sitemap 상태가 `성공` 이고 발견된 페이지가 그 시점 live `<loc>` 수와 같다 | `가져올 수 없음` / `0` (§7). **이 수치는 배포마다 바뀐다 — 닫을 때 live `<loc>` 와 맞춘다** |
| 2 | 허브 4개(`(ko/)apps/`·`(ko/)manual/`)가 전부 색인 | **`/ko/manual/` 하나뿐**, 나머지 3개는 `크롤링됨 - 현재 색인이 생성되지 않음`. **2026-09-22 보고서(기준일 9-18)에서도 셋 다 미색인 목록에 그대로 있다 — 색인이 +26 오르는 동안 움직이지 않았다** |
| 3 | 옛 스텁의 검색 결과가 새 `/apps/<app>/` 로 교체된다 | **시작되지 않았다.** 옛 스텁 10개가 색인 유지(§1.2)이고 **노출도 줄지 않았다**(§1.4) |
| 4 | Performance 보고서에서 `페이지` 기준으로 옛 URL 노출 감소 / 새 URL 노출 증가를 확인한다 | **2026-09-17 최초 판독(§1.4).** 새 URL 증가는 확인됐고 **옛 URL 감소는 확인되지 않았다.** 재판독은 §1.4 대로 **10-03 이후** |
| 5 | 미발견 URL 이 발견 단계로 올라간다 | §2.3 의 관찰 대상들. **2026-09-20 실시간 검사에서 4개가 여전히 `아직 알려지지 않은 URL` 이고, 9-22 에도 그 4개가 sitemap 미색인 30에 그대로 있다**(§1.2.1) |

**조건 2·5 를 2026-09-20 에 실시간 URL 검사로 확인했다 — 보고서와 같은 값이므로 "기준일 9-14 라
아직 안 보이는 것" 이 아니다.**

| URL | 판정 | 최종 크롤링 |
|---|---|---|
| `/apps/` | 크롤링됨 - 현재 색인이 생성되지 않음 | `2026-08-14 11:18:31` |
| `/ko/apps/` | 〃 | `2026-08-14 11:22:32` |
| `/manual/` | 〃 | `2026-08-14 11:24:32` |
| `/ko/manual/` | **URL이 Google에 등록되어 있음** | `2026-08-14 11:26:34` |
| `/ko/blog/` | 크롤링됨 - 현재 색인이 생성되지 않음 | `2026-09-10 09:30:27` |
| `/ko/tech-notes/bt-keyboard/` | 〃 | `2026-08-17 08:25:16` |
| `(ko/)manual/bt-keyboard/` | **아직 알려지지 않은 URL** | `해당사항 없음` |
| `(ko/)apps/yt-downloader/` | **아직 알려지지 않은 URL** | `해당사항 없음` |

**허브 4개는 8-14 요청 당일 크롤 이후 39일째(9-22 기준) 재크롤이 없다.** 같은 날 기술 글 28개는
요청 2~4분 뒤 크롤됐으므로(§2.3.2) **"요청하면 크롤이 온다" 가 이 허브들에는 성립하지 않았다.**
왜인지는 GSC 가 말하지 않는다. **§4.1 이 같은 URL 반복 요청을 금지하므로 재요청은 후보가 아니다** —
조건 2 는 기다리는 항목이다.

**Tech Notes 허브 2개 실시간 URL 검사는 2026-09-17 에 수행해 닫았다**(구판의 조건 3번). `/tech-notes/`
와 `/ko/tech-notes/` 둘 다 `URL이 Google에 등록되어 있음` 이고 **`Google에서 선택한 표준 URL` 이
`검사된 URL` 이라 잠식이 없다.** 최근 크롤링은 한·영 모두 **`2026. 8. 14. 오전 11:50:38` 로 초까지
같고 그 검사일(9-17) 기준 34일째 재크롤이 없었다.** 참조 사이트맵은 둘 다 `감지된 참조 사이트맵이 없습니다`(§7 과 정합),
참조 페이지는 서로를 가리킨다(§1.3 의 "Google 은 내부 링크를 인지한다" 를 한 번 더 확인해 준다).

**Page indexing 보고서의 `사이트맵 필터링` 은 쓸 수 없다 — 비활성이다.** 2026-09-17 실측:
`알려진 모든 페이지` 칩을 감싼 `[role=listbox]` 가 **`aria-disabled="true"`**(Google 의 disabled
클래스 `RDPZE`)이고 라벨이 회색이며 좌표 클릭에도 열리지 않는다. **UIA 때 "드롭다운이 열리지
않는다" 로 본 것도 UIA 의 한계가 아니라 이 비활성 상태였을 가능성이 높다**(그때를 측정한 것은
아니지만 §7 조건은 그때도 같았다). **어느 쪽이든 구판이 "경로가 막힌 것이 아니라 쓰지 않은 것"
이라고 적은 것은 틀렸다 — 지금 실측으로 막혀 있다.** 거의 확실한 이유는 §7 이다(처리된 사이트맵이 없으니 필터할
대상이 없다). **따라서 "색인 목록 ∩ live sitemap" 교집합이 차선책이 아니라 현재 유일한 경로이고,
§7 이 풀리기 전에는 이 항목을 다시 시도하지 않는다.**

**Google 은 소규모 사이트도 URL 이동 처리가 수 주 걸릴 수 있고 색인 요청이 색인을 보장하지 않는다고
안내한다. 수치가 바뀌지 않아도 반복 제출하거나 redirect 를 제거하지 않는다.**

### 2.7 완료 — `/ko/blog/trending/economy/` 11개 (2026-09-22 배포 · 2026-09-23 요청 11/11 종료)

**2026-09-22 사용자 지시로 색인 요청 대상에 포함됐다. 같은 날 배포(`7f70c03`)와 1일차 10건 요청까지
끝냈고, 남은 것은 11번째 하나와 관찰이다.**

| 단계 | 상태 |
|---|---|
| 배포 | **완료** — `7f70c03`, push 18:53:47 KST |
| live 검증 | **완료** — 11/11 `200`, sitemap `<loc>` **119**, `sitemap-all.xml` 과 바이트 동일, 저장소 파일과 일치 |
| 사전 점검(4조건) | **11/11 통과** |
| 1일차 요청 | **10/10 접수**(18:55:59~19:03:55 KST). 할당량 초과 0 |
| 2일차 요청 | **완료** — `severance-pay-calculation-check` 1건, 2026-09-23 09:18 접수 |
| 관찰 | 1일차 10개 **9-27~10-06**, 2일차 1개 **9-28~10-07** |

**착수 시점의 상태(기록용) — 저장소에만 있고 live 에는 없었다.** 11개 전부 untracked 였고
`sitemap.xml`·`ko/blog/index.html` 도 미커밋이었다. **그 상태로 요청하면 `404` 를 요청하는 것이다.**
**다음에 새 글을 낼 때도 배포 → live 검증 → 사전 점검 → 요청 순서를 건너뛰지 않는다.**

| 항목 | 값 |
|---|---|
| 경로 | `/ko/blog/trending/economy/<slug>/` 11개 |
| 슬러그 | `civil-servant-pay-raise-2027` · `crypto-tax-timing` · `deposit-protection-combination` · `early-reemployment-allowance` · `gift-tax-exemption-limit` · `irp-account-opening` · `nonghyup-short-term-deposit` · `parking-account-rates` · `severance-pay-calculation-check` · `tontine-pension-explained` · `us-stock-market-hours` |
| 언어 | **국문 전용. 영문 짝이 없다**(`blog/trending/` 디렉터리 자체가 없다) |
| sitemap | 로컬 `sitemap.xml` 에 11개가 이미 들어가 **108 → 119**. `<lastmod>` 전부 `2026-09-22` |
| 내부 링크 | `ko/blog/index.html` 에 11개 링크가 추가돼 있다(+55줄) |
| self canonical | **11/11 정확** |
| `robots` meta | **11/11 0개** |
| `hreflang` | **11/11 0개** — 아래 참조 |

**이 묶음은 사전 점검 기준이 기존과 다르다.** §2.1 이 쓴 다섯 조건 중 **`hreflang` 3종은 적용할 수
없다** — 영문 짝이 없으므로 넣을 대상이 없다. **이것을 결함으로 보고 `hreflang` 을 채워 넣지 말 것.**
남는 조건은 넷이고 그것으로 게이트를 삼는다 — **`200` · self canonical 문자열 일치 · `robots` meta
0개 · live sitemap 포함.**

**하루 10개 한도(§4.1) 때문에 11개는 하루에 끝나지 않는다.** §4.1 은 8-14 실측(11개 통과, 12번째
거절)을 근거로 **"10개를 채웠으면 그날은 끝낸다"** 로 못박았다. **11번째를 오늘 밀어 넣지 않는다** —
1일차 10개 · 2일차 1개로 가른다. **어느 10개를 먼저 할지는 §2.1 이 쓴 규칙(sitemap = `/ko/blog/`
편집 순)을 그대로 이어 정한다.**

**`sitemap-all.xml` 과 갈라진다 — 배포할 때 함께 정해야 한다.** §7.4 의 복사본 실험은 **"바이트
그대로"** 가 전제이고 §7.4.1 이 blob 해시 동일을 근거로 삼았다. 지금 `sitemap.xml` 만 119 가 되어
복사본 108 과 갈라졌다. **실험의 변수는 경로 하나여야 하므로 배포 시 `sitemap-all.xml` 도 같은
내용으로 갱신해 동일성을 유지한다**(두 파일이 다르면 어느 쪽 결과인지 가릴 수 없다).

**허브 `/ko/blog/trending/`·`/ko/blog/trending/economy/` 는 `index.html` 이 없다 — §2.2 4번과 같은
구조다.** sitemap 밖이고 저장소에서 이 허브를 가리키는 링크도 없으므로 **크롤러가 도달할 경로가
없다.** §2.2 4번의 후보 셋(그대로 둔다 / `meta refresh` / 갈래 목록 페이지)이 그대로 적용되며,
**이 묶음 때문에 그 결정을 앞당기지 않는다.** 허브는 색인 요청 대상이 아니다.

**`datePublished` 는 §2.2 미정 사안과 같은 패턴이다** — 11개 전부 초고일(`2026-08-23`~`09-02`)이고
`dateModified`·`lastmod` 는 배포일 `2026-09-22` 다. 기술 글 28개와 똑같은 형태이고 **2026-09-17
사용자 결정("그대로 둔다")의 연장으로 보고 고치지 않는다.**

**1일차 집행 기록(2026-09-22 18:55:59~19:03:55 KST — 접수 시각 기준).** 10/10 이 §5.4 의 게이트
2개를 통과한 뒤 요청됐고 전부 `우선순위 크롤링 대기열` 을 받았다. **요청 전 10/10 이
`Google에는 아직 알려지지 않은 URL입니다` 였고**(배포 당일이니 당연하다) **대화상자를 닫은 뒤
재조회에서도 `할당량` 문구는 한 번도 나타나지 않았다.** 마지막 화면 재확인도 같다.
요청 순서는 sitemap = `ko/blog/` 편집 순 그대로다 — `gift-tax-exemption-limit` ·
`deposit-protection-combination` · `crypto-tax-timing` · `nonghyup-short-term-deposit` ·
`civil-servant-pay-raise-2027` · `tontine-pension-explained` · `parking-account-rates` ·
`irp-account-opening` · `early-reemployment-allowance` · `us-stock-market-hours`.

**건당 20~36초로 끝났다 — 기술 글 때의 46~69초보다 빠르다.** 8분에 10건이 아니라 **8분에 10건을
쓰고도 대기가 남았다**(전체 8분 중 상당 부분이 첫 건의 시행착오다). 요청 간격은 20초 이상을 지켰고
가장 짧은 간격이 정확히 20초였다.

**2일차 집행 기록(2026-09-23, `severance-pay-calculation-check` 1건).** 09:00 게이트를 기다렸다가
§6.6.1 의 절차 표를 그대로 돌려 **한 호출에 접수됐다**(09:18:11 직전 — 접수 시각을 찍는 코드의
형식 오류로 초 단위를 놓쳤고, 함수가 끝난 직후 시각이 09:18:11 이다). 요청 전 상태는
**`아직 알려지지 않은 URL`** 이라 하루 사이 색인된 것이 아님을 확인했고, 닫은 뒤 재조회에서
`할당량` 문구는 없었다. **이로써 경제 11편 요청이 11/11 로 끝났다.**
**사전 점검은 4조건(§2.7 기준)으로 재측정해 통과했다** — 09:15 실측 `200` · self canonical ·
`robots` 0 · live sitemap 포함.

**이 배치가 Aside 요청 경로의 첫 검증이었고 통과했다** — §6.6 의 미검증 구간 셋(버튼 클릭 · 성공
대화상자 · 할당량 재확인)이 모두 재현됐다. **그 과정에서 새로 드러난 함정 셋은 §6.6 에 적었다.**

### 2.8 완료 — `(ko/)tech-notes/bt-keyboard/voice-input-over-bluetooth-hid.html` 2개 (2026-09-23 배포 · 요청 2/2 종료)

**2026-09-23 사용자 지시로 등록됐고 같은 날 배포까지 끝났다. 영문 1건은 09:18:40 에 접수됐고 국문 1건은
09:19:35 할당량 거절 뒤 19:06:05 에 접수됐다** — 그 거절이 §4.1 의 할당량 이해를 바꾼 사건이다(아래 '롤링 24시간').

| 단계 | 상태 |
|---|---|
| 배포 | **완료** — 커밋 `be21861`(2026-09-23 09:15:02 KST, 다른 세션이 집행) |
| live 검증 | **완료** — 09:15:36 실측 **2/2 `200`**, live sitemap `<loc>` **121** 에 쌍 포함, `sitemap-all.xml` 과 **바이트 동일**(`cmp` 통과) |
| 사전 점검(5조건) | **2/2 통과** — `200` · self canonical · `hreflang` 3종 · `robots` 0 · live sitemap 포함 |
| 요청 | **2/2.** 영문 09:18:40 접수 / 국문 09:19:35 할당량 거절 → **19:06:05 접수** |
| 관찰 | 영문 1개 **9-28~10-07** · 국문 1개 **9-28~10-07** |

**국문 1건 — `/ko/tech-notes/bt-keyboard/voice-input-over-bluetooth-hid.html` 은 롤링 창 예측대로 풀렸다.**
**2026-09-23 19:05 에 재시도해 19:06:05 에 접수됐다**(9-22 마지막 접수 19:03:55 + 24h 직후). 요청 전 상태는
여전히 `아직 알려지지 않은 URL` 이었고, 닫은 뒤 재조회에서 `할당량` 문구는 없었다. §2.9 첫 창 배치의 1번째 건이다.

| 항목 | 값 |
|---|---|
| 경로 | `/tech-notes/bt-keyboard/voice-input-over-bluetooth-hid.html` · `/ko/tech-notes/bt-keyboard/voice-input-over-bluetooth-hid.html` |
| 제목 | en `Voice typing on hosts without speech input: on-device recognition sent over Bluetooth HID` / ko `음성 입력이 없는 기기에 말로 입력하기: 온디바이스 인식을 Bluetooth HID로 보내는 방법` |
| 언어 | **en·ko 쌍이다** — §2.7 의 경제 11편과 달리 짝이 있다 |
| sitemap | 로컬 `sitemap.xml` 에 쌍으로 들어가 **119 → 121**. `<lastmod>` 둘 다 `2026-09-23`, `priority 0.6` |
| `sitemap-all.xml` | **이미 같은 내용이다**(121, `cmp` 동일 — 2026-09-23 실측). §7.4 복사본 실험의 "바이트 그대로" 전제가 유지돼 있다 |
| 내부 링크 | `(ko/)tech-notes/bt-keyboard/index.html` 양쪽이 수정돼 있다 |
| self canonical | **2/2 정확** |
| `robots` meta | **2/2 0개** |
| `hreflang` | **2/2 3종**(`en`·`ko`·`x-default`) |

**§2.7 과 달리 사전 점검 5조건을 전부 적용한다.** 경제 11편은 영문 짝이 없어 `hreflang` 3종을 뺐고
그것을 결함으로 보지 말라고 §2.7 이 적었지만, **이 둘은 쌍이므로 §2.1 이 쓴 다섯 조건 —
`200` · self canonical 문자열 일치 · `hreflang` 3종 · `robots` meta 0개 · live sitemap 포함 — 을
그대로 게이트로 삼는다.** 앞의 셋은 저장소 파일에서 이미 통과했고, 나머지 둘은 배포 없이는 잴 수 없다.

**할당량이 여유 있다고 본 것이 틀렸다 — 이 항목이 그것을 드러냈다.** 등록 시점에는 "2026-09-23 에
예정된 것이 §2.7 의 1건뿐이니 3건(1+2)은 하루 10개 안" 이라고 적었다. **달력 날짜로 계산한 것이
오판이었고 3건째가 거절됐다.** 근거와 새 계산법은 §4.1 의 롤링 창 절에 있다.

**요청 순서는 기존 규칙 그대로 sitemap = 파일 편집 순이다** — 영문 `/tech-notes/…` 을 먼저,
국문 `/ko/tech-notes/…` 을 뒤에 둔다(§2.1 이 9-17·9-18 에 쓴 배치와 같다).

**배포 커밋에서 확인할 것 — `sitemap-all.xml` 동일성.** 지금은 두 파일이 바이트 동일하지만
**한쪽만 커밋에 들어가면 §7.4 실험이 무효가 된다**(§2.7 이 같은 이유로 겪은 일이다). §5.2 대로
커밋을 `--stat` 전체로 먼저 읽는다.

**배포 뒤 §1 의 수치가 낡는다** — §1 은 2026-09-22 실측 기준이고 sitemap `<loc>` **119** 로 적혀
있다. 이 2개가 나가면 **121** 이 된다. §2.5 의 판독 조건과 묶어 다음 판독 때 함께 고친다.

### 2.9 진행 중 — 2026-09-23 신규 페이지 12개 (앱 8 · 블로그 4 — 전부 배포 · 요청 7/12)

**2026-09-23 사용자 지시로 등록했다.** 같은 날 만든 앱 둘(`receipt`·`namecard`)의 제품 페이지·기술 노트·블로그 글이다
(블로그 E·F 는 같은 날 두 번째 지시로 더했다). **6개 쌍 모두 en·ko 짝이 있으므로 사전 점검은 §2.8 과 같은 5조건** — `200` · self canonical 문자열 일치 ·
`hreflang` 3종 · `robots` meta 0개 · live sitemap 포함 — 을 그대로 게이트로 삼는다. §4.2 원장에는 12개 중
**하나도 없다**(2026-09-23 16:32 대조, `receipt`·`namecard` 문자열 0건. 블로그 slug 는 이날 처음 생겼다) — 전부 신규 요청 대상이다.

| 묶음 | 경로 | 배포 | 사전 점검 | 요청 (2026-09-23 접수 시각) |
|---|---|---|---|---|
| A. receipt 제품 | `/apps/receipt/` · `/ko/apps/receipt/` | **완료** — `a5a0b16`·`fb5400f`(origin/main) | **2/2 통과** — 16:32 live 실측 `200` · canonical 일치 · `hreflang` 3 · `robots` 0 · live sitemap(123) 포함 | **2/2** — 19:06:29 · 19:07:08 |
| B. namecard 제품 | `/apps/namecard/` · `/ko/apps/namecard/` | **완료** — `644b707` | **2/2 통과** — 16:44 live 실측 `200` · canonical 일치 · `hreflang` 3 · `robots` 0 · live sitemap(129) 포함 | **2/2** — 19:07:38 · 19:08:14 |
| C. receipt 기술 노트 | `/tech-notes/receipt/` · `/ko/tech-notes/receipt/` | **완료** — `644b707` | **2/2 통과** — B 와 같은 16:44 실측 | **2/2** — 19:08:44 · 19:09:17 |
| D. namecard 기술 노트 | `/tech-notes/namecard/` · `/ko/tech-notes/namecard/` | **완료** — `644b707` | **2/2 통과** — B 와 같은 16:44 실측 | **1/2** — 영문 19:09:50. **국문 미요청** |
| E. receipt 블로그 | `/blog/products/scan-receipts-on-android-without-uploading/` · `/ko/blog/products/scan-receipts-on-android-without-uploading/` | **완료** — `4791381`(18:07 커밋, origin/main) | **2/2 통과** — 18:28 live 실측 `200` · canonical 일치 · `hreflang` 3 · `robots` 0 · live sitemap(133) 포함 | **0/2** |
| F. namecard 블로그 | `/blog/products/scan-business-cards-into-android-contacts/` · `/ko/blog/products/scan-business-cards-into-android-contacts/` | **완료** — `4791381` | **2/2 통과** — E 와 같은 18:28 실측 | **0/2** |

**E·F 는 `4791381` 로 배포돼 live sitemap 이 133 이 됐다**(2026-09-23 18:28 실측 — live `sitemap.xml`·live
`sitemap-all.xml`·저장소 `sitemap.xml` 셋이 `cmp` 동일). 같은 실측에서 A~F 12개와 §2.8 의 쌍까지 **14/14 가
5조건을 다시 통과했다.** `hreflang` 은 `<link rel="alternate">` 3종(`en`·`ko`·`x-default`)만 센다 — 페이지 본문의
언어 전환 링크에도 `hreflang` 속성이 있어 줄 단위 `grep -c` 로는 5~6 이 나온다. 결함이 아니다.
E·F 는 첫 `products` 갈래 글이다 — 갈래 허브 `/blog/products/` 는 `index.html` 이 없고(§2.2 4번과 같은 구조)
요청 대상이 아니다.

**함께 바뀐 기존 페이지는 요청하지 않는다.** 이번 변경으로 `(ko/)apps/`·`(ko/)tech-notes/`·`(ko/)blog/` 허브, 홈 두 개,
기존 제품 페이지 전부(역방향 링크 한 줄)의 `lastmod` 가 움직였지만 — 허브는 §4.2 에 이미 요청 이력이
있고(`(ko/)apps/` 8-14, `/tech-notes/` 8-14, `/blog/` 9-05, `/ko/blog/` 9-10), `/ko/tech-notes/`·홈은 이미 색인돼 있다. **§4.1 의 반복 요청
금지를 그대로 적용한다.** 새 페이지는 이 허브들의 내부 링크로도 발견된다.

**`sitemap-all.xml` 이 이미 갈라져 있다 — §7.4 실험의 전제가 깨진 상태다.** 2026-09-23 16:32 실측에서
live `sitemap.xml` 은 `<loc>` **123**, live `sitemap-all.xml` 은 **121** 이다. 원인은 A 의 배포 커밋
`a5a0b16` 이 `sitemap.xml` 에만 receipt 쌍을 넣고 복사본을 갱신하지 않은 것이다(HEAD 에서도 두 파일이
다르다 — §5.2 의 "`--stat` 전체로 먼저 읽기" 를 그 커밋이 건너뛰었다). **`644b707` 에서 두 파일을 다시 바이트 동일하게 맞췄고 16:44 live 에서도 `cmp` 동일이다.**
**E·F 를 배포한 `4791381` 도 `sitemap-all.xml` 을 함께 맞췄다**(133, live `cmp` 동일). §7.4.1 의 관찰은
두 파일이 달랐던 구간(`a5a0b16` 배포 ~ `644b707` 배포)을 빼고 읽는다.

**첫 창 집행 기록(2026-09-23 19:05:48~19:09:55 KST, Aside).** 계획대로 **8건**을 넣고 멈췄다 —
① §2.8 국문 1건 ② A 2건 ③ B 2건 · C 2건 · D 영문 1건. **8/8 이 §5.4 게이트 2개를 통과한 뒤 요청됐고 전부
`우선순위 크롤링 대기열` 을 받았다. 요청 전 8/8 이 `아직 알려지지 않은 URL` 이었고, 닫은 뒤 재조회와 마지막
화면 재확인 어디에서도 `할당량` 문구가 나타나지 않았다.** 건당 25~35초, 요청 사이 대기는 15초 + 함수 앞 8초로
간격 20초 이상을 지켰다. **이 시점 지난 24시간 접수는 10건(9-23 09:18 의 2 + 이 8)이다.**
**관찰 창은 9-28~10-07 이다. 재요청하지 않는다.**

**남은 5건 — 두 번째 창.** 순서는 그대로 ④ D 국문 `/ko/tech-notes/namecard/` ⑤ E 영문 → 국문 ⑥ F 영문 → 국문.
**할당량은 롤링 24시간으로 센다(§4.1).** 창에서 빠지는 시각은 이렇다 —
- **2026-09-24 09:19 이후**: 9-23 아침 2건(09:18:11 직전 · 09:18:40)이 빠져 창 안이 8건 → **2건까지**(④ + E 영문).
- **2026-09-24 19:10 이후**: 이 첫 창 8건(마지막 19:09:50)이 전부 빠진다 → **5건 모두** 한 번에 넣을 수 있다.
- **권장은 9-24 19:10 이후 한 번에 5건**이다 — 창을 둘로 쪼개면 그 뒤 배치의 해제 시각 계산만 복잡해진다.
  아침에 2건을 먼저 넣었다면 나머지 3건은 그 2건과 무관하게 19:10 이후 가능하다(창 안 2 + 3 = 5).
- 매 건 §6.6.1 절차를 그대로 돌리고, 요청 전 `아직 알려지지 않은 URL` 인지(= 그새 색인되지 않았는지) 본다.
  **`할당량` 문구가 나오면 즉시 중단한다.**
- 접수된 것만 §4.2 원장에 올리고, 이 절의 표를 갱신한다. 관찰 창은 §4.3 편차를 감안해 접수일 +5~14일로 잡는다.

**namecard 문구 후속 수정 — 앱 변경이 커밋됐고 사이트 동기화를 진행 중이다(2026-09-23 저녁).** name-card 의
`273828d`(초안·확정·확인 표시 제거, 이름 필수만으로 저장·내보내기, QR 링크 열기 버튼)와 `ecebf71`(목록 일괄 삭제,
처리 못한 인식 조각을 메모 초안으로)이 들어와 B·D·F 와 처리방침 `#privacy-namecard` 의 "초안" "확정" 서술을
고친다(F 는 원고 `site.{en,ko}.md` 를 먼저 고쳐 재생성). **그 수정은
`dateModified`·`lastmod` 갱신 대상일 뿐 재요청 사유가 아니다**(§4.1 반복 금지).

**namecard 스크린샷이 준비되면 페이지를 갱신해야 한다 — 지금은 스크린샷 없이 게시했다.**
2026-09-23 기준 원본 `MyApps/Mobile/_screenshot/` 에 namecard 폴더가 없어 제품 페이지 B 에 `Screens`/`화면`
절과 JSON-LD `screenshot` 이 없고, 블로그 글(`/blog/products/scan-business-cards-into-android-contacts/`
en·ko)에도 그림이 없다. 원본이 생기면 사이트 `README.md` "고칠 때" 의 스크린샷 규칙대로 한 묶음으로 고친다 —
① `assets/shots/namecard/{en-US,ko-KR}/*.webp` 축소본 생성 ② `(ko/)apps/namecard/` 에 화면 절·JSON-LD
`screenshot` ③ 앱 허브 카드 샷(스토어 `200` 일 때만) ④ 블로그 발행 자산 `blog/scan-business-cards-into-android-contacts/assets/{en-US,ko-KR}/`
에 1080px PNG 를 두고 원고 `site.{en,ko}.md` 에 그림을 넣어 재생성 ⑤ 손댄 페이지의 `dateModified`·`lastmod` 갱신.
**이것도 본문 갱신이지 재요청 사유가 아니다** — 이미 접수된 URL 이면 다시 요청하지 않는다(§4.1).

### 2.10 대기 — 2026-09-23 idphoto(도토리 증명사진) 신규 페이지 6개 (2026-09-24 배포 · 요청 0/6)

**2026-09-23 사용자 지시로 등록했다**("현재 play console 등록 작업 중… photo work tree 작업 내용 참조해서 진행").
§2.9 와 같은 날 만든 세 번째 앱이다. 처리방침 `#privacy-idphoto` 는 `921d0e7` 로 먼저 배포됐다 — **홈 안의
`id` 라 URL 이 아니고 요청 대상도 아니다**(§3.2). 세 쌍 모두 en·ko 짝이 있으므로 사전 점검은 §2.8 의 5조건이다.
§4.2 원장에 `idphoto` 문자열은 0건이다 — 전부 신규 요청 대상이다.

| 묶음 | 경로 | 배포 | 사전 점검 | 요청 |
|---|---|---|---|---|
| G. idphoto 제품 | `/apps/idphoto/` · `/ko/apps/idphoto/` | **완료** — `13b1fa4`(2026-09-24 08:05 커밋, origin/main) | **2/2 통과** — 2026-09-24 08:46 live 실측 `200` · canonical 일치 · `hreflang` 3 · `robots` 0 · live sitemap 포함 | 0/2 |
| H. idphoto 기술 노트 | `/tech-notes/idphoto/` · `/ko/tech-notes/idphoto/` | **완료** — `13b1fa4` | **2/2 통과** — G 와 같은 08:46 실측 | 0/2 |
| I. idphoto 블로그 | `/blog/products/make-korean-id-photos-on-android/` · `/ko/blog/products/make-korean-id-photos-on-android/` | **완료** — `13b1fa4` | **2/2 통과** — G 와 같은 08:46 실측 | 0/2 |

- `13b1fa4` 배포로 live sitemap 이 **133 → 139** 가 됐다(그 커밋에서 `sitemap.xml`·`sitemap-all.xml` 은 `cmp` 동일).
  이어 §2.11 의 `184bf68` 이 **145** 로 올렸다.
- `13b1fa4` 는 `assets/shots/idphoto/{en-US,ko-KR}/*.webp`(800px 축소본 5장씩)도 함께 배포했다 — 이 저장소의
  페이지가 아니라 **네이버·Blogger·Medium 용 블로그 원고와 §2.11 의 `id-photo-app` 글이 참조하는 자산**이다.
  G·H·I 는 여전히 스크린샷 없이 게시돼 있다(아래 문단의 갱신은 그대로 남아 있다).
- **순서는 §2.9 의 남은 5건 뒤다.** §2.9 가 권장한 "9-24 19:10 이후 5건" 을 먼저 넣고, 이 6건은 그 창에 남는
  여유(10 − 5 = 5건)만큼 G → H → I 순(각 쌍 영문 → 국문)으로 넣는다. **I 국문 1건은 다음 창으로 넘어간다.**
  배포 → live 검증 → 사전 점검 → 요청 순서와 §6.6.1 절차, `할당량` 즉시 중단 규칙은 §2.9 와 같다.
- **함께 바뀐 기존 페이지는 요청하지 않는다** — 허브·홈·기존 제품 페이지의 역방향 링크 한 줄은 §2.9 와 같은 이유(§4.1)다.

**idphoto 스크린샷이 준비되면 페이지를 갱신해야 한다 — 지금은 스크린샷 없이 게시한다.** 2026-09-23 기준
사용자가 스크린샷을 준비 중이고(Play Console 스토어 등록정보 최종 저장도 스크린샷 2장이 없어 보류 상태다 —
photo 워크트리 `apps/id-photo/play/README.md`). 2026-09-23 19:28~19:40 에 `_screenshot/idphoto/{en-US,ko-KR}/01~04.png` 가
생겼지만 사용자가 아직 준비 중이라 사이트에는 반영하지 않는다 — 사용자의 확정 신호를 기다린다. 준비되면 namecard 항목(§2.9)과
같은 ①~⑤ 묶음으로 고친다 — `assets/shots/idphoto/` 축소본, `(ko/)apps/idphoto/` 화면 절·JSON-LD `screenshot`,
허브 카드 샷(스토어 `200` 일 때만), 블로그 자산 `blog/make-korean-id-photos-on-android/assets/{en-US,ko-KR}/` 와 원고
재생성, `lastmod`. **원고 재생성은 photo 브랜치가 master 에 병합되기 전에는 `--apps` 를 지정한다** — 생성기의 기본
`apps.tsv` 는 `F:\Git\kwak\mobile\master\apps.tsv` 이고 거기 idphoto 행이 아직 주석이므로
`bash F:/Git/kwak/blogger/pages.sh idphoto/make-korean-id-photos-on-android --apps 'F:\Git\kwak\mobile\photo\apps.tsv'` 로 돌린다.
**스크린샷 속 인물은 AI 생성 가상 인물이다**(App content 초안의 AI 애셋 선언) — 캡션에 그 사실을
밝혀 Console 선언과 맞춘다. **이것도 본문 갱신이지 재요청 사유가 아니다**(§4.1).

### 2.11 대기 — 2026-09-24 세 앱 제품 홍보글 신규 페이지 6개 (배포 완료 · 요청 0/6)

**2026-09-24 사용자 지시로 등록했다**("신규 추가 page 를 색인 요청 목록 대상으로 추가"). `blogger` 저장소의
`{ko,en}/products/2026-09-24-*.md` 원고 쌍을 `pages.sh` 로 생성한 글이다 — 같은 원고가 네이버·Medium 에도
나갔다. **같은 앱의 `products` 글이 §2.9 E·F·§2.10 I 에 이미 한 편씩 있다** — 이번 글은 사용 상황 쪽 홍보글이고,
같은 앱 글이 두 편이 되는 것은 사용자가 감수하기로 했다(2026-09-24). 세 쌍 모두 en·ko 짝이 있으므로 사전
점검은 §2.8 의 5조건이다. §4.2 원장에 세 slug 는 0건이다(2026-09-24 08:46 대조) — 전부 신규 요청 대상이다.

| 묶음 | 경로 | 배포 | 사전 점검 | 요청 |
|---|---|---|---|---|
| J. receipt 홍보글 | `/blog/products/receipt-scanner-app/` · `/ko/blog/products/receipt-scanner-app/` | **완료** — `184bf68`(2026-09-24 08:40 커밋, origin/main) | **2/2 통과** — 2026-09-24 08:46 live 실측 `200` · canonical 일치 · `hreflang` 3 · `robots` 0 · live sitemap(145) 포함 | 0/2 |
| K. namecard 홍보글 | `/blog/products/business-card-scanner-app/` · `/ko/blog/products/business-card-scanner-app/` | **완료** — `184bf68` | **2/2 통과** — J 와 같은 08:46 실측 | 0/2 |
| L. idphoto 홍보글 | `/blog/products/id-photo-app/` · `/ko/blog/products/id-photo-app/` | **완료** — `184bf68` | **2/2 통과** — J 와 같은 08:46 실측 | 0/2 |

- **`184bf68` 이 `sitemap-all.xml` 을 갱신하지 않았다** — `pages.sh` 는 `sitemap.xml` 만 고친다. 그래서 live 에서
  `sitemap.xml` 145 / `sitemap-all.xml` 139 로 갈라졌다(08:46 실측, §2.9 의 `a5a0b16` 과 같은 사고). 저장소에서
  `sitemap-all.xml` 을 `sitemap.xml` 복사본으로 되맞췄고(145, `cmp` 동일) **아직 배포 전이다.** §7.4.1 의 관찰은
  `184bf68` 배포부터 그 되맞춤이 배포될 때까지의 구간을 빼고 읽는다.
- **순서는 §2.9 의 남은 5건과 §2.10 의 6건 뒤다.** §2.10 이 적은 대로 9-24 19:10 이후 창은 §2.9 5건 + G·H·I
  5건으로 차고 I 국문 1건이 다음 창으로 넘어간다. **이 6건은 그다음 창에서 I 국문 1건과 함께 넣는다**(1 + 6 = 7건,
  한도 10 안). 순서는 J → K → L(각 쌍 영문 → 국문)이다. 그 창이 열리는 시각은 9-24 저녁 창의 실제 마지막 접수
  시각 + 24시간으로 다시 계산한다(§4.1 '롤링 24시간').
- 배포 → live 검증 → 사전 점검 → 요청 순서와 §6.6.1 절차, `할당량` 즉시 중단 규칙은 §2.9 와 같다. 요청 전에
  `아직 알려지지 않은 URL` 인지 다시 본다.
- **함께 바뀐 기존 페이지는 요청하지 않는다** — `(ko/)blog/` 목록의 `<li>` 세 줄과 `lastmod` 는 §2.9 와 같은 이유(§4.1)다.
  갈래 허브 `/blog/products/` 는 `index.html` 이 없어 요청 대상이 아니다(§2.9).

---

## 3. 고정 정책 — 바꾸려면 명시 결정이 필요하다

**이번 변경은 도메인·protocol 변경이 아니라 같은 속성 안의 URL path 이동이다.** 그래서:

- 기존 속성을 그대로 쓰고 **새 속성을 만들지 않는다.**
- **주소 변경(Change of Address) 도구를 쓰지 않는다** — Google 은 같은 사이트 안의 path 이동에는
  redirect 와 sitemap 갱신을 쓰라고 명시한다.
- **삭제(Removals) 도구로 옛 URL 을 지우지 않는다** — content 이동용이 아니며 redirect·canonical
  신호의 정상 처리를 방해한다.
- sitemap 제출 주소는 계속 `https://droidactor.github.io/sitemap.xml` 이다.

### 3.1 옛 URL 의 이동 신호 — `meta refresh` + canonical 둘뿐이다

GitHub Pages 는 server-side `301` 을 설정할 수 없어 아래 구성을 쓴다. Google 은 0초 `meta refresh` 를
permanent redirect 신호로 처리한다.

- 0초 `meta refresh` → 대응하는 새 URL 로 직접 이동 (1:1)
- 새 URL 을 가리키는 `rel="canonical"`
- sitemap 과 내부 링크에서는 옛 URL 을 제거

**`noindex,follow` 는 2026-08-14 `d6e04f0` 으로 14개 전부 제거했다. 되살리려면 이 절을 뒤집는 결정이
필요하다** — Google 은 사이트 이동 시 옛 URL 의 `noindex` 를 권하지 않는다(이동 신호를 죽인다).
**향후 server redirect 가 가능한 hosting 으로 옮기면 `301`/`308` 로 교체한다.**

**이것을 `noindex` 일률 금지로 읽지 말 것** — 옛 스텁에서 뺀 것은 canonical 신호를 살리기 위해서다.
**시험용·임시 페이지처럼 색인될 이유가 없는 것에는 `noindex` 를 넣는 편이 싸다.**

**옛 제품 URL 14개** (7제품 × 한·영). 이 목록은 §8 잠식의 위험 모집단이기도 하다.

| 이전 URL | 새 canonical URL |
|---|---|
| `/bt-keyboard/` · `/ko/bt-keyboard/` | `/apps/bt-keyboard/` · `/ko/apps/bt-keyboard/` |
| `/bt-ppt/` · `/ko/bt-ppt/` | `/apps/bt-ppt/` · `/ko/apps/bt-ppt/` |
| `/bt-mouse/` · `/ko/bt-mouse/` | `/apps/bt-mouse/` · `/ko/apps/bt-mouse/` |
| `/wifi-scout/` · `/ko/wifi-scout/` | `/apps/wifi-scout/` · `/ko/apps/wifi-scout/` |
| `/ssh-scout/` · `/ko/ssh-scout/` | `/apps/ssh-scout/` · `/ko/apps/ssh-scout/` |
| `/yt-downloader/` · `/ko/yt-downloader/` | `/apps/yt-downloader/` · `/ko/apps/yt-downloader/` |
| `/lgtv/` · `/ko/lgtv/` | `/apps/lgtv/` · `/ko/apps/lgtv/` |

### 3.2 하지 말아야 할 것

- 옛 제품 URL 을 `404` 로 바꾸지 않는다.
- 옛 제품 URL 을 홈이나 앱 허브 하나로 몰아 보내지 않는다.
- redirect 를 여러 단계로 연결하지 않는다.
- 옛 제품 URL 을 sitemap 에 다시 넣지 않는다.
- 옛 URL 에 색인 생성 요청을 하지 않는다.
- **옛 URL 호환 페이지를 최소 2027-08-14 까지 삭제하지 않는다.**
- 주소 변경 도구를 쓰지 않는다. 삭제 도구로 옛 URL 을 숨기지 않는다.
- **새 URL 색인 요청을 같은 날 반복하지 않는다.**
- **Search Console sitemap 행 삭제·재제출은 사용자의 명시 지시가 있을 때만 한다.**
  8-24 에 금지했고 9-12 사용자 지시로 1회 해제해 삭제 후 재제출을 실행했다. **그 해제는 1회분이며
  이 문서가 자동으로 반복하지 않는다.** 금지의 근거였던 관찰("재제출을 반복해도 상태가 바뀌지
  않았다")은 §7 대로 여전히 유효하다.
- **소유권 확인 파일 3개 중 하나도 삭제하지 않는다** — `googlec8773d84a9d25688.html`,
  `google6e10b6b19ade6d42.html`(Google), `naver291cd179e909bd205a8a0bf7179d3588.html`(네이버).
  2026-08-19 저장소 스윕으로 확정한 목록이며 **미사용 토큰은 0개**다. 루트의 Google 확인 파일 둘은
  각각 **다른 소유자**의 살아 있는 확인 수단이고 지워도 되는 잔재가 아니다 —
  **하나라도 지우면 그 소유자의 인증이 풀린다.**
  (두 번째 소유자의 이메일 주소는 여기 적지 않는다 — 이 저장소는 공개다. 필요하면 GSC
  `설정 > 사용자 및 권한` 에서 직접 본다.)
- **Play Console 의 `스토어 설정 > 웹사이트` 가 루트 `https://droidactor.github.io` 인 것을
  `/apps/<app>/` 로 "고치지" 않는다.** 2026-08-19 실측으로 6개 앱이 전부 루트이고 옛 제품 URL 을 쓰는
  앱은 0개임을 확인한 뒤 **"변경하지 않는다" 로 못박은 상태**다.
- **개인정보 처리방침 URL `/#privacy-*`·`/ko/#privacy-*` 를 바꾸지 않고, sitemap 에도 넣지 않는다** —
  **홈 페이지 안의 `id` 이지 별도 URL 이 아니다.**

---

## 4. 색인 생성 요청 — 실행 규칙과 이력

### 4.1 실행 규칙

- **KST 09:00 이후 시작한다.**
- **하루 10개를 기준으로 한다.** 2026-08-14 실측에서 **11개까지 통과하고 12번째가 거절**됐다
  (`할당량 초과 — 일일 할당량을 초과하여 이 요청을 처리할 수 없습니다. 내일 다시 제출해 주세요.`).
  **여유 목표치가 아니라 한도 바로 아래 값이므로 10개를 채웠으면 그날은 끝낸다.**
- **달력 날짜가 바뀌어도 할당량은 리셋되지 않는다 — 2026-09-23 실측.** 9-22 저녁에 10건을 쓰고
  (18:55:59~19:03:55) 날이 바뀐 9-23 아침에 요청했는데 **2건만 통과하고 3건째가 거절**됐다
  (09:18 접수 2건, **09:19:35 거절**). 거절 문구는 8-14 와 한 글자도 다르지 않다. **어제 10 + 오늘 2
  = 12건이 통과하고 13번째가 막힌 것이므로, 창은 달력 날짜가 아니라 직전 요청들로부터 굴러가는
  24시간에 가깝다.** 한도값 자체(11~13 사이)는 두 실측이 정확히 일치하지 않으므로 고정값으로 쓰지
  않는다. **실무 규칙은 이것이다 — 요청 계획은 "오늘 몇 건 했나" 가 아니라 "지난 24시간에 몇 건
  했나" 로 센다.** 해제 시각은 §4.2 원장에서 24시간 전 요청들의 접수 시각 + 24h 로 계산한다.
  **`내일 다시 제출해 주세요` 라는 문구를 달력 날짜로 읽지 말 것** — 9-23 이 그 함정이었다.
  **같은 날 저녁 이 해석이 실측으로 뒷받침됐다** — 9-22 마지막 접수(19:03:55) + 24h 직후인 **9-23 19:05 에
  재시도하자 아침에 거절된 그 URL 이 19:06:05 에 접수됐고, 이어 7건이 더 통과했다**(§2.9, 창 안 합계 10건,
  `할당량` 문구 0회). 달력 기준이었다면 같은 날 저녁에 풀릴 이유가 없다. **다만 한도 경계(11~13)를 재 본 것은
  아니다** — 10건에서 스스로 멈췄다.
- **할당량 초과 메시지가 나오면 그날은 즉시 중단한다.**
- **같은 URL 을 반복 요청하지 않는다.** 판정은 §4.2 의 이력으로만 한다(이유는 §5.3).
- 요청 전 `실제 URL 테스트` 를 통과시킨다 — **`색인 생성 요청` 버튼이 실시간 테스트를 자동 수행하므로
  이 경로로 충족된다.** 반대로 `실제 URL 테스트` 버튼을 먼저 눌러 그 탭에서 요청하면 흐름이 꼬여
  모달이 멈춘다(실측 5분 이상 `데이터 가져오는 중` 정지). **요청은 결과 화면에서 바로 누른다.**
- **연속 제출이 빠르면 `색인 생성 요청을 제출하는 중에 문제가 발생했습니다` 가 난다** — 할당량 초과와
  다른 메시지이고 다른 원인이다. **요청 사이에 20초 이상 두면 재현되지 않는다.**

**성공 판정은 두 단계다.** GSC 는 버튼을 누른 직후 성공 대화상자를 먼저 띄우고, 서버가 할당량을
확인해 거절하면 그 대화상자를 닫은 **뒤에** "할당량 초과" 를 새로 띄운다. 결과 카드의 인라인
`✓ 색인 생성 요청됨` 도 거절된 요청에 그대로 남는다. **따라서 성공 대화상자를 닫은 다음 초과
대화상자가 뒤따르는지까지 확인해야 한다.**

- 성공 = `색인 생성 요청됨` + `URL이 우선순위 크롤링 대기열에 추가되었습니다`
- 실패 = 그 뒤에 `할당량` 문구를 가진 요소가 나타남
- **성공 대화상자 위에 `Google 색인에서 데이터 가져오는 중` 모달이 겹쳐 뜬다** — 위에 덮인 것만 보고
  "요청이 안 끝났다" 고 읽으면 오판이다.
- **버튼을 누른 뒤 성공 대화상자까지 25초로는 부족할 때가 있다**(9-15 의 7건 중 3건). 화면에는
  `실제 URL의 색인을 생성할 수 있는지 테스트 중 / 1~2분 정도 걸릴 수 있습니다` 가 떠 있다.
  **없다고 실패로 읽지 말고, 버튼을 다시 누르지도 말고, `대기열` 문구가 나타날 때까지 폴링한다**
  (실측 최대 150초).

**재요청은 실효가 없다.** GSC 성공 모달이 `페이지를 여러 번 제출해도 대기열 위치나 우선순위가
변경되지 않습니다` 라고 명시한다. 8-24 에 `(ko/)apps/lgtv/` 를 재요청해 본 적이 있고 아무것도
바뀌지 않았다 — **§8 의 잠식에 대해 색인 요청은 이미 써 본 카드다.**

### 4.2 요청 이력 원장 — 재요청 금지 판정의 유일한 근거

**고유 URL 114개가 요청됐다**(9-23 아침 2건 + 저녁 8건 포함). 아래에 없는 URL 만 새로 요청할 수 있다.
**거절된 요청은 접수가 아니므로 이 표에 올리지 않는다** — 9-23 09:19:35 의 국문 `voice-input` 거절이 그렇고,
그 URL 은 같은 날 19:06:05 에 접수돼 저녁 행에 올라 있다.

| 날짜 | 접수 | 대상 |
|---|---|---|
| 2026-08-14 | **11** | `(ko/)apps/` 2 · `(ko/)manual/` 2 · `(ko/)apps/bt-keyboard/` 2 · `(ko/)apps/wifi-scout/` 2 · `(ko/)apps/lgtv/` 2 · `/tech-notes/` 1 |
| 2026-08-17 | **10** | `(ko/)manual/{bt-keyboard, wifi-scout, lgtv}/` 6 · `(ko/)apps/{bt-ppt, bt-mouse}/` 4 |
| 2026-08-24 | 2 | `(ko/)apps/lgtv/` — **8-14 와 중복. 규칙 위반이며 신규 아님** |
| 2026-08-31 | **10** | `(ko/)tech-notes/{bt-keyboard, bt-ppt, bt-mouse, wifi-scout, ssh-scout}/` |
| 2026-09-02 | **10** | `(ko/)tech-notes/lgtv/` 2 · `(ko/)apps/ssh-scout/` 2 · `(ko/)manual/{bt-ppt, bt-mouse, ssh-scout}/` 6 |
| 2026-09-05 | **10** | `(ko/)apps/yt-downloader/` 2 · `(ko/)tech-notes/bt-keyboard/{why-google-tv-does-not-detect-bluetooth-hid, why-korean-english-key-labels-drift, sharing-one-bluetooth-hid-session}.html` 6 · `/ko/` 1 · `/blog/` 1 |
| 2026-09-10 | **5** | `/ko/blog/` 1 · `(ko/)blog/use-android-phone-as-bluetooth-mouse/` 2 · `(ko/)blog/use-android-phone-as-powerpoint-remote/` 2 |
| 2026-09-14 | 2 | `/apps/roman/` 1 + **같은 URL 중복 1건**(자동화 사고, §5.4) |
| 2026-09-15 | **7** | `/ko/apps/roman/` · `(ko/)manual/roman/` 2 · `(ko/)apps/currency/` 2 · `(ko/)apps/numpad/` 2 |
| 2026-09-16 | 2 | `(ko/)apps/unit/` |
| 2026-09-17 | **10** | `/blog/techs/<slug>/` 영문 상위 10 (**갈래 허브 `/blog/techs/` 자체는 요청하지 않았다 — §2.2 4번대로 `404` 다**) — `windirstat-find-disk-hogs` · `windows-package-manager` · `makefile-as-command-list` · `explorer-vs-terminal` · `claude-desktop-vs-code-cli` · `alias-function-script` · `git-bash-windows-symlink` · `wezterm-unix-domain-persistence` · `wezterm-leader-cheatsheet` · `what-to-delegate-to-ai` |
| 2026-09-18 | **10** | 영문 잔여 4 `/blog/techs/{wezterm-windows-korean-tmux, git-worktree-linear-merge, git-worktree-basics, claude-codex-shared-instructions}/` · 국문 상위 6 `/ko/blog/techs/{windirstat-find-disk-hogs, windows-package-manager, makefile-as-command-list, explorer-vs-terminal, claude-desktop-vs-code-cli, alias-function-script}/` |
| 2026-09-19 | **8** | 국문 잔여 8 `/ko/blog/techs/{git-bash-windows-symlink, wezterm-unix-domain-persistence, wezterm-leader-cheatsheet, what-to-delegate-to-ai, wezterm-windows-korean-tmux, git-worktree-linear-merge, git-worktree-basics, claude-codex-shared-instructions}/` — **기술 글 28개 요청이 이 행으로 끝났다** |
| 2026-09-22 | **10** | 경제 트렌딩 국문 10 `/ko/blog/trending/economy/{gift-tax-exemption-limit, deposit-protection-combination, crypto-tax-timing, nonghyup-short-term-deposit, civil-servant-pay-raise-2027, tontine-pension-explained, parking-account-rates, irp-account-opening, early-reemployment-allowance, us-stock-market-hours}/` — **11개 중 10개. 나머지 `severance-pay-calculation-check` 는 9-23 예정**(§2.7). **저녁 배치(18:55~19:03 KST)이고 Aside 로 요청한 첫 배치다** |
| 2026-09-23 | **2** (+거절 1) | `/ko/blog/trending/economy/severance-pay-calculation-check/` (09:18:11 직전, §2.7 을 11/11 로 닫았다) · `/tech-notes/bt-keyboard/voice-input-over-bluetooth-hid.html` (09:18:40, §2.8) — **3건째 `/ko/tech-notes/bt-keyboard/voice-input-over-bluetooth-hid.html` 는 09:19:35 에 `할당량 초과` 로 거절됐고 접수가 아니다.** 요청 전 3건 모두 `아직 알려지지 않은 URL` 이었다. **전날 10건을 쓴 뒤 날이 바뀌었는데도 막힌 첫 사례이고 그 해석은 §4.1 의 롤링 창 절에 있다** |
| 2026-09-23 저녁 | **8** | `/ko/tech-notes/bt-keyboard/voice-input-over-bluetooth-hid.html` (19:06:05, 아침 거절분 — §2.8 을 2/2 로 닫았다) · `(ko/)apps/receipt/` 2 · `(ko/)apps/namecard/` 2 · `(ko/)tech-notes/receipt/` 2 · `/tech-notes/namecard/` 1 (19:06:29~19:09:50, §2.9). **`/ko/tech-notes/namecard/` 와 블로그 E·F 4건은 아직 요청하지 않았다.** 요청 전 8건 모두 `아직 알려지지 않은 URL` |

**2026-09-22 오후에 이 표를 URL 단위로 전개해 검산했다 — 그 시점 고유 94개로 기재와 정확히 일치했다.**
같은 시점에 94개를 sitemap 미색인 30개(§1.2.1)와 대조한 결과 **요청 이력이 없는 URL 은 0개**였다.
sitemap 108 중 이력이 없는 것은 14개인데(홈 `/`, 기존 블로그 글 12편, `/ko/tech-notes/`)
**전부 이미 색인돼 있어 요청 대상이 아니었다.** **즉 새 URL 을 배포하기 전에는 §4.1 을 지키면서
요청할 수 있는 대상이 없었고, 같은 날 저녁 배포(`7f70c03`)로 비로소 대상이 생겼다**(§2.7).
**다음 세션은 이 계산을 다시 하지 말고 새 배포가 있었는지부터 본다.**

**슬러그만으로 원장을 조회하지 말 것 — `/blog/techs/` 와 `/ko/blog/techs/` 는 슬러그가 같다.**
9-17 행의 10개는 전부 **영문**이고 그 행에 `/ko/blog` 는 한 건도 없다. **저장소 `sitemap.xml` 에서
`/blog/techs/` 14개와 `/ko/blog/techs/` 14개의 슬러그가 완전히 같음을 확인했다**(2026-09-18 실측) —
슬러그로 grep 하면 국문 6개가 "이미 요청됨" 으로 잡힌다. 2026-09-18 집행 중 실제로 이 오판 직전까지
갔다(자기보고다). **판정은 언어 접두사까지 포함한 전체 URL 로 한다.**

**요청되지 않은 채 종결된 것 하나 — `/ko/tech-notes/`.** 2026-08-14 의 12번째 시도가 할당량 초과로
실패했고, 다음날 검사했더니 이미 색인돼 있어 요청 없이 닫혔다. **그날 11:47 에 통과한 `/tech-notes/`
요청이 3분 뒤 크롤을 불렀고 Googlebot 이 `hreflang`·내부 링크를 따라 국문까지 스스로 발견했다**
(참조 페이지 칸이 영문 허브를 가리켰다). **할당량 초과가 결과적으로 손실이 아니었던 사례다.**

### 4.3 이 속성에서 관찰된 요청 → 색인 소요

| 묶음 | 요청일 | 결과 |
|---|---|---|
| 9-02 10건 | 9-02 | **3일 만에 전량 색인** |
| 9-05 bt-keyboard 상세 6 | 9-05 | 5일 만에 **영문 3/3 색인 · 국문 3/3 크롤 단계에서 정지** |
| 9-10 5건 | 9-10 | 6일이 지나도 단계가 오르지 않았다. 블로그 글 4개는 크롤조차 붙지 않았다 |
| 8-31 10건 | 8-31 | 크롤을 부른 것은 1건뿐. 8개는 10일째 미발견 |
| 9-14·9-15 8건 | 9-14~15 | **1~2일 만에 8/8 색인** |
| **9-17 영문 10** | 9-17 | **요청 2~4분 뒤 10/10 크롤. D+2(9-19) 실시간 검사에서 9/10 색인. D+5(9-22)에도 9/10** |
| **9-18 영문4+국문6** | 9-18 | **요청 2~4분 뒤 10/10 크롤. D+1(9-19) 실시간 검사에서 9/10 색인. D+4(9-22)에도 9/10** |
| **9-19 국문 8(3일차)** | 9-19 | **요청 2~4분 뒤 8/8 크롤. D+1(9-20) 실시간 검사에서 6/8 색인. D+3(9-22)에도 6/8** |
| **9-22 경제 트렌딩 국문 10** | 9-22 | **요청 직후는 미판독. 배포 당일 요청이라 요청 전 10/10 이 미발견이었다. 관찰 창 9-27~10-06** |
| **9-23 voice-input 2 + receipt·namecard 7** | 9-23 | **요청 직후는 미판독. 배포 당일 요청이라 요청 전 전부 미발견. 관찰 창 9-28~10-07** |

**편차가 크다. "요청하면 며칠 내 색인된다" 로 일반화하지 않는다.** 같은 표 안에 **D+1 에 9/10**
(9-18 묶음)과 **10일째 미발견 8건**(8-31 묶음)이 함께 있다.

**첫 검사에서 나온 숫자가 그 뒤로 변하지 않았다** — 1일차는 D+2(9-19)에 9/10, 2일차는 D+1(9-19)에
9/10, 3일차는 D+1(9-20)에 6/8 이었고 **9-22 재검사에서 셋 다 그대로다.**
**색인이 정확히 언제 붙었는지는 이 문서가 모른다** — 검사한 날이 9-19·9-20·9-22 뿐이라 그 사이를
재지 않았다(1일차는 D+1 에 검사하지 않았다). **"D+1~D+2 에 붙는다" 로 일반화하지 말 것.**
**미색인 4개는 요청일 크롤 이후 재크롤 자체가 없다**(§2.3.3). **"며칠 더 기다리면 차례로 들어온다"
는 이 묶음에서는 관측되지 않았다** — 다만 관찰 창(최장 10-03) 안이므로 결론이 아니다.

**기술 글 28개는 요청이 곧바로 크롤을 불렀다** — 세 묶음 **28/28** 이 요청 2~4분 뒤에 크롤됐다
(§2.3.1·§2.3.2). 8-31 묶음이 "크롤을 부른 것은 1건뿐" 인 것과 정면으로 다르므로, **8-31 의 실패를
"요청은 원래 잘 안 먹는다" 로 일반화하지 않는다.**

> **2026-09-19 에 이 자리에 적었던 "갈리는 것은 크롤 이후이지 크롤 자체가 아니다" 는 틀렸다 —
> 2026-09-20 실측으로 철회한다.** `(ko/)manual/bt-keyboard/`(8-17 요청)와
> `(ko/)apps/yt-downloader/`(9-05 요청)는 **오늘도 `아직 알려지지 않은 URL` 이고 크롤 칸이
> `해당사항 없음` 이다.** 같은 UI 로 요청했는데 **크롤 자체가 붙지 않은 사례가 실재한다.**
> 28/28 이라는 관측은 **기술 글 묶음에 한정된 사실**이지 이 속성의 일반 법칙이 아니다.
> **무엇이 갈랐는지는 GSC 가 말하지 않으므로 확정하지 않는다.**

---

## 5. 반복되는 함정 — 여기서 여러 번 당했다

### 5.1 `lastmod` / `dateModified` — 기준은 "본문 변경" 이다

**배포마다 반복하는 점검이고 8-17 이래 거의 매번 적중했다**(8-17·8-18·8-19·9-13·9-15·9-16 등).
**9-16 기술 글 배포에서 처음으로 어긋남 0건이 나왔다.**
(구판은 적중 횟수를 "일곱 번" 과 "여덟 번" 으로 다르게 세는 자리가 있었다. **횟수는 운영에 쓰이지
않으므로 세지 않는다** — 중요한 것은 "배포마다 본다" 이다.)

1. sitemap `<lastmod>` ↔ **그 URL 파일의 마지막 "본문 변경" 배포일**
2. 페이지 JSON-LD `dateModified` ↔ 같은 기준일

**둘은 성격이 다르고, 그래서 정당하게 갈릴 수 있다** — **`lastmod` 는 *페이지* 가 바뀐 날이고
`dateModified` 는 *글* 이 바뀐 날이다.** 링크 `href` 만 바뀌면 `lastmod` 는 올리고 `dateModified` 는
올리지 않는다.
3. **`lastmod` 를 고칠 때는 같은 URL 의 `dateModified`·화면 표시 날짜를 짝으로 함께 본다.**
   8-17 정정이 sitemap 만 고치고 짝이 되는 페이지 `dateModified` 를 놓쳐, 다음 배포(`9a397f5`)가
   그 반쪽을 드러낸 전례가 있다.

**거짓 양성을 만드는 커밋이 두 종류 있다. 둘 다 날짜를 당기지 않는다.**

- **날짜 필드만 고친 정정 커밋** — 독자가 읽는 내용이 한 글자도 바뀌지 않았다. 이것을 세면 정정할
  때마다 날짜가 하루씩 밀려 영구히 수렴하지 않는다.
- **혼합 커밋** — `git log -1` 로 읽으면 안 된다. 실측 사례 `4bb277f`(8-31)는 44개 파일 중 **40개가
  Play 링크에 `&hl=` 을 붙인 것뿐이고 본문이 실제로 바뀐 것은 4개**였다. 판정은 추정이 아니라
  기계적으로 한다 — 해당 변경만 되돌린 뒤 `HEAD` 와 diff 해 잔여 차이를 센다.
  **`lastmod` 는 반대로 44개 전부 올린다** — `href` 변경도 페이지 변경이다.

**`dateModified` 30건이 `lastmod` 와 다른 것은 정상이다. 고치지 말 것.**

| 묶음 | 개수 | `lastmod` | `dateModified` | 근거 |
|---|---|---|---|---|
| 8-31 배포 코호트 | 28 | `2026-08-31` | `08-03`~`08-27` | `4bb277f` 는 산문을 바꾸지 않았다 |
| `(ko/)manual/roman/` | 2 | `2026-09-16` | `2026-09-14` | 페이지 배포는 9-16, 산문은 9-14 |

**단순 대조 스크립트는 이 30건을 전부 어긋남으로 보고한다.** 그 보고를 보고 일괄 정정하면 모든 글에
"개정됐다" 는 거짓 신호를 보낸다. **대조 결과는 반드시 그 배포의 diff 가 산문을 바꿨는지 확인한 뒤
방향을 정한다.**

**남겨 둔 오기 하나도 고치지 말 것 — `assets/site.css?v=20260817-blog-play-link`.**
`9a397f5` 가 12개 페이지에 붙인 CSS 캐시 토큰의 날짜가 `20260817` 인데 **실제 배포일은 8-18** 이다.
**일부러 두었다** — 토큰을 바꾸면 그 12개 페이지 본문이 또 바뀌어 `lastmod` 를 다시 갱신해야 하고,
`Cache-Control: max-age=600` 이라 캐시 문제도 없다. 하이픈이 없어 `2026-08-17` grep 에도 걸리지 않는다.
**이 토큰을 "8-17 에 배포가 있었다" 는 증거로 읽지 말 것 — 8-17 자 커밋은 없다.**

**처리방침의 `Effective date` 는 별도 규칙을 따른다** — **처리방침 본문이 실질적으로 늘거나 바뀌면 그
배포일로 올리고, 그 외에는 유지한다.** 앵커 추가처럼 본문이 늘어난 경우가 전자다.

### 5.2 배포 커밋은 `--stat` 전체로 먼저 읽는다

**9-16 에 이 규칙을 어겨 판정이 뒤집혔다.** `cbb10d8` 의 템플릿 주석만 보고 "규약 위반" 2건을 적었는데,
같은 커밋이 `assets/site.css`·`README.md`·템플릿을 함께 고쳐 **규약과 배포본이 같은 커밋에서 들어온
것**이었다. 파일 목록을 끝까지 읽었다면 나오지 않았을 오판이다.

### 5.3 GSC 화면에서 신호로 읽으면 안 되는 것들

- **URL 검사 화면의 `Sitemaps` 칸은 시점·URL 마다 흔들린다.** 같은 세션 안에서 `/apps/lgtv/` 는
  `감지된 참조 사이트맵이 없습니다`, `/ko/apps/lgtv/` 는 `일시적인 처리 오류` 였고, 다른 날에는 한·영이
  반대였다. **사이트맵이 `isPending` 인 이상 두 문구 어느 쪽도 URL 단위의 사실로 쓰지 않는다.**
- **`색인 생성 요청` 버튼의 UIA 이름은 요청 이력을 구분하지 못한다.** 이력이 전혀 없는 URL 에서도
  UIA 가 읽는 이름은 `색인 생성 요청다시 요청` 이다(숨은 대체 라벨이 이어 붙는다). 화면에 *보이는*
  라벨은 이력을 따라 바뀌지만 사람이 스크린샷을 봐야 읽힌다. **자동화 경로에서 신뢰할 수 있는 것은
  §4.2 의 이력 표뿐이다.**
- **`페이지가 변경되었나요? 색인 생성 요청` 문구로 색인 여부를 가릴 수 없다** — 색인되지 않은 URL 에도
  똑같이 붙는다.
- **Sitemaps 목록 화면과 세부 화면이 어긋난다** — 목록은 `마지막으로 읽은 날짜` 가 빈칸인데 세부는
  날짜를 보여준다(두 번 관측). **API 에 `lastDownloaded` 가 없는 이상 세부 화면의 그 날짜를
  "읽은 날" 로 읽지 않는다.**
- **보고서(데이터 기준일이 며칠 전)와 실시간 URL 검사가 다른 값을 줄 수 있다.** 8-24 보고서가
  `크롤링됨` 으로 분류한 URL 을 8-31 검사는 `아직 알려지지 않은 URL` 이라고 답했다. **어느 쪽이 맞는지
  GSC 가 말하지 않으므로 추정을 사실로 인용하지 않는다.**

### 5.4 요청 자동화에는 게이트 2개가 필수다

**없어서 9-14 에 중복 요청 1건이 들어갔다.** 붙여넣기가 실패했는데 스크립트가 그대로 다음 단계로
내려가 **직전 URL 의 결과 화면에 남아 있던 요청 버튼을 다시 눌렀다.**

1. **입력(붙여넣기·Enter)이 성공하지 않으면 버튼을 누르지 않는다.**
2. **결과 카드에 대상 URL 이 찍힌 것을 확인한 뒤에만 누른다.**

**게이트 2번에는 예외가 하나 있다 — 대화상자가 떠 있으면 배경의 URL 텍스트 조회가 실패한다.
그 실패를 "페이지가 바뀌었다" 로 읽지 않는다.** 대화상자를 먼저 닫고 다시 읽는다.

**게이트 2개만으로는 부족하다 — 그 앞에 입력 착지 검증, 그 뒤에 기록 시점이 있다.**
§6.2 의 `activeElement` 착지 검증(없으면 배치가 첫 URL 에서 멈춘다)과, 접수 확인 직후 저장
(없으면 접수와 기록 사이 5초에 죽었을 때 같은 URL 을 다시 요청한다)까지가 한 세트다. 둘 다
2026-09-18 에 추가했고 상세는 §6.2 와 §2.1 의 인계 문단에 있다. **2026-09-19 3일차 8/8 도 같은
판으로 돌았고 게이트 실패·중복은 0건이다.**

**9-15 의 7건과 9-16 의 2건은 이 게이트를 통과한 뒤 요청했고 중복 0건이다.**
8-24 의 재요청(이력 확인 누락)과 9-14 의 중복(자동화가 실패를 삼킴)은 원인이 다르지만 **공통점은
"요청 직전에 대상이 무엇인지 확인하지 않았다" 이다.**

---

## 6. GSC 조작 방법 — 기본은 Aside, 폴백은 Chrome CDP

**경로가 두 번 바뀌었다.** 2026-09-16 사용자 지시로 `/uia` → CDP, **2026-09-22 사용자 지시로
CDP → Aside**(`/aside-browser`). **지금의 기본 경로는 Aside 이고 절차는 §6.6 에 있다.**
**§6.1~§6.5 의 CDP 절차는 지우지 않고 폴백으로 남긴다** — Aside 가 꺼져 있거나 그 계정으로
로그인돼 있지 않을 때, 그리고 §6.6 이 아직 다루지 않은 화면(APIs Explorer·실적 보고서)이 있다.

**전환의 근거는 전역 지침이다** — 브라우저 작업의 기본 경로가 `/aside-browser` 이고, `/browser-cdp`
는 ① W7 기밀 ② Aside 미실행·미로그인 ③ 저장소 직접 출력 ④ 화면에 탭도 곤란할 때의 폴백이다.
**GSC 는 ②에 걸리지 않는다** — 속성 소유 계정 `droidactor@gmail.com` 이 Aside 에 `[0]` 으로
로그인돼 있음을 2026-09-22 에 확인했다(§6.6). ③도 걸리지 않는다 — 조회 결과를 텍스트로 받아
메인 세션이 이 문서에 적으면 된다.

### 6.1 띄우기

```
chrome.exe --remote-debugging-port=<빈 포트> \
  --user-data-dir=%LOCALAPPDATA%\claude-play-console \
  --no-first-run --no-default-browser-check
```

- **전용 프로필 `claude-play-console` 을 재사용한다** — Play Console·AdMob 용으로 만들어 둔 것이고
  **droidactor 로그인이 살아 있어 GSC 가 바로 열린다.** 새 프로필을 만들면 로그인을 한 번 해야 한다.
  **주의: 이 프로필은 `%LOCALAPPDATA%\claude-play-console` 에 있어 `browser.sh` 가 쓰는 루트
  (`claude-cdp\<용도>`) 밖이다.** `chrome-cdp.ps1` 을 직접 호출한다(소유 마커는 이미 있다).
- **포트는 쓰기 전에 `inspect` 로 `NOPORT` 를 확인한다.** 9333 을 세 세션 연속 썼고, **2026-09-19 에
  9333 이 남에게 점유돼 9334 로 옮겼다**(아래).
- **작업이 끝나면 정리하되, 종료는 `내가 띄웠을 때만` 한다.** 내가 기동한 인스턴스면 그 프로필의
  프로세스만 종료하고 포트가 닫혔는지 확인한다. **남의 인스턴스에 붙어 쓴 경우에는 내가 만든 탭만
  닫고 인스턴스는 남긴다**(아래 실측). **이 구분을 지키지 않으면 다른 세션의 작업이 날아간다.**

**빈 포트를 찾았다고 띄울 수 있는 것이 아니다 — 프로필이 이미 점유돼 있는지를 먼저 본다**
(2026-09-17 실측). 9333·9334 가 `NOPORT` 여서 띄우려는데 `ensure-profile` 이
`profile chrome is running` 을 냈고, `Win32_Process.CommandLine` 으로 보니 **다른 세션이 08:42 에
같은 프로필을 포트 9222 로 띄워 Play Console 작업 중**이었다. **Chrome 은 같은 `--user-data-dir` 로
두 번째 인스턴스를 띄우지 못하고 기존 인스턴스에 창만 더 열며 그때 `--remote-debugging-port` 는
무시된다.** 그래서 새로 띄우지 않고 **그 포트에 붙어 내 탭만 쓰고, 남의 탭은 건드리지 않았으며,
끝나고도 인스턴스를 종료하지 않았다**(내가 띄우지 않았기 때문이다).

**`inspect` 만으로는 "그 포트를 누가 쓰는가" 를 알 수 없다 — 포트 번호와 프로필을 둘 다 넘겨야
답하고, 넘기지 않은 포트는 보지 않는다**(2026-09-19 실측). 그날 `inspect -Port 9334` 는 `NOPORT`,
`-Port 9222` 도 `NOPORT` 였는데 **9333 에는 다른 프로필(`claude-cdp-chrome\medium`)의 인스턴스가
listen 중이었다.** 요청 스크립트는 9333 하드코딩이라 **그대로 돌렸으면 남의 브라우저에서 GSC 를
조작했다.** 판별은 `inspect` 가 아니라 이것으로 한다 — `Win32_Process` 의 `chrome.exe` 를 훑어
`--user-data-dir` 과 `--remote-debugging-port` 를 뽑고, `Get-NetTCPConnection -State Listen` 으로
그 포트가 실제로 물려 있는지 본다.

**`chrome-cdp.ps1` 의 `-Profile` 은 프로필 *이름* 이 아니라 전체 경로다**(2026-09-19 실측).
`-Profile claude-play-console` 은 상대 경로로 해석돼 `profile must be a strict descendant of
LocalAppData` 로 죽는다. `-Profile "$env:LOCALAPPDATA\claude-play-console"` 로 준다.
**`profile-pids` 가 `'ProcessId' 속성을 찾을 수 없습니다` 로 죽는 것은 고장이 아니라 그 프로필로
도는 프로세스가 0개라는 뜻이다**(StrictMode 가 빈 배열에서 터진다).

**종료할 때 남의 인스턴스가 같이 죽을 걱정은 하지 않아도 된다** — `kill-profile` 은
`ChromeCdp.psm1` 의 `Get-CdpOwnedChromeProcesses` 가 `--user-data-dir` 을 정규화해 그 프로필과
일치하는 프로세스만 고른다(`:289-292`). **다만 `Stop-Process ... -ErrorAction Stop` 이라
자식이 부모보다 먼저 죽으면 `프로세스를 찾을 수 없습니다` 로 비영 종료 코드를 낸다. 종료 자체는
성공한 것이니 `inspect` 가 `NOPORT` 인지로 판정한다.**

**`cdp.mjs` 의 탭 소유 규칙에 걸리기 쉽다** — `connect({port})` 는 **호출마다 새 탭**을 만들고,
`connect({port, match})` 는 기존 탭에 붙되 **`ownsTab` 이 false 라 `close()` 가 웹소켓만 닫고 탭은
남긴다.** 2026-09-17 에 요청 스크립트를 네 번 나눠 부르면서 GSC 탭 4개가 남의 브라우저에 쌓였고,
`/json` 으로 `type: page` 를 열거해 **내가 만든 것만 `/json/close/<id>` 로 닫았다.** 남의 창을
빌려 쓸 때는 **끝에 탭 목록을 눈으로 대조한다.**

### 6.2 URL 검사 — deep link 가 없다

`…/inspect?resource_id=<속성>&id=<URL>` 은 **세 변형 전부 `404`** 다(인코딩 유무, `hl=ko`, `/u/0/`).
**GSC 는 검사 대상을 URL 이 아니라 내부 토큰으로 가리키고 그 토큰은 검사할 때마다 다르다.**
**따라서 반드시 앱 안의 검사창을 거친다.**

**막힌 것은 URL 검사 하나뿐이다** — `…/search-console/index?resource_id=…`(페이지 색인 생성),
`…/links?resource_id=…`, `…/sitemaps?resource_id=…` 같은 **보고서 딥링크는 정상 동작한다**(실측).
보고서까지 앱 내비게이션으로 돌아가지 않는다.

검사창은 `input[role=combobox]`, `aria-label` 이 `https://droidactor.github.io/에 있는 모든 URL 검사`.

| 단계 | 결과 |
|---|---|
| `Input.insertText` | **값은 박히는데 Enter 가 먹지 않는다** |
| 문자별 `dispatchKeyEvent`(`keyDown` + `text`) | **된다** (40자 내외에 1초 미만) |
| Enter 를 `rawKeyDown` 으로 | **먹지 않는다** |
| Enter 를 `keyDown` + `text: '\r'`(CR) 로 | **된다** |

**성공 판정은 `el.value` 일치가 아니라 `location.href` 가 `/inspect` 로 바뀌는 것이다.**
`색인 생성 요청`·`닫기` 는 **좌표 마우스 이벤트**로 누른다 — `el.click()` 은 Google 앱에서 반응하지 않는다.

**이 입력 사다리는 CDP 폴백에서만 필요하다 — Aside 에서는 `fill()` + `press('Enter')` 한 번에
된다**(§6.6). **다만 "무엇을 확인하고 넘어가는가"(§5.4 게이트 2개, 착지 검증)는 경로와 무관하게
그대로 지킨다** — 9-22 에 Aside 에서도 같은 자리에서 값이 밀렸다.

**SPA 대기 조건을 흔한 낱말로 잡으면 안 된다.** GSC 는 검사 페이지로 이동한 뒤에도 직전 개요 화면의
본문을 잠시 들고 있고 사이드바에 `색인생성` 이 늘 있어 그 조건이 즉시 참이 된다. **검사 화면에만 있는
문구(`실제 URL 테스트`·`색인 생성 요청`)로 기다린다.**

**개요 화면으로 *돌아올* 때 검사창 클릭이 포커스를 못 잡는 일이 있다 — 2일차(9-18) 배치가 여기서
멈췄다.**

**실측된 것만 먼저 적는다.**

- 개요 주소로 재이동 → `waitFor(txt.includes('URL 검사'))` 통과 → 검사창 좌표 클릭 → `typeText` 가
  `편집 요소에 포커스가 없다` 로 예외. **첫 URL 에서 죽어 요청 0건이었다**(할당량 손실은 없지만 그날
  배치가 통째로 멈춘다).
- **같은 클릭이 단일 이동 진단에서는 정상 착지했다** — `document.activeElement` 가 검사창이었다.
- 1일차(9-17)는 **같은 코드로 10/10 통과**했다.
- 클릭 뒤 `activeElement` 가 그 검사창인지 확인하고 아니면 다시 클릭하게 고치자 **10/10 통과**했고,
  재시도 로그는 한 번도 찍히지 않았다.

**추정 — 사실로 인용하지 말 것.** 재이동 뒤 SPA 가 늦게 재렌더하면서 입력 노드를 교체해 포커스가
날아갔을 수 있다. `URL 검사` 라는 문자열이 검사창 `aria-label` 과 사이드바 양쪽에 있어 재렌더 전에
대기 조건이 참이 되는 것은 사실이지만, **노드 교체 자체는 관측하지 않았다**(MutationObserver 를 걸지
않았다). 오버레이 가림·포커스 트랩·스크롤로 인한 좌표 어긋남도 배제되지 않았다.

**그래서 고친 방향이 "대기 문구" 가 아니라 "착지 검증" 이다.** 원인을 확정하지 않아도 막히고,
문구를 바꾸는 길은 애초에 없다 — 개요 화면에는 검사 준비 완료를 구분해 줄 고유 문구가 없다.

- **경합이라 재현이 들쭉날쭉하다.** **"어제 됐으니 코드는 맞다" 로 넘기면 다음에 또 멈춘다.**
- **검사창을 `width > 100` *단독*으로 고르지 않는다.** 화면에는 `input[role=combobox]` 가 둘이고
  (검사창 366px · 속성 선택 156px — 2026-09-18 실측) 둘 다 이 조건을 통과한다. DOM 순서가 바뀌면
  조용히 엉뚱한 칸에 타이핑한다. **`aria-label` 의 `모든 URL 검사` 를 주 조건으로 하고 `width > 100`
  은 보조 가드로 AND 해서 남긴다. 입력값 확인도 같은 selector 로 한다.**

### 6.3 `sitemaps.get` — APIs Explorer 는 iframe 안에서만 산다

1. `https://developers.google.com/webmaster-tools/v1/sitemaps/get` 을 연다. 쿠키 동의 대화상자가
   떠 있으면 먼저 닫는다.
2. **눌러야 하는 것은 본문 안의 `지금 사용해 보기` 링크(`<a>`)다.** `사용해 보기` 로 잡히는 요소는
   셋인데 둘은 크기 0(숨은 `button.devsite-show-apix`, 좌측 내비)이고 **눈에 보이는 하나는
   `devsite-concierge-notification` 오버레이**라 클릭이 `covered` 로 거부된다.
3. **iframe 의 `src` 를 새 탭으로 열면 거부된다**(`API Explorer currently only supports usage in an
   isolated frame.`). `Page.createIsolatedWorld({frameId})` 로 그 프레임의 DOM 을 다루고, 좌표 클릭은
   iframe 의 `getBoundingClientRect()` 오프셋을 더한다.
4. **입력칸을 `cdp.mjs` 의 `typeText` 로 채울 수 없다** — 포커스 검사를 **메인 프레임**
   `document.activeElement` 로 하는데 iframe 에 포커스가 가면 그 값이 `IFRAME` 이라 예외를 던진다.
   **격리 월드에서 네이티브 `value` setter + `input`/`change` 이벤트로 넣는다.**
5. 파라미터는 `siteUrl`·`feedpath` 둘. `Show scopes` 를 펼치면 `webmasters`(쓰기)와
   `webmasters.readonly` 가 **둘 다 체크된 기본값**이다 — **쓰기 scope 를 해제하고 읽기 전용만 남긴다.**
   **2026-09-20 에는 이 단계가 막혔다.** 격리 월드에서 `input[type=checkbox]` 를 전부 열거해도
   `Google OAuth 2.0` 과 `API key` 둘만 나오고 **scope 체크박스는 DOM 에 없었다.** `Show scopes` 를
   여는 요소도 텍스트로 잡히지 않았다(웹 컴포넌트 안으로 보이나 확인하지 않았다 — 추정이다).
   **쓰기 scope 를 끄지 못하면 Execute 하지 않는다.** 그날은 Sitemaps 화면 값으로 대체했다(§7.1).
   같은 세션에서 Execute 를 눌러도 50초 동안 응답 본문이 나타나지 않았다(원인 미확인).
6. `Execute` 후 응답은 같은 프레임 본문에 `200` 과 JSON 으로 나온다.
   **성공 판정은 값 일치가 아니라 응답 JSON 의 `path` 가 대상 sitemap 과 일치하는 것이다.**

### 6.4 실적 보고서 — 딥링크가 절반만 듣고, 표 컨트롤에 숨은 쌍둥이가 있다

**2026-09-17 최초 판독(§1.4)에서 나온 것들이다. 셋 다 조용히 실패해 오판을 만든다.**

- **기간 딥링크는 프리셋 값만 듣는다.** `…/performance/search-analytics?resource_id=…&num_of_days=28`
  은 정확히 듣지만 **`num_of_days=32` 는 무시돼 기본 구간으로 떨어지고, `start_date`·`end_date` 는
  아예 반영되지 않는다.** 둘 다 **오류를 내지 않고 캡션만 전체 구간으로 남으므로**, 표를 받기 전에
  반드시 `…까지의 총 클릭수` 캡션으로 실제 구간을 확인한다. **임의 구간은 `더보기 → 맞춤` 대화상자로만
  된다** — 시작일·종료일은 `input[type=text]`(값 `2026-08-01` 형식)이라 실제 키 입력으로 채우고
  `적용` 을 누른다.
- **표 컨트롤은 같은 selector 로 0-size 숨은 쌍둥이가 먼저 잡힌다.** `페이지당 행 수` 셀렉트
  (`div[role=listbox]`)와 `Next page` 버튼은 **다른 탭의 표에 딸린 사본이 DOM 에 먼저 있고 rect 가
  전부 0** 이다. 거르지 않고 첫 번째를 누르면 **아무 일도 일어나지 않는데 오류도 나지 않는다** —
  실측으로 `Next page` 를 12번 눌러 **같은 10행을 12번** 받았다. **`getBoundingClientRect().width > 0`
  으로 거른 뒤 고른다.** 50행을 한 번에 받으려면 페이지네이션 대신 행 수를 `100` 으로 바꾼다.
- **좌측 내비의 `페이지`(Page indexing)와 실적 보고서의 `페이지` 탭은 이름이 같다.** 텍스트로 누르면
  내비가 잡혀 **보고서를 떠나 버린다.** 탭은 `[role=tab]` 로 잡는다(`검색어 수`·`페이지`·`국가`·
  `기기`·`검색 노출`·`일` 여섯 개).
- **기본 창의 뷰포트(1078×1157)보다 표가 아래에 있어 `getBoundingClientRect()` 가 `0,0,0,0` 을
  준다.** `Emulation.setDeviceMetricsOverride` 로 `1600×2400` 을 주면 좌표 클릭이 그대로 된다.
- **45일 표와 28일 표가 둘 다 `총 50행` 이고 A 구간 17행이 전부 B 에 포함된다** — 그래서 "45일 표에
  없다" 는 "28일 표에 없다" 와 같은 말이다. 다음에 판독할 때 이 항등이 깨지면(행 수가 달라지면)
  `/apps/lgtv/` 판정(§1.4 판정 3)을 두 표에서 각각 다시 확인한다.
- **재판독할 때는 그 사이에 움직인 변수를 함께 적는다** — 색인 요청 묶음·새 배포·§2.4 의 sitemap 복사본
  실험이 겹치면 실적 변화를 어느 쪽에 귀속할지 가를 수 없다(§2.4 가 색인에 대해 지키는 원칙과 같다).

### 6.5 남는 제약

- **CDP 가 `/uia` 를 대체하는 것은 GSC 에 한정된다.** 사용자의 기존 로그인 창이나 브라우저
  chrome(주소창·확장)이 대상이면 CDP 는 답이 아니다. **이미 떠 있는 창에는 사후 부착이 안 된다.**
- **이 경로는 사용자 화면을 빌리지 않으므로 실패해도 사용자가 알아채지 못한다.** 그래서 §5.4 의
  게이트 2개와 상태 재조회가 `/uia` 때보다 더 중요하다.
- **UIA 로 할 때는 화면 잠금·RDP 끊김에서 물리 입력만 거부된다**(읽기와 `invoke` 는 동작). CDP 로
  바꾼 뒤로는 해당 없다.

### 6.6 Aside 경로 — 2026-09-22 전환, 이것이 기본이다

**한 세션(9-22)에서 보고서 전수 판독 · 미색인 드릴다운 · 색인 88개 수집 · Sitemaps 확인 ·
URL 검사 4건 · 그리고 같은 날 저녁에 색인 생성 요청 10건(§2.7)을 전부 이 경로로 했다.
읽기·검사·요청이 모두 재현됐다.**

**도구는 `mcp__aside__repl` 이다(MCP 툴).** 1-shot `aside repl "..."` 은 호출 사이에 상태가 끊겨
`page` 가 null 이 되고 **그 세션에서 연 탭을 나중에 닫을 방법이 없다.** MCP 쪽은 세션이 지속돼
`openTab` → 여러 호출 → `closeTab` 이 성립한다(9-22 에 탭 0으로 정리 확인). CLI 는
`%LOCALAPPDATA%\Aside\CLI\current\aside.exe` 이고 **PATH 에 없다.**

**계정 확인이 첫 단계다** — Aside 의 `google-accounts` skill 대로 `await googleAccounts.print()`.
9-22 실측에서 이 속성의 소유 계정이 **`[0] DroidActor Dotori`** 였다(다른 계정도 하나 더 로그인돼
있는데 **이 저장소는 공개이므로 여기 적지 않는다** — §3.2 가 두 번째 소유자 이메일에 대해 정한 것과
같은 이유다. 필요하면 `googleAccounts.print()` 로 그 자리에서 본다).
**`[0]` 이므로 딥링크에 `/u/0/` 를 넣는다. 다만 계정 번호는 로그인 구성이 바뀌면 달라지므로
배치를 돌리기 전에 매번 확인한다.**

```
https://search.google.com/u/0/search-console/index?resource_id=https%3A%2F%2Fdroidactor.github.io%2F&hl=ko
https://search.google.com/u/0/search-console/sitemaps?resource_id=…
```

**CDP 에서 고생하던 세 가지가 Aside 에서는 한 줄이다.**

| 조작 | CDP(§6.2) | Aside |
|---|---|---|
| 검사창 입력 | `Input.insertText` 는 값만 박히고 Enter 불가 → **문자별** `dispatchKeyEvent` | `locator.fill(url)` |
| Enter | `rawKeyDown` 불가 → CR 실은 `keyDown` | `locator.press('Enter')` |
| 버튼·표 행 클릭 | `el.click()` 반응 없음 → **좌표 마우스 이벤트** | `locator.click()` (표 행 드릴다운도 그대로 된다) |
| 숨은 쌍둥이 거르기 | `getBoundingClientRect().width > 0` | `await locator.isVisible()` |

**검사창 selector 는 `input[role="combobox"][aria-label*="모든 URL 검사"]` 를 쓴다** — §6.2 가 경고한
대로 `role=combobox` 는 화면에 둘(검사창·속성 선택)이라 `aria-label` 이 필수다. ref(`e3` 등)도
동작했지만 snapshot 마다 무효화되므로 **여러 호출에 걸치는 루프에는 CSS 를 쓴다.**

**Aside 고유의 함정 넷 — 전부 9-22 에 실제로 당했다.**

1. **`textContent()` 로 통째 긁으면 이전 화면의 DOM 잔재가 섞인다. 판정은 반드시 `snapshot()` 으로
   한다.** 색인 목록에서 `총 88행` 인데 `tbody tr` 수집은 **90행**을 줬다 — 앞 2행이 직전에 본
   `중복 페이지` 표의 잔재였다. URL 검사에서는 더 고약하다: `snapshot()` 에는 대상 URL 의 결과가
   제대로 떠 있는데 `body.textContent()` 에는 **직전 검사 URL 의 값**이 먼저 잡혀, 크롤 시각이
   한 칸씩 밀려 기록된다. **§5.4 게이트 2번("결과 카드에 대상 URL 이 찍힌 것을 확인")을 지키면
   드러나고, 안 지키면 조용히 틀린다.**
2. **`isVisible()` 은 요소가 없으면 `false` 가 아니라 예외를 던진다**(Playwright 표준과 다르다).
   존재가 불확실하면 `count()` 를 먼저 본다.
3. **snapshot 트리의 `main`·`dialog` 는 ARIA role 이라 CSS selector 로는 안 잡힌다.**
   `[role="main"]` 이나 `body` 를 쓴다.
4. **`goBack()` 은 SPA 라 `Timed out waiting for navigation` 으로 죽는다.** 뒤로 가려면 화면 안의
   탐색경로 링크를 클릭하거나 딥링크로 `goto` 한다. **`allTextContents()` 도 없다** — `count()` +
   `nth(i).textContent()` 루프로 대신한다.

**착지 게이트는 이렇게 짠다.** 결과 화면 상단의 **`- text: "<대상 URL>"`** 이 snapshot 트리에
나타날 때까지 3초 간격으로 폴링한다. **`사용자 선언 표준 URL <대상>` 을 게이트로 쓰지 말 것** —
**크롤된 적 없는 신규 URL 에서는 그 칸이 `해당사항 없음` 이라 영원히 참이 되지 않는다**(9-22 배치가
전부 그랬다). 상단 URL 텍스트는 색인·미색인·미발견 어느 상태에서도 나온다.

**`데이터 가져오는 중` 모달이 떠 있는 것을 실패 조건에 넣지 말 것** — 그 모달은 결과 카드 위에
겹쳐 있을 뿐이고 모달이 있어도 카드 값은 유효하다(9-22 실측: 모달이 뜬 채로 읽은 값이 보고서
드릴다운 값과 일치했다). **§4.1 이 요청 대화상자에서 관측한 것과 같은 성질이지만, 검사 결과
카드에서 확인한 것은 이번이 처음이다.** 9-22 에 이 조건을 AND 로 걸어 멀쩡한 판정을 **한 번**
`landed:false` 로 버렸다.

**색인 목록 전수 수집 절차**(§1.2·§1.2.1 의 근거) — 보고서 → `색인이 생성된 페이지에 대한 데이터
보기` → `페이지당 행 수` 를 **보이는 쪽** 옵션에서 `100` 으로 → `tbody tr` 수집. 그 뒤
**직전 표의 잔재를 반드시 손으로 걷어낸다**(위 함정 1). 받은 목록과 저장소 `sitemap.xml` 의
`<loc>` 를 `comm` 으로 대조하면 교집합·차집합이 바로 나온다.
**이 표는 `URL` · `최종 크롤링` 2열이라 한 번 받으면 색인 여부와 크롤 날짜를 함께 얻는다** —
§2.3 의 `(ko/)apps/unit/` 판정(2/2 색인, 최종 크롤링 `2026-09-16`)이 이 표에서 나온 것이고, 그
2개 때문에 URL 검사를 따로 돌리지 않았다. 행 텍스트는 `경로 + 날짜` 가 붙어 나오므로
`2026. M. D.` 를 떼어 URL 만 남긴다.

### 6.6.1 색인 생성 요청 루프 — 9-22 에 10/10 을 접수시킨 절차 그대로

**§2.7 1일차 배치에서 실제로 돌아간 것이다. 다음 배치는 이 순서를 그대로 쓰고, 아래 함정 5~7 을
피하는 것이 이 절차의 존재 이유다.** §5.4 게이트 2개와 §4.1 의 2단계 성공 판정이 이 안에 들어 있다.

**호출 단위** — `mcp__aside__repl` 은 호출당 **120초**라 **한 호출에 2건이 한계**다(건당 20~36초 +
사이 대기). 첫 건은 혼자 돌려 결과를 눈으로 확인하고, 그 뒤부터 2건씩 묶는다.

**함수 하나를 `globalThis` 에 정의해 두고 호출마다 재사용한다** — MCP REPL 은 세션이 지속되므로
매번 다시 붙여 넣을 필요가 없다(`const`/`let` 은 재선언 충돌이 나므로 `globalThis.<name> =` 로 둔다).
**다만 Aside 자체가 재시작되면(자동 업데이트 등) 세션이 통째로 초기화된다** — 2026-09-23 에 18:31 에 정의한
함수가 19:05 첫 호출에서 `ReferenceError: 'gscRequest' is not defined` 로 죽었고, 그때 연 GSC 탭도 사라져 있었다
(`listBrowserTabs()` 에 Aside changelog 탭이 새로 떠 있었다). **요청 전에 한참 기다렸다면 첫 호출 전에
`typeof globalThis.gscRequest` 부터 보고, 없으면 재정의 + `openTab` 을 다시 한다.** 이 실패는 요청 전 단계라
할당량 손실이 없다.

| 단계 | 하는 일 | 실패 시 |
|---|---|---|
| 0 | 검사창 `count()` 확인 → 0 이면 개요 딥링크로 `goto` + 5초 대기 → 다시 0 이면 중단 | `NO_SEARCH_BOX` (함정 5) |
| 1 | `click()` → `fill(url)` → **`inputValue()` 가 url 과 일치하는지 확인** | `INPUT_GATE_FAIL` — **§5.4 게이트 1. 여기서 멈추지 않으면 직전 URL 의 버튼을 다시 누른다** |
| 2 | `press('Enter')` → snapshot 에 **`- text: "<url>"`** 이 뜰 때까지 3초 × 최대 12회 | `LANDING_GATE_FAIL` — §5.4 게이트 2 |
| 3 | 그 트리에서 **요청 전 상태**를 읽어 기록(`아직 알려지지 않은 URL` / `등록되어 있음` / 그 밖) | — |
| 4 | 같은 트리에서 요청 버튼 **ref 를 정규식으로 뽑아** `locator(ref).click()` | `NO_REQUEST_BUTTON` (함정 6) |
| 5 | `우선순위 크롤링 대기열` 이 뜰 때까지 3초 × 최대 25회. **`할당량` 이 먼저 보이면 즉시 중단** | `QUOTA_EXCEEDED` → **그날 배치 전체를 끝낸다**(§4.1) |
| 6 | 접수 시각을 **이때 바로** 기록한다(`acceptedAt`) | — |
| 7 | `닫기` 버튼 ref 를 뽑아 클릭 → 4초 대기 → **재조회해 `할당량` 문구가 뒤따르는지 확인** | `QUOTA_AFTER_CLOSE` — §4.1 의 2단계 판정 |

```
요청 버튼: /- button "색인 생성 요청[^"]*" \[ref=(e\d+)\]/
닫기 버튼: /- button "닫기" \[ref=(e\d+)\]/
검사창   : input[role="combobox"][aria-label*="모든 URL 검사"]
개요     : https://search.google.com/u/0/search-console/index?resource_id=<인코딩된 속성>&hl=ko
```

**요청 간격은 §4.1 대로 20초 이상**이다. 9-22 실측에서 함수 한 번이 20~36초를 쓰므로 **연속 호출만으로
대개 충족되지만 경계까지 갔다**(가장 짧은 간격이 정확히 20초). **한 호출에 2건을 넣을 때는 사이에
`sleep(15000)` 을 명시로 넣는다.**

**단계 6의 위치가 규약이다** — §2.1 이 CDP 판에서 정한 것과 같다. 접수 문구를 본 **즉시** 기록하고
할당량 재확인은 그 뒤에 한다. 반대로 하면 그 5초 사이에 죽었을 때 같은 URL 을 다시 요청하게 된다.

**아직 Aside 로 해 보지 않은 것** — ① 실적 보고서(§6.4 의 기간 딥링크·표 컨트롤) ② APIs Explorer 의
`sitemaps.get`(§6.3 — iframe 격리 월드가 필요한데 Aside 의 snapshot 은 child iframe 을 평탄화해
주므로 더 쉬울 가능성이 있으나 **확인하지 않았다**).

**색인 생성 요청은 2026-09-22 저녁 배치(§2.7)로 검증됐다 — 10/10 이 이 경로로 접수됐다.**
버튼 클릭 · 성공 대화상자(`색인 생성 요청됨` + `우선순위 크롤링 대기열`) · 닫기 · 할당량 재확인이
모두 재현됐다. **그 과정에서 요청 전용 함정 셋이 새로 드러났고, 위 절차가 그것을 피한 결과다.**

5. **요청을 마치면 GSC 가 검사창을 접는다 — 다음 URL 을 이어서 넣을 수 없다.** banner 의
   `combobox` 가 사라지고 그 자리에 검사된 URL 텍스트가 남는다. **남아 있는 `검색` 버튼을 눌러
   펼치려 해도 `Timeout waiting for element to be ready` 로 막힌다.** **개요 딥링크로 `goto`
   재이동하면 검사창이 돌아온다**(실측: 재이동 5초 뒤 `count()` 1). **배치 루프는 매 건 시작에서
   검사창 존재를 `count()` 로 확인하고 없으면 재이동하는 것으로 짠다.**
6. **요청 버튼을 텍스트 selector 로 잡을 수 없다.** `button:has-text("색인 생성 요청")` 은
   `Selector not found` 로 죽는다(`닫기` 도 같다). **snapshot 트리에서 ref 를 정규식으로 뽑아
   `locator('e43')` 처럼 눌러야 한다** — `/- button "색인 생성 요청[^"]*" \[ref=(e\d+)\]/`.
   **ref 는 snapshot 마다 무효화되므로 그 건의 착지 게이트가 받은 트리에서 뽑아 곧바로 쓴다.**
7. **요청에 성공하면 그 버튼의 보이는 라벨이 `색인 생성 요청` → `다시 요청` 으로 바뀐다.**
   접근성 이름은 `색인 생성 요청다시 요청` 으로 변하지 않는다(§5.3 이 UIA 에서 본 것과 같다).
   **이것을 요청 이력 판정에 쓰지 말 것 — 같은 화면 세션에서 방금 요청한 것만 반영한다.**
   9-22 오전에 검사한 기술 글 4개는 **이미 요청 이력이 있는데도** 보이는 라벨이 `색인 생성 요청`
   이었다. **§4.1 의 "자동화가 신뢰할 수 있는 것은 §4.2 이력 표뿐" 은 그대로 유효하다.**

**셋 다 첫 건에서 걸렸고 세 번 고쳐서 통과시켰다** — 함정 5는 2번째 URL 에서 `Timeout waiting for
element to be ready`, 함정 6은 그다음 시도에서 `Selector not found` 로 나타났다. **1번째 건이
우연히 통과한 것은 그때 snapshot 의 ref 를 직접 썼기 때문이고, 그것이 정답이었다.**
**다음 배치가 이 셋을 다시 만나지 않도록 §6.6.1 의 표를 그대로 따른다.**

---

## 7. 미해결 ① — sitemap 이 처리되지 않는다

**현재 확인된 상태는 제출 건의 미처리(`isPending: true`)이고 원인은 미확인이다.**

### 7.1 관측 이력

| 시점 | `lastSubmitted` | `isPending` | `lastDownloaded` | 비고 |
|---|---|---|---|---|
| 2026-09-10 | `2026-08-12T23:52:31.587Z` | `true` | 없음 | 제출(`2026-08-13 08:52 KST`) 후 28일 |
| 2026-09-12 | `2026-09-12T06:30:07.930Z` | `true` | 없음 | **행 삭제 후 재제출.** 제출 8분 뒤라 판별력 없음 |
| 2026-09-16 | 동일 | `true` | 없음 | 재제출 4일째 |
| 2026-09-17 | 동일 | `true` | 없음 | 재제출 5일째. **세 번 연속 한 글자도 다르지 않다** |
| 2026-09-20 | 동일 | (미조회) | (미조회) | **화면 값은 여섯 칸 모두 동일**(재제출 8일째). `sitemaps.get` 은 §6.3 이 막혀 받지 못했다 |
| 2026-09-22 | 동일 | (미조회) | (미조회) | **화면 값 동일**(재제출 10일째). **`sitemap-all.xml` 행도 제출 2일째 같은 값**(§7.4.1) |

화면 값도 계속 같다 — 유형 `알 수 없음` · 제출 `2026. 9. 12.` · **마지막으로 읽은 날짜 (빈칸)** ·
상태 **`가져올 수 없음`** · 발견된 페이지 `0`.
**2026-09-20·09-22 Sitemaps 화면 재확인도 여섯 칸이 전부 같다.** 9-20 제출 전까지는 제출된
사이트맵이 1행이었고, **그날 `sitemap-all.xml` 을 더해 지금은 2행이며 둘 다 같은 값이다.**
`sitemaps.get` 은 9-20·9-22 둘 다 받지 못했다 — §6.3 의 `Show scopes` 단계가 막혀 **쓰기 scope 가
붙은 채로 Execute 하지 않고 중단했다.** 화면 값이 API 응답과 같은 사실을 주므로 판정에 공백은 없다.

### 7.2 판별점과 해석 갈래

| 관찰 | 해석 |
|---|---|
| `lastSubmitted` 만 갱신 | 새 제출 기록. 처리 진전은 확인되지 않음 |
| **`lastDownloaded` 가 생김** | 다운로드 기록이 생김. 처리 성공은 별도 확인 |
| **`isPending: false`, `type`·`contents` 가 나타남** | 처리 결과 확인 가능. `errors`·발견 URL 수까지 대조 |
| 계속 `isPending: true`, 다운로드·형식·내용 필드 없음 | **미처리 지속. 원인·대기 순서는 알 수 없음** ← 현재 |

**`errors: "0"` 은 기록된 오류 수이며 미처리 상태에서 XML 검증 통과를 뜻하지 않는다.**

**9-12 재제출에 대해서는 양쪽 다 단정하지 않는다** — "재제출이 처리를 앞당겼다" 는 증거가 없지만,
**"재제출이 효과가 없었다" 도 단정하지 않는다.** 30일 된 옛 행과 5일 된 새 행을 같은 지표로 비교할 수
없기 때문이다.

### 7.3 배제된 것 — 사이트 쪽 결함은 없다

2026-09-10 실측: live 파일이 일반 UA·Googlebot UA 모두 `200` + `application/xml`, 리디렉션 0,
XML 파싱 성공, 정규 `urlset` namespace, 중복 0, 저장소 파일과 동일. `robots.txt` `200` + `Allow: /` +
사이트맵 주소 정확. 실제 URL 테스트도 크롤링 허용 `예` / 페이지 가져오기 `성공` / 색인 생성 허용 `예`,
직접 조치 보고서 `감지된 문제 없음`. **수정할 결함이 발견되지 않았다.**

**철회된 해석들 — 다시 꺼내지 말 것.** "8-13 에 읽기를 시도해 실패했다", "날짜가 그대로이므로 재시도가
없다", "재시도 백오프가 원인이다", "낮은 크롤링 수요가 원인이다" 는 전부 근거 없이 단정한 것이라
철회했다. **화면의 날짜·상태만으로 과거 접근 시도나 재시도 이력을 확정할 수 없다.**
**`2026-08-31` 은 최근 *일반 크롤링* 시각이지 사이트맵 처리 시작이 아니다.**

### 7.4 계획 — 다른 경로에 복사본을 제출해 "경로가 원인인가" 를 가른다

**2026-09-17 사용자 결정으로 채택됐고 실행은 §2.1 의 3일치가 끝난 뒤다 — 그 3일치는 2026-09-19 에
끝났으므로 9-20 부터 실행할 수 있다. 관찰 창과의 겹침은 §2.4 를 본다.**

| 항목 | 정한 것 | 이유 |
|---|---|---|
| 파일 | `sitemap.xml` 을 **바이트 그대로** 복사한 새 경로(예: `sitemap-all.xml`) | 내용을 바꾸면 변수가 둘이 된다 |
| `robots.txt` | **넣지 않는다** | GSC 제출만으로 충분하고, 격리해 두면 되돌릴 것이 줄어든다 |
| 기존 `/sitemap.xml` 행 | **건드리지 않는다** | 삭제는 §3.2 가 금지한다 |
| 판별점 | §7.2 그대로 | |

**읽는 법.** 새 경로도 `가져올 수 없음` 이면 **경로·파일·제출행이 원인이 아니라는 것이 굳어져**
Search Central 커뮤니티 문의의 근거가 된다(문의는 아직 작성·게시하지 않았고 승인받은 적도 없다).
새 경로가 처리되면 기존 경로가 갇힌 것이므로 **버리는 것이 아니라 `robots.txt` 를 새 경로로 옮겨
그쪽을 정본으로 삼는다.**

**기대값은 낮다.** 9-12 의 행 삭제·재제출이 이미 "새 제출 레코드" 실험이었고 5일째 같은 값이다.
남는 차이는 `feedpath` 문자열뿐이고 경로별로 다르게 처리된다는 근거는 없다. 파일 쪽 결함도 §7.3 으로
배제돼 있다. **그럼에도 두 결과 모두 정보이고 되돌리기 쉬워서 해 볼 값이 있다.**

**메인이 이 논의에서 틀린 것 둘 — 같은 실수를 반복하지 않도록 남긴다.**

1. **"`lastmod` 2벌 관리 비용" 은 과장이었다.** §5.1 의 함정은 *"이 파일의 마지막 본문 변경일이
   언제인가" 를 매번 판단*하는 일이다. 복사본에는 그 판단이 없다 — `cp` 한 줄, `diff` 로 검증 끝이다.
   **기계적인 일을 판단이 필요한 일과 같은 급으로 묶어 말했다.**
2. **`<sitemapindex>` 로 감싸는 변형을 권한 것은 실험 설계로 틀렸다.** 그 index 는 자식으로
   `/sitemap.xml` 을 가리키는데 **그 경로가 바로 용의자**다. 부모가 처리돼도 자식에서 같은 곳에 막히면
   경로 변수를 가르지 못한다. **관리 비용만 보고 실험의 핵심 변수를 지운 셈이다.**

### 7.4.1 집행 기록 — 2026-09-20

**배포**(`a88c1bc`, push 완료). `sitemap-all.xml` 은 `sitemap.xml` 의 **바이트 그대로 복사본**이다 —
저장소 blob 해시가 `5ab9f4c9…` 로 **둘이 같다**(`git rev-parse HEAD:sitemap.xml` == `git hash-object`).
`robots.txt` 는 §7.4 대로 **건드리지 않았고**, 기존 `/sitemap.xml` 제출 행도 **손대지 않았다.**

**live 검증**(push 후 약 30초에 배포 완료) — `https://droidactor.github.io/sitemap-all.xml` 이
`200` · `application/xml` · `58,428 bytes` · **리디렉션 0** · Googlebot UA 도 `200` ·
XML 파싱 성공 · 정규 `urlset` namespace · `<loc>` **108** · **내려받은 바이트가 `sitemap.xml` 과 동일**
(SHA-256 `76e2ce80…`). **§7.3 이 원본에 대해 배제한 항목을 복사본에서도 그대로 확인했다.**

**GSC 제출**(2026-09-20). 입력칸 `사이트맵 URL 입력` 에 `sitemap-all.xml` 을 넣고 제출 직전에
값을 재조회해 일치를 확인한 뒤(§5.4 의 게이트를 그대로 적용) `제출` 을 눌렀고
**`사이트맵이 제출됨`** 을 받았다. 표가 **2행**이 됐다.

| Sitemap | 유형 | 제출 | 마지막으로 읽은 날짜 | 상태 | 발견된 페이지 |
|---|---|---|---|---|---|
| `/sitemap-all.xml` | 알 수 없음 | **2026. 9. 20.** | (빈칸) | **가져올 수 없음** | 0 |
| `/sitemap.xml` | 알 수 없음 | 2026. 9. 12. | (빈칸) | 가져올 수 없음 | 0 |

> **새 행이 제출 직후부터 `가져올 수 없음` 인 것을 "복사본도 실패했다" 로 읽지 말 것 — 판별력이 없다.**
> 9-12 에 `/sitemap.xml` 을 재제출했을 때도 **8분 뒤 같은 표시**였고 §7.1 이 그것을 "제출 8분 뒤라
> 판별력 없음" 으로 기록했다. **판정은 §7.2 의 판별점(`lastDownloaded` 생성 · `isPending: false` ·
> `type`·`contents` 출현)으로만 하고, 최소 며칠은 둔다.**

**2026-09-22 재확인 — 두 행이 여섯 칸 모두 그대로다**(제출 2일째). §7.2 의 판별점은 하나도
나타나지 않았고, 위 경고대로 **이것을 "복사본도 실패했다" 로 읽지 않는다.**

**다음에 읽을 것** — §7.2 그대로. 읽는 법은 §7.4 본문("새 경로도 `가져올 수 없음` 이면…").
**조회 빈도는 §2.5 를 따른다(매일 조회하지 않는다).**

**버릴 때의 순서** — 파일을 먼저 지우면 제출된 sitemap 이 `404` 가 되어 그 행이 오류로 남는다.
**GSC 행 삭제 → 파일 삭제** 순서이고, 그 삭제는 §3.2 상 사용자 지시를 한 번 더 받는다.
**이제 이 순서는 `sitemap-all.xml` 에도 그대로 적용된다.**

---

## 8. 미해결 ② — 옛 URL 이 새 URL 의 표준을 잠식한다

**추정이 아니라 GSC 가 값으로 답한 사실이다.**

| URL | 사용자 선언 표준 URL | Google에서 선택한 표준 URL |
|---|---|---|
| `/apps/lgtv/` | `/apps/lgtv/` | **`/lgtv/`** (옛 스텁) |
| `/ko/apps/lgtv/` | `/ko/apps/lgtv/` | **`/ko/lgtv/`** (옛 스텁) |

한·영이 완전히 대칭이고, 처음 감지된 날짜는 **2026-08-15** — 개편 배포 바로 다음날이다.
**2026-09-16 재확인에서 최근 크롤링이 영문 `2026-08-24 09:51:21` · 국문 `2026-08-24 09:55:22` 로
초 단위까지 그대로였다 — 23일째 재크롤이 없다.** 참조 페이지도 옛 스텁 하나뿐이다.
**2026-09-22 보고서 드릴다운에서도 둘 다 최종 크롤링 `2026. 8. 24.` 로 29일째 같고, `처음 감지된
날짜` 도 `26. 8. 15.` 그대로다. 사유별 페이지 수도 여전히 2다**(§8.3 의 확산 감시와 정합).

**2026-09-17 에 이 잠식이 색인 라벨이 아니라 실제 노출 손실임이 확인됐다**(§1.4). 개편 이후 28일
(8-18~9-14) 실적에서 **`/apps/lgtv/`·`/ko/apps/lgtv/` 는 노출 0 으로 표에 존재하지 않고**, 옛 스텁
`/lgtv/` 16 · `/ko/lgtv/` 18 이 **34노출을 가져간다.** 나머지 다섯 제품은 새 URL 이 전부 노출을
받으므로(bt-keyboard 28 · bt-ppt 38 · bt-mouse 26 · wifi-scout 13 · ssh-scout 6) **lgtv 만 예외다.**
같은 구간에 `/tech-notes/lgtv/` 가 156노출로 사이트 최다라 **주제 수요가 없어서가 아니다.**
**비용을 과장하지 말 것 — "그 제품 페이지가 검색에 나오지 않는다" 가 아니다.** 옛 스텁은 §3.1 대로
0초 `meta refresh` + canonical 이라 **클릭하면 새 페이지로 간다**(같은 구간 `/lgtv/` 는 16노출에
**2클릭**으로 CTR 5.9%, 사이트 평균 3.1%보다 높다). **실제 손실은 트래픽이 아니라 노출의 얼굴이다** —
검색 결과에 나가는 제목·스니펫이 본문 한 줄(`This page has moved`)짜리 스텁의 것이고(§8.2),
제품 페이지의 내용이 순위 산정에 쓰이지 않는다.

### 8.1 배제된 가설 셋 — 전부 실측으로 닫혔다

1. **"같은 제품의 다른 새 URL 과 본문이 겹쳐서" 는 반증됐다.** 6-gram shingle 중복도가
   `apps/lgtv` ↔ `manual/lgtv` = **0.037**, ↔ `tech-notes/lgtv` = **0.003** 이고, 대조군 `bt-mouse` 가
   오히려 더 높다(0.063). **lgtv 만 특별한 중복이 없다.**
2. **"한·영이 서로를 중복 판정" 도 아니다.** 둘 **다** 중복으로 판정됐으므로 표준은 제3의 URL 이다.
3. **"내부 링크에 옛 URL 이 남아서" 도 아니다.** 저장소 `*.html`·`*.xml` 전수 검색에서 호환 스텁
   자신을 제외하면 **0건**이다.

**사이트 선언도 전부 정확하다** — 새 페이지 2개는 self canonical, 옛 스텁 2개는 대응 새 URL 로 가는
`meta refresh` + canonical. **고칠 잘못된 태그가 없다.**

### 8.2 추정 — 사실로 인용하지 말 것

Google 은 canonical 로 묶인 두 페이지가 실질적으로 중복이 아니면 그 선언을 무시한다. 옛 스텁은 본문이
`This page has moved` 한 줄(17행)이고 새 페이지는 10KB 짜리 완전한 제품 페이지다 — **내용이 전혀 다른
두 URL 을 canonical 로 묶은 형태**다. `meta refresh` 는 301 보다 약하고, 여기에 외부 링크 0 ·
sitemap 미수집이 겹쳐 **새 URL 쪽에 신호가 약하다.**
**"내부 링크 그래프 0" 을 이 추정의 근거로 쓰지 말 것** — 그것은 GSC 링크 보고서의 공백이지 사이트의
사실이 아니다(§1.3). 실제로 이 URL 의 `참조 페이지` 는 옛 스텁으로 잡혀 있다.

**GSC 는 "왜 무시했는지" 를 알려주지 않는다. 이 문단은 그 공백을 메우는 추론이다.**
**반례가 하나 있다** — 8-24 재요청이 재크롤을 불러냈는데도(8-14 → 8-24) 판정이 바뀌지 않았다.
즉 "재크롤이 없어서 재평가가 안 된다" 는 선행 조건은 해소됐는데 결과가 같다.

### 8.3 확산 여부 — 반증 쪽이지만 감시는 계속한다

`중복 페이지` 사유의 페이지 수가 8-24 이래 **2 로 고정**이고, 표본
`(ko/)apps/{bt-keyboard, wifi-scout}/` 4개는 전부 색인이다. **lgtv 한·영 2개에 머물러 있다.**

**다만 잠식의 재료는 줄지 않았다** — 색인 유지 중인 옛 스텁이 8-17 의 9개에서 **10개**로 늘었고
2026-09-17 에도 10개 그대로다(§1.2). **위험 모집단은 §3.1 의 14개 전체로 보고 나머지 12개를 감시한다.**

### 8.4 남은 조치 — 기다리는 것뿐이다

| 후보 | 지위 |
|---|---|
| 색인 생성 요청 | **이미 써 봤다** — 8-14·8-24. GSC 가 "여러 번 제출해도 우선순위가 바뀌지 않는다" 를 명시 |
| 옛 스텁에 `noindex` 재도입 | **§3.1 이 명시적으로 되돌린 결정이다.** 되살리려면 §3.1 을 뒤집어야 한다 |
| 옛 URL 을 `404` 로 | **§3.2 가 금지한다** |
| 삭제 도구로 숨김 | **§3·§3.2 가 둘 다 금지한다** |
| **손대지 않고 재크롤을 기다린다** | **§3 의 기존 방침과 일치. 정적 호스팅에서 가능한 정석이다** |

**기존 정책을 지키는 한 남는 것은 마지막 줄뿐이다.** 다른 후보로 가려면 §3.1 또는 §3.2 를 고치는
결정이 선행해야 한다 — **그 결정 없이 손대지 않는다.**

---

## 9. URL 인벤토리

**URL 목록의 SSOT 는 저장소의 `sitemap.xml` 이다.** 이 문서는 목록을 복제하지 않고 유래만 적는다.

**"URL 수" 는 증분이다.** 첫 행만 개편 이전(34)에서 출발하므로 증분과 누적을 나눠 적는다.

| 배포 | 내용 | URL 수 | 누적 `<loc>` |
|---|---|---|---|
| (개편 이전) | `c48305c`(8-08) 이래의 sitemap | — | 34 |
| 2026-08-14 `f300f26` | 경로 개편 | **+16** | 50 |
| 2026-08-14 `ffb3c23` | 6개 앱 한·영 Tech Notes | +14 | 64 |
| 2026-08-14 `3b6948e` | Google TV HID 조사 글 한·영 | +2 | 66 |
| 2026-08-16 | bt-keyboard 상세 노트 | +4 | 70 |
| 2026-09-13 | 로마 숫자 변환기 제품·설명서 | +4 | 74 |
| 2026-09-14 `2c23df2` | 환율 계산기·블루투스 넘패드 제품 | +4 | 78 |
| 2026-09-15 (1) | 단위 변환기 제품 | +2 | 80 |
| 2026-09-15 (2) | **신규 앱 4종 Play 출시 반영** | **0** | 80 |
| 2026-09-16 `cbb10d8` | **기술 글 14편 한·영** — `/blog/techs/<slug>/` | **+28** | **108** |
| 2026-09-20 `a88c1bc` | `sitemap-all.xml` 복사본 배포(§7.4.1) | 0 | 108 |
| 2026-09-22 `7f70c03` | **경제 트렌딩 글 11편 국문 전용** — `/ko/blog/trending/economy/<slug>/` | **+11** | **119** |
| 2026-09-23 `be21861` | bt-keyboard 음성 입력 상세 노트 한·영(§2.8) | +2 | 121 |
| 2026-09-23 `a5a0b16` | 영수증 스캐너 제품 한·영(§2.9 A) — **이 커밋은 `sitemap-all.xml` 을 갱신하지 않았다** | +2 | 123 |
| 2026-09-23 `644b707` | 명함 스캐너 제품 + 두 앱 기술 노트 한·영(§2.9 B·C·D). `sitemap-all.xml` 재동기화 | +6 | 129 |
| 2026-09-23 `4791381` | 두 앱 블로그 글 한·영 — `/blog/products/<slug>/`, 첫 `products` 갈래 글(§2.9 E·F) | +4 | 133 |
| 2026-09-24 `13b1fa4` | 증명사진 제품·기술 노트·블로그 글 한·영(§2.10 G·H·I). 두 sitemap `cmp` 동일 | +6 | 139 |
| 2026-09-24 `184bf68` | 세 앱 제품 홍보글 한·영(§2.11 J·K·L) — **이 커밋은 `sitemap-all.xml` 을 갱신하지 않았다**(저장소에서 되맞춤, 배포 전) | +6 | 145 |

**9-15 에 성격이 다른 배포가 둘 있었다** — 앞은 `(ko/)apps/unit/` 2개를 더한 것이고, 뒤는
**신규 앱 4종(roman·currency·numpad·unit)의 Play 출시를 반영한 것으로 신규 URL 이 0개**다.
그 뒤 배포로 "미출시 앱의 URL" 분류가 전부 닫혔다 — **9-15 시점 6+4 = 10종이 출시 상태였고 미출시는
`yt-downloader` 하나뿐이었다.** 9-23 에 `receipt`·`namecard` 가 추가돼 다시 미출시 앱이 셋이 됐다
(두 앱은 스토어 로그아웃 `404`. 사이트는 "곧 출시" 가정으로 블로그 CTA 에 스토어 배지를 이미 걸었다 — 2026-09-23 사용자 지시). 날짜만 보고 둘을 섞지 않는다.

**단일 커밋 기준 최대 증가는 `cbb10d8` 의 +28 이다**(경로 개편 `f300f26` 은 +16이고, 8-14 하루 전체를
합쳐야 +32 로 앞선다).
**`/blog/techs/` 아래는 제품·매뉴얼이 아니라 개발 도구 주제의 기술 글이고, 갈래 전용 index 페이지가
없다**(§2.2 4번).

---

## 10. 관측 이력 요약 — 한 줄씩

**세부는 `git show cc532b1:todo-update-google-search.md` 에 있다.**

| 날짜 | 요청 | 배포 | 그날의 핵심 |
|---|---|---|---|
| 08-14 | 11 | `f300f26`·`d6e04f0` 외 | 경로 개편. 옛 URL `noindex` 제거. 12번째 요청이 할당량 거절 |
| 08-15 | 0 | — | 옛 URL 4개 색인 유지 확인(정상). 링크 보고서 0/0 |
| 08-17 | 10 | `9a397f5` | 우선순위 1의 10개 요청. `lastmod` 26건 정정 |
| 08-18 | 0 | — | 날짜 신호 34건 정정. `bt_mouse2` Play 404 발견 |
| 08-19 | 0 | `a806fbd`·`5cc2907` | 처리방침 앵커. 소유권 확인 파일이 3개임을 발견 |
| 08-24 | 2 | — | Page indexing 첫 판독. **canonical 잠식 확정**. lgtv 2개 재요청(규칙 위반) |
| 08-31 | 10 | `4bb277f` | 색인 38 의 정체 확정. Tech Notes 10개 요청. Play 링크 `hl` 정정 |
| 09-02 | 10 | — | 요청 10건 접수 |
| 09-05 | 10 | — | 9-02 요청분 10/10 색인 확인. 할당량 소진 |
| 09-10 | 5 | — | **보고서 첫 전진**(색인 38→54). sitemap API 기준선 확립 |
| 09-12 | 0 | — | **sitemap 행 삭제 후 재제출**(8-24 금지 1회 해제) |
| 09-13~15 | 2 + 7 | `2c23df2` 외 | roman·currency·numpad·unit 배포와 요청. 신규 앱 4종 출시 반영 |
| 09-16 | 2 | `5e998bd`·`cbb10d8` 외 | 미배포 정정 배포. **GSC 조작을 CDP 로 전환**. 기술 글 28 URL 배포 |
| 09-17 | **10** | `cc532b1`·`708e59e`(문서) | **보고서 9-04→9-14 전진**(색인 54→62). 9-14·9-15 요청분 8/8 색인. **기술 글 영문 10개 요청(1일차)**. 이 문서를 운영 항목만 남기고 정리 |
| 09-17 (저녁) | 0 | — | **실적 보고서 최초 판독**(§1.4) — 옛 URL 노출 감소 없음, 새 URL 0→111, **lgtv 새 URL 노출 0**. **Tech Notes 허브 2개 실시간 검사로 잠식 없음 확정**. **사이트맵 필터가 `aria-disabled` 임을 확인**(§2.6) |
| 09-18 | **10** | — | **기술 글 영문 잔여 4 + 국문 상위 6 요청(2일차)**. 보고서는 기준일 `9-14` · 색인 62 · 미색인 12 로 9-17 판독과 동일(기준일은 나흘 묵음). **개요 화면 복귀 시 `URL 검사` 대기 조건의 포커스 경합을 발견해 요청 스크립트를 착지 검증 방식으로 고쳤다**(§6.2) |
| 09-19 | **8** | — | **기술 글 국문 잔여 8 요청(3일차) — 28개 3일치가 이 날로 끝났다**(09:00:34~09:06:06 KST, 할당량 초과 0). 보고서는 기준일 `9-14` · 색인 62 · 미색인 12 로 사흘째 동일(기준일은 닷새 묵음). **요청 스크립트가 하드코딩한 포트 9333 을 남의 프로필이 점유 중이어서 9334 로 옮겨 실행했다**(§6.1) |
| 09-19 (저녁) | 0 | — | **1·2일차 요청분 20개 실시간 검사 — 18/20 색인 확인**(각 9/10). **요청 2~4분 뒤 20/20 크롤**로 `우선순위 크롤링 대기열` 의 실효를 처음 확인(§2.3.1). **보고서 숫자로 요청 성과를 읽지 말 것을 §1 에 박제** |
| 09-20 | 0 | — | **기술 글 28개 전수 판정 — 24/28 색인**(3일차 6/8, 미색인 4개는 재크롤 없음). **§2.6 조건 2·5 를 실시간 검사로 확정**(허브 3/4 미색인 · 37일째 재크롤 없음, 미발견 4개 불변). **9-19 에 적은 "갈리는 것은 크롤 이후" 를 실측으로 철회**(§4.3). 보고서 엿새째 `9-14`. **§2.4 sitemap 복사본 실험 실행 — `sitemap-all.xml` 배포(`a88c1bc`) 후 GSC 제출, 표가 2행이 됐다**(§7.4.1) |
| **09-22** | 0 | — | **보고서가 `9-14`→`9-18` 로 전진 — 색인 62→88(+26) · 미색인 12→16.** 9-19 에 적은 "전진하면 크게 오른다" 가 맞았다(§1). **색인 88개 전수를 받아 sitemap 108 과 대조**(∩78 · 밖 10 · 미색인 30, §1.2.1). 미색인 16 = 기존 12 + 기술 글 4 로 **기존 구성원은 하나도 해소되지 않았다**. 미색인 4개 개별 검사 — 넷 다 재크롤 없음(§2.3.3). `(ko/)apps/unit/` 2/2 색인으로 닫힘. Sitemaps 2행 불변. **조작 경로를 CDP 에서 Aside 로 전환**(§6.6). **§4.2 원장을 URL 단위로 전개해 94개 일치 검산 — 그 시점 요청 가능한 URL 이 0개였다** |
| **09-22 (저녁)** | **10** | `7f70c03` | **경제 트렌딩 글 11편 배포**(`/ko/blog/trending/economy/`, 국문 전용, sitemap 108→119, `sitemap-all.xml` 동기화). 사전 점검 11/11 통과 후 **1일차 10건 요청**(18:55:59~19:03:55 KST, 할당량 초과 0) — **Aside 로 요청한 첫 배치이고 §6.6 의 마지막 미검증 구간이 통과했다.** 요청 루프 함정 셋을 새로 기록(검사창이 접힘 · 버튼은 ref 로 · 라벨 `다시 요청` 변화). 11번째는 9-23 로 |

---

## 11. 공식 참고 자료

- [Site Moves and Migrations](https://developers.google.com/search/docs/crawling-indexing/site-move-with-url-changes)
- [Redirects and Google Search](https://developers.google.com/search/docs/crawling-indexing/301-redirects)
- [Change of Address tool](https://support.google.com/webmasters/answer/9370220)
- [URL Inspection tool](https://support.google.com/webmasters/answer/9012289)
- [Build and submit a sitemap](https://developers.google.com/search/docs/crawling-indexing/sitemaps/build-sitemap)
- [Sitemaps report](https://support.google.com/webmasters/answer/7451001)
- [Sitemaps API — 리소스 정의](https://developers.google.com/webmaster-tools/v1/sitemaps) ·
  [읽기 전용 get](https://developers.google.com/webmaster-tools/v1/sitemaps/get)
- [Canonicalization](https://developers.google.com/search/docs/crawling-indexing/consolidate-duplicate-urls)
- [Removals tool](https://support.google.com/webmasters/answer/9689846)
